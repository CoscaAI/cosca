---
id: 52c5815c-2adc-42a9-84c8-97b180be791b
type: session
layer: project
scope: project
created_at: 2026-09-04T14:31:30.6264572-03:00
updated_at: 2026-09-04T14:31:30.6264572-03:00
ttl: 0s
priority: 0
version: 0
metadata:
    agent: cosca-mcp
    provenance: P0
    source: EVIDENCE
---

Documentation sync (README + LICENSE) for Cosca v1.5.0 on develop (2026-09-04).

REAL measured values (CLI is authoritative, reads modular DBs not the legacy monolith):
- `cosca status` now reports "Vectors: 55453" — confirms the 2026-09-04 CLI fix landed (reads vector-*.db modules, not monolith).
- Version 1.5.0, Go 1.26.5, module github.com/CoscaAI/cosca. Binary bin/cosca.exe + Linux bin/cosca.
- Knowledge modular (ADR-013): vector-*.db modules each <100MB (max vector-embed-core.db=46.5MB), total 8 modules=223.8MB, plus core.db, graph.db, projects.db. knowledge.db=585MB is the LEGACY monolith left out of gold-modular.
- Runtime counts (CLI registry, superset): agents=61, skills=88, workflows=39. Framework directory (`.opencode/cosca/` canonical): agents=53 dirs, skills=29 dirs, engines=34 dirs, workflows=30 md. IMPORTANT: counts diverge by source — runtime registry counts embedded+fallback+registered superset; framework dir counts are the versioned canonical set. NEVER conflate; label the source.
- catalog.manifest lists 53 agents / 26 skills / 29 engines / 0 workflows (only INDEX.md tracked entries).
- Security deps: grpc v1.83.2 (fixes GHSA-vp52-pcj8-j9qc), x/crypto v0.56.0 (fixes ssh DoS). serve listens loopback only (127.0.0.1:14120). No HIGH/CRITICAL; residual GO-2026-5932 (openpgp UNKNOWN, unused — bcrypt used instead).
- Auto-update via core.hooksPath=.githooks: post-commit.cmd (Windows delegates bash→cmd), post-commit (bash/WSL). Rebuilds binary on code change (cmd/,internal/,pkg/,api/,sdk/,go.mod,go.sum,Makefile) and restarts serve (port 14120 on Windows; systemctl --user restart cosca-serve.service on Linux). Docs/.opencode/.cosca/.githooks never trigger. Branch-agnostic. Never blocks commit (exit 0).
- Branch flow: main=stable (no CLI fix), develop=integration (has CLI fix + security fix), gold-modular=frozen restore point + tag gold-modular-2026-09-04 (11 DB modules ~292MB, each <100MB, excludes secrets.db/auth_tokens.db/knowledge.db).
- manifest.yaml is at .cosca/knowledge/manifest.yaml (NOT .cosca/manifest.yaml). F2 references it.
- Verify commands: `cosca db check --gate` (ADR-013 100MB gate, read-only), `cosca security scan` (osv-scanner), `cosca license verify` (3 factors), `cosca status`.

Documentation drift lesson (recurring): README badges/stats based on framework dir vs runtime registry diverge. Always measure BOTH and label source. Previous sync used 53 agents (framework dir) and README claims 28 skills but SDK dir = 29.
