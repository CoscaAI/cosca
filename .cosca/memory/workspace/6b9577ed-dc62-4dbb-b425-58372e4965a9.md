---
id: 6b9577ed-dc62-4dbb-b425-58372e4965a9
type: pattern
layer: workspace
scope: kernel
created_at: 2026-09-04T04:03:57.9784733-03:00
updated_at: 2026-09-04T04:03:57.9784733-03:00
ttl: 0s
priority: 0
version: 0
metadata:
    agent: cosca-mcp
    provenance: P0
    source: EVIDENCE
---

ADR-041 — SSRF hardening IMPLEMENTADO (AUGMENT do guard existente). 2026-09-04. Proveniência: princípio de osiris/src/lib/ssrf-guard.ts (MIT), reimplementado sob contratos COSCA. Nenhum código importado.

O QUE MUDOU:
1) internal/security/netguard.go: novo `CheckHostname(host) error` — blocklist de hostname de metadata ANTES do DNS (localhost, *.localhost, *.local, *.internal, host.docker.internal, metadata.google.internal), defesa em profundidade. `IsPublicIP` ampliado: +documentação 2001:db8::/32, +site-local fec0::/10, +NAT64 local-use 64:ff9b:1::/48 (fail-closed, bloqueia mais ranges reservados).
2) internal/security/safehttp.go (novo): `NewSafeClient(timeout, maxRedirects)` → http.Client com CheckRedirect que REVALIDA CADA SALTO (hostname + re-resolve + todas IPs públicas, fail-closed), teto de redirects, só http/https. Fecha a brecha redirect→privado.
3) internal/mcpserver/tools.go handleWeb: `CheckHostname` no host inicial + troca do client por `NewSafeClient` (redirects revalidados) + decisão ssrf_guard_ok atualizada.

GAP REAL FECHADO: antes o web tool validava só a URL inicial, mas `client.Get` seguia redirects automaticamente SEM revalidar (redirect→metadata/privado). Agora redirects validados hop-by-hop.

EVIDÊNCIA: go vet security+mcpserver pass; go test -race security pass (CheckHostname + IPv6 reservado + redirect-block/metadata/maxRedirects/não-http + request inicial permitido); go test mcpserver pass (regressão handleWeb). 1 teste mcpserver ajustado: input localhost→127.0.0.1 (hostname agora rejeitado antes do DNS; IP literal preserva o caminho 'não é público').

NÃO FEITO (deliberado/escopo mínimo): migrar internal/acquisition (guard antigo mais fraco: net.ParseIP lenient + blockedCIDRs sem CGNAT/multicast/test-net) para netip+CheckHostname — pendência dos capos, sem refatoração ampla. TOCTOU/IP-pinning não coberto (ADR-041 nota como grau opcional; guard é primeira barreira).
