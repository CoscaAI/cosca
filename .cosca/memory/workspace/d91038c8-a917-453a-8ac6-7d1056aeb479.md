---
id: d91038c8-a917-453a-8ac6-7d1056aeb479
type: session
layer: workspace
created_at: 2026-08-30T04:29:00.3413801-03:00
updated_at: 2026-08-30T04:29:00.3413801-03:00
ttl: 0s
priority: 0
version: 0
metadata:
    agent: cosca-mcp
    provenance: P0
    source: EVIDENCE
---

SOLITEK frontend enterprise VALIDADO de ponta a ponta (E2E) em 2026-08-30. Stack: Next.js 15 (App Router) + shadcn/ui (13 componentes) + TanStack Query + Zustand + React Hook Form + Zod. Padrao de auth enterprise: JWT em cookie httpOnly (nunca localStorage) -> login seta cookie solitek_token httpOnly=True, logout limpa GET /auth/me valida via cookie. Fluxo completo comprovado via PROXY do frontend (localhost:3001 -> backend :3000 usando rewrites no next.config.ts, source /api/:path*): 1) POST /api/auth/login -> role=admin + cookie httpOnly 2) GET /api/clients autenticado via cookie 3) GET /api/service-orders autenticado via cookie. Isto mitiga XSS (Frontend Chief standard). MONOREPO + next: node_modules e hoisted na raiz, entao o binario do next nao existe em apps/frontend/node_modules/.bin -> usar npx next start (hospeda o binario da raiz) em vez de node node_modules/.../next. Caminho do entrypoint do backend NestJS no build e dist/src/main.js (tsconfig inclui prisma/**, preserva src/). O cookie nao pode ser lido via frontend (httpOnly) mas o fetch usa credentials:'include' para envia-lo automaticamente.
