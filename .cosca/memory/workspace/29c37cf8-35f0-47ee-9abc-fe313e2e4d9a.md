---
id: 29c37cf8-35f0-47ee-9abc-fe313e2e4d9a
type: session
layer: workspace
scope: runtime
created_at: 2026-08-31T02:56:30.93526-03:00
updated_at: 2026-08-31T02:56:30.93526-03:00
ttl: 0s
priority: 0
version: 0
metadata:
    agent: cosca-mcp
    provenance: P0
    source: EVIDENCE
---

ModelRouter nil-pointer panic: chat.ChatRegistry.Get() can return (nil, true) when a registered provider's factory fails (e.g. isolated client project with no LLM API key). getOrCreate() returns nil on factory error but Get() still reports ok==true. Any call to provider.Model() on that nil then panics. Fix: guard every `provider.Model()`/`Name()` call with `ok && provider != nil` (model_router.go: matchProviderName x2, selectByCategory "first available", plus new hasReadyProvider() short-circuit in Select returning fallbackWithReason("no ready LLM provider registered, using default")). Root cause file internal/chat/registry.go:230-234. Graceful degradation is the correct behavior, never crash.
