---
id: eb4d0e15-be43-4796-a23a-50ace283bcc4
type: pattern
layer: workspace
scope: kernel
created_at: 2026-09-04T04:09:26.0693759-03:00
updated_at: 2026-09-04T04:09:26.0693759-03:00
ttl: 0s
priority: 0
version: 0
metadata:
    agent: cosca-mcp
    provenance: P0
    source: EVIDENCE
---

ADR-042 — Conditional GET + optional() IMPLEMENTADO. 2026-09-04. Proveniência: princípio de osiris/src/lib/httpJson.ts (MIT), reimplementado sob contratos COSCA. Nenhum código importado.

O QUE MUDOU (aditivo/alocação em internal/acquisition):
1) conditional.go (novo): `Validators{ETag,LastModified}` + `ConditionalResult{Changed,Body,Validators}` + `(*Client).FetchConditional(ctx, url, v)` — GET condicional via client endurecido (SSRF no dial, timeout, teto redirect, limite corpo). Replay ETag/Last-Modified → 304 (Changed=false, Body=nil, sem erro — resposta VÁLIDA, não erro); 200 → decodifica pelo Content-Encoding header (gzip/deflate; sem dep brotli) + retorna validators atualizados. UA identificador honesto.
2) optional.go (novo): `Optional[T](fn) (T, bool)` — enriquecimento opcional que falha → (zero,false), nunca derruba o pipeline; distingue zero-legítimo (ok=true) de indisponível (ok=false).
3) acquisition.go (modificado): extraído `httpClient()` (plumbing endurecido comum a FetchAll/FetchConditional, reduz duplicação sem mudar assinatura) + const `acquisitionUA` (identificador honesto) + `req.Header.Set("User-Agent", acquisitionUA)` no FetchAll.

VALOR/FECHADO: re-baixar dump grande que não mudou num poll vira 304 (barato); corpo decodificado pelo header; UA honesto (endpoints rejeitam browser/default); enriquecimento opcional não mata o pipeline.

EVIDÊNCIA: go vet acquisition e cli pass; go test -race acquisition pass (304, Last-Modified, gzip decode por header, UA honesto, non-200 erro, Optional success/error/zero-vs-indisponível); build consumidores (cmd/acquireall, cli, knowledge) pass — regressão zero.

LIMITE: brotli (br) decodificado como raw (sem dependência); documentado. FetchAll ganhou UA header (mudança ínfima, aditiva, sem mudar resposta/hash — comportamento preservado).
