PREV: 3cf12f767a7ff643db377a7391b8940a1006bc9264cb174b4bc6412c12b2a0ba
ID: 2026-07-30
TIME: 2026-07-30
LEVEL: 
TAGS: #rest #handlers #timeout #run #configurability #g8
---
### 2026-07-30 — Replaced hardcoded 5min timeout in RunHandler with configurable timeout
| Field | Value |
|-------|-------|
| **Agent** | cosca-backend |
| **Task** | G8 — Make the hardcoded `context.WithTimeout(r.Context(), 5*time.Minute)` in RunHandler.Execute (run.go:125) and RunHandler.Stream (run.go:281) configurable |
| **Technique** | Level 3 — Gap fix: added `timeout time.Duration` field (zero = default), `SetTimeout(d)` setter (ignores d<=0), `requestCtx(r)` helper falling back to 5min, wired both call sites to `h.requestCtx(r)` |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #rest #handlers #timeout #run #configurability #g8 |
| **Related** | api/rest/handler/run.go, api/rest/server.go, internal/config/config.go |
| **Learned** | 1) The two hardcoded timeout call sites had IDENTICAL surrounding lines (`// Execute.\n\tstartTime := time.Now()\n\tctx, cancel := context.WithTimeout(r.Context(), 5*time.Minute)\n\tdefer cancel()`) — disambiguated edits by including the following unique lines (`response, err := registry.Chat(...)` vs `opts.Stream = true`). 2) rest.Config (api/rest/server.go) has NO chat/run execution timeout field — only HTTP server timeouts (ReadTimeout 30s, WriteTimeout 60s, IdleTimeout 120s); WriteTimeout is HTTP-response-writing bound and SHORTER than the 5min chat default, so connecting SetTimeout to it would be a behavior regression. 3) internal/config.TimeoutConfig.Request exists (default 30s, `yaml:"request"`) but is a provider-layer HTTP request timeout NOT wired into rest.Config or server.go — not accessible from api/rest/server.go without touching internal/config + internal/cli/serve.go (out of scope). 4) Decision: left RunHandler at default (5min) in server.go; SetTimeout is opt-in for future wiring. 5) Constructor signature unchanged per spec — zero-value field keeps all 148 handler tests green. |
| **Next** | Potential future: add RunTimeout field to rest.Config + wire from internal/config.TimeoutConfig, then call runH.SetTimeout in server.go:254 |

## Session: 2026-07-28 — API Architecture Audit
