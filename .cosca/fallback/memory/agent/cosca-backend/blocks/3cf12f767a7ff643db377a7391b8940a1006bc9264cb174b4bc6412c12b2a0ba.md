PREV: c81a2d37c209f2b53fa9fbd7267a6bec373b5c55dafa8439cb310665b6243e1f
ID: 2026-07-31
TIME: 2026-07-31
LEVEL: 
TAGS: #chat #streaming #failover #resilience #registry #internal-chat
---
### 2026-07-31 — ChatStream failover wrapper in internal/chat/failover_stream.go
| Field | Value |
|-------|-------|
| **Agent** | cosca-backend |
| **Task** | Implement mid-stream failover: if a provider dies DURING streaming, reconnect on the next provider instead of killing the stream |
| **Technique** | Level 3 — Gap fix: new `failoverStream` wrapper (Recv/Close) with `sawContent` gate, `providerIdx` cursor, `maxReconnects` budget; `ChatRegistry.ChatStream` now wraps EVERY successful stream open |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #chat #streaming #failover #resilience #registry #internal-chat |
| **Related** | internal/chat/failover_stream.go, internal/chat/failover_stream_test.go, internal/chat/registry.go |
| **Learned** | 1) Failover trigger = Recv error AND sawContent AND reconnects < maxReconnects. A stream dying BEFORE content is an open failure (error surfaces); dying AFTER content = mid-flight interruption (reconnect). 2) Normal end contract: finish chunk = non-empty FinishReason + err nil — must NOT trigger reconnect; `(nil, nil)` (eventChatStream exhaustion) is also normal end, not an error. 3) Design decisions: no provider wrap-around (a provider that failed to open is not retried in the same Recv), maxReconnects=2 (tolerates full primary+2-fallback chain failure; prevents unbounded loop). Each successful open consumes a budget slot EVEN IF the new stream dies on its first Recv (the loop retries remaining providers in the same call). 4) `run.go` consumer breaks ONLY on `err != nil` — the wrapper must never return an error for a recoverable mid-stream death (it returns the first chunk of the reconnected stream instead). 5) mockStream returns `(nil, s.err)` when exhausted — for a reconnect test with manual `failoverStream` construction, MUST set `fallback.streamResp` or reconnect returns `(nil, nil)` stream → nil deref on next Recv. 6) sawContent also counts tool call deltas (stream dying mid-tool-call should also reconnect), not just text content. |
| **Next** | Consider exposing maxReconnects as a ChatRegistryConfig knob if ops needs tuning; G1 agent (run.go) should NOT need changes since contract is preserved. |

## Session: 2026-07-30 — G8 RunHandler Configurable Timeout
