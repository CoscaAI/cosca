PREV: 9d480f3206a2473f557d76c911f44c0d608a660a2a12f6d394373bcf0de83832
ID: L198
TIME: 2026-08-14
LEVEL: 5
TAGS: #cosca-trader #sqlite-busy #busy-timeout-per-connection #SetMaxOpenConns #conexao-unica #intent-duravel #stop-loss #causa-raiz #level-5
---
## L198 — 2026-08-14 — cosca-trader: SQLITE_BUSY corrigido (SetMaxOpenConns(1)) — busy_timeout é PER-CONNECTION | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Smoke test da Fase 4B (take-profit/trailing/sizing): o stop-loss automático falhava com "ordem inválida" e o log mostrava SQLITE_BUSY (5) no intent durável — investigar a causa raiz. |
| **Technique** | Level 5 — **(1)** O erro "ordem inválida" era SINTOMA: o log completo revelou "falha ao persistir intent ... database is locked (5) (SQLITE_BUSY)" — o stop-loss falhava porque o INTENT durável (Fase 2A) não conseguia gravar. **(2) CAUSA RAIZ: o PRAGMA busy_timeout é PER-CONNECTION** — o pool do database/sql abre várias conexões, e cada conexão nova nasce SEM o pragma, conflitando com a principal em WAL. **(3) Fix definitivo: `db.SetMaxOpenConns(1)` + `SetMaxIdleConns(1)`** — uma única conexão serializada vira o mutex do banco (correto para SQLite embarcado com múltiplos produtores: market data + user stream + OMS). **(4)** Prova ao vivo: ordem market → posição aberta → stop-loss automático colocado (paper-2 stop sell stop=57668.63 new) — ZERO SQLITE_BUSY no log. |
| **Level** | 5 |
| **Outcome** | success — SQLITE_BUSY eliminado, stop-loss automático funcionando ao vivo (prova real no smoke test), build/vet/test/-race verdes (13 pacotes). Commit 160460f. O erro "ordem inválida" que parecia bug do stop era o intent preso pelo banco. |
| **Confidence** | 0.96 (validado: smoke test real — stop-loss colocado ao vivo com zero BUSY; antes da correção, BUSY em 100% dos testes) |
| **Tags** | #cosca-trader #sqlite-busy #busy-timeout-per-connection #SetMaxOpenConns #conexao-unica #intent-duravel #stop-loss #causa-raiz #level-5 |
| **Related** | L278 (ordem sagrada — commit antes de assinar), L277 (Fase 4A risk), L275 (Fase 2A intent durável — o sintoma apareceu aí), F007 (P12) |
| **Learned** | **(1) Erro de superfície ≠ causa raiz: "ordem inválida" parecia bug do validateOrder, mas era o INTENT que falhava por SQLITE_BUSY — o log completo (Join com newline) revelou a verdade; o log resumido (grep de uma linha) escondia.** **(2) busy_timeout em SQLite Go é PER-CONNECTION: aplicar PRAGMA uma vez não cobre as conexões que o pool abre depois — para SQLite embarcado com escritores concorrentes, SetMaxOpenConns(1) é a resposta certa (o lock do banco vira o mutex).** **(3) A prova ao vivo vale ouro: ver o stop-loss de 57668 nascer no /orders depois do fix é a confirmação que nenhum teste unitário dá (eles não exercitavam o pool real).** |
| **Next** | (1) Fase 5 (F5 formal: estratégias plugáveis + backtest HTTP + gráfico de equity) quando o Don aprovar. (2) Testnet com chaves do Don. (3) Revisar se outros pontos usam pool de conexão com PRAGMA per-connection (padrão de auditoria). |
