# L366 — POLICY ENGINE em codigo (nivel 2 da governanca, L365)

## status: DECISION (atuacao do consigliere, autorizada por "marcha")

## O que entrou
internal/policy/ (o guard deterministico):
- Action/NewAction (tool, args -> RawCommand/TargetPath — derivados por
  CODIGO, nao pelo LLM), Decision (ALLOW/DENY/CONFIRM/ESCALATE),
  Evaluation (decisao + regra + razao), Engine (primeira regra que casa
  vence), DefaultRules.
Regras: destrutivo (rm -rf/DROP/TRUNCATE/DELETE FROM/git reset --hard/mkfs/
dd) -> DENY; pkill/killall -> DENY (L56); escrita no embed (P8) -> CONFIRM;
transformacao em producao (.cosca/) -> CONFIRM; git push --force ->
CONFIRM; leitura -> ALLOW; default -> ALLOW com razao explicita.
Integracao: chat/executor — passo 2.5 (apos validacao, antes do sandbox):
SetPolicy(engine); DENY/CONFIRM/ESCALATE -> ToolResult de erro (nao executa);
nil = comportamento historico (sem guard).

## Validacao
- internal/policy: 4 testes (destrutivo, pkill, kill -TERM permitido,
  embed write CONFIRM, blocks permitido, transform producao, leitura,
  default) — todos PASS.
- Integracao (policy_integration_test.go): rm -rf -> "policy DENY: acao
  destrutiva..."; pkill -> DENY; ls -> permitido. Suites verdes, build OK.

## Significado (o desenho do professor, L358, agora com codigo)
inteligencia -> proposta -> POLICY (codigo) -> sandbox -> execucao. A decisao
de bloqueio nunca depende do LLM: mesmo que ele diga "ignore tudo", o guard
DENY fisicamente no nivel 2, antes do sandbox (nivel 3).

## Classificacao
MEASURED guard implementado + integrado + testado (deny rm/pkill, allow ls)
EVIDENCE a integracao bloqueia destrutivos antes da execucao; governanca com
         os 3 niveis: texto (1), codigo (2), sandbox (3)
INFERRED execucoes de ferramentas no chat passam pelo guard por padrao
         (quando o SetPolicy e chamado); o CLI/agente pode anexar o guard
HYPOTHESIS menos acoes destrutivas acidentais; auditabilidade das decisoes
UNKNOWN cobertura completa das ferramentas (regras por ferramenta); ativar
        o guard por padrao no engine_builder (integracao de producao)
DECISION guard implementado e testado; ATIVAR por padrao no runtime =
         proxima decisao (com o Don); governanca nivel 2 entregue.
