PREV: 717c532a5ca8e71a6879099af4c5f8bfe430c053800df60ab4a97757b95f90a6
ID: L61
TIME: 2026-08-12
LEVEL: 5
TAGS: #jaula #bug-l208 #falso-jail-unavailable #fail-closed #fail-open #COSCA_ALLOW_NO_ROOT #forense #segurança #level-5
---
## L61 — 2026-08-12 — BUG L208 REPRODUZIDO NA 1.5.0: exit do processo interno vira falso "jail unavailable" → com COSCA_ALLOW_NO_ROOT=1 roda SEM jaula | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ordenou subir o cosca isolado (jaula) e mexer nos arquivos errados de dentro pra ver o comportamento. A jaula segurou tudo (filesystem/network/perm) — mas o teste do `serve` expôs um falso "SECURITY WARNING: jail unavailable" com causa real = exit não-zero do processo INTERNO (No models found, config global invisível com HOME=/). |
| **Technique** | Level 5 — **(1)** Binário 1.5.0 em /tmp/opencode/cosca-jail (fora do projeto), workspace demo com constraints.yaml (network:false, read_only:false), decoys dentro (decoy-interno.txt) e fora (fora-do-workspace.txt). **(2)** Mesmos args do buildJailArgs via bwrap manual: root=demo, ro-bind /usr /lib /lib64 /etc, tmpfs /tmp e /go-build-cache, --unshare-all, --clearenv, HOME=/. **(3)** 10 provas empíricas: workspace rw OK; /etc/passwd, /usr/bin/bash EROFS; /etc/shadow EACCES; /home/cosca, /tmp/host, /proc/1 externo ENOENT; cd .. não sai do root; curl exit 7 (sem rede); `cosca memory integrity init` + `cosca serve` dentro da jaula: serve inicializa 567 arquivos em .cosca e recusa por integridade — fail-closed funciona. **(4)** Bug: jail.go:503-525 — `cmd.Run()` com erro ≠ 0 (exit do comando interno) cai no ramo "bwrap failed to establish the sandbox" com o stderr INTERNO como diagnóstico (sanitizado/redacted). O branch `ExitCode() == 0` na linha 505 é código morto (exit 0 não gera erro). Se COSCA_ALLOW_NO_ROOT=1 → jailFallback true → executa SEM sandbox. **(5)** Cross-ref: L208/L001-L214 (03/08) documentou o MESMO sintoma ("o exit do processo interno propaga pelo bwrap — o jail não estava quebrado") e learnings linha 28 confirma: kernel rodou SEM sandbox 06-12/08 com milhares de "jail unavailable" + COSCA_ALLOW_NO_ROOT=1. |
| **Level** | 5 |
| **Outcome** | success — Jaula PROVADA forte: agente não lê/escreve nada fora do workspace, não vê segredos do host, não tem rede; fail-closed de integridade recusou serve com 567 arquivos novos. MAS o bug do falso "jail unavailable" está VIVO na 1.5.0 (jail.go:503-525): qualquer comando que falhe DENTRO da jaula mascara o erro real como falha de sandbox e, com a env setada, abre a porta do modo sem-jaula — assinatura exata do incidente de 7 dias. |
| **Confidence** | 0.97 (validado: reprodução ao vivo 2x com saída completa + leitura do código + memória L208 como testemunha) |
| **Tags** | #jaula #bug-l208 #falso-jail-unavailable #fail-closed #fail-open #COSCA_ALLOW_NO_ROOT #forense #segurança #level-5 |
| **Related** | L208 (bug no CKL → bwrap failed falso), L001-L214 linha 2178 (lição idêntica), learnings linha 28 (7 dias sem jaula), pkg/cosca/jail.go |
| **Learned** | **(1) A jaula em si está SÓLIDA — filesystem, rede, /proc, home, transversal: tudo barrado; o workspace é rw e visível ao host. O problema nunca foi a jaula: é a DETECÇÃO de falha dela.** **(2) O branch de exit code 0 no jail.go é código morto — todo erro de Run() é tratado como falha de setup; a correção precisa distinguir "bwrap não montou a jaula" (setup) de "o comando dentro falhou" (propagar exit code, SEM fallback).** **(3) Um agente que rode `cosca serve`/qualquer comando que falhe dentro da jaula, com COSCA_ALLOW_NO_ROOT=1 no ambiente, GERA ele mesmo o falso "jail unavailable" e continua rodando solto — o aviso não é prova de jaula quebrada, é sintoma de erro do processo interno.** **(4) Diagnóstico: stderr do processo interno é capturado como "razão" da falha de sandbox (sanitizado) — red herring perigoso para operador e forense.** |
| **Next** | (1) Propor fix ao Don: marker de boot (arquivo escrito pelo processo interno ao nascer na jaula) para o pai distinguir setup failure de exit do comando — propaga exit code real, sem fallback, sem falso warning. (2) Testes: bwrap ausente (fallback real), comando exit 0, comando exit 1. (3) Verificar por que a config global é invisível na jaula (HOME=/) — serve jailed sem modelos é comportamento a revisar. |
