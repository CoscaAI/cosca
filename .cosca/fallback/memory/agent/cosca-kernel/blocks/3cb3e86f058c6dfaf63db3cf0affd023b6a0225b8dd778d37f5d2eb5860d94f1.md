# L360 — Execucao da limpeza historica (autorizada; backup + marcacao)

## Pre-requisitos cumpridos
- Backup: .cosca/backup/knowledge-pre-cleanup-20260817-130234.db (VACUUM INTO,
  consistente com WAL) — o teste ABORTA se nao houver backup.
- Snapshot L357, dry-run L359 (expectativa ~9.674) — base de comparacao.

## Operacao (reversivel via backup; nada de conteudo perdido)
1. Migracao leve (idempotente): chunks.is_trivial INTEGER DEFAULT 0,
   chunks.dedup_of TEXT DEFAULT ''.
2. Marcacao: 8.734 chunks triviais (T1-T4); 3.248 duplicatas de conteudo
   (dedup_of -> canonico). Os CHUNKS permanecem (provenance intacta).
3. Remocao dos VETORES de lixo via store.Delete (1.495 triviais + 3.248
   duplicados = 4.743) — vetores sao DERIVADOS (re-embedavel); version bump
   do indice automatico.

## Resultados (validados)
vetores: 14.449 -> 9.706 (-4.743, -32.8%)  [dry-run L359: ~9.674 — a
diferenca ~32 e o db crescer entre as medicoes; consistente]
recall pos-limpeza (spot-check 100 queries, deterministico):
  int8 jaccard = 0.9357  (dry-run: 0.9285)
  int16 jaccard = 0.9902 (dry-run: 0.9928)
estado final: 9.706 vetores, 8.734 chunks marcados is_trivial, 3.248
marcados dedup_of. Suites verdes (vector/indice/dot; build/vet).

## Observacoes de producao
O `cosca serve` ativo pode manter o indice em memoria ANTIGO (o version
bump e por processo) — recomendado reiniciar o serve para refletir a
limpeza (CARRO_PROTOCOL). Nao executado (nao autorizado explicitamente).

## Classificacao
MEASURED 9.706 vetores finais; recall pos-limpeza 0.936/0.990
EVIDENCE a execucao real bate com o dry-run (32.8% vs 32.9% esperado);
         int16 quase-oracle confirmado no corpus limpo REAL
INFERRED o indice agora e 33% menor e o top-10 tem ~10 conteudos unicos
         (diversidade 4.91->10, L359); o re-embed futuro so processa
         os 48.621 orfaos NAO triviais e NAO duplicados
HYPOTHESIS a latencia de producao cai (~67% do scan); o RAG melhora a
           diversidade (a medir em uso)
UNKNOWN impacto em producao durante a transicao; quando o serve reflete
        a limpeza (apos reinicio)
DECISION limpeza executada e validada; PARAR — proximas decisoes:
         (1) reiniciar o serve; (2) int16 como default pos-limpeza;
         (3) re-embed dos orfaos limpos — todas aguardando o Don.
