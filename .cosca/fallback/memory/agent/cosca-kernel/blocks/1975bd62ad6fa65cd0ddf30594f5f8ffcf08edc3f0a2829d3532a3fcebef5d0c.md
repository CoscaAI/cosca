# L389 — H1 respondido (REFUTADO: nao e bias do FTS) + re-medicao do #3 (81%)

## status: DECISION (read-only; um experimento por hipotese — H1)

## CORRECAO P13 (a licao mais importante)
O experimento #3 (L388) e a 1a rodada do H1 rodaram COM O VETOR DESLIGADO:
o teste usava knowledge.New direto SEM registrar os providers
(ollama.Register()/local.Register() — so o bootstrap/serve registra) ->
"unknown provider: ollama" -> embedFunc nil -> searchVector nunca roda.
O recall 39% do #3 era o FTS vs FTS+pool (NAO o bounded real). O voto de
REJECT foi baseado em medicao invalida. LICAO: qualquer benchmark de busca
precisa do mesmo registro de providers do serve.

## H1 (a pergunta do professor): "Com os mesmos candidatos, o merge
## altera excessivamente a ordem?" — RESPOSTA: SIM, mas NAO a favor do FTS
Medido (20 queries, corpus real, vetor ligado):
- merge∩FTS: 2.5/10 (26%) — o merge preserva pouco do top-10 do FTS
- merge∩vec: 4.2/10 (42%) — preserva mais do vetor
- merge src=vector: 4.2/10 vs src=fts: 3.9/10 — o VETOR domina o merge
- FTS isolado: 3.9/10 (a fonte FRACA — acha pouco)
- Vector isolado: 7.5/10 (a fonte RICA)
O Ranker (BM25 0.25 + vec 0.35 + graph 0.20 + fresh 0.10 + pop 0.10,
NormalizeScores) REMIXA os top-10 de ambos radicalmente. O merge NAO
favorece o FTS — o vetor e a fonte dominante. H1 = REFUTADO (o bias
pró-FTS nao existe; o "FTS-first" do top-10 observado no #3 era o vetor
desligado).

## Re-medicao do #3 (a correcao — P13: inconclusivo -> medir de novo)
Com o vetor LIGADO: recall@10 bounded vs full = 81.0% (8.1/10) — o
bounded REAL reproduz a maior parte do full. Speedup 2.86x (0.754ms vs
0.263ms — marginal). As 2 queries com recall 0.0 (sqlite vector index
query plan, merkle tree epoch files): o FTS VAZIO -> o bounded cai no
POOL DE RECENCIA (CandidatePool=250) -> resultados por recencia.
VEREDITO FINAL: o REJECT do #3 SE MANTEM (a regressao funcional existe —
2/20 queries com recall 0.0 pelo pool de recencia) — MAS o culpado NAO e
o bounded em si: e o POOL DE RECENCIA quando o FTS nao acha nada. O
bounded sem o pool (fallback para o full scan quando o FTS vazio) seria
~100% de recall. O ganho (0.5ms) nao justifica — a busca ja e <1ms.

## Classificacao
MEASURED H1 (26%/42%/4.2 vs 3.9/7.5) e #3 re-medido (81%, 2.86x)
EVIDENCE o vetor desligado no #3 (unknown provider: ollama); o pool de
  recencia causa o recall 0.0 quando o FTS vazio
INFERRED o bounded sem o pool seria ~100% seguro (a testar se o Don quiser)
HYPOTHESIS H2 (triviais poluem o top-10?) e H3 (recall semantico real) —
  experimentos separados, com o Don
UNKNOWN o recall de producao real (a medir com o provider do serve)
DECISION H1 REFUTADO; #3 REJECT mantido (pool de recencia); licao P13
  registrada; H2 na proxima fase
