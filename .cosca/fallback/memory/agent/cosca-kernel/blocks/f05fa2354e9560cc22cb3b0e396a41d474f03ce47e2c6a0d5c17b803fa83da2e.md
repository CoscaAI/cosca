PREV: bf6db94613a4cf63569732476d6d4ee543fc5d620592d87a5ac06c660f4225c4
ID: L138
TIME: 2026-08-13
LEVEL: 4
TAGS: #ablacao #metacognicao #alone-vs-managed #estatistica #mediana #iqr #harness #deterministico #level-4
---
## L138 — 2026-08-13 — Protocolo + harness de ablação da metacognição (alone vs managed) determinístico | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim, quero e com muito cuidado. Da o seu melhor pra nunca ter mais problema" — montar o experimento de ablação (Cosca sozinho vs Cosca+gerente) com rigor máximo. |
| **Technique** | Level 4 — **(1)** `ABLATION_PROTOCOL.md`: desenho experimental preciso — pergunta falsificável ("a metacognição muda o caminho?"), UMA variável (memória: empty history vs full history), instrumento (oráculo + solver + DiscoveryMetrics), N≥5 trials, **estatística robusta a heavy-tail (mediana + IQR, nunca média)**, controles/confundidores (pré-treinamento do LLM não se zera, não-determinismo, oráculo com bug, oráculo mutável, adaptou-vs-recomeçou) e limites honestos. **(2)** `ablation.go`: harness determinístico — `Verifier`/`Solver` interfaces, `RunTrial` (unidade atômica), `RunCondition` (N trials → mediana/IQR), `RunAblation` (same solver com empty vs full history = a ÚNICA variável), `ManagedWins`. **(3)** Colisão de nome pega no build: já existia `Oracle` struct (config) em oracle.go; renomeei a interface para `Verifier` — separação limpa entre o CONFIG do oráculo e o MECANISMO verificador. **(4)** 5 testes determinísticos (RunTrial, budget exhausted, stats median/IQR, ManagedWins, median helpers) + CLI `cosca eval ablation` demo (secret 42: alone 0% vs managed 100%/42 attempts). |
| **Level** | 4 |
| **Outcome** | success — Harness determinístico, testado, smoke E2E via CLI (alone 0% solved, managed 100%/42). O instrumento está separado do sujeito (LLM): o harness é verificável por teste ANTES de plugar o LLM. `go build`/`vet`/testes limpos. |
| **Confidence** | 0.96 (validado: 5 testes + smoke E2E + build/vet limpos) |
| **Tags** | #ablacao #metacognicao #alone-vs-managed #estatistica #mediana #iqr #harness #deterministico #level-4 |
| **Related** | L236 (oracle eval), L235 (spec), L234 (ledger — alvo futuro), conversa do Don (desenho experimental) |
| **Learned** | **(1) O desenho de ablação correto tem UMA variável: memória (empty vs full history). O "gerente" é operacionalizado como memória das tentativas — o núcleo mensurável da metacognição — e o harness roda o MESMO solver com empty vs full history, garantindo que nada mais muda.** **(2) LLM é heavy-tailed e não-determinístico → estatística é mediana+IQR, NUNCA média; e o critério de evidência é mediana(managed)<mediana(alone) com IQR não-sobreposto, não teste-t (N pequeno + não-normal).** **(3) O harness é SEPARADO do sujeito: `RunTrial`/`RunCondition`/`RunAblation` são determinísticos e testáveis com um solver falso ANTES de plugar o LLM — isso é o que torna o experimento "à prova de problema": você prova o instrumento sem o sujeito.** **(4) O confundidor mais traiçoeiro é o pré-treinamento do LLM: "duas instâncias sem memória" ainda compartilham os PESOS do modelo — não se "zera" o pré-treinamento; a mitigação é problema sintético + registrar a limitação.** **(5) Naming collision Oracle(struct) vs Oracle(interface) num mesmo pacote → pego no build; a separação config-vs-mecanismo (Oracle=config, Verifier=interface) é a divisão certa. |
| **Next** | (1) PLUGAR O SUJEITO REAL: implementar `LLMSolver` (propor candidato via `cosca run`/pipeline com o histórico no prompt) — este é o passo de maior cuidado. (2) Montar o problema-alvo real (o `internal/ledger` como caso B). (3) Rodar N≥5 trials reais e interpretar com mediana+IQR. (4) F4/F5 (solution_class, existence). |
