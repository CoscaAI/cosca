PREV: 5d0afe2803cdf163b0a109c02654d84f92a116ff5b83fbb2fdaad24affffbdd7
ID: L5
TIME: 2026-08-09
LEVEL: 4
TAGS: #ollama #embedding #api-embed #endpoint #v1 #404 #batch-embedding #local-first #gpu #rocm #reindex #level-4
---
## L5 — 2026-08-09 — Fix crítico: ollama /v1/api/embed → /api/embed + batch embedding + local-first | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's orders: reindex travado → diagnosticar → "faca isso" (corrigir). Bug: `embedding.base_url: http://127.0.0.1:11435/v1` fazia o provider ollama concatenar `/v1` + `/api/embed` = `/v1/api/embed` → 404 no ollama (endpoint nativo é `/api/embed`, sem `/v1`). Mesmo bug no chat (`/v1/api/chat`) e tags (`/v1/api/tags`). |
| **Technique** | Level 4 — Diagnóstico via journal (`journalctl --user -u ollama` revelou 404s em massa no `/v1/api/embed`) + leitura direta do código (`internal/providers/ollama/ollama.go:200`: `endpoint := p.cfg.BaseURL + "/api/embed"`). Fix delegado ao cosca-backend: `strings.TrimSuffix(baseURL, "/v1")` em 3 pontos (embed, chat, tags) + batch embedding (50 textos/request em vez de 1). Rebuild + `make install` + restart runtime. Reindex rodando com 200 ✅. |
| **Level** | 4 |
| **Outcome** | success — Commit `dcee135` (15 arquivos, +405/-112). Endpoint corrigido: `/api/embed` → 200 (antes 404). Journal limpo. knowledge.db 90MB+ com vetores reais. GPU RX 6700 XT (ROCm) sendo usada para embeddings. Sessão paralela do Don (prompt-history 50 msgs) também contribuiu: local-first defaults (ollama/qwen), gate COSCA_ENABLE_EXTERNAL_PROVIDERS, graph SQL persistence, FTS batch rebuild, search force vector layer. |
| **Confidence** | 0.97 (validado: journal 200, knowledge stats 16914+ entries, build/tests limpos) |
| **Tags** | #ollama #embedding #api-embed #endpoint #v1 #404 #batch-embedding #local-first #gpu #rocm #reindex #level-4 |
| **Related** | L169 (DeerFlow+Argo+Crossplane), L168 (OTel+FGA+Dapr), L163 (vector search), L103 (provider openai vs ollama), L62 (provider ollama bug endpoint) |
| **Learned** | **(1) O embedding NUNCA funcionou com `base_url` contendo `/v1`.** O provider ollama concatenava `base_url + "/api/embed"` — se o usuário segue a convenção OpenAI (`/v1` no base_url), o endpoint vira `/v1/api/embed` que não existe no ollama. O fix é trivial: `TrimSuffix(baseURL, "/v1")` antes de concatenar o path nativo. **(2) O mesmo bug afetava o chat provider (`/v1/api/chat`) e a listagem de modelos (`/v1/api/tags`)** — mesma causa, mesma correção. **(3) Ollama `/api/embed` suporta batch nativo: `{"input": ["texto1", "texto2", ...]}` retorna `{"embeddings": [[...], [...]]}`** — reduz chamadas HTTP em 50x. **(4) Journal do ollama (`journalctl --user -u ollama`) foi a ferramenta de diagnóstico decisiva: mostrou os 404s em tempo real que o código silenciava.** **(5) `cosca knowledge index` depende do embedding provider configurado no `config.yaml` — sem o fix, a indexação gerava DB sem vetores (só FTS5).** **(6) O provider `local` vs `ollama` no registro de embedding: o `local` tem prioridade 100 (maior que ollama 20) e usa TF-IDF como fallback — por isso o sistema não crashava, mas também não usava GPU.** **(7) Sessões paralelas do OpenCode compartilham o mesmo workspace: alterações da sessão anterior (lazy providers, local-first, graph SQL) estavam unstaged e foram commitadas junto com o fix.** |
| **Next** | (1) `make install` após commit para embed sincronizar. (2) Monitorar reindex até completar (~28K arquivos). (3) Testar cosca-chat com o cérebro funcional: "acessar o cérebro" não deve mais falhar. |
