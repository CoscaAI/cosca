PREV: d020669a09595f2863bba7d7500bdcb6cf7410540f1b78f34add1fe69c87d037
ID: L293
TIME: 2026-08-16
LEVEL: 5
TAGS: #hft #bot #dois-cerebros #kernel-rapido #semantic-vectors #node-edge #grafo #operacoes #latencia #microssegundos #arquitetura #level-5
---
## L293 — 2026-08-16 — O BOT HFT: arquitetura de dois cérebros (kernel lento + bot µs) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don: "dar atenção HFT pra ele se tornar melhor que qualquer um visto" — construir o bot trader cognitivo de alta frequência no cosca-trader. |
| **Technique** | Level 5 — **(1)** ARQUITETURA DE DOIS CÉREBROS: kernel (chat/LLM, estratégico, segundos) + bot (memória pura, tático, microssegundos). O kernel COMPILA conhecimento; o bot EXECUTA. O bot NUNCA chama LLM no caminho quente. **(2)** `internal/bot/`: `semantic.go` (vetores semânticos dedicados — feature hashing determinístico + `Enrich()` pra embedding real nomic-embed-text), `graph.go` (grafo node-edge: símbolo→estratégia→sinal→ação→regime), `bot.go` (operações pré-compiladas + `Execute(cmd)` em 2 caminhos: exato/mapa e semântico/vetor). **(3)** Benchmark provou: load 40µs · execute exato 631ns · execute semântico 6µs · busca 5µs. |
| **Level** | 5 |
| **Outcome** | success — bot HFT operacional e provado (benchmark µs), integrado ao domínio real (6 estratégias, 5 símbolos, gate macro). |
| **Confidence** | 0.95 (validado: benchmark real no Ryzen 7 5700X3D, build+vet+testes verdes) |
| **Tags** | #hft #bot #dois-cerebros #kernel-rapido #semantic-vectors #node-edge #grafo #operacoes #latencia #microssegundos #arquitetura #level-5 |
| **Related** | L148 (cosca-node observabilidade), L147 (a mina self-evolution), L145 (Hermes self-evolution), cosca-trader internal/bot |
| **Learned** | **(1) O segredo da velocidade é a DIVISÃO COMPILADO vs INTERPRETADO: o LLM (interpretador, lento) compila conhecimento no grafo/vetores; o bot (compilado, rápido) consulta em MEMÓRIA — µs, não segundos.** **(2) Para espaços vetoriais DEDICADOS pequenos (<1000 conceitos), varredura linear com produto escalar é MAIS rápida que HNSW (sem overhead de índice, cache-friendly).** **(3) Feature hashing (n-gramas → bucket com sinal) é o fallback de embedding determinístico — sem rede, µs, captura sobreposição de tokens; embedding real (nomic-embed-text) entra via `Enrich()` só no startup, NUNCA no hot path.** **(4) Dois caminhos de execução: exato (mapa, sub-µs) + semântico (vetor→conceito, µs) — comandos exatos são instantâneos, comandos fuzzy ainda são rápidos.** |
| **Next** | (1) Integrar o bot ao chat (kernel orienta o bot: "start ema-cross on DOGE" → executa). (2) Enriquecer vetores com embedding real em produção. (3) Considerar o padrão de dois cérebros para outros domínios (não só trading). |
