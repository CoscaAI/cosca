PREV: 05539cefaa9af6510d672974d2ef45d37ca7647eb211d2eb249b69f698ded1da
ID: L336
TIME: 2026-08-16
LEVEL: 5
TAGS: #protocolo #graph #knowledge-graph #24-protocolos #documentacao #jaula #armadilhas #level-5
---
## L336 — 2026-08-16 — GRAPH_PROTOCOL.md criado — o 24º protocolo da casa, consolidando as lições do L335 (adapter morto, export que fingia, --setenv varrido pelo --clearenv) num mapa operacional de navegação do grafo | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | "crie protocolo graph" |
| **Technique** | Formato canônico dos protocolos (versão/status/dono/criado/propósito + seções numeradas + tabelas + runbook) + integração nos 2 mapas (DESPERTAR.md referências + COSCA_PROTOCOL.md §4) |
| **Level** | 5 |
| **Outcome** | success — GRAPH_PROTOCOL.md criado, DESPERTAR atualizado, mapa 23 → 24 protocolos |
| **Confidence** | 0.97 |
| **Tags** | #protocolo #graph #knowledge-graph #24-protocolos #documentacao #jaula #armadilhas |
| **Related** | L335 (conserto do graph — o conteúdo do protocolo), L332 (limpeza DB), KNOWLEDGE_PROTOCOL (busca) |
| **Learned** | **O PROTOCOLO COBRE 7 SEÇÕES:** (1) o que é o grafo — SQLite é a fonte da verdade, memória é projeção; (2) comandos — graph show/stats/query/export/evidence/link; (3) modelo de dados — entities (id UUID, entity_type, name, path) + relationships (source_id, target_id, rel_type, weight); (4) arquitetura — CLI → NewGraph(dir) → newGraphAdapter → modernc/sqlite (NUNCA "sqlite3"/mattn), NUNCA graph.New() direto no CLI; (5) armadilhas da jaula em tabela — caminho absoluto não existe na bolha (reescrever via COSCA_PROJECT_DIR), --setenv DEPOIS do --clearenv, stderr engolido pelo bwrap (debug via arquivo no workspace), SilenceErrors no root; (6) manutenção — testes com DB fake em t.TempDir() (nunca path absoluto), teste real do CLI pós-mudança (a jaula engole erros que o unitário não vê); (7) relação com outros protocolos. **PROCESSO:** o DESPERTAR.md referencia os protocolos em ordem, o COSCA_PROTOCOL.md §4 tem o mapa com contagem por categoria (Operação 8 → 9 com GRAPH). **ARMADILHA DA CHAIN (reincidente):** o append da chain.dat NÃO garante newline final — a linha L334 não tinha \n e o L335 colou nela; SEMPRE verificar o fim do arquivo antes de append (tail -1 | xxd | tail -1) ou usar append com \n explícito via python. |
| **Next** | Retomar o daemon TTS (RTF 2.2x em thread do HTTP handler vs 0.12x main thread — testar warmup na main thread no startup) e seguir para Fase C (vídeo generativo Wan2.1 1.3B / LTX-Video 2B). |
