PREV: 21a209cff32130094d03409504223794a7187aab2cda04cda2655795316a75bd
ID: L131
TIME: 2026-08-13
LEVEL: 4
TAGS: #mineracao #vscode-go #gopls #lsp #delve #dap #helper-process #ipc #test-runner #command-factory #telemetria #patterns #level-4
---
## L131 — 2026-08-13 — Mineração golang/vscode-go: 26 padrões extraídos (LSP lifecycle + helper process + debug/test) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "agora quero que minere: https://github.com/golang/vscode-go.git" — minerar a extensão VS Code para Go (gopls + Delve + go test) e extrair padrões reutilizáveis. |
| **Technique** | Level 4 — **(1)** Clone raso + 3 agentes explore em paralelo (lifecycle LSP, helper vscgo+IPC+tools, debug/test/commands), cobrindo 134 .ts + 74 .go. **(2)** Consolidação em `knowledge/patterns/vscode-go-extension-patterns.md` (26 padrões em 3 seções: A=lifecycle LSP, B=helper process+IPC+tools, C=debug/test/commands) com mapeamento explícito "como se aplica ao Cosca". **(3)** Registro no INDEX (Enterprise Platform Patterns → 8). **(4)** Descoberta-chave: o helper `vscgo` é MENOR que o esperado — só 5 subcomandos (`inc_counters`, `dump-pprof`, `serve-pprof`, `version`, `help`); resolução de módulos fica no TS orquestrando `go` CLI. |
| **Level** | 4 |
| **Outcome** | success — Documento de padrões criado (0.93) + INDEX atualizado (20 padrões). 10 padrões priorizados mapeados ao Cosca: RuntimeManager (orquestrador), circuit breaker, provider swap, helper cosca-indexer refatorado, IPC 3 modos, catálogo versionado de providers, dlv-dap proxy no DebugStart, TestTool dual-parser, CommandFactory de bindings, telemetria batch. |
| **Confidence** | 0.93 (validado: 3 agentes independentes com evidência de arquivos/linhas reais, padrões cruzados consistentes) |
| **Tags** | #mineracao #vscode-go #gopls #lsp #delve #dap #helper-process #ipc #test-runner #command-factory #telemetria #patterns #level-4 |
| **Related** | L209 (padrão produto), L202 (cosca-desktop Wails), L191 (evals harness), L164-L167 (minerações Backstage/Argo/Temporal), L190 (esteira real) |
| **Learned** | **(1) A extensão vscode-go é a referência canônica de "gestão de ciclo de vida de um processo servidor dependente a partir de um cliente de UI" — resolve exatamente o que o Cosca precisa para serve/runtime/ollama: orquestrador central com mutex (A1), circuit breaker com contador de crash + remediação (A3), provider swap com dispose (A8).** **(2) O helper `vscgo` segue o padrão "main fino + internal importável" e é o ponto de extensão para lógica que não pertence ao servidor nem ao debug adapter — o `cosca-indexer` tem a MESMA função mas está monolítico (lógica de walk no main, sem dispatcher de subcomandos); refatorar para `cmd/cosca-indexer/main.go` fino + `internal/indexer` com surface `index/query/search/stats/version`.** **(3) O IPC de 3 modos (stream line-framed, one-shot JSON, handshake+HTTP) é o contrato que falta no cosca-indexer — hoje o desktop não consulta o indexer incrementalmente.** **(4) O `dlv-dap` proxy (spawnar `dlv dap` + proxy de socket, parseando só `Content-Length` framing) é DRAMATICAMENTE mais simples que reimplementar DAP — o DebugStart do desktop deveria adotar em vez do legacy adapter.** **(5) O dual-parser de teste (`go test -json` + fallback regex) resolve a classe de bug de "parse de output" que a esteira enfrentará — sempre `-json` + `-test.fullpath=true`.** **(6) A gestão de ferramentas com version tiers (gopls pinado por versão do Go) mapeia para providers/modelos do Cosca (pinar modelo por capacidade do engine) — falta "missing/outdated" + autoUpdate persistente.** **(7) Metodologia de mineração confirmada de novo (clone raso + N agentes paralelos com escopo disjunto + consolidação com mapeamento ao Cosca): 3 agentes entregaram achados não-redundantes e consistentes entre si. |
| **Next** | (1) Refatorar `cosca-indexer` (main fino + dispatcher de subcomandos + JSON-lines no stdout). (2) Adotar dlv-dap proxy no DebugStart do desktop. (3) Aplicar circuit breaker ao runtime/ollama (N crashes → parar + diagnosticar). (4) Adicionar "missing/outdated" + version tiers ao catálogo de providers/modelos. |
