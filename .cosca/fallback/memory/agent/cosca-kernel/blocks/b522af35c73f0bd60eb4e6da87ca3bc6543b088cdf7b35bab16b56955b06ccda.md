PREV: 214e93ca2445ed2feca063c9006531039f99c6b541696878d4a7f71f693a8847
ID: L128
TIME: 2026-08-13
LEVEL: 4
TAGS: #esteira #pipeline #eval #auditoria #desktop #executor #ollama #loadchatenv #bugfix #nil-json #data-race #symlink #rce #level-4
---
## L128 — 2026-08-13 — Teste completo da esteira + auditoria cosca-desktop: 2 bugs reais no executor corrigidos | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "faz um teste completo na esteira auditando cosca desktop" — provar a esteira end-to-end (mecânica + planejamento + execução) e auditar o cosca-desktop como payload. |
| **Technique** | Level 4 — **(1)** Prova mecânica: `cosca pipeline-test` (MockRunner, sem LLM) — Plan→StepRunner→Recovery→Reconciler→Checkpoint→History: 5/5 tasks, crash recovery 5/5, hash chain verified, autonomia 100%. **(2)** Prova de wiring real: `cosca eval run smoke` (buildPipelineWiring) — 2/3 casos passam (planejamento 19 e 8 tasks); o caso `execute-adhoc-task` falhou com `executor_failed`. **(3)** Diagnóstico do executor quebrado: (a) `.env` com `OLLAMA_HOST=http://127.0.0.1:11435` (stale — ollama real em 11434); (b) `loadChatEnv` (internal/cli/chat.go) só propagava provider `openai` para env vars, NUNCA `ollama` — logo o provider ollama caía no default "llama3" (não instalado). **Correções**: `.env` 11435→11434 + `COSCA_OLLAMA_MODEL`; e `loadChatEnv` ganhou branch `ollama` (propaga model→COSCA_OLLAMA_MODEL, base_url→OLLAMA_HOST). Rebuild `make build-dev`. Pós-fix: `cosca run "OK"` responde (33/2 tokens). **(4)** Auditoria cosca-desktop em 2 agentes explore paralelos (segurança + qualidade): 0 CRITICAL, 4 MEDIUM segurança (symlink traversal, DebugStart RCE, exfiltração de chave via base_url, git checkout injection) + 4 HIGH qualidade (2 data races, nil→JSON null quebrando React, ErrorBoundary ausente). |
| **Level** | 4 |
| **Outcome** | success — Esteira provada mecânica (5/5) e planejamento (2/2); execução consertada após 2 bugs. 2 bugs reais no executor corrigidos (código + .env). Auditoria do desktop entregue com 8 achados de severidade média/alta. Fricção registrada: auditar projeto externo exige `cosca memory integrity init` e `cosca run` falha silencioso (exit 1) em dir não inicializado. |
| **Confidence** | 0.93 (validado: pipeline-test 5/5, eval 2/3→executor OK pós-fix, cosca run com resposta real do ollama) |
| **Tags** | #esteira #pipeline #eval #auditoria #desktop #executor #ollama #loadchatenv #bugfix #nil-json #data-race #symlink #rce #level-4 |
| **Related** | L226 (serve religado L3), L225 (cofre), L202 (cosca-desktop Wails), L191 (evals harness), L190 (esteira real), L186 (chain git-anchored) |
| **Learned** | **(1) A esteira tem 3 camadas de prova: `pipeline-test` (mecânica, MockRunner) → `eval run` (wiring real) → `workflow run` (execução completa).** Testar só uma não basta — o executor quebrado só apareceu no `eval run`, não no `pipeline-test`. **(2) BUG DE CLASSE: `loadChatEnv` só mapeava provider `openai` para env vars; o provider `ollama` (declarado no config como `provider.name: ollama`) caía no default "llama3" NÃO instalado → `executor_failed` em TODA execução LLM.** A correção é simétrica: qualquer provider local declarado no config precisa ter seu model/url propagado às env vars que o factory lê. **(3) `.env` stale é armadilha silenciosa: `OLLAMA_HOST=11435` (porta antiga) vs ollama real em 11434 — o serviço sobe healthy, o capability reporta L3, mas a execução LLM falha.** Validação de execução exige um `cosca run "OK"` real, não só health check. **(4) O DoD baseado em evidência (L190) funciona como projetado: um prompt de ANÁLISE (sem code/build/test) falha `code_exists/builds/tests_pass` — não é defeito, é o gate recusando "true vazio".** O eval `execute-adhoc-task` com prompt de análise é caso mal calibrado, não bug da esteira. **(5) Auditar projeto externo via esteira tem fricção: (a) `cosca memory integrity init` obrigatório (gate fail-closed), (b) `cosca run` em dir sem `.cosca/config.yaml` falha com exit 1 e ZERO output (stdout+stderr vazios) — bug de UX a investigar. **(6) cosca-desktop tem os MESMOS bug de família: nil slices Go → JSON null → crash React (H3, 5 casos) e ErrorBoundary ausente (React 19 desmonta árvore) — o L209 já codificou essas lições, mas o desktop as violou. |
| **Next** | (1) Commit das correções (loadChatEnv + .env) e do L227. (2) Corrigir os 8 achados do desktop (prioridade: M1 symlink, M3 exfiltração, H1/H2 data races, H3 nil→null, H4 ErrorBoundary). (3) Investigar `cosca run` exit-1 silencioso em dir externo. (4) Recalibrar o eval `execute-adhoc-task` para prompt de code-gen (ou aceitar DoD parcial em análise). |
