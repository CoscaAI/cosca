PREV: 67732aaa4c2bc9ca50961d39d812dbfca5ba1e0cae8af52d4b0f3ce51c298f4a
ID: L161
TIME: 2026-08-13
LEVEL: 4
TAGS: #cosca-trader #trading #binance #b3 #crypto #desktop #wails #architecture #github-research #regra-de-negocio #level-4
---
## L161 — 2026-08-13 — cosca-trader: discovery local + reconhecimento GitHub (arquitetura e plano de fases propostos) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: criar projeto novo `cosca-trader` em `~/Documents/projects/` — plataforma de trade profissional (desktop, multiplataforma) com IA cognitiva. Operar cripto (Binance PRIMEIRO; Coinbase/CoinEx depois da plataforma funcionando) e bolsa brasileira (B3 — depois). Requisitos: gráficos elegantes, sistema de plugins, OMS completo (ordens/fills/posições/saldos), controle de risco, indicadores, assistente IA, opções com controle de risco, rastreamento total, nível enterprise. Reutilizar padrões do cosca-code. Regra de negócio imposta pelo Don: sempre que faltar conhecimento, buscar no GitHub com o padrão `https://github.com/search?q=($search)&type=repositories&s=stars&o=desc`. |
| **Technique** | Level 4 — **(1)** Discovery local: `~/Documents/projects/` tem 4 ativos reutilizáveis — `cosca-code` (core headless Go + event bus + sistema de extensões + provider engine + segurança por default, spec §92 em 5 fases), `cosca-node` (event store append-only + time travel + replay + causal), `cosca-neural-link` (loop cognitivo + contratos gRPC/HTTP + A MINA), `cosca-desktop` (Wails v2.14.0). **(2)** Regra de negócio executada via API do GitHub (`api.github.com/search/repositories?q=...&sort=stars&order=desc`) — é o MESMO query ordenado por estrelas do Don, mas devolve JSON limpo; o web search é SPA JS que webfetch não parseia. Limite não-autenticado: 10 req/min, 60/h → sleep 6s entre queries, ~27 queries em 2 rodadas. **(3)** Extração por jq dos campos name/stars/language/description. |
| **Level** | 4 |
| **Outcome** | success — Arquitetura + plano de fases apresentados e aprovados (Binance F1, só desktop Wails, B3 F7). Mapa de referências do ecossistema consolidado. |
| **Confidence** | 0.9 (validado: API respondeu 27 queries reais, discovery local confirmou os 4 projetos reutilizáveis) |
| **Tags** | #cosca-trader #trading #binance #b3 #crypto #desktop #wails #architecture #github-research #regra-de-negocio #level-4 |
| **Related** | L256 (cosca-code §92), L258 (A MINA), L251-L255 (cosca-node/neural-link), L201 (HornFit monorepo cliente) |
| **Learned** | **(1) Regra de negócio do GitHub = API search, não web: `api.github.com/search/repositories` com `sort=stars&order=desc` retorna o ranking do Don em JSON; usar `--data-urlencode` do curl pra lidar com espaços; respeitar o rate limit com sleep.** **(2) Mapa de referências (por estrelas): crypto bot → freqtrade 53k / ccxt 43k / hummingbot 19k; engine algorítmico → Lean 21k / StockSharp 10k; Go nativo → orderbook (matching engine), goex (SDK multi-exchange), ccxt/go-binance, cinar/indicator (indicadores em Go); charts → KLineChart 4k (zero-dep) / TradingView lightweight-charts; IA orquestrada → openai/swarm, n8n 200k, dify 152k; UI → shadcn/ui 121k (React+Vite+Tailwind = nossa stack).** **(3) Achado honesto B3: NÃO existe lib open-source dominante pra operar direto na bolsa brasileira — o caminho real é ponte MetaTrader5 (Genial/Toro/Rico/Clear/XP) ou API do broker; `brapi.dev` (gratuito) cobre cotações/fundamentos.** **(4) Não existe "CCXT em Go" — a camada unificada de exchanges precisa ser construída como um Provider Engine (igual ao cosca-code, mas pra exchanges em vez de LLMs): cada corretora = adapter plugável.** **(5) O mapeamento 1:1 do cosca-code pra trading: event bus → market data/order flow; sistema de extensões → indicadores/estratégias; provider engine → adapters de exchange; event store (cosca-node) → rastreamento total/journal; cognitive AI (neural-link) → assistente de trade.** **(6) shadcn/ui funciona em Vite+React (nossa stack Wails) — o Don citou Next.js/Vercel/AWS, mas para desktop-only esses são tangenciais; os padrões de design (shadcn, dark theme Spotify-style, ag-grid/TanStack p/ tabelas) são o que transfere.** |
| **Next** | (1) F0 cosca-trader: monorepo Go + Wails/React + event bus + event store + SQLite + schema (instrumentos/candles/ordens/posições/journal). (2) F1: adapter Binance + market data em tempo real (WebSocket) + candles no gráfico. (3) F2: OMS completo. (4) F3-F7 conforme o roadmap aprovado. |
