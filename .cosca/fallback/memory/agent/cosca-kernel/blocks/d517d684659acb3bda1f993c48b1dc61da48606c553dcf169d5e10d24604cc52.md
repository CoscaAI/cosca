# L363 — Tie-breaker deterministico implementado (decisao do Don)

## O que entrou (especificacao L351 -> implementacao)
- scoredLess: ordem canonica (score DESC, id ASC para scores EXATAMENTE
  iguais) — o desempate NAO depende mais da ordem de chegada dos workers.
- insertScored: usa scoredLess (janela top-K deterministica em scoreRange/
  scoreRange8/scoreRange16).
- Os 3 merges finais (scoreParallel/scoreParallel8/scoreParallel16):
  sort com (score desc, id asc).
- Sem epsilon: scores distintos tem ordem deterministica por construcao;
  o tie-breaker age apenas em igualdade exata (o caso das duplicatas).

## Validacao
- TestInsertScoredTieBreak: a MESMA janela top-K em 3 ordens de insercao
  (direta, invertida, aleatoria) -> resultado IDENTICO.
- Suites verdes (vector 43.9s, knowledge 33.9s), build/vet OK.
- Binario reinstalado + serve reiniciado (doctor 26/26 ✓); determinismo
  top-10 pos-tie-breaker: 0/20 variacao (consistente; agora GARANTIDO por
  construcao, nao por sorte de agendamento).

## Classificacao
MEASURED tie-breaker testado (3 ordens -> mesmo ranking); suites verdes
EVIDENCE o ranking agora e deterministico por construcao (contrato
         (score desc, id asc)); producao com o binario novo
INFERRED queries com scores empatados devolvem o MESMO ranking em qualquer
         execucao — fim do nao-determinismo do scoreParallel (L349)
HYPOTHESIS resultados de producao estaveis entre execucoes (a monitorar)
UNKNOWN impacto em workloads com muitos empates (provavelmente positivo)
DECISION tie-breaker ATIVO em producao. Dividas tecnicas restantes:
         re-embed (provider real) e governanca (L358) — aguardando o Don.
