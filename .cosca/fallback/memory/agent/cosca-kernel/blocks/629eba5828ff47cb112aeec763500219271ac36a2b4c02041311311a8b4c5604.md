PREV: 3bc763c9f8328336f1e9542630bceaf692dbb7447a6217d1abdee12f16f8ac2b
ID: L110
TIME: 2026-08-13
LEVEL: 3
TAGS: #novelty-detection #memoria-ativa #secao-15 #secao-12 #integracao-grpc #spec-completo #level-3
---
## L110 — 2026-08-13 — Neural Link: novelty detection + memória ativa (seções 15/12) — spec completo | Level 3

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "continue". Fechar os 2 furos restantes do spec do Neural Link: seção 15 (novelty detection — comparar descoberta com o knowledge base) e seção 12 (memória ativa — Store/Search via contrato, não só stats). |
| **Technique** | Level 3 — **(1)** Leitura dos protos para os campos exatos: `KnowledgeService.Search` (query/limit → results com score/title/snippet) e `MemoryService.Store/Search` (type/layer/scope/content/priority → id). **(2)** `contracts`: `SearchKnowledge`, `StoreMemory`, `SearchMemory` como DTOs próprios (não expõe pb types). **(3)** `loop.ClassifyNovelty`: heurística por score — KNOWN (≥0.8), COMBINATION (≥2 hits ≥0.4), DERIVED (1 hit), NOVEL_WITHIN_KNOWLEDGE_BASE (resto). Nunca "novidade global", só relativa à base (seção 15). **(4)** app.go: `NoveltyCheck` (busca + classifica), `PersistDiscovery` (registra na memória), `RecallMemory`. **(5)** 3 bugs de integração descobertos e corrigidos pelos testes de integração: (a) `resolveCoscaBin` pegava `~/.cosca/bin/cosca` ANTIGO (07:12) cujo JSON da ablação serializava `Attempts` como NUMBER, não lista — corrigido para preferir o workspace (`~/Documents/cosca/bin/cosca`); (b) layer de memória "semantic" NÃO existe no Cosca (layers reais: global/workspace/project/session/temp) — trocado por "session"; (c) SearchKnowledge estourava timeout de 5s (knowledge base 11k entidades) — 15s. |
| **Level** | 3 |
| **Outcome** | success — Spec do Neural Link 100% coberto (25 seções). Testes de integração verdes contra o runtime REAL do Cosca: SearchKnowledge (5 hits), StoreMemory (id retornado), Ablation (managed vence). Build+vet+testes verdes, commit `69a9e93`. |
| **Confidence** | 0.9 (validado: 3 testes de integração contra runtime real, unit de novelty, build/vet) |
| **Tags** | #novelty-detection #memoria-ativa #secao-15 #secao-12 #integracao-grpc #spec-completo #level-3 |
| **Related** | L198 (F5), L195 (integração CLI), L190 (runtime), L191 (instalação global) |
| **Learned** | **(1) O binário global `~/.cosca/bin/cosca` está DESATUALIZADO (07:12 vs workspace 07:29) — o formato JSON da ablação mudou entre versões (`Attempts` int → `Attempts` lista); resolver binário pelo WORKSPACE primeiro evita o drift. PENDÊNCIA do Don: `make install` para atualizar o global.** **(2) Os layers de memória do Cosca são global/workspace/project/session/temp — NÃO semantic/episodic/procedural (que são o modelo CONCEITUAL do spec, seção 12); o mapeamento correto é registrar histórico experimental em "session".** **(3) `KnowledgeService.Search` no runtime real responde com score baixo (~0.13) para consultas genéricas — a base tem 11k entidades e o search é por similaridade, não keyword exata; o timeout de 5s é insuficiente, 15s resolve.** **(4) Testes de integração são a rede de segurança real contra drift de contrato: o binário antigo teria passado despercebido sem eles (o parse unitário usava JSON de exemplo novo).** **(5) Novelty honesta = relativa à base acessível, nunca "ninguém no mundo" — o nível máximo é NOVEL_WITHIN_KNOWLEDGE_BASE, e a classificação documenta essa limitação.** |
| **Next** | (1) PENDÊNCIA do workspace: `make install` para atualizar `~/.cosca/bin/cosca` (formato antigo pode afetar outros consumidores). (2) README consolidado do estado final do laboratório (spec 100%). (3) Rotação das API keys pendentes do L187 (ainda em aberto). |
