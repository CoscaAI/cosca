PREV: 13a6b76888605f426145ef3b662af09db3c714771cfcda4b0c3ded2f351b98c4
ID: L185
TIME: 2026-08-14
LEVEL: 5
TAGS: #cosca-trader #frontend #bug-real #infinity #nan #maximumFractionDigits #fmtMoney #range-error #level-5
---
## L185 — 2026-08-14 — cosca-trader: bug REAL do painel corrigido — Infinity/NaN em fmtMoney derrubava o frontend (maximumFractionDigits out of range) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don abriu o painel e reportou: "sem dados, o token eh o que?" + o console mostrou `Uncaught RangeError: maximumFractionDigits value is out of range` no fmtMoney (App.tsx:52) — o painel QUEBROU na primeira navegação. |
| **Technique** | Level 5 — **(1)** O Don usou o token certo (meu-token) e os dados carregaram, mas o painel crashou com RangeError. **(2) CAUSA RAIZ**: `fmtMoney(lastPrice.price, 1)` com `price = Infinity` (tick malformado do SSE) — `Number.isNaN(Infinity)` é FALSE, então o `Math.abs(Infinity).toLocaleString()` lança "maximumFractionDigits value is out of range". **(3)** Também `maximumFractionDigits: digits` com valor fora de 0-20 estoura. **(4) FIX**: `Number.isFinite(n)` (pega NaN E Infinity) + `Math.min(Math.max(Math.trunc(digits),0),20)` em fmtMoney E fmt. **(5)** O 503 do /convergence é esperado (sem --shadow) — o catch trata e o card mostra "inativo". |
| **Level** | 5 |
| **Outcome** | success — commit (fix). Build OK, HMR aplicou no painel ao vivo, HTTP 200 em tudo. O painel não quebra mais com dados malformados. |
| **Confidence** | 0.97 (validado: reproduzido o RangeError, corrigido com isFinite, build + verificação HTTP 200 no painel) |
| **Tags** | #cosca-trader #frontend #bug-real #infinity #nan #maximumFractionDigits #fmtMoney #range-error #level-5 |
| **Related** | L291 (porta fixa 5273), L290 (porta única 24120), L289 (frontend completo) |
| **Learned** | **(1) `Number.isNaN` NÃO pega Infinity — `Infinity` passa por `isNaN` e explode no toLocaleString; a validação correta para valores de display é `Number.isFinite`.** **(2) O primeiro usuário real (o Don) quebrou o painel em segundos com um dado malformado que os testes não cobriam — dados de SSE não têm contrato garantido, o display tem que sobreviver a qualquer valor.** **(3) O HMR do Vite é a ferramenta perfeita para corrigir o painel ao vivo: o Don viu o crash, eu corrigi, e a tela recarregou sem reiniciar nada.** |
| **Next** | (1) O Don navega sem crash. (2) Conectar divergência ao OMS. (3) Testnet. |
