PREV: e47e0c018f03cd65119c636a7cbe3d810e5ba05c1c4ec0f9a592473492afad38
ID: L40
TIME: 2026-08-11
LEVEL: 4
TAGS: #google #adk-go #workflow #edge #route #typed-routing #node #joinnode #concurrency #mining #level-4
---
## L40 — 2026-08-11 — Mining Google adk-go: typed Edge+Route workflow model → internal/workflow additive engine | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don showed the search technique with https://github.com/orgs/google/repositories?q=ai (46 AI repos found), then ordered mining adk-go (8.6K stars, Go, Apache-2.0, "An open-source, code-first Go toolkit for building, evaluating, and deploying sophisticated AI agents"). Don: "sim" → implemented the typed Edge+Route workflow model. |
| **Technique** | Level 4 — adk-go mining (cloned, 21MB): **(1)** unified Agent interface (Name/Description/SubAgents/Run(ctx) iter.Seq2[*session.Event, error]/FindAgent) — ALL agent types implement the same interface; **(2)** Workflow model: Edge{From Node, To Node, Route Route} with typed routes StringRoute/IntRoute/BoolRoute/MultiRoute[T comparable] — value-based routing (node returns a value, edge decides next), NOT by task ID; **(3)** Node types: AgentNode (delegate subagent), FunctionNode, ToolNode, JoinNode (merge parallel), DynamicNode (generic IN/OUT); **(4)** Workflow options: WithMaxConcurrency(n), WithStateSchema; **(5)** Memory.Service (AddSessionToMemory/SearchMemory with Query+UserID+AppName — user-scoped sessions). **Implementation:** internal/workflow/ (new package, additive, non-breaking): Route interface + StringRoute/IntRoute/BoolRoute/MultiRoute; Node interface (Name + Run(ctx, input) (Route, map, error)); Edge{From,To,Route}; Workflow{New(name, edges, opts), WithMaxConcurrency, WithDefaultRoute, Run}; nodes.go: NewFunctionNode/NewToolNode/NewAgentNode (func contracts, no internal/chat import — avoids cycles), JoinNode (sequential/concurrent with semaphore, errors.Join, output merge); execution: starts at sources (no incoming edges), follows typed route matches (valuesEqual tolerant of bare Go values), nil-route edges, then WithDefaultRoute, else ErrNoRouteMatch; cycle detection per-path (DAG fan-in legal, self-loop errors ErrCycle, closed cycles rejected at New); input state shallow-copied per node (race-free concurrency). **Adapter:** internal/pipeline/run_workflow.go RunWorkflow(ctx, w, initial) — planner/steprunner untouched (backward compat). **CLI:** cosca workflow graph-run demo. Go forbids field+method sharing Name identifier → constructor-based NewFunctionNode(name, fn) with embedded nodeBase. |
| **Level** | 4 |
| **Outcome** | success — 20 tests + Example (fetch→transform→join{worker-a,worker-b}→save) PASS with -race; go build/vet PASS; existing pipeline/workflows/cli tests PASS (non-breaking proof); E2E cosca workflow graph-run prints routing path + final state. Chain 49 blocks valid. |
| **Confidence** | 0.96 (validado: 20 testes + Example com -race, build/vet limpos, E2E graph-run com routing path + estado final, chain 49 blocos) |
| **Tags** | #google #adk-go #workflow #edge #route #typed-routing #node #joinnode #concurrency #mining #level-4 |
| **Related** | L198 (mining Google osv-scanner), L197 (mining Anthropic skills), L196 (mining OpenAI evals), L195 (mining OpenAI codex), L192 (mining methodology) |
| **Learned** | **(1) Value-based typed routing (Route as return value + Edge match) is strictly better than ID-based DependsOn — it eliminates the dangling-dependency bug class we fixed in the esteira Frente 2, and makes control flow explicit and self-documenting.** **(2) A unified Agent interface with Run(ctx)→event-stream is cleaner than our separate runner/planner/steprunner layers — future refactor direction.** **(3) Additive packages (new internal/workflow, adapter RunWorkflow) are the safe way to evolve a core engine: the classic path keeps working while the new engine proves itself.** **(4) The mining methodology continues to pay: every org (10x, harbor, lore, models.dev, codex, evals, skills, osv-scanner, adk-go) yields a native feature.** |
| **Next** | (1) Migrate the 39 markdown workflows to the typed Edge+Route model gradually (non-breaking). (2) Consider unifying the Agent interface (Run→event stream). (3) Consider JoinNode usage in the esteira for parallel steps. (4) Keep mining: Google perfetto (tracing), fuzztest (fuzzing), magika (file types). |
