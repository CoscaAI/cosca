PREV: cc2127a022495835d048649024a311aec7f2e790b3038b5b42d5123404402eb9
ID: L221
TIME: 2026-08-14
LEVEL: 5
TAGS: #stallwatch #reliability #timeout #retry #backoff #fallback #watchdog #provider-stall #observabilidade #level-5
---
## L221 — 2026-08-14 — WATCHDOG DE STALL (ordem do Don, conversa do provider travado): `internal/stallwatch` — nenhuma chamada externa espera indefinidamente + observabilidade de stalls no executor | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don trouxe uma conversa sobre um problema REAL observado: OpenCode eternamente em loading, CPU idle, ~700s sem resposta do provider, recuperação manual (Esc ×2 + continue). Ordem: implementar watchdog (opção b — no kernel/agente), escopo controlado, sem tocar no benchmark. |
| **Technique** | Level 5 — **(1) Verificação prévia no código**: `chatWithRetry` JÁ tinha timeout por tentativa, retry com backoff exponencial, retry só de transientes, circuit breaker (5 falhas/30s) e failover — o GAP era a OBSERVABILIDADE do stall (nada registrava estruturadamente) e um watchdog GENÉRICO reutilizável. **(2) `internal/stallwatch/`** (pacote novo, 93,7% cobertura): `Watchdog.Watch(ctx, WatchSpec{Timeout, MaxRetries, RetryDelay, Fallback}, fn)` — detecta stall (deadline exceeded = "provider parou de responder"), registra Event estruturado (timestamp/operation/provider/model/waited/attempt/action/error), retry com backoff, fallback, respeita cancelamento do pai (nunca classifica cancel como stall). `Collector` + `Report()` geram as métricas de comparação que o Don pediu (timeouts, retries, recoveries, total wait, by operation). **(3) Integração mínima no Executor**: `SetStallCollector` + `recordStall` — observabilidade SEM mudar fluxo; o `chatWithRetry` agora registra stall/retry/recovered/failed. **(4) Testes**: 8 unitários do watchdog (stall+retry, retries esgotados, fallback ok, fallback falha, cancelamento do pai, cancel durante backoff, clear, defaults) + 3 de integração executor↔collector (stall real do provider recuperado, falha final, nil no-op). |
| **Level** | 5 |
| **Outcome** | success — `internal/stallwatch/` 93,7% de cobertura, race limpo; orchestration suíte verde; build OK; global estável 67,7% (sem regressão). O requisito da família agora tem enforcement E evidência: todo stall de provider é detectado (timeout), registrado (evento), tratado (retry/backoff/fallback) e mensurável (Report) — a próxima execução real com collector anexado produz a comparação "OpenCode puro vs + watchdog" que o Don definiu. |
| **Confidence** | 0.96 (validado: race limpo, testes de integração com provider real que stall, build OK, global re-medido) |
| **Tags** | #stallwatch #reliability #timeout #retry #backoff #fallback #watchdog #provider-stall #observabilidade #level-5 |
| **Related** | Conversa do Don (stall do OpenCode: loading eterno, Esc×2+continue), L318 (itens A-D), diagnóstico da rodada, `chatWithRetry` (executor.go:606) |
| **Learned** | **(1) O requisito "nenhuma chamada externa espera indefinidamente" já era PARCIALMENTE cumprido no código (timeout+retry+circuit breaker), mas sem evidência — um stall que se recupera sozinho é invisível; a observabilidade estruturada é o que transforma reliability em dado de comparação.** **(2) A distinção crítica do watchdog: deadline do attempt ≠ cancelamento do pai — só o primeiro é stall; cancelamento nunca deve ser registrado como falha do provider (false positive nas métricas).** **(3) Integração mínima vence: `SetStallCollector` opcional (nil = comportamento idêntico ao anterior) permitiu adicionar observabilidade sem tocar o fluxo nem arriscar regressão — os 3 testes de integração provam stall→retry→recovered e o no-op com collector nil.** **(4) O protocolo de recuperação do Don (timeout→retry→backoff→fallback→resume) agora é um componente reutilizável — pode envolver QUALQUER operação externa (subprocesso, rede, API), não só LLM.** |
| **Next** | (1) Anexar um Collector na próxima execução real (runner/daemon) e registrar o Report — a comparação "antes vs depois" que o Don pediu. (2) Nada commitado. (3) Rodada A completa: global 67,7%. |
