PREV: 69e0bdb45e0e87fcf34b8a116c03ce247b2384aef97378afaff8ed893c4ad801
ID: L22
TIME: 2026-08-10
LEVEL: 3
TAGS: #tema #dark-light-system #next-themes #fouc #prefers-color-scheme #matchmedia #build-error #processo-orfao #next-start #e2e #level-3
---
## L22 — 2026-08-10 — HornFit: tema dark/light/system corrigido (classe light nunca era aplicada) | Level 3

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "o modo tema ta bugado dark/light/system o botao pra mudar tbm". Toggle de tema não mudava nada e não existia modo system. |
| **Technique** | Level 3 — **(1)** Diagnóstico: theme-provider alternava apenas a classe `dark` e NUNCA adicionava `.light`; como o `:root` base já é a paleta escura, togglar pro claro era no-op visual (bg seguia escuro). Não havia modo "system". **(2)** Reescrever o provider com 3 modos (light/dark/system): `resolvedTheme` derivado de matchMedia no modo system + listener de mudança do SO em tempo real; aplica `dark` OU `light` (remove o outro); `toggleTheme` alterna dark↔light baseado no resolved; storage key nova `hornfit-theme-mode`. **(3)** Script inline `themeInitScript()` no <head> do root layout (via dangerouslySetInnerHTML) para aplicar classe ANTES do paint (sem FOUC) lendo localStorage + prefers-color-scheme. **(4)** ERRO de build pego: função exportada de arquivo com "use client" chamada no layout server → "Attempted to call themeInitScript() from the server but it's on the client" → mover para lib/theme-init.ts sem diretiva client. **(5)** DEBUG de processo órfão: `next start` antigo (PID) segurava a porta 3000 com build velho — `pkill -f "next start"` não batia no cmdline do processo (`next-server (v15.5.23)`) e o novo start falhava em silêncio (EADDRINUSE); matar por PID do `ss -tlnp` e conferir o HTML servido (grep da string do script) antes de validar. |
| **Level** | 3 |
| **Outcome** | success — Tema validado E2E em 6 cenários: (1) system+SO light → classe light/bg claro; (2) toggle → dark/bg escuro/stored=dark; (3) escolher Claro → light/stored=light; (4) escolher Sistema → stored=system; (5) SO muda p/ dark em tempo real → app segue (media listener); (6) SO volta p/ light → segue. Zero erros. Seletor de tema (Claro/Escuro/Sistema com dot indicador) no dropdown de perfil + botão toggle no header baseado em resolvedTheme. Web em produção (next start) servindo build correto (script confirmado no HTML servido). |
| **Confidence** | 0.95 (validado: 6 cenários E2E headless com emulação de prefers-color-scheme, build exit 0, HTML com script novo) |
| **Tags** | #tema #dark-light-system #next-themes #fouc #prefers-color-scheme #matchmedia #build-error #processo-orfao #next-start #e2e #level-3 |
| **Related** | L178 (monitoração live), L177 (settings), L176 (redesign), sessão 2026-08-10 HornFit |
| **Learned** | **(1) Bug clássico de tema: alternar só a classe `dark` sem aplicar `.light` — se o :root base é escuro, o "claro" nunca acontece. Regra: aplicar `dark` OU `light` (toggle do outro) e `colorScheme` no <html>.** **(2) Padrão canônico de tema sem FOUC em Next App Router: script inline no <head> do root layout (arquivo sem "use client", importável do server) + provider que espelha a lógica no client + matchMedia listener no modo system.** **(3) Função usada no layout server NÃO pode viver em arquivo "use client" — mover para lib/ (sem diretiva) e re-exportar do provider.** **(4) `pkill -f "next start"` não mata `next-server (vX.Y.Z)` (cmdline diferente) — processo órfão segura a porta e o novo start falha silenciosamente; método confiável: `ss -tlnp | grep :3000` → kill PID, e VALIDAR o HTML servido (grep por string única do build novo) antes de acreditar no deploy.** **(5) pkill no shell do Cosca pendura (timeout) — preferir kill por PID.** |
| **Next** | (1) Commit do trabalho no git (pendente, aguardando ordem). (2) Migrar classes .glow-volt/.text-gradient-volt para nomes de marca (aliases hoje). (3) Rotação de refresh token. (4) Paginação nas listagens. |
