PREV: dad978f63aba843310bde9cb44cb88326d4f0ad61fe5387c8db4cbc1ccbc813d
ID: L163
TIME: 2026-08-13
LEVEL: 4
TAGS: #cosca-trader #f0 #fundacao #core-headless #event-bus #event-store #sqlite #wails #react #vite #level-4
---
## L163 — 2026-08-13 — cosca-trader F0: fundação (core headless + event bus + event store + schema) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "pode começar, pé no fundo" — construir a F0 do cosca-trader (fundação): monorepo Go + Wails/React, event bus, event store, SQLite, schema (instrumentos, candles, ordens, posições, journal). |
| **Technique** | Level 4 — **(1)** Reaplicação direta dos padrões da família: event bus tipado do cosca-code (`internal/event/bus.go`, pub/sub por tipo + subscribe-all), event store append-only do cosca-node (`internal/store/store.go`), hub pub/sub do cosca-node (`internal/stream/hub.go`). **(2)** Event model de trading: merge do bus (Payload any + CorrelationID) com o node model (CausationID + Severity) — ~40 tipos de evento (market/connectivity/order/trade/position/balance/risk/strategy/system). **(3)** `internal/engine`: composição raiz com `Emit` = caminho único (preenche ID/timestamp/severity → Store.Append → DB.PersistEvent → Hub.Publish → Bus.Publish) — persistir ANTES de publicar garante que nenhum consumidor vê evento fora do rastro. **(4)** SQLite modernc.org/sqlite (pure-Go) com schema idempotente (CREATE TABLE IF NOT EXISTS) cobrindo events + 6 tabelas de domínio; `PersistEvent`/`AllEvents` com round-trip de payload via JSON. **(5)** serve.go: /health /timeline /events (SSE) na porta 14126 (família de portas). **(6)** Frontend React 19 + Vite 7 + TS dark theme com health-check ao core. |
| **Level** | 4 |
| **Outcome** | success — 10 testes verdes, gofmt/vet limpos, smoke test end-to-end real (/health + /timeline + /events SSE + SQLite persistido). Binário 14.5MB. Frontend builda (29 módulos). Commit `f27d63f` (34 arquivos, 3567 inserções). |
| **Confidence** | 0.93 (validado: smoke test real com curl, SQLite round-trip em teste, build+vet+gofmt verdes) |
| **Tags** | #cosca-trader #f0 #fundacao #core-headless #event-bus #event-store #sqlite #wails #react #vite #level-4 |
| **Related** | L259/L260 (discovery + reconhecimento), L256 (cosca-code §92), L251-L255 (cosca-node/neural-link padrões) |
| **Learned** | **(1) O padrão "core headless + clientes finos" do cosca-code é o maior ativo: reapliquei event bus + event store + hub quase verbatim — só mudou o EVENT MODEL (domínio trading) e o schema. Não há nada a inventar na fundação.** **(2) `Emit` como caminho único com persistir-before-publish é o invariante de "rastreamento total": o teste `TestEmitBusOrdering` prova que no dispatch o store já tem o evento — sem isso, um handler poderia observar estado que não sobrevive a restart.** **(3) SQLite com schema idempotente (CREATE TABLE IF NOT EXISTS) resolve migração de F0: reabrir o banco reaplica sem erro (teste de idempotência). O round-trip de payload usa JSON (RawMessage na leitura) — lossless para any.** **(4) Porta 14126 segue a família (14120 serve, 14123 runtime, 14124 neural-link, 14125 node) — coerência de endereçamento, não número arbitrário.** **(5) gofmt pegou 3 arquivos com alinhamento de struct desalinhado (campos de nomes diferentes) — a lição do L193 se repetiu: rodar `gofmt -w` antes de commitar.** **(6) Testar com `pkill -f bin/cosca-trader` mata a própria shell (o pattern casa com o command line do bash -c) — usar pgrep do PID ou padrão mais específico.** |
| **Next** | (1) F1: camada de exchanges (interface + adapter Binance) + market data em tempo real via WebSocket → eventos MarketTick/CandleClosed. (2) F2: OMS. (3) F3: gráficos + indicadores. |
