PREV: 3dc21d1947a748fad50844643006f70c7a6c0b32201b4435ecf07255d61386cc
ID: L303
TIME: 2026-08-16
LEVEL: 5
TAGS: #professor #adaptive-performance-engine #procedural-kernel-system #scene-graph #node-graph #performance-memory #auto-tuning #benchmark-runtime #pipeline #cosca-engine #level-5
---
## L303 — 2026-08-16 — A DICA 2 DO PROFESSOR: Adaptive Performance Engine + ordem corrigida (Procedural Kernel System → Scene Graph → métricas → só depois Visual Compiler) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Absorver a segunda dica do Professor: como o Cosca descobre a implementação mais barata por tarefa, mede e adapta — e a ORDEM de construção corrigida |
| **Technique** | Runtime adaptativo de computação visual: benchmark como parte do runtime, performance memory, auto-tuning, kernel fusion, temporal reuse — IA como DIRETOR, benchmark real como DECISOR |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.90 |
| **Tags** | #professor #adaptive-performance-engine #procedural-kernel-system #scene-graph #node-graph #performance-memory #auto-tuning #benchmark-runtime #pipeline #cosca-engine #level-5 |
| **Related** | L302, L250, L251, L86, L168, L221, L222 |
| **Learned** | O Professor delineou o Adaptive Performance Engine: em vez de "tarefa → algoritmo fixo → resultado", o Cosca faz "tarefa → decomposição → gerador de estratégias → benchmark ultrarrápido → escolhe menor custo → executa → mede → aprende → atualiza o plano". Princípios: (1) Performance Brain — tabela interna TASK/hardware → BEST strategy (resize_4k_to_1080p: GPU_SHADER_CACHED 0.17ms), não re-descobre. (2) HARDWARE PROFILE por máquina (CPU cores/cache/SIMD; GPU VRAM/CUs/bandwidth/Vulkan/ROCm) — Cosca A (RX 6700 XT) ≠ Cosca B (RTX 5090) ≠ Cosca C (Apple Silicon), mesmo workflow com executores diferentes. (3) Aprender com TAMANHO da tarefa: pixels<500k→CPU, 500k-4M→GPU, >4M→GPU tiled — limites DESCOBERTOS por benchmark, não codificados. (4) AUTO-TUNING: testar BLOCK_SIZE 8/16/32/64 → escolher ótimo → salvar (kernel/hardware/resolution/optimal). (5) ALGORITHM GENERATOR: produzir variantes (blur_v1..v4: CPU/SIMD/GPU/Vulkan/tile/memory/precision) → benchmark + accuracy + performance → winner. (6) REGRA FUNDAMENTAL: otimizar COST = latency+memory+energy+VRAM+CPU+quality_penalty+failure_risk — QUALITY ≥ threshold PRIMEIRO, depois minimize latency. (7) FRONTEIRA DE PARETO: manter FAST/BALANCED/QUALITY/ULTRA_QUALITY. (8) CACHE DE CONHECIMENTO: .cosca/performance/{hardware,kernels,benchmarks,strategies,models,profiles}.db — experiência vira conhecimento operacional reutilizável. (9) APRENDIZAGEM ONLINE: expected vs actual — hardware mudou → nova rodada de tuning. (10) COMPILAÇÃO ADAPTATIVA: COSCA DSL → IR → Optimizer → backends (CPU/GPU/Vulkan/HIP/WGSL) → benchmark → best executable (JIT/AOT/auto-vectorization/kernel fusion). (11) KERNEL FUSION: Resize+Color+Sharpen+Composite → SUPER_KERNEL (menos memória/cópias/sincronização/kernel launches). (12) TEMPORAL REUSE para vídeo: background/camera/lighting em cache, só moving object recomputa (Frame 1: 100%, Frame 2: 37%). (13) COSCA PERFORMANCE POLICY: 12 regras (não use IA se algoritmo clássico resolve; não use CPU se GPU é claramente superior; não use GPU se overhead supera o benefício; não recompute inalterado; fundir operações; cachear; autotuning; medir cada pipeline; aprender com benchmarks; reavaliar quando hardware/contexto muda; nunca sacrificar qualidade abaixo do limite; nunca trocar segurança por performance). ORDEM CORRIGIDA (o Professor REFINOU o L302): NÃO ir direto ao Visual Compiler. Primeiro: (a) PROCEDURAL KERNEL SYSTEM como famílias — Noise (Perlin/Simplex/Worley/Voronoi/FBM), Math (Add/Multiply/Remap/Clamp/Lerp/Curve), Pattern (Gradient/Checker/Stripes/Cells/Rings) — não nós soltos; (b) SCENE GRAPH como grafo de ENTIDADES ESPACIAIS separado do Node Graph (grafo de computação) — cada entidade APONTA para operações do node graph (evita monolito); (c) RASTER+Vector→Compositor→Renderer + CV algorithms; (d) PERFORMANCE ENGINE NASCENDO JUNTO do Render Graph — cada operação produz metadados (operation/input/backend/implementation/latency/memory/quality/cache_hit) → performance memory; (e) SÓ DEPOIS Visual Compiler (Scene DSL → Scene Graph → Node Graph → Render Plan → Render Engine; LLM produz o DSL, o compilador transforma em nós, o runtime executa). O que preservar: determinismo + cache + resumability + node graph JÁ EXISTENTES na casa — a base é sólida e rara (muitos sistemas de IA não têm). |
| **Next** | (1) Implementar o Procedural Kernel System (famílias Noise/Math/Pattern como nós do grafo — puro Go determinístico). (2) Scene Graph separado do Node Graph (entidades espaciais apontando para nós). (3) Metadados de performance por operação desde o início (operation/backend/latency/memory/quality) — o embrião do performance memory. (4) Só depois Visual Compiler. (5) Não codificar limites de tamanho manualmente — benchmark descobre. |
