PREV: 8beee491b5c98ff3df8752179f527b9e21b4486fa3a810bc84be10e6b5c02f5a
ID: L49
TIME: 2026-08-12
LEVEL: 4
TAGS: #google #skills #search #github-search-api #batch-select #npx-skills-add #catalog #rate-limit #mining #ecosystem #level-4
---
## L49 — 2026-08-12 — Mining Google skills parte 3: `cosca skill search --remote` (GitHub Search API) + `cosca skill repo list/install` (batch-select estilo npx skills add) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don: "continue" (google/skills). Próximos itens do L205: search no catálogo + batch-select. A API do skills.sh (`https://skills.sh/api/v1/skills/search`) requer token Vercel OIDC (401 público — testado) → canal público escolhido: GitHub Search API (funciona sem auth, rate limit 10 req/min — testado). |
| **Technique** | Level 4 — **(1)** `internal/skills/catalog.go` — `SearchCatalog(query, opts{Owner, Limit, MinStars, AllowRemote})`: fail-closed (exige opt-in), query sanitizada (strip shell metacharacters) + URL-encode, `q=<query> in:readme topic:skills` + qualifiers ` org:`/` user:`/` stars:>N`; HTTP client endurecido (10s timeout, 3-redirect cap) + `Accept: application/vnd.github+json` + `User-Agent` obrigatório (GitHub 403 sem UA); 403/rate-limit → erro claro com a dica de 10/min + `--owner`; base URL em var de pacote (`catalogAPIBase`) para httptest. **(2)** `install_remote.go` ADD — batch-select: `downloadRepoCandidates` (helper compartilhado que devolve cleanup pro caller — senão o defer interno apagava os candidatos antes do consume), `ListRepoSkills` (numera 1-based com descriptions parseadas), `ListRepoSkillsLocal` (mesmo código de listagem testável sem rede), `InstallRepoSkills(ref, indexes, allowRemote)` (instala SÓ os índices pedidos via installStandardSkill; vazio = todos; fora de range → erro com range válido). **(3)** CLI — `cosca skill search <query> [--owner] [--limit] [--remote]` (local SEMPRE + catálogo GitHub se --remote, hint `cosca skill install <full_name>`), `cosca skill repo list <ref>` e `cosca skill repo install <ref> <indexes...>` com --allow-remote. |
| **Level** | 4 |
| **Outcome** | success — 90 testes PASS em internal/skills (-race), 76 em cmd/cosca-chat. Build + vet limpos. E2E real: `search gke --remote --limit 5` → 5 catálogo matches com google/skills #1 (★17808); `repo list google/skills/skills/cloud` → 94 skills numeradas com descriptions; `repo install ... 1 2` → instalou EXATAMENTE agent-platform-alert-configuration + agent-platform-deploy (verificado no disco); `99` fora de range → erro com range 1-94; list/install sem --allow-remote → fail-closed. |
| **Confidence** | 0.96 (validado: 166 testes -race, E2E real search+list+select install, build+vet limpos) |
| **Tags** | #google #skills #search #github-search-api #batch-select #npx-skills-add #catalog #rate-limit #mining #ecosystem #level-4 |
| **Related** | L205 (plugins+marketplace), L204 (canal GitHub), L203 (AWS durable), L192 (mining methodology) |
| **Learned** | **(1) A API do skills.sh NÃO é pública — exige Bearer token Vercel OIDC (401 sem auth, testado). O canal de busca público universal é a GitHub Search API: funciona sem token com `in:readme` + `topic:skills`; sem `in:readme` o repo google/skills NÃO aparece para "gke" (repo search não indexa conteúdo); `q=gke topic:skills` → 0 resultados, `q=gke in:readme topic:skills` → 28.** **(2) GitHub Search API exige `User-Agent` header (403 sem ele) e rate-limita a 10 req/min sem token — sempre tratar 403/429 com mensagem acionável (narrow com --owner) em vez de erro genérico.** **(3) Batch-select é o fluxo que o ecossistema usa (npx skills add: lista → usuário escolhe): numerar 1-based + instalar por índice com validação de range transforma um repo de 94 skills em instalação cirúrgica.** **(4) Bug sutil de cleanup em helpers que baixam tarball: `defer cleanup()` DENTRO do helper apaga os candidatos antes do caller consumir — o cleanup precisa ser devolvido ao caller (padrão `dir, cleanup, err := ...; defer cleanup()`).** **(5) Search local (Manager.Search) + search remoto são complementares: local primeiro (sempre funciona, zero rede), remoto com opt-in explícito (--remote = o próprio opt-in, sem --allow-remote duplicado).** |
| **Next** | (1) Wire do mcp.json no runtime MCP (internal/chat/mcp) — hoje o config é registrado e persistido, mas não auto-conecta servidores. (2) Curated bundle das WAF pillars no catálogo embutido. (3) `cosca skill audit` via skills.sh (requer token OIDC — opcional). (4) Busca com paginação/cache no catálogo. |
