PREV: 0d6693a5253563996ab050a8d354378ddd04c1aaf8fcf11fd0231763f397ac81
ID: L115
TIME: 2026-08-13
LEVEL: 3
TAGS: #neural-link #reproducibility #multiple-runs #human-baseline #safety #integralidade #secao-26 #level-3
---
## L115 — 2026-08-13 — Neural Link F6: reprodutibilidade, multiple runs, human baseline, safety — critério final completo | Level 3

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "continue" — completar o critério final de sucesso (seção 26): "REPRODUZ" (reprodutibilidade, seção 16) e "COMPARA COM EXECUÇÕES ANTERIORES" (multiple runs, seção 19), além de human baseline (seção 20) e safety (seção 21). |
| **Technique** | Level 3 — **(1)** `loop/repro.go`: `VerifyReproducible(objective, solver, oracle, events, n)` roda o desafio n vezes com parâmetros determinísticos e reporta `AllIdentical`/`Converged` (reprodutibilidade = mesma solução em todas as execuções). **(2)** `loop/safety.go`: `SafetyPolicy.Check(run)` verifica invariantes de integridade — append-only (sequência monotônica de eventos), VERIFIED sem refutação, e contagem resultados==experimentos — com severidade DETECT/ALERT/HALT/AUDIT. **(3)** `loop/baseline.go`: `HumanBaseline` + `CompareBaseline` — equivalência por contenção da forma canônica normalizada (a solução "a função escondida é f(x)=x²" CONTÉM "f(x)=x²"). **(4)** Exposição no app.go: `VerifyReproducible`, `SafetyCheck`, `CompareBaseline`. **(5)** Smoke test CLI ampliado para demonstrar a pipeline COMPLETA (loop → grafo → manager → repro → safety → baseline). |
| **Level** | 3 |
| **Outcome** | success — Critério final (seção 26) coberto ponta a ponta: smoke test mostra (1) descoberta autônoma VERIFIED, (2) grafo 22 nós/arestas com proveniência, (3) manager saudável, (4) reprodutibilidade "convergiu: true | 3 execuções idênticas", (5) safety "nenhuma violação", (6) baseline "equivalente: true (humano f(x)=x² vs cosca)". Testes verdes (5 pacotes), gofmt/vet limpos, binário rebuildado 21MB. |
| **Confidence** | 0.93 (validado: smoke test completo, testes verdes, vet/gofmt limpos) |
| **Tags** | #neural-link #reproducibility #multiple-runs #human-baseline #safety #integralidade #secao-26 #level-3 |
| **Related** | L193 (F2-F4), L192 (F0-F1), README.md do projeto |
| **Learned** | **(1) Reproductibilidade é natural quando o sistema é determinístico: seed fixa no refuter + solver determinístico → rodar N× dá a MESMA solução; o teste é barato e prova o "REPRODUZ" do critério final.** **(2) Safety por invariantes é mais honesto que por "permissões": verificar append-only (sequência monotônica), verificação-sem-refutação e contagem de evidências detecta violações de integridade sem precisar de um sistema de permissões completo.** **(3) Comparação com baseline humano por equivalência semântica: normalizar (minúsculas/sem espaços) + contenção da forma canônica — a solução do solver tem prefixo narrativo ("a função escondida é..."), então igualdade exata falharia.** **(4) O critério final da seção 26 não é um checklist de UI — é um encadeamento de capacidades que agora forma uma pipeline fechada (descobrir→refutar→verificar→reproduzir→comparar), todas verificáveis por teste.** |
| **Next** | (1) F5: API REST (`/experiments`, `/challenges`, ...) + React Flow (grafo interativo) + playback temporal. (2) Plugar solver LLM (provider do Cosca) — a interface `Solver` já está pronta; este é o salto de valor real do laboratório. (3) git init + commit inicial (aguardando ordem). |
