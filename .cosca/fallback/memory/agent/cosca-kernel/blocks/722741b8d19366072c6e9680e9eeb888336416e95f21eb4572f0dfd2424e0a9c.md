PREV: e74ed2966d7f9fc52280f5430ddcfaf1064684f0b6e954b85b39f92f79b5b16a
ID: L124
TIME: 2026-08-13
LEVEL: 5
TAGS: #religada #serve #jaula #fix-l218 #info-fd #jwt #jail-secrets #serve.env #lixo #produção #level-5
---
## L124 — 2026-08-13 — SERVIÇO RELIGADO COM JAULA: config projetado + serve.env restaurado do lixo → serve UP em 14120 (healthy) com o fix L218 ao vivo | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don aprovou a religada do serviço ("sim"/"continue"). Motor novo instalado (bin/cosca 1.5.0 fix L218), manifesto de memória atualizado (571 protegidos), config projetado criado, serve.env do lixo restaurado como .env (600) e o serve subiu NA JAULA: /health → {"healthy":true} em 127.0.0.1:14120. |
| **Technique** | Level 5 — **(1)** gate de memória: init recusa com manifesto existente (--force exigido); verify mostrou 567 ADDED (zero Changed/Missing) = espelho .cosca/fallback do framework (desovado pelo binário novo às 00:07) — init --force legítimo (fluxo oficial do erro). **(2)** Jaula manual sem --dev quebra o git do integrity ("git rev-parse HEAD: open /dev/null: no such file or directory" → falso GIT COMMIT REWRITTEN — fail-closed em gatilho errado); o AUTO-JAIL monta --dev /dev + --proc /proc (jail.go:878-879) — fiel, chain integrity PASSA dentro da bolha. **(3)** serve.env do lixo = produção do Don: COSCA_JWT_SECRET, OPENAI_API_KEY/BASE_URL, COSCA_OPENAI_MODEL, COSCA_DEV_MODE, COSCA_ENABLE_REGISTRATION, COSCA_CORS_ORIGINS, OLLAMA_HOST (334B, 0600) — restaurar como .env (env.Load main.go:71) → propagate no pai → jail-secrets.env estagiado 0600 (writeJailSecretsFile jail.go:157) → LoadJailSecrets dentro (jail.go:123) — JWT NUNCA no argv (confirmado no ps). **(4)** boot completo na jaula: chain ✓, runtime ✓, daemon ✓, orchestration ✓, 61 agentes, 87 skills, 10 providers, 39 workflows; FTL COSCA_JWT_SECRET desbloqueado pelo .env. |
| **Level** | 5 |
| **Outcome** | success — serviço do Don NO AR: processo 225320 + bwrap (die-with-parent), /health healthy:true, auth fechada (401 na raiz). Fix L218 vivo: argv mostra --info-fd 4 /proc/self/fd/3 (canal de boot). Git limpo (chain runtime fora do git; .env gitignored). |
| **Confidence** | 0.97 (validado: endpoint respondendo, argv ao vivo, zero warnings, segredos fora do argv) |
| **Tags** | #religada #serve #jaula #fix-l218 #info-fd #jwt #jail-secrets #serve.env #lixo #produção #level-5 |
| **Related** | L222 (par fiel), L221 (lixo = ambiente original), L219 (fix L218), L218 (bug), pkg/cosca/jail.go:157 (writeJailSecretsFile), main.go:71 (env.Load) |
| **Learned** | **(1) Jaula manual de teste PRECISA de --dev /dev + --proc /proc: sem eles o git (e qualquer coisa que use /dev/null) quebra e o integrity acusa commit reescrito — falso positivo em ambiente errado, não é breach.** **(2) O serve.env do Don É o ambiente de produção (JWT só existe lá; config global não tem) — o lixo era a única cópia; restauração restaurou também OPENAI_API_KEY (600, gitignored).** **(3) O fix L218 se prova em produção: o pai mostra o boot e o erro REAL do processo preso quando precisa; quando o processo sobe, sobe limpo — sem falso "jail unavailable".** **(4) Ciclo completo da casa operando: gate → jaula → chain → runtime: cada camada independente e verificável.** |
| **Next** | (1) Purga do lixo (decisão do Don — serve.env/config.yaml originais JÁ restaurados/registrados; security.logs, models.json, cosca-chat, data/, scripts/ ainda lá). (2) Registrar usuário/auth do serve (COSCA_ENABLE_REGISTRATION=false — acesso via registro manual). (3) Monitorar saúde (watchdog 30s interno). (4) cosca session index quando o binário tiver o CLI de sessão. |
