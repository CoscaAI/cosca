# L404 — LACUNA DA ABI FECHADA: @PROJECT name v1 no registry (ordem do Don)

## status: DECISION (autorizado: "sim" — fechar a lacuna da ABI)

## O que entrou
1. PROMPT_REGISTRY.md: contrato `@PROJECT name v1` (Nível 2 — Operações)
   - MEANING: ativar/verificar o PROJECT_PROTOCOL para um projeto registrado
     (manifest tipo/versão, isolamento .cosca próprio, repo git)
   - REQUIRED_STATE: projeto registrado em `cosca project list`
   - ALLOWED: project list / manifest / leitura de .cosca/ / repo git
   - FORBIDDEN: mutação (new/remove via CLI com --force; nada escrito no
     .cosca do projeto)
   - OUTPUT: PROJECT_REPORT
   - GUARDIANS: escopo (projeto nunca toca a casa), integridade
2. PROMPT_PROTOCOL.md §2: tabela de níveis atualizada (Operações + @project)

## A lacuna (contexto)
PROMPT-EFF-001 tarefas 1-2: "protocolo projeto cosca code" executou bem mas
a ABI não representaria a ordem sem ambiguidade (PROJECT fora do registry).
Registrada 2x, apresentada ao Don, autorizada a correção.

## Classificacao
MEASURED lacuna fechada; "protocolo projeto X" agora resolve deterministicamente
EVIDENCE contrato @PROJECT no registry + tabela do PROMPT_PROTOCOL §2
INFERRED as próximas ordens de projeto usarão o comando sem lacuna
UNKNOWN fidelidade medida nas próximas tarefas da campanha
DECISION registry v1 do @PROJECT; nada mais sem nova autorização
