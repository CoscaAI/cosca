PREV: df54553ae8c4d97bbae1a3032461fe22676a2989b5fee5fd4525b5a71323746c
ID: L414
TIME: 2026-08-17
LEVEL: 5
TAGS: #seguranca #firewall #ufw #docker #exposicao #0.0.0.0 #hornfit #brecha #P1 #ports #defesa-em-profundidade #level-5
---
## L414 — 2026-08-17 — Brecha P1: HornFit exposto em 0.0.0.0 rodando como ROOT — UFW furável pelo docker; containers parados + regras DENY aplicadas | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Auditoria de segurança da casa (a pergunta do Don: "brecha de segurança não acha?") + resposta/containment |
| **Technique** | Varredura de portas (ss -tln), fingerprint de processos (ps/forest, /proc), inspeção de permissões críticas (chaves 0600, serve.env, .cosca), teste de rotas HTTP, análise UFW (ufw.conf, before.rules, default policies), root cause do compose do HornFit |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.96 |
| **Tags** | #seguranca #firewall #ufw #docker #exposicao #0.0.0.0 #hornfit #brecha #P1 #defesa-em-profundidade |
| **Related** | L406, L259, L46, L263, L102, L124, L199 |
| **Learned** | 1) **A BRECHA que o Don sentiu**: HornFit (projeto cliente) rodava 5 containers docker (api/web/postgres/redis/minio) com portas 3000/3333 publicadas em **0.0.0.0** (`${API_PORT:-3333}:3333` no compose) — acessíveis de QUALQUER máquina da LAN (192.168.1.0/24), com os processos **next-server e node rodando como ROOT** (containerd-shim). UFW estava active+enabled com DEFAULT_INPUT_POLICY=DROP, MAS o docker fura o UFW: injeta regras na chain DOCKER (NAT/FORWARD) avaliadas ANTES das regras do UFW + docker-proxy faz bind 0.0.0.0 → firewall "ativo" mas inútil contra portas publicadas. 2) **Containment imediato (ordem do Don)**: `docker stop` nos 5 containers → portas fechadas (ss limpo, HTTP 000), dados preservados nos volumes (reversível). 3) **Root cause**: compose com `restart: unless-stopped` + publica em 0.0.0.0 → o boot religa exposto. 4) **Fix em 3 fases**: F1 `ufw deny 3000/tcp` + `deny 3333/tcp` + reload (bloqueia fora, libera local — a regra `-A ufw-before-input -i lo -j ACCEPT` do before.rules é a PRIMEIRA da chain, confirmada via grep root); F2 `/etc/docker/daemon.json` com `{"iptables": false}` (docker respeita o UFW; cuidado: quebra NAT de saída dos containers); F3 bind `127.0.0.1:porta` no compose (definitivo — a porta nem existe fora). 5) **Verificações corretas da casa**: chaves Ed25519 0600, serve.env 0600, config.yaml 0600, .cosca 700, sem resíduo jail-secrets, exec/mcp fail-closed, segredos no git = só fixtures de teste. 6) **P2**: COSCA_DEV_MODE=true no serve.env (produção) — criar admin com senha aleatória em dev mode; registro fechado (401) mitiga. 7) Lição: UFW "active" ≠ protegido — docker exposto em 0.0.0.0 anula o firewall; auditoria de segurança deve SEMPRE checar `ss -tln` por binds em todas as interfaces + processos root + compose files. |
| **Next** | (a) confirmar `sudo ufw status numbered` com DENY 3000/3333; (b) decidir Fase 2 (daemon.json) e/ou Fase 3 (bind 127.0.0.1 no compose do HornFit); (c) COSCA_DEV_MODE=false; (d) considerar firewall rules para a LAN se o Don quiser acesso remoto ao HornFit. |
