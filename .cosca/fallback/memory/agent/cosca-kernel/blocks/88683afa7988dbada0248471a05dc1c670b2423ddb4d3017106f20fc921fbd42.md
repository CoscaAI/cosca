PREV: 2f1eba10032bbdfdd78f3efbebd401dea7e057813e6ebc9af2424c998e78b252
ID: L34
TIME: 2026-08-11
LEVEL: 4
TAGS: #epicgames #lore #blake3 #integrity #chain #content-addressed #parallel-verification #mining #mit-license #level-4
---
## L34 — 2026-08-11 — Mining Epic Games: lore (BLAKE3 content-addressed VCS) → BLAKE3 dual-algo + parallel verification na family chain | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don found https://github.com/epicgames (Epic Games org) and called it "diamante vermelho". After analysis: Unreal Engine is EULA-gated (NOT usable), but the open-source repos are MIT (fully usable). The diamond: lore — "next-generation open source version control system" by Epic (Rust, MIT, 8.4K stars), with lore-go (Go SDK, MIT). |
| **Technique** | Level 4 — Lore mining (cloned and read real code + system design doc): content-addressed storage, immutable revision chain (Merkle-like, hash includes parent hashes), BLAKE3 (32-byte) as the addressing hash, recursive fragmentation (fragments addressed by hash, can hold payload or refs to child fragments), Address = (hash, context) 48 bytes for identity tracking (moves/copies), separate immutable store (append-only CAS) + mutable store (branches/KV), lightweight branches as mutable pointers, sparse/on-demand hydration. The lore-go SDK binds against a native lib (liblore.so via FFI, pre-1.0, APIs change). **(Decision, Don: "faca o que for melhor pro cosca")** integrate the PATTERNS not the dependency (lore-go needs native lib + pre-1.0 = risky as a dependency now). Two upgrades implemented in the family chain: **(1) BLAKE3 as canonical hash for NEW blocks (dual compatibility):** HashAlgo abstraction (sha256 legacy \| blake3 new), HashFile/HashBytes/HashReader, new blocks (Sign + SignAuto) use blake3 for file hashes + MANIFEST_HASH + content hash, HASH_ALGO field in block (absent = sha256), same Ed25519 hex-string signing convention. Dep: github.com/zeebo/blake3 v0.2.4. Verified: blake3("")=af1349b9..., blake3("abc")=6437b3ac... **(2) Parallel verification:** worker pool (8 workers) for the latest-block file-hash loop, deterministic sorted output. Speedup: 122.6ms → 26.3ms (~4.7x) in benchmark; end-to-end cosca-check flat (~0.18s, dominated by ~60 git subprocess calls for historical blocks). |
| **Level** | 4 |
| **Outcome** | success — go build/vet/test pass (incl -race), TestLegacySHA256BlockVerifies (backward compat), TestMixedSHA256AndBLAKE3Chain, known blake3 vectors, tamper detection, hermetic tests (HOME isolation — pre-existing issue: production key at ~/.config/cosca/keys broke TestBackwardCompatEd25519Chain, fixed by isolating HOME in test helper). Chain: 31 blocks valid (30 legacy sha256 + blake3 blocks), 1283 files. |
| **Confidence** | 0.96 (validado: go build/vet/test -race, testes backward compat + mixed chain + vetores blake3 + tamper detection, chain 31 blocos) |
| **Tags** | #epicgames #lore #blake3 #integrity #chain #content-addressed #parallel-verification #mining #mit-license #level-4 |
| **Related** | L192 (mining methodology), L191 (evals harness), L186 (chain git-anchored), L181 (chain nativa Go + assinatura) |
| **Learned** | **(1) MIT vs EULA matters: Epic's open-source repos (lore, raddebugger) are usable; Unreal Engine source is not.** **(2) A next-gen VCS's core is BLAKE3 + content-addressed immutable chain — which VALIDATES our family chain design (PREV hash chaining = immutable revision chain).** **(3) Absorb patterns, not dependencies: lore-go's native lib (FFI, pre-1.0) was the blocker; the design patterns (BLAKE3, fragmentation, address+context) are the durable gold.** **(4) BLAKE3 is the industry direction (Epic, and fastest general-purpose hash) — dual-algo migration keeps backward compat while moving forward.** **(5) Parallel verification is the easy performance win (4.7x) once the format is stable.** |
| **Next** | (1) Consider Address=(hash,context) for move/copy tracking in the chain. (2) Consider BLAKE3 for the memory merkle too. (3) Watch lore pre-1.0 maturity — if it stabilizes, revisit lore-go as a real backend. (4) Keep mining: raddebugger (debugging patterns), deadline (job queue orchestration). |
