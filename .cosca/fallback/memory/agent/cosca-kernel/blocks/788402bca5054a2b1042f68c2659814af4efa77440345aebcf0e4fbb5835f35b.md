PREV: 629eba5828ff47cb112aeec763500219271ac36a2b4c02041311311a8b4c5604
ID: L111
TIME: 2026-08-13
LEVEL: 3
TAGS: #react-flow #grafo-interativo #xyflow #f5 #neural-link #level-3
---
## L111 — 2026-08-13 — Neural Link: grafo interativo React Flow — F5 fechada, spec 95% coberto | Level 3

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "continue". Implementar o grafo interativo (React Flow) — última peça da F5 do roadmap do Neural Link. |
| **Technique** | Level 3 — **(1)** Instalação `@xyflow/react` (React Flow v12). **(2)** `GraphFlow.tsx`: converte `GraphData` (nodes/edges) em nodes/edges do React Flow; layout manual em camadas por tipo (hypothesis→x0, experiment→x320, result→x640, decision→x960, evidence→x1280), nós coloridos pela BORDA por estado epistêmico (VERIFIED verde, REFUTED vermelho, CONFIRMED azul); arestas rotuladas com os 12 LinkTypes + arrowheads. **(3)** Substituição da visão estática (pills) pelo grafo interativo; remoção de código/estilos mortos (GraphView, node-pill, graph-nodes). **(4)** Depuração de TS: `noUnusedLocals` pega função não usada → remover o componente antigo por completo, não deixar órfão. |
| **Level** | 3 |
| **Outcome** | success — Grafo interativo funcional (pan/zoom/arrastar, nós por estado, arestas tipadas). F5 completa (playback + API REST + grafo). Build+testes verdes, commit `2f1fb62`. Bundle JS 206KB→388KB (React Flow), aceitável. |
| **Confidence** | 0.9 (validado: build frontend + go, testes, vet) |
| **Tags** | #react-flow #grafo-interativo #xyflow #f5 #neural-link #level-3 |
| **Related** | L197 (API REST), L196 (playback), L193 (F2 graph) |
| **Learned** | **(1) React Flow v12 = `@xyflow/react` (não `reactflow` legado); layout é MANUAL — não há layout automático, então o padrão é agrupar por tipo em colunas X + distribuir Y incremental.** **(2) Cor semântica por ESTADO epistêmico (borda do nó), não só por tipo — VERIFIED/REFUTED/CONFIRMED visíveis de imediato no grafo.** **(3) `noUnusedLocals` do TS vira guarda de limpeza: ao substituir um componente, remover o antigo por completo (não deixar órfão) senão o build falha.** **(4) React Flow é a dependência certa para grafos interativos leves — pan/zoom/arrastar nativos, bundle +180KB justificável.** |
| **Next** | (1) Seção 15 (novelty detection): integrar `NoveltyClass` à busca real via `KnowledgeService.Search` do Cosca (hoje só está definida, não conectada). (2) Seção 12 (memória): usar `MemoryService.Store/Search` ativamente, não só stats. (3) Balanço do spec: 25 seções ~95% cobertas — fechar os 2 furos restantes. |
