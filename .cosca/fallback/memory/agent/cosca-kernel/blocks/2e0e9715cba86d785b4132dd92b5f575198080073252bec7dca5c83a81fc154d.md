PREV: 7ef70b5ca4e13b9e0932ddf7f8971310cb7ed15fcd23223bfa794be7a59bd23f
ID: L301
TIME: 2026-08-16
LEVEL: 5
TAGS: #mineracao #runtime #ux #sd-cpp #forge #fooocus #invokeai #gguf #vulkan #rocm #vram-offload #presets #rx6700xt #level-5
---
## L301 — 2026-08-16 — MINERAÇÃO Runtime+UX: stable-diffusion.cpp + Forge + Fooocus + InvokeAI — velocidade em AMD e UX de produto | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Mineração de 4 projetos que resolvem: (a) inferência rápida em hardware AMD consumidor, (b) UX de produto de geração — para o Cosca Video/Audio Engine |
| **Technique** | Arqueologia arquitetural → padrões de runtime nativo, otimização VRAM, presets e canvas |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.85 |
| **Tags** | #mineracao #runtime #ux #sd-cpp #forge #fooocus #invokeai #gguf #vulkan #rocm #vram-offload #presets #rx6700xt #level-5 |
| **Related** | L298, L299, L67, L52 |
| **Learned** | (1) stable-diffusion.cpp: runtime nativo C/C++ (ggml) — ÚNICO com suporte nativo AMD Vulkan/ROCm + já roda VÍDEO (Wan2.1/2.2, MiniMax-H3, LTX-2.3, HunyuanVideo 1.5); quantização GGUF single-file (q8 = sweet spot), separação runtime backend × parameter backend, block streaming (modelos 3-4× maiores que a VRAM), graph-cut do forward pass (--max-vram), flash attention = economia de VRAM (600-1400MB), caches de passos com threshold de erro (UCache/EasyCache/DBCache/TaylorSeer), TAESD + VAE tiling; TEM BINDING GO (cgo e non-cgo) — casa com o stack Cosca. (2) Forge: reescreveu o grafo de computação — SEM flags de VRAM, memory manager por componente com troca assíncrona (SDXL em 4GB, SD1.5 em 2GB); UnetPatcher (grafo patchable com hooks em vez de monkey-patch — FreeU/SAG/ControlNet em ~100 linhas); dtypes por componente; ganhos +30-75% vs A1111 em GPUs pequenas. (3) Fooocus: UX "um botão" — presets como JSON declarativo com auto-download (modelo/refiner/LoRAs/sampler/aspect ratio = um produto); pipeline de qualidade EMBUTIDO (expansor de prompt GPT-2 offline, negative ADM guidance, SAG calibrado, CFG+TSNR); refiner swap dentro de UM k-sampler (reaproveita momentum do ODE); Advanced colapsado; wildcards + reweighting A1111. AMD: ROCm ≈1.5× mais lento que RTX 3xxx. (4) InvokeAI: backend profissional (FastAPI + invocations node-based, CLI auto-gerado dos metadados); Unified Canvas (bounding box de geração ≠ máscara; processar menor + upscale); low-VRAM mode (streaming de camadas RAM↔VRAM, caches dinâmicos, 3GB working memory reservada); FP8 storage com cast por camada (~50% VRAM, quase lossless, funciona sem tensor cores); SDNQ 4-bit + correção SVD rank-32 (Flux schnell ~12GB vs 33GB); LoRA como sidecar (residual paralelo, não merge em peso quantizado); Model Manager com SQLite + imohash + download queue. AMD: suportado SÓ Linux via ROCm. |
| **Next** | (1) CAMINHO MAIS DIRETO para "produzir áudio/vídeo com a velocidade que temos": stable-diffusion.cpp via Vulkan/ROCm com binding Go non-cgo — single-binary, sem torch, já faz Wan local. Validar na 6700 XT. (2) Padrão UX: presets JSON declarativos (Fooocus) + canvas/bounding-box (InvokeAI) — sem o monolito web. (3) Padrões do engine: offload por componente/dtype, FP8 storage por camada, quantização + correção SVD, caches de steps com threshold. (4) NÃO copiar: dependência CUDA/xformers para velocidade (Vulkan é o piso seguro AMD), auto-download multi-GB no 1º boot em produção, refiner de 2 passadas separadas, monolito React/FastAPI para produto single-user. |
