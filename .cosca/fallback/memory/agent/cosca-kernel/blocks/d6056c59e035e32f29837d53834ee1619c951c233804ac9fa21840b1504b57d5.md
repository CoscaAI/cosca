PREV: ad4837e38673c05eb3d8142ba2b9ce5b23cadd4806f3d0cec3eb71269f24e099
ID: L149
TIME: 2026-08-13
LEVEL: 3
TAGS: #falso-negativo #auto-jail #bwrap #unshare-pid #project-root #status #doctor #runtime-status #level-3
---
## L149 — 2026-08-13 — Falso negativo do status/doctor/runtime: auto-jail bwrap monta workspace em "/" e remapeia PID | Level 3

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "resolve" (após diagnóstico pré-desafio). Corrigir o falso negativo em que `status`/`doctor`/`runtime status` reportavam "Runtime: stopped/uninitialized" e "Project: /" apesar do runtime daemon estar de pé e saudável (L3, health verde, portas 14120/14123/11434 escutando). |
| **Technique** | Level 3 — **(1)** Diagnóstico de contradição: `ss` (portas escutando) + `health` ("All systems operational") + `capability level` (L3) CONTRADIZIAM `status` ("stopped", "Project: /"). **(2)** Rastreio da cadeia: status.go usa `os.Getwd()` + `rt.New().State()`; runtime status usa `readDaemonPID`+`processAlive`. **(3)** Causa raiz em `cmd/cosca/main.go` `isAdminCommand()`: comandos de leitura NÃO-admin reexecutam no auto-jail bwrap, que monta o workspace em "/" (`--bind <workspace> /`, `--chdir /`) e faz `--unshare-pid` — dentro da jaula `os.Getwd()="/"` e `processAlive(pid-do-host)` dá ESRCH. Confirmado via `ps --forest`: serve/runtime = `./bin/cosca` (pai, PID host) → `bwrap --bind /home/cosca/Documents/cosca / ... --unshare-pid` → `/proc/self/fd/3` (filho na jaula). **(4)** Correção dupla: (a) `isAdminCommand()` passa a tratar status/doctor/health/capability/fabric + subcomandos `runtime status/stop/logs/info` como admin (fora da jaula — leitura/controle puro, sem código de agente); `runtime start/restart` permanecem na jaula. (b) `status.go` troca `rt.New().State()` (instância nova → sempre "uninitialized") por `readDaemonPID`+`processAlive` (mesma lógica do doctor) + helper `processUptime` lendo `/proc/<pid>/stat` (starttime). (c) `status.go` reusa `activeProviderFromConfigDoctor` para reportar "Provider: ollama" em vez de vazio. **(5)** Teste `TestIsAdminCommandIncludesTerminal` ampliado para os novos casos. |
| **Level** | 3 |
| **Outcome** | success — `status` agora: 🟢 Runtime: running | Provider: ollama | Project: /home/cosca/Documents/cosca (era 🔴 uninitialized / vazio / "/"); `runtime status`: running PID 849424; `doctor`: ✓ Runtime Daemon + Health. gofmt/vet limpos, testes verdes (cmd/cosca + internal/cli). Binário rebuildado e reinstalado no global (~/.cosca/bin/cosca). Serviços reais intactos — nada foi reiniciado (a correção é só no path de leitura/diagnóstico). |
| **Confidence** | 0.95 (validado: 3 comandos verdes contra o runtime real, vet/testes limpos, ps --forest confirmou a jaula ativa) |
| **Tags** | #falso-negativo #auto-jail #bwrap #unshare-pid #project-root #status #doctor #runtime-status #level-3 |
| **Related** | L190 (runtime religado), L189 (reload serve), L173 (detecção de capacidade via TCP dial), L199 (binário global desatualizado) |
| **Learned** | **(1) O auto-jail bwrap monta o workspace em "/" (--bind <workspace> /) e remapeia PIDs (--unshare-pid): QUALQUER comando dentro da jaula que use os.Getwd() vê "/" e processAlive(pid-do-host) vê ESRCH — comandos de DIAGNÓSTICO (status/doctor/runtime status) precisam rodar FORA da jaula para reportar a verdade do host.** **(2) `health`/`capability` já reportavam correto DENTRO da jaula porque usam TCP dial (atravessa via --share-net), não os.Getwd() nem processAlive — o sintoma estava restrito aos comandos que leem PID/path.** **(3) `status` instanciava `rt.New()` e lia `.State()` — SEMPRE "uninitialized" porque é instância nova nunca iniciada; o daemon real é outro processo. PID file + processAlive é a única fonte de verdade (o doctor já sabia, o status não).** **(4) `make build` gera bin/cosca 644 de propósito ("protegido"); `make install` copia para ~/.cosca/bin/cosca com +x. Não dar chmod +x no local — é comportamento pretendido.** **(5) `providers.NewManager()` nunca carrega a config → `Status().Active` é sempre vazio; o provider ativo vem de `config.Load().Provider.Name` via `activeProviderFromConfigDoctor`.** |
| **Next** | (1) 2 commits pendentes de push para origin/main-v2 (L246 MINA + L247 COSCA NODE) — aguardando ordem do Don. (2) Avaliar `make install` completo (atualiza também cosca-chat/desktop). (3) Editor integration (.vscode/tasks.json) continua warning — opcional. |
