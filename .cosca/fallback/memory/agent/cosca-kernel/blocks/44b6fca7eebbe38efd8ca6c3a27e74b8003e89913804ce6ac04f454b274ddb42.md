PREV: c79f66786a84ee45d1154cb57e3c50491d0e6e98121623a592ee91fcbea6a0f7
ID: L288
TIME: 2026-08-16
LEVEL: 4
TAGS: #checkup #serve #systemd #transient-unit #environmentfile #jwt #jaula #religada #root-cause #exit-code #level-4
---
## L288 — 2026-08-16 — Checkup do carro: serve caído por unit transient (sem serve.env) — religado na jaula | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ordenou "carro" (= checkup do Cosca). Serve daemon estava CAÍDO desde 00:44 (status=2/INVALIDARGUMENT, ~3h28min). Investigar a causa e religar. |
| **Technique** | Level 4 — **(1)** Diagnóstico em camadas: `cosca doctor` (Runtime daemon ⚠) + `systemctl --user status` (transient unit, exit-code 2) + `journalctl` (histórico de quedas) + `lspci`/`rocminfo` (VERDADE do hardware: RX 6700 XT gfx1030, ROCm presente — `cosca hardware` reportava "no" = falso negativo). **(2)** Causa raiz por difusão: a unit ATIVA era `/run/user/1000/systemd/transient/cosca-serve.service` — criada programaticamente, SEM `EnvironmentFile` e SEM `Restart` — em contraste com `deploy/cosca-serve.service` (correta: `EnvironmentFile=%h/.config/cosca/serve.env` + `Restart=always` + hardening) que NUNCA foi instalada. Sem o serve.env, o `COSCA_JWT_SECRET` não chega ao processo (serve.go:551 recusa). **(3)** Religada: copiar as units corretas → `reset-failed` para despejar a transient fantasma → `daemon-reload` → `enable --now`. Serve subiu NA JAULA (bwrap `--dev /dev --proc /proc`, JWT via jail-secrets, NUNCA no argv) → `/ready` = knowledge/memory/runtime healthy. |
| **Level** | 4 |
| **Outcome** | success — serve UP e saudável em 14120 (NRestarts=0), doctor 100% verde, auth fechada (401 na raiz). |
| **Confidence** | 0.92 (validado: endpoint `/ready` respondendo com subsistemas healthy, argv ao vivo sem JWT, unit persistente carregada e enabled) |
| **Tags** | #checkup #serve #systemd #transient-unit #environmentfile #jwt #jaula #religada #root-cause #exit-code #level-4 |
| **Related** | L124 (religada com jaula + serve.env), L119/L120 (reload/religada do runtime), L149 (falso negativo do status), L56 (kill -TERM, nunca pkill), L199 (ordem sagrada commit→sign), serve.go:551 (JWT obrigatório), jail.go:626 (jailExitCodeFromRun) |
| **Learned** | **(1) Unit transient do systemd "sombreia" a persistente de mesmo nome: `systemctl --user enable` falha com "Unit ... is transient or generated" — a correção é `reset-failed` (despeja a transient falha da memória do systemd) → `daemon-reload` → `enable`.** **(2) O serve É dependente de `EnvironmentFile=serve.env`: sem ele, `COSCA_JWT_SECRET` não existe e o serve recusa/degrada — a unit transient rodava `cosca serve` pelado e por isso caía.** **(3) "status=2/INVALIDARGUMENT" do systemd é só o NOME que o systemd dá ao exit code 2 — não é erro de "InvalidArgument"; o 2 real é o exit code do processo interno da jaula propagado por `jailExitCodeFromRun`.** **(4) `cosca hardware` reporta "ROCm: no" mas `rocminfo` mostra gfx1030 presente — falso negativo do probe; a régua P13 manda medir com lspci/rocminfo, nunca assumir.** |
| **Next** | (1) Verificar se há outras units transient fantasma (serve/runtime/voice) e instalar as persistidas de `deploy/`. (2) Monitorar `NRestarts` nas próximas horas. (3) Investigar a causa do exit 2 ORIGINAL (se era OOM do MemoryMax=1G ou outra) se voltar a cair. |
