# L383 — verify --fix corrigido: o repair respeita as marcacoes + o recheck nao engole erro

## status: DECISION (correcoes aplicadas; investigacao dos 336 concluida)

## O que a rodada revelou (a autorizacao do re-embed refutou a hipotese)
- "336 orphaned chunks" do verify = chunks SEM DOCUMENTO PAI (nao sem
  vetor!) — a hipotese inicial (re-embed) REFUTADA: nao ha orfaos limpos
  sem vetor (0; os 11.906 sem vetor sao os triviais 8.671 + duplicados
  3.235 marcados — por design da L360).
- ORIGEM dos 336: blocos de MEMORIA indexados como chunks (conteudos
  "PREV:", "ID: L296", "TIME:", "LEVEL:", "TAGS:") com um document_id
  (b004848c...) nao persistido em documents. Estado: 252 ja com vetor
  (do repair anterior), 84 sem.

## Correcoes aplicadas
1. RepairOrphanVectors: selecao agora respeita is_trivial=0 AND
   dedup_of='' — o fix NAO reintroduz o lixo marcado (o repair anterior
   embedaria os 11.906, incluindo os marcados!).
2. countOrphanVectors: mesmo filtro — o log de init nao alerta mais sobre
   os marcados (por design).
3. verify --fix recheck: o erro de ke.Verify() NAO e mais engolido (era
   `recheck, _ := ke.Verify()` — "no issues found" FALSO quando o Verify
   falhava).

## Descoberta adicional
O recheck SEMPRE falhou: "knowledge engine already initialized" (o Verify
do ke ja inicializado retorna erro) — o "no issues found" do fix era falso
historicamente. A correcao expoe o bug: o recheck precisa de um ke fresco
OU o Verify tem um problema com o estado inicializado (investigacao propria).

## Classificacao
MEASURED 336 sem documento (252 com vetor, 84 sem); repair/count/recheck
EVIDENCE a limpeza funciona (11.906 = marcados); o fix anterior era perigoso
INFERRED os 336 sao lixo de indexacao da memoria (duplicado do embed)
HYPOTHESIS remover os 336 chunks orfaos de documento e seguro (conteudo
           ja no embed) — transformacao que requer autorizacao
UNKNOWN o "already initialized" do recheck (origem exata); os 84 sem vetor
DECISION correcoes aplicadas; proximas: (1) investigar o "already
         initialized"; (2) decidir a limpeza dos 336 — aguardando o Don.
