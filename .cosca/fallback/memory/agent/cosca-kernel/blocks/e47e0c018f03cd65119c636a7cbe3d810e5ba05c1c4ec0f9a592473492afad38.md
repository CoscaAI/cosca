PREV: df837ec06a4ef11007b741d69a99bf2b7bf4587d7575ed518b7cce5149f54d22
ID: L39
TIME: 2026-08-11
LEVEL: 4
TAGS: #google #osv-scanner #osv-dev #vulnerability-scanning #cve #dependencies #security #supply-chain #level-4
---
## L39 — 2026-08-11 — Mining Google (2,891 repos): osv-scanner → cosca security scan (114 CVEs found, vitest critical fixed) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don found https://github.com/google (2,891 repos) and called it "diamante azul". Analysis highlighted osv-scanner (10.8K stars, Go, Apache-2.0, SLSA 3 — "Vulnerability scanner written in Go which uses the data provided by osv.dev") as the top priority: it closes a real gap (cosca never scanned its dependencies for CVEs). Also identified perfetto (tracing), fuzztest (fuzzing), zx (scripts), styleguide, tunix (LLM post-training). Don: "sim" → implemented cosca security scan. |
| **Technique** | Level 4 — osv-scanner mining: OSV.dev database (open, covers GitHub Security Advisories/RustSec/Ubuntu, 11+ languages, 12+ package managers, machine-readable OSV format), Go library API (osvscanner.DoScan → models.VulnerabilityResults), guided remediation (recommends upgrade versions by dependency depth/severity/fix strategy/ROI), call analysis (fewer false positives — checks if vulnerable function is actually used), osv-scalibr extensible engine underneath. Implementation: internal/security/scan.go (wraps osvscanner.DoScan v2.3.0, CVSS severity via pandatix/go-cvss, severity filter, result model), cosca security scan [dir] (text/JSON output, CVE/GHSA IDs + severity + OSV URL, exit 0/1/2, --json --severity --exit-zero), integration: doctor Security check + qgate blocks CRITICAL/HIGH (--skip-security-vulns), security in admin bypass (runs outside jail with network). Pinned v2.3.0 (go 1.25.4) to minimize go.mod churn. |
| **Level** | 4 |
| **Outcome** | success — CRITICAL FINDING: scanning the cosca repo itself revealed 114 vulnerabilities: 2 CRITICAL, 35 HIGH, 18 MEDIUM, 3 LOW, 56 UNKNOWN. Critical: vitest@1.6.1 GHSA-5xrq-8626-4rwp (arbitrary file read/exec via Vitest UI server). High (Go): containerd 1.7.29 (runAsNonRoot bypass), docker/cli 28.3.3 + docker/docker 28.3.3 (LPE, docker cp race, AuthZ bypass), go-git v5.16.3 + go-billy 5.6.2 (crafted-object parse, symlink follow), moby/buildkit 0.23.2 (file escape). These Go deps are TRANSITIVE of osv-scalibr — used only during scanning (container analysis), NOT in production runtime. Fixed the critical vitest: 1.6.0 → 4.1.10 in sdk/typescript (both package-lock + pnpm-lock). Validation: all tests PASS; E2E security scan . → reports 114 vulns with IDs/severity, exit 1; --json valid; doctor includes Security check; qgate blocks. Chain 46 blocks valid. |
| **Confidence** | 0.96 (validado: all tests PASS, build/vet clean, E2E security scan 114 vulns, exit codes 0/1/2, doctor+qgate integration, chain 46 blocos) |
| **Tags** | #google #osv-scanner #osv-dev #vulnerability-scanning #cve #dependencies #security #supply-chain #level-4 |
| **Related** | L197 (mining Anthropic skills), L196 (mining OpenAI evals), L195 (mining OpenAI codex), L174 (auditoria enterprise — segurança) |
| **Learned** | **(1) The scanner found real problems the code audits missed — dependency CVEs are a blind spot that only automated scanning catches.** **(2) "The tool that finds vulnerabilities brings its own vulnerabilities": osv-scalibr's transitive deps (docker/containerd/buildkit) are themselves vulnerable — acceptable trade-off since they're scan-time only, but worth documenting.** **(3) exit-code discipline (0 clean / 1 vulns / 2 error) is essential for CI gates.** **(4) Google's OSV.dev open database is superior to closed scanners — community-maintained, machine-readable, covers all ecosystems.** **(5) Every mining session should include a self-scan of the cosca's own deps — the car needs a mechanic's inspection, not just new parts.** |
| **Next** | (1) Update the remaining HIGH Go deps if safe (some are scan-time only — evaluate). (2) Consider call analysis to reduce false positives. (3) Wire security scan into CI (already in qgate). (4) Keep mining: perfetto (tracing), fuzztest (fuzzing). |
