PREV: d06d62fbbbfb8365ed27ba6edcf8e73d1f5da7a35d38c6ec969dd023fc5208de
ID: L44
TIME: 2026-08-11
LEVEL: 4
TAGS: #aws #durable-execution #event-sourcing #replay #idempotency #pipeline #resume #crash-recovery #hash-chain #mining #level-4
---
## L44 — 2026-08-11 — Mining AWS: DURABLE EXECUTION (event sourcing + replay) → DurableEventLog com hash-chain + resume idempotente | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don found https://github.com/orgs/aws/repositories (554 repos) and asked to mine the Go repos specifically (3 pages, 77 Go repos). Analysis identified: aws-xray-sdk-go (tracing), amazon-cloudwatch-agent (observability), amazon-ec2-spot-interrupter (chaos engineering), go-kafka-event-source (event sourcing), aws-node-termination-handler (graceful shutdown), aws-ebpf-sdk-go, amazon-ec2-instance-selector, amazon-ssm-agent. The diamond: DURABLE EXECUTION (event sourcing + replay) — the pattern that makes workflows survive crashes (AWS Lambda Durable Execution / Temporal). Don: "sim" → implemented. |
| **Technique** | Level 4 — **(1)** `internal/pipeline/durable_events.go` — DurableEventLog: append-only JSONL per run with SHA-256 hash chaining (genesis=zeros, VerifyChain detects tampering), events plan_created/step_started/step_completed/step_failed/run_completed/run_failed, monotonic seq, cached result on completions, trace_id persisted in first event and reused across crash/resume. **(2)** Idempotency — state = replay of events; completed steps' cached success results are cloned and SKIPPED on resume; failures never cached (RecoveryLoop retries); nil log = legacy path (backward compat). **(3)** StepRunner gained durable log + trace store, SetDurableLog/EnableDurableLog/ResumeDurable/WithDurableLog/NewStepRunnerWithOptions, integrated into RunPlanWithProgress and RunPlanParallel; planner.go gained TaskResult.IsSuccess()/Clone(). **(4)** CLI — `--resume` flag on `cosca pipeline run` and `workflow run`, stale-run auto-detection (events without terminal), stable run IDs per prompt (PIPE-PROMPT-<sha256>), maybeResumeDurable + noticeStaleRuns; pipeline_wiring.go enables durable (.cosca/durable-events/) + trace store for workflow/pipeline/run/eval paths; terminal.go enables durable + stale notice. **(5)** Trace integration — each durable append mirrored as trace.Event (Actor=pipeline, Action=UPPER(type)) into .cosca/trace.db. |
| **Level** | 4 |
| **Outcome** | success — 6 new tests PASS (-race clean): append/load round-trip, tamper detection, idempotent resume (3-task run executes each once; fresh runner replays → call count stays 3), crash resume (t1-t2 done → new runner resumes at t3), failed-step retry, backward compat. E2E REAL: `cosca pipeline run` with qwen2.5-coder:14b ran 5 tasks, was KILLED mid-run → stale log (seq 1-16); `--resume` → "Completed Steps: 1/5 → Resumed from event log", executed only steps 2-5, appended seq 17-35, final 35 events, single trace ID TRACE-20260812-1AA98266 across the crash boundary. Chain 61 blocks valid. |
| **Confidence** | 0.96 (validado: 6 testes com -race, E2E real kill→resume com execução parcial, chain 61 blocos) |
| **Tags** | #aws #durable-execution #event-sourcing #replay #idempotency #pipeline #resume #crash-recovery #hash-chain #mining #level-4 |
| **Related** | L202 (cosca-desktop), L201 (Enterprise Command Center), L199 (mining adk-go), L198 (mining osv-scanner), L192 (mining methodology) |
| **Learned** | **(1) Durable execution (event sourcing + replay) is the pattern that makes an automation platform INDESTRUCTIBLE — a crash is an event, not a failure; state is always reconstructable.** **(2) The "state = replay of events" model + idempotent steps is elegant: you never store mutable state, only facts; completion is a cached event, not a flag.** **(3) Failures must NOT be cached — retries need the original failure to re-attempt (only successes are durable).** **(4) Backward compat via nil-log opt-in is the safe integration pattern for a core engine.** **(5) Trace ID persisting across crash/resume keeps the desktop execution tree WHOLE — observability survives crashes too.** **(6) The mining arc continues to validate: each org yields a durable-worthy pattern; the AWS diamond was DURABILITY (vs the previous features).** |
| **Next** | (1) Durable checkpointing for the desktop arena (multi-model parallel). (2) Chaos testing in cosca eval (spot-interrupter pattern — kill a service mid-eval). (3) CloudWatch-style host metrics for the HUD. (4) Consider exposing durable runs in the desktop sessions panel. |
