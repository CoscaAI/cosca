PREV: ef4cd8b7c2d74ccf2a936c29d00db9fc23bc202a6a51f03388c8ce2c8836a192
ID: L152
TIME: 2026-08-13
LEVEL: 3
TAGS: #cosca-node #time-travel #event-store #replay #fold #timeline #level-3
---
## L152 — 2026-08-13 — COSCA NODE Fase 2: Time Travel (event store append-only + replay engine + timeline UI) | Level 3

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "continue de onde parou da fase 2" — implementar o Time Travel do COSCA NODE (spec seções 21/22: voltar no tempo e reconstruir "como o cérebro chegou aqui"). |
| **Technique** | Level 3 — **(1)** `internal/store/store.go`: Event Store append-only (log cronológico = fonte de verdade para replay; RWMutex; All() devolve cópia ordenada; TimeRange). **(2)** `internal/replay/replay.go`: Replay Engine — `Reconstruct(events, at)` faz FOLD dos eventos (timestamp ≤ at) num grafo novo, e `Frames` devolve um instantâneo por evento. **O grafo é uma FUNÇÃO dos eventos — replay é determinístico.** **(3)** Endpoints: `/timeline` (todos os eventos + range) e `/graph?at=<timestamp>` (grafo reconstruído naquele instante). **(4)** Frontend `useTimeline.ts`: carrega o histórico, gerencia play/pause/rewind/FF/live + slider, e reconstrói o grafo via `/graph?at=` conforme navega. **(5)** Simulação agora gera IDs ÚNICOS por ciclo (sufixo -c<N>), então o grafo CRESCE — Time Travel mostra um cérebro menor no passado (verificado: 7 nodes no evento 5 → 34 nodes no presente). |
| **Level** | 3 |
| **Outcome** | success — Time Travel funcional de ponta a ponta: store + replay + endpoints + timeline UI + grafo que cresce. Verificado: `/graph?at=` reconstrói 7 nodes no passado vs 34 no presente. A base para path tracing/causal explorer (seções 23/24) está pronta — é só navegar o fold. |
| **Confidence** | 0.90 (validado: /timeline + /graph?at= reconstroem corretamente, build Go + frontend limpos) |
| **Tags** | #cosca-node #time-travel #event-store #replay #fold #timeline #level-3 |
| **Related** | L247 (COSCA NODE fundação), L192 (Neural Link), spec seções 21/22/23/24 |
| **Learned** | **(1) O padrão "grafo é um FOLD sobre eventos" é a chave do Time Travel: em vez de guardar snapshots, guarda-se o LOG append-only e reconstrói-se qualquer estado aplicando eventos ≤ T. Determinístico, barato, e o mesmo mecanismo alimenta live (fold incremental) e replay (fold até T).** **(2) O Event Store é a fonte de verdade; o "grafo vivo" é só o fold incremental — um é derivável do outro, nunca se mantém dois estados divergentes.** **(3) Para o Time Travel ser visualmente significativo, a simulação precisa CRESCER o grafo (IDs únicos por ciclo), senão o replay satura no mesmo conjunto de nós e o "voltar no tempo" não mostra diferença — foi o bug que pegou na verificação (grafo saturado em 14 nodes).** **(4) O correlation_id/causation_id do L247 agora tem consumidor real: a timeline reconstrói a CADEIA causal (evento → evento → evento), e o próximo passo (path tracing/causal explorer) é só navegar esse fold.** **(5) Detalhe operacional: `setsid nohup` em background morre quando o comando do shell estoura o timeout — subir o daemon num comando DEDICADO e verificar em outro evita o "backend sumiu".** |
| **Next** | (1) Fase 3: Path Tracing + Causal Explorer (seções 23/24 — o "WHY?" navegando o fold). (2) Cosca Connector (eventos REAIS). (3) Wails desktop. (4) Performance (instancing/culling). |
