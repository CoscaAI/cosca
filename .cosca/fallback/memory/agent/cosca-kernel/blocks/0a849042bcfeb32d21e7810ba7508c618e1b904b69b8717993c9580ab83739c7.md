PREV: 2d9dee6b5e87aeb6a92674ae16f4a44b6fb0cd7836e76d40e2b6b5b1a47862db
ID: L88
TIME: 2026-08-13
LEVEL: 4
TAGS: #model-registry #modelreg #models #capabilities #vram #quantization #license #tasks #fase-1 #cosca-engine #level-4
---
## L88 — 2026-08-13 — Fase 1.4: Model Registry — modelos concretos ligados às 18 tasks (§18) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuação do Cosca Creative Ecosystem (L218-220): etapa 1.4 — Model Registry: registrar modelos CONCRETOS do projeto (formato, quantização, VRAM, capabilities, licença) e ligá-los às 18 tasks do AI Task Engine. |
| **Technique** | Level 4 — **(1)** Novo package `internal/modelreg` (separado de `internal/models` que é o catálogo models.dev com context/pricing): `Model` (ID/Provider/Version/Format/Quantization/VRAM/Capabilities[aitask.Type]/Kind local|remote/License/Source) + `Registry` em `<projeto>/.cosca/models/index.yaml`, chave única provider/id/version. **(2)** Ligação direta com o aitask: `FindForTask(task)` devolve os modelos que executam cada uma das 18 tasks — o orquestrador pergunta "quem faz STT?" e recebe whisper. **(3)** Formatos/quantizações canônicos (safetensors/gguf/onnx/torch/tensorrt; fp16/int8/int4) validados. **(4)** License é campo obrigatório de conhecimento (§35) — provenance desde o registro. **(5)** CLI `cosca model add/list/info/remove`: `--task` repetível, filtros por task/kind, chave com prefixo, `--json`. **(6)** Convenção de nomes: `models` = catálogo externo, `model` = registry do projeto. |
| **Level** | 4 |
| **Outcome** | success — Registry + CLI implementados, 12 testes core + 1 CLI verde, esteira completa verde, testado E2E (whisper STT + tesseract OCR registrados, filtro por task, prefixo, JSON, remove). |
| **Confidence** | 0.93 (validado: ligação tasks↔modelos, persistência entre reopens, CLI E2E) |
| **Tags** | #model-registry #modelreg #models #capabilities #vram #quantization #license #tasks #fase-1 #cosca-engine #level-4 |
| **Related** | L220 (aitask — o registry liga modelos às 18 tasks), L219 (asset), L216 (provenance §33/§35) |
| **Learned** | **(1) Separar catálogo (models.dev — o que existe no mundo) de REGISTRY (modelreg — o que ESTE projeto usa): um informa, o outro decide.** **(2) A chave provider/id/version é o contrato de unicidade — mesmo id com versões diferentes coexistem (whisper large-v3 vs small).** **(3) Ligar modelos a TASKS (não a produtos) é a jogada de arquitetura: o orquestrador resolve "quem faz essa tarefa?" em vez de cada produto saber de modelos.** **(4) VRAM em bytes desde o registro permite ao GPU Probe (1.5) e ao scheduler (§28) decidir antes de agendar.** **(5) Licença do MODELO é separada da licença do CÓDIGO (§35) — modelo MIT ≠ código MIT.** |
| **Next** | (1) Etapa 1.5: GPU Probe (rocminfo programático + scheduler CPU/GPU/remote). (2) Etapa 1.6: Node Graph v1 (serializável + cache por assinatura). (3) Integrar registry+task ao orchestrator para resolver executores reais. |
