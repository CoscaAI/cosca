PREV: 441fd8c9fd38ecbe384eef10ac8eb15a7b0d3a9bacf5c32e687fd8883379e7c4
ID: L86
TIME: 2026-08-13
LEVEL: 5
TAGS: #node-graph #ngraph #workflow #dag #topological-order #signature #cache #content-addressable #comfyui #p1 #fase-1 #cosca-engine #level-5
---
## L86 — 2026-08-13 — Fase 1.6: Node Graph v1 — grafo serializável + cache por assinatura de inputs (§21/§23) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuação do Cosca Creative Ecosystem (L218-222): etapa 1.6 — o coração da plataforma: Node Graph serializável (§21) com execução por demanda e cache por assinatura de inputs (§23), princípio P1 do ComfyUI. |
| **Technique** | Level 5 — **(1)** Novo package `internal/nodegraph`: `Node` (ID/Type/Params/Inputs) + `Graph` (Name/Nodes/EngineVersion) — DADOS PUROS serializáveis em JSON (round-trip preserva assinaturas). **(2)** Validação: IDs únicos, inputs existentes, DAG sem ciclos (Kahn). **(3)** `TopoOrder()` — ordenação topológica determinística (dependências primeiro, fila por inserção). **(4) O CORAÇÃO — `Signature(nodeID)`: hash sha256 de (type + params canônicos ordenados + assinaturas recursivas dos inputs). Cache por assinatura RECURSIVA = mudança em qualquer ancestral invalida o descendente (anti-padrão A3 do Blueprint: nunca resultado obsoleto).** **(5)** Executor por demanda: `Run(ctx, exec, cache)` — ordem topológica, cada nó recebe saídas dos inputs, cache hit não re-executa (§23). Testes provam: 2ª execução = 0 executados/3 hits; mudar param do ancestral = 3 re-executam; nó independente = hit. **(6)** CLI `cosca ngraph validate/sig/run [--cache]` — nome ngraph (node graph) porque `cosca graph` pertence ao knowledge graph do kernel. **(7) ERRO DO CONSELHEIRO reconhecido: sobrescrevi o internal/cli/graph.go existente (knowledge graph) com meu comando — restaurado imediatamente via git, renomeado para ngraph. Regra 38 (nunca destruir funcionalidade existente) quase violada; verificado antes de continuar.** |
| **Level** | 5 |
| **Outcome** | success — Node Graph + executor + cache implementados, 17 testes verdes, CLI E2E (workflow video-subtitles: validate→sig→run, ciclo detectado, cache provado). |
| **Confidence** | 0.95 (validado: assinatura recursiva testada (ancestral muda→descendente invalida), round-trip JSON, cache hits/misses E2E) |
| **Tags** | #node-graph #ngraph #workflow #dag #topological-order #signature #cache #content-addressable #comfyui #p1 #fase-1 #cosca-engine #level-5 |
| **Related** | L222 (scheduler — cada nó decidirá onde roda), L221 (modelreg — executor resolve modelo da task), L220 (aitask — tipos de nó são tasks), L210 (cosca-media — executores reais) |
| **Learned** | **(1) O node graph é o CONTRATO da plataforma: dados puros serializáveis, executados por demanda — produtos (Editor/Image/Cinema) montam grafos, as engines executam.** **(2) Cache por assinatura RECURSIVA é o que diferencia um node graph profissional de um script: re-execução idempotente (0 nós re-executados), invalidação em cascata correta (ancestral muda → descendentes re-executam), nós independentes preservados.** **(3) Params canônicos (keys ordenadas) garantem assinatura estável entre builds do mesmo grafo.** **(4) NUNCA sobrescrever arquivo existente sem verificar — o `graph.go` do kernel (knowledge graph, 427 linhas) quase foi destruído; o reflexo correto é verificar `git show HEAD:arquivo` ANTES de escrever em arquivo novo do repo.** |
| **Next** | (1) Etapa 1.7: Render Engine (preview/draft/final + cache por nó + resumível). (2) Executores REAIS no ngraph: ligar tipos de nó às tasks do aitask + modelos do modelreg + scheduler do sched (cada nó decide onde roda). (3) Workflows de exemplo (image-pipeline, video-pipeline) commitados. |
