PREV: 4dc3558e17b34c304734f1754aed0b671d896fdd4ef35671152dee84c508a864
ID: L311
TIME: 2026-08-16
LEVEL: 5
TAGS: #autopsia #performance #vector-search #brute-force #sqlite-modernc #full-scan #gargalo #evidencia #cosca-vector-search-autopsy #level-5
---
## L311 — 2026-08-16 — AUTÓPSIA DO VECTOR SEARCH: o gargalo é ARQUITETURA (sem índice, full-scan O(N) via SQLite puro em Go) — não é hardware | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Autópsia de performance da busca vetorial (missão do Don + Professor) — descobrir empiricamente por que é mais lenta que as demais operações |
| **Technique** | Autópsia em 15 fases: execution map, instrumentação, microbenchmark, cold/warm, escala, dimensão, brute force vs index, memória, CPU, I/O, hardware, pprof, A/B, matriz de causas |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.92 |
| **Tags** | #autopsia #performance #vector-search #brute-force #sqlite-modernc #full-scan #gargalo #evidencia #cosca-vector-search-autopsy #level-5 |
| **Related** | L254, L126, L12, L305, L303 |
| **Learned** | VEREDICTO COM EVIDÊNCIA: o gargalo é COMBINAÇÃO (O) com hierarquia clara. Causa raiz 1 (dominante, ~85%): NÃO EXISTE ÍNDICE VETORIAL — Stats() reporta index_type="flat_bruteforce"; busca sem candidatos lexicais faz full-scan O(N) re-decodificando 100% do store via SQLite puro em Go (modernc: pread + page decode + VM) a cada query. Busca completa em produção (60.358 vetores) ≈ 250ms — a latência perceptível. Causa raiz 2 (~10%): cada BLOB é alocado DUAS vezes (columnBlob 47,4% + bytes.Clone 46,9% — 2,1× amplificação, 649MB/op a 100K, pressão de GC). Causa raiz 3 (~5%): dot escalar não-vetorizado com decode byte-a-byte (52% do compute). REFUTADO com evidência: NÃO é hardware (compute usa ~1,7 núcleos médios, NVMe subutilizado a 787MB/s, RAM sobra); NÃO é CPU (paralelo escala 5,9× até 8 cores e wall não melhora — o serial domina); NÃO é lock (block profile só WaitGroup 2,3%). ACHADO LATENTE: sqrt() Newton customizado (vector.go:194) = 40,6ns vs math.Sqrt 0,25ns (161×) — fora do hot path da busca, mas afeta embedding/NormalizeVector. DADOS: scaling linear perfeito O(N) 10K=48ms/100K=412ms/1M=4,1s (throughput ~0,24 Mvec/s constante); cold→warm só −12% (o steady-state ~380ms é custo de decode, não disco frio); caminho bounded (candidatos FTS5 + pool 250) = 2,1ms = 214× mais rápido; crossover: bounded só perde quando M≈N−250. O caminho da busca em produção: LayeredSearch (FTS5→BM25→vectorLayer) com MinFTSCount=0/MinBM25Score=999 FORÇANDO L3 toda query. |
| **Next** | (1) Experimentos recomendados com before/after: full-scan in-memory (carregar float32 em array contíguo, ganho estimado scan 374→<30ms ≈90%); BLOB contíguo SoA; dot com unroll/SIMD; ANN real (HNSW) para caso sem candidatos (250ms→<5ms); trocar sqrt() por math.Sqrt; pool de buffers (evitar clone por linha). (2) Registrar cada otimização comprovada no Performance Brain com contexto (quando usar / quando NÃO usar). (3) NÃO otimizar no escuro — toda otimização com before/after (régua do Professor). |
