PREV: be86ba94c7c4699c48cede0425359c4d8de4e302ca9770bbec54e1224d8c974a
ID: L32
TIME: 2026-08-11
LEVEL: 4
TAGS: #evals #benchmark #harness #10x #yaml-suites #esteira #pipeline #verify #timeout #level-4
---
## L32 — 2026-08-11 — `cosca eval`: benchmark harness YAML inspirado no design de evals da 10x (o ouro era o harness, não o Swift) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don found https://github.com/10x-app-builder/10x (267-star open-source SwiftUI app builder with an eval harness) and said "acho que achei ouro". After analysis, the gold was NOT the Swift code (PolyForm Noncommercial license, macOS-only) but the EVAL HARNESS DESIGN: YAML-declared benchmark suites with scripted steps, timeouts per phase, question strategies, and verification commands. |
| **Technique** | Mined their eval design (evals/README.md, scripts/evals, smoke-suite.yml, ROADMAP.md). Key patterns: suite YAML (suite/defaults/cases), steps (wait_for_plan, send_message, wait_for_preview, stop), question_strategy (skip/answers_then_skip/fail), timeouts per phase, artifacts persisted, --case selector, CLI wrapper (list/smoke/run). Implemented `cosca eval` — native Go command, Cosca-first. internal/evals/ package (10 files): Suite/Case YAML parsing, LoadSuite (file OR embedded FS), RunSuite with per-case timeout via goroutine+select (never hangs), QuestionStrategy (skip/answers_then_skip/fail with scripted answers), VerifyRunner with per-command timeout + process-group SIGKILL (prevents orphan children), Report persisted to .cosca/evals/reports/<suite>-<ts>.json (0600). internal/cli/eval.go: list/run/smoke/report commands wired to the REAL pipeline via buildPipelineWiring (planner→stepRunner→recovery→DoD — no fake runner). Bundled suites: smoke.yml (3 cases) + esteira.yml (5 cases) in internal/embed/cosca/evals/suites/. |
| **Level** | 4 |
| **Outcome** | success — 21 unit tests pass; E2E: eval list shows cases; eval smoke → 2 passed / 1 failed-with-clear-error (provider down = valid error outcome, NOT a hang) / 0 timeouts, report persisted. Chain 23 blocks valid. |
| **Confidence** | 0.96 (validado: 21 testes unitários, E2E eval list/smoke, report persistido, chain 23 blocos) |
| **Tags** | #evals #benchmark #harness #10x #yaml-suites #esteira #pipeline #verify #timeout #level-4 |
| **Related** | L190 (esteira real), L153 (esteira autônoma), L186 (chain git-anchored) |
| **Learned** | (1) The gold in any repo is often the DESIGN PATTERNS, not the code — license/stack constraints (PolyForm Noncommercial, Swift/macOS) made copying impossible, but the eval harness concept is stack-agnostic and directly applicable. (2) A benchmark harness is the missing piece to measure "is the esteira healthy?" — eval smoke gives a health score. (3) Provider-down must surface as a clear error outcome, never a hang — timeout+goroutine+select is the pattern. (4) Verify commands (go build/go vet) are the Cosca analog of their simulator screenshot — real evidence, not simulation. (5) The eval harness reuses the SAME real wiring as workflow/pipeline run — consistency. |
| **Next** | (1) Add more benchmark cases (refactoring, security-audit real runs). (2) Wire eval smoke into CI as a health gate. (3) Consider a quality score per case (like their Strategy Quality Score). (4) Push to remote. |
