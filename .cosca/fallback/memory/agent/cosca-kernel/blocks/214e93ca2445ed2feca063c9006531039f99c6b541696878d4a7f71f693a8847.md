PREV: 5a39bdcdaf5bdd802fdca15195f49af138d8f6ff30950d4d7ef82398ef563eb7
ID: L127
TIME: 2026-08-13
LEVEL: 2
TAGS: #servicos #serve #runtime #L3 #autonomo #jaula #bwrap #capability #two-port #reload #level-2
---
## L127 — 2026-08-13 — Serve religado: L3 restaurado — autonomous_execution exige DUAS portas (serve 14120 + runtime 14123) | Level 2

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "liga" — religar o `cosca serve` (14120) que caiu e restaurar a cadeia L3-autônomo completa. |
| **Technique** | Level 2 — **(1)** Diagnóstico: `ss` + `ps` mostraram runtime (14123) e ollama (11434) de pé, mas serve (14120) fora do ar; `tcpDial` confirmou 14123 ABERTO e 14120 FECHADO. **(2)** Detecção de estado suspeito: `capability level` reportava "runtime não habilitado / autonomous_execution ✗" MESMO com 14123 respondendo — a inconsistência era o serve caído, não o runtime. **(3)** Subida no padrão pós-incidente (sem systemd, processo direto + self-jail bwrap): `setsid nohup ./bin/cosca serve >> ~/.cosca-serve.log 2>&1 < /dev/null &` — o serve self-jails via bwrap (--unshare-all, COSCA_ENABLE_REGISTRATION=false) igual ao runtime. **(4)** Validação em trio: `ss` (14120 LISTEN) + `curl /health` ({"healthy":true}) + `curl /ready` (knowledge/memory/runtime healthy) + `capability level` → **L3-autônomo com autonomous_execution ✓ disponível**. |
| **Level** | 2 |
| **Outcome** | success — Cadeia L3 completa restaurada: ollama (11434, provider) + serve (14120, REST+jaula) + runtime (14123, gRPC+jaula). `capability level` → L3-autônomo, `autonomous_execution ✓`, 13/13 capacidades disponíveis. |
| **Confidence** | 0.97 (validado: tcpDial 14120/14123, health+ready HTTP, capability L3) |
| **Tags** | #servicos #serve #runtime #L3 #autonomo #jaula #bwrap #capability #two-port #reload #level-2 |
| **Related** | L189 (reload serve+ollama), L190 (runtime religado L3), L173 (detecção via TCP dial), L225 (cofre de conhecimento) |
| **Learned** | **(1) `autonomous_execution` (L3) é um check de DUAS portas — `tcpDial(14123)` (runtime) E `tcpDial(14120)` (serve) — NÃO só o runtime.** Com o serve caído, o capability reporta "runtime não habilitado / autonomous_execution ✗" mesmo com o runtime healthy e o dial 14123 abrindo — sintoma enganoso: o primeiro instinto é culpar o runtime, mas a causa real é o serve. Diagnóstico correto: testar as DUAS portas antes de mexer no runtime. **(2) O serve self-jails via bwrap no startup (padrão pós-incidente L187) — nenhuma flag extra necessária; `setsid nohup ./bin/cosca serve` basta, igual ao runtime.** **(3) Ordem de subida não importa para o capability check (ele só faz dial), mas importa para o runtime em si: ollama (provider) antes do serve.** **(4) A tríade de validação serve/runtime: `ss` (porta) + `curl /health`+`/ready` (HTTP) + `capability level` (nível) fecha o diagnóstico sem reiniciar nada.** |
| **Next** | (1) Commit "memoria: L226 serve religado L3 (ancora 8)" + `cosca-check --sign-auto` para a chain git-anchored (aguardando ordem do Don para commit). (2) Recriar units systemd (serve/runtime/voice) + linger para daemon 24/7 — hoje ainda processo direto. (3) Testar a esteira de verdade com um workflow (`cosca workflow run`) agora que L3 está no ar. |
