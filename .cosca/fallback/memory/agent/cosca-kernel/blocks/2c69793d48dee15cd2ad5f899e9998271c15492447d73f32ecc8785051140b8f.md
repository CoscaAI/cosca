PREV: 0a849042bcfeb32d21e7810ba7508c618e1b904b69b8717993c9580ab83739c7
ID: L89
TIME: 2026-08-13
LEVEL: 4
TAGS: #ai-task-engine #18-tasks #task-catalog #canonical #modality #hardware-class #p3 #fase-1 #cosca-engine #level-4
---
## L89 — 2026-08-13 — Fase 1.3: AI Task Engine — 18 tarefas canônicas normalizadas (§4) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuação do Cosca Creative Ecosystem (L218/L219): etapa 1.3 do roadmap — AI Task Engine: normalizar as 18 tarefas do manifesto §4 em tipos canônicos com dispatch, sem amarrar a execução. |
| **Technique** | Level 4 — **(1)** Novo package `internal/aitask`: enum canônico das **18 tarefas** (§4) + catálogo com metadados (Input/Output modality, HardwareClass cpu/gpu/any/remote, Description, ExecutorHints, RequiresModel). **(2)** P3 do Blueprint aplicado: "Centralize a definição, NÃO a execução" — o Task Engine define o CONTRATO (contrato canônico); executores reais (whisper, tesseract, rembg, diffusers) aparecem como *hints* de conhecimento da Fase 0, plugados depois pelo Model Registry (1.4) e Node Graph (1.6). **(3)** ExecutorHints refletem o terreno real: whisper/faster-whisper/whisper.cpp para STT (ROCM), rembg já instalado para segmentation, tesseract para OCR, llama.cpp/ollama local + deepseek-v4-flash API para text_generation. **(4)** HardwareClass reflete a doutrina: gpu para image/video generation/segmentation/upscale/detection (alvo ROCm gfx1030), cpu para ocr/embedding/simulation/tts, remote para LLM/VLM. **(5)** CLI `cosca task list [--hardware] [--json]` + `cosca task info <type>` com executores. **(6)** Reuso: nada de providers/models foi tocado — catálogo independente. |
| **Level** | 4 |
| **Outcome** | success — 18 tarefas catalogadas, 7 testes verdes, CLI funcional (list com filtro de hardware, info com executores, JSON), esteira completa verde. |
| **Confidence** | 0.92 (validado: enum completo vs manifesto §4, metadados coerentes com o hardware ROCm real, CLI testado) |
| **Tags** | #ai-task-engine #18-tasks #task-catalog #canonical #modality #hardware-class #p3 #fase-1 #cosca-engine #level-4 |
| **Related** | L219 (asset), L218 (Fase 0+1.1), L216 (doutrinas agents/rag — o loop MODEL→GOVERNANCE usa essas tasks), L210 (cosca-media executa segmentation/ocr de verdade) |
| **Learned** | **(1) Normalizar ANTES de executar: um enum canônico das tarefas com modalidades e hardware permite ao orquestrador escolher o pipeline certo (CPU/GPU/remote) por tarefa — o oposto de acoplar cada produto a um provider.** **(2) O catalogo é CONHECIMENTO, não dependência: ExecutorHints documentam o que a Fase 0 descobriu (whisper roda em ROCm, tesseract já está instalado) sem importar nenhuma lib — a execução pluga depois.** **(3) A classificação de hardware é doutrina: geração/segmentação/upscale/detecção pedem GPU; OCR/embedding/simulação/TTS rodam CPU; LLM/VLM ficam remote (ou local via ollama/llama.cpp).** **(4) 18 é o número canônico (§4) — não inventar tarefas fora do contrato; novas tarefas exigem revisão do manifesto.** |
| **Next** | (1) Etapa 1.4: Model Registry (modelos → tasks, formatos/quantização/VRAM/licença). (2) Etapa 1.5: GPU Probe (rocminfo programático). (3) Etapa 1.6: Node Graph v1 (grafo serializável + cache por assinatura). (4) Ligar ExecutorHints a executores reais via providers/cosca-media. |
