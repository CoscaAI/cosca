PREV: b522af35c73f0bd60eb4e6da87ca3bc6543b088cdf7b35bab16b56955b06ccda
ID: L129
TIME: 2026-08-13
LEVEL: 4
TAGS: #desktop #auditoria #nil-json #errorboundary #data-race #symlink #rce #exfiltracao #git-injection #cosca-run #loadchatenv #bugfix #level-4
---
## L129 — 2026-08-13 — Correção dos achados do cosca-desktop (10 fixes) + root cause do `cosca run` exit-1 silencioso | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" — executar as pendências: corrigir os achados da auditoria do cosca-desktop e investigar o `cosca run` exit-1 silencioso em diretório externo. |
| **Technique** | Level 4 — **(1)** Correções backend Go no cosca-desktop (10 arquivos): nil→JSON null (5 casos — workspace.go ListModifiedFiles, ide.go ListWorkspaceFiles/GitStatus, docker.go Containers/ComposeServices/Ports, plugins.go ListPlugins); ErrorBoundary React 19 (frontend/src/components/ErrorBoundary.tsx + main.tsx); 2 data races (engine.busy movido para engine.mu em SendMessage, engine.ring protegido por h.mu em emit); symlink traversal (EvalSymlinks antes de pathWithin em resolveWorkspacePath/GetFileContent/GetFileDiff); DebugStart RCE (pathWithin confina ao workspace); exfiltração de chave (providerKeyTestBaseURL ignora env para API-key providers); git injection (GitCheckout rejeita prefixo `-`); porta stale 11435→11434 (ide.go GetPorts + app.go). **(2)** Root cause do exit-1: `cosca run` (run.go) NÃO chamava `config.Load()`/`loadChatEnv()` — em dir sem `.env`, o provider ollama caía no default "llama3" (não instalado) e o erro era engolido (SilenceErrors + `os.Exit(code)` sem print). **Fix**: run.go agora propaga o provider do config via `loadChatEnv`. Validação: `go vet ./...` (desktop) + `tsc --noEmit` (frontend) limpos; `cosca run` de dir externo volta a responder (exit 0). |
| **Level** | 4 |
| **Outcome** | success — 10+ correções no desktop (todas HIGH/MEDIUM de segurança e qualidade) + fix do `cosca run`. `go vet` e `tsc` limpos. `cosca run` de diretório externo funcional. Descoberta crítica: **o cosca-desktop NÃO é um repo git** (sem .git) — todo o código está sem versionamento (risco pós-L187). |
| **Confidence** | 0.92 (validado: go vet + tsc limpos, cosca run exit 0 de dir externo, build do engine OK) |
| **Tags** | #desktop #auditoria #nil-json #errorboundary #data-race #symlink #rce #exfiltracao #git-injection #cosca-run #loadchatenv #bugfix #level-4 |
| **Related** | L227 (auditoria desktop), L209 (padrão produto), L202 (cosca-desktop), L187 (incidente — git perdido), L174 (auditoria) |
| **Learned** | **(1) O cosca-desktop violou as PRÓPRIAS lições codificadas no L209: nil slices Go → JSON null (5 casos), ErrorBoundary ausente, data races — o padrão existe no papel mas não foi aplicado na implementação.** Governança de padrão exige enforcement (lint/check no build), não só documentação. **(2) `cosca run` e `workflow run` são DOIS caminhos de código separados: buildPipelineWiring chama loadChatEnv, run.go NÃO chamava.** Um fix num caminho não conserta o outro — a propagação config→env precisa ser centralizada. **(3) A porta `11435` apareceu em TRÊS lugares (`.env` OLLAMA_HOST, ide.go GetPorts, app.go providerKeyTestBaseURL) — uma porta stale se espalha por cópia.** Buscar TODAS as ocorrências de um valor, não só a primeira. **(4) `os.Exit(code)` em main.go sem print + SilenceErrors = erro engolido em CLI: quando o erro carrega ExitCodeError, sai mudo.** CLI precisa imprimir a mensagem ANTES de os.Exit. **(5) Auditoria com 2 agentes explore paralelos (segurança + qualidade) entrega achados não-redundantes e acionáveis — o mesmo padrão de delegação paralela da esteira funciona para auditoria.** **(6) `cosca memory integrity init` com "0 protected files" num projeto externo passa o gate, mas o projeto segue sem memória/índice — auditar externo via esteira exige popular o corpus antes, senão o LLM não enxerga o código. |
| **Next** | (1) `git init` no cosca-desktop (URGENTE — sem versionamento pós-L187). (2) Centralizar a propagação config→env (loadChatEnv) num único entrypoint usado por run/workflow/pipeline/eval. (3) Fazer CLI imprimir erro antes de os.Exit(code). (4) Corrigir achados MEDIUM/LOW restantes do desktop (arena registry leak, EventsOff, SSRF COSCA_MODELS_API_URL, shortID). (5) Reconstruir o binário desktop (wails build) e smoke test. |
