PREV: c73d85c933204204fc0be939fe2beafed726e17470ea90e9db3b18c86402983f
ID: L223
TIME: 2026-08-14
LEVEL: 5
TAGS: #testes-de-sistema #invariantes #crash-recovery #security-boundary #provenance #concorrencia #isolamento #conflito #level-5
---
## L223 — 2026-08-14 — testes de sistema: invariantes, crash mid-write, security boundary real, provenance 1-byte, stress, isolamento, conflito v1-vs-v2 | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Testar o Cosca como sistema (estado × concorrência × falha × recuperação × invariantes), não como conjunto de funções isoladas. |
| **Technique** | Level 5 — recon prévio para não duplicar (durable crash-resume e gate transitions já existiam; gaps reais = 7); verificação das APIs reais (Rails resolve symlink via EvalSymlinks; MemoryEngine tem AgentFilter/OwnerFilter; ConflictStore + StatusWithConflicts); 7 arquivos de teste de sistema (19 testes): invariantes cross-entity, crash mid-write (JSONL lenient + VerifyIntegrity strict), security boundary com Rails real (symlink escape, path traversal, glob escape, .git bloqueado), provenance 1-byte, stress de concorrência (100 tasks × 8 workers), isolamento de memória (A→B→A, IDOR owner), conflito v1-vs-v2. |
| **Level** | 5 |
| **Outcome** | success — 19 testes verdes, race limpo, suítes completas, build OK, global 67,7→67,8%. Nenhum bug novo (as proteções — symlink, traversal, isolamento, integridade — já existiam e funcionam; o teste é a prova). Commit `bb9016e`. |
| **Confidence** | 0.96 (race limpo inclusive nos testes com goroutines; suítes completas; build) |
| **Tags** | #testes-de-sistema #invariantes #crash-recovery #security-boundary #provenance #concorrencia #isolamento #conflito #level-5 |
| **Related** | L320 (stallwatch), L319 |
| **Learned** | **(1) O recon prévio evitou duplicação: muito do P0 já existia (durable crash-resume, gate transitions, Rails symlink) — o valor real estava em 7 gaps específicos, e o teste provou que as proteções funcionam.** **(2) Load lenient + VerifyIntegrity strict: o JSONL ignora linhas truncadas (crash mid-write), mas a rede de segurança acusa — divisão de responsabilidades (Load perdoa, Verify acusa).** **(3) Testar com o Rails REAL (não mock) mudou tudo: mock prefix-based não resolve symlinks e deixaria o escape passar; só o validator real (EvalSymlinks) prova o boundary.** **(4) FTS é case/acento-sensível — ajustar o termo de busca real, não o isolamento.** **(5) Testes de sistema valem mais que unit tests para pegar bugs de interação.** |
| **Next** | (1) P1: replay parcial, idempotência Resume×3, tool extremo, adversarial prompts. (2) P2: chaos test. (3) Global 67,8%. |
