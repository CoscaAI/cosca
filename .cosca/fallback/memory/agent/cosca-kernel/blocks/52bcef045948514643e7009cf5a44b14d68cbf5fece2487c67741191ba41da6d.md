PREV: d98caa5a81386e90b7a7b25898a20a8ebdd59f2343c98feef763ce20fec60a99
ID: L62
TIME: 2026-08-12
LEVEL: 5
TAGS: #jaula #bug-l208 #fix #info-fd #collision-fd #memfd #fail-closed #segurança #level-5
---
## L62 — 2026-08-12 — FIX L218 APLICADO E PROVADO AO VIVO: canal --info-fd separa falha de jaula de falha de comando; 2ª rodada achou colisão de fd que matava TODO comando preso | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don aprovou o fix do bug L218 (exit do comando interno → falso "jail unavailable" → fallback sem jaula). Aplicado, testado e provado em campo de provas (demo2/demo3 em /tmp/opencode/cosca-jail). |
| **Technique** | Level 5 — **(1)** Canal de prova de boot: pipe pai→bwrap via `--info-fd` (bwrap escreve a info da sandbox assim que a montagem termina, ANTES de executar o comando). Pós-Run(): byte no canal → jaula subiu → propaga exit code do comando SEM fallback; EOF → falha genuína de montagem → fail-closed atual (warning + opt-in). **(2) 2ª rodada — RAZÃO REAL do "tudo exit 1":** o memfd tem caminho `/proc/self/fd/N` com N = fd do PAI; com ExtraFiles[0]=pipe (info), o fd 3 do FILHO virou o PIPE → bwrap tentava exec do pipe → todo comando preso morria 1. Fix: o binário vira ExtraFiles[0] (fd 3 do filho = caminho ESTÁVEL `/proc/self/fd/3`) e o canal vira ExtraFiles[1] (fd 4 = --info-fd 4). createJailBinary/createMemfd/createTempFile refatorados pra devolver *os.File. **(3)** Helpers puros testados: jailBooted (pipe), jailExitCodeFromRun (subprocesso real exit 3), jailInjectInfoFD (ordem no argv). **(4)** Prova ao vivo com bwrap-espião (script fake validado por validatedBwrapPath): argv capturado mostra `--info-fd 4` imediatamente antes de `/proc/self/fd/3` + doctor; exit 0. |
| **Level** | 5 |
| **Outcome** | success — E2E no campo: (1) doctor --help JAILED com stdout→arquivo = exit 0, 2002 bytes, zero warning (antes: exit 1 mudo); (2) serve (falha real) = exit 1 + ZERO "SECURITY WARNING" + ZERO "bwrap failed" (antes: warning + reason mentiroso); (3) serve + COSCA_ALLOW_NO_ROOT=1 = exit 1 + zero warning → porta do modo-sem-jaula FECHADA; (4) bwrap ausente = warning + exit 1 → fail-closed preservado. Build+vet+suite pkg/cosca+race limpos; repo inteiro 108 pacotes ok (1 flaky pré-existente: TestMemorySDK_RestoreSnapshot, connection refused sob carga, passa isolado 3x). |
| **Confidence** | 0.97 (validado: 6 provas E2E ao vivo + argv do espião + suite + race + colisão de fd diagnosticada com teste manual A/B) |
| **Tags** | #jaula #bug-l208 #fix #info-fd #collision-fd #memfd #fail-closed #segurança #level-5 |
| **Related** | L218 (bug reproduzido), L208 (sintoma original), learnings linha 28 (7 dias sem jaula), pkg/cosca/jail.go |
| **Learned** | **(1) O exit != 0 do processo interno JAMAIS pode disparar fallback sem-jaula: o canal --info-fd separa os dois mundos com precisão de kernel (bwrap garante: montagem completa → byte → só então exec).** **(2) Colisão de fd é um tipo de bug traiçoeiro: `/proc/self/fd/N` é resolvido NO FILHO — se ExtraFiles realoca os fds em 3+i, o caminho do memfd aponta pra qualquer coisa. O caminho SÓ é confiável se o fd for fixado pelo próprio ExtraFiles.** **(3) O stderr do processo preso vai pro buffer de diagnóstico (cmd.Stderr) e NUNCA chega ao terminal — falha de UX pré-existente (o comentário original fala em MultiWriter, o código só tem buffer): serve que falha dentro da jaula é MUDO pra operador. Pendência para próximo fix.** |
| **Next** | (1) stderr do processo preso: io.MultiWriter(terminal, buffer) — restaurar o comportamento documentado (diagnóstico + visibilidade ao vivo). (2) Substituir o binário de produção (/home/cosca/Downloads/cosca build via `go build ./cmd/cosca`) e religar o serviço do Don com a jaula. (3) Novos testes de regressão: rodar suite em CI-like após fechar. |
