PREV: dcb4132150c043f95cf943904d7cf9e2fa021184dfee7667044da7ac763c7178
ID: L64
TIME: 2026-08-13
LEVEL: 4
TAGS: #fix #node-graph #contrato #producer-path #diffusion #remove-bg #rgba #encadeamento #level-4
---
## L64 — 2026-08-13 — Fix contrato do node graph: produtor devolve CAMINHO (string) — diffusion→remove_bg 400 resolvido | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don reportou erro real: `/api/engine/x/run` retornando 400 no canvas (fluxo diffusion→remove_bg). Corrigir o bug de integração. |
| **Technique** | Level 4 — **(1)** Reproduziu o 400 com curl: `node "bg-1" (remove_bg): input "gen-1" não é um caminho` — o diffusion retornava um OBJETO `{path,prompt,steps}`, mas o remove_bg (e os nós de imagem) esperam uma STRING de caminho como input. **(2)** Fix: o contrato do node graph é **nó produtor devolve caminho (string)** para encadear; o detalhe fica em `result["meta"]`. O executor diffusion agora devolve o path; o handler image/generate adaptado. **(3)** PROVA: diffusion→remove_bg HTTP=200 — gen-1.png (RGB 512) → bg-1.png (**RGBA** 512, fundo removido com canal alpha). |
| **Level** | 4 |
| **Outcome** | success — bug corrigido, fluxo completo diffusion→remove_bg funcionando E2E com arquivos reais. |
| **Confidence** | 0.95 (validado: HTTP 200 + RGBA confirmado por file — fundo removido de verdade) |
| **Tags** | #fix #node-graph #contrato #producer-path #diffusion #remove-bg #rgba #encadeamento #level-4 |
| **Related** | L242 (diffusion — o retorno errado), L223 (nodegraph — contrato do executor), L243 (canvas — o Don rodou e achou o bug) |
| **Learned** | **(1) O contrato do node graph é rígido: nós PRODUTORES devolvem CAMINHO (string), nós consumidores esperam string — objeto detalhado vai em meta. Violar isso = 400 misterioso.** **(2) O dogfooding/canvas pegou o bug que testes unitários não pegam: o teste do diffusion só verificava o retorno, não o ENCADEAMENTO com o próximo nó.** **(3) RGBA ≠ RGB é a prova visual do remove_bg: canal alpha = fundo removido. |** |
| **Next** | (1) Adicionar teste de ENCADEAMENTO diffusion→remove_bg (para nunca regredir). (2) Continuar 3.3 (headers de painel enterprise). (3) O Don validar o canvas de novo. |
