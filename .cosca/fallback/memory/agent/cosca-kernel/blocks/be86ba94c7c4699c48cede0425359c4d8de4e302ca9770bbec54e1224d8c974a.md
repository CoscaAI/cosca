PREV: 867aab01cea47f1ad743e6f9238725b395a28ff7e951da33061525fc24677d54
ID: L31
TIME: 2026-08-11
LEVEL: 4
TAGS: #esteira #pipeline #fake-success #security #jail #fallback #merkle #ci #production-ready #level-4
---
## L31 — 2026-08-11 — Full production readiness: deep independent audit exposed FAKE esteira + security/memory/CI fixes | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ordered full production readiness after the deep independent audit (5 parallel agents) found the assembly line ("esteira") was FAKE and knowledge/memory systems had inconsistencies. "resolve tudo que vamos parti pra producao". |
| **Technique** | Level 4 — Deep audit (NOT trusting existing checks), 5 parallel agents. **Findings**: (1) CRITICAL `cosca workflow run`/`cosca pipeline run` reported success while executing nothing (10ms sleep simulation, 11 zero-step workflows); (2) CRITICAL jail fail-open (COSCA_ALLOW_NO_ROOT=1, 15K security warnings); (3) HIGH JWT_SECRET in process argv; (4) HIGH private signing key readable inside jail; (5) HIGH .cosca/fallback incomplete (only knowledge/); (6) HIGH merkle root stale; (7) HIGH learning-ID collisions active↔archive; (8) HIGH chain.dat 174/228 malformed rows; (9) CI red (11 stale tests); (10) DoD vacuous, RecoveryLoop auto-rewrites files, dangling DependsOn drops tasks. **Fixes — Frente 1 (security)**: removed COSCA_ALLOW_NO_ROOT (jail re-activated, bwrap confirmed working), JWT secrets via file (no argv leak), kernel_private.key moved to ~/.config/cosca/keys (outside jail), perms tightened. **Frente 2 (esteira)**: buildPipelineWiring — real orchestrator→planner→stepRunner→recovery→DoD for workflow/pipeline run; stubs rejected with clear error; DoD evidence-based; RecoveryLoop AutoFix consent gate (default false) + workDir escape guard; DependsOn validation; CMI real outcome; chaos-testing typo fixed. **Frente 3 (memory)**: MaterializeFallback embed→disk (924 files); merkle regenerated (7 epochs, 221 blocks); chain.dat rebuilt (hash/prev preserved); archive renamed L001-L214 + collision note; learnings header 29→30; knowledge.db tags fixed; backups retention 7→3; security.log rotated. **Frente 4 (CI)**: 11 stale tests updated to current behavior (ollama/qwen defaults, L3-autonomous, full command list). |
| **Level** | 4 |
| **Outcome** | success — go build/vet pass, pipeline/workflows/config tests pass. E2E: `workflow run knowledge-pipeline` → "is a stub (0 steps) — not executable" (exit 1, correct); `workflow run feature-development` → real execution (steps connecting to provider); `pipeline run --prompt` → real execution. Chain: 18 blocks valid. Services 4/4. Working tree clean after commit 89d346a. |
| **Confidence** | 0.96 (validado: go build/vet, testes pipeline/workflows/config, E2E dos 3 comandos de esteira, chain 18 blocos, 4/4 serviços, tree limpo) |
| **Tags** | #esteira #pipeline #fake-success #security #jail #fallback #merkle #ci #production-ready #level-4 |
| **Related** | L189 (production readiness hornfit), L187 (jail dev-in-jail), L181 (jail + chain nativa), L174 (auditoria enterprise — segurança), L153 (esteira autônoma) |
| **Learned** | **(1) The "fake success" trap — code that returns nil/true without doing work is worse than an error; always validate E2E that a pipeline actually executes.** **(2) Splitting work into parallel agents on the SAME repo causes integration breaks (Frente 1 committed terminal.go depending on Frente 2's reverted code → build broke → needed minimal SetWorkDir/AutoFix/Pipeline re-add). Parallel agents need disjoint scopes or a single integrator.** **(3) The git-anchor chain works in practice: every commit changes HEAD → re-sign with --sign-auto (no passphrase).** **(4) Fail-closed beats fail-open: COSCA_ALLOW_NO_ROOT=1 silently disabled the sandbox for 5 days.** |
| **Next** | (1) Push to remote (coscaV2) — 100+ commits pending. (2) Consider the runtime-code chain for Go source (internal/, cmd/, pkg/) not just embed. (3) Wire a live LLM test of the esteira with ollama running. |
