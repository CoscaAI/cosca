PREV: e302bbbbbd75243d0d843ed0ef4425f2f1214c58cd29512e97425a4a67163f9a
ID: L53
TIME: 2026-08-12
LEVEL: 4
TAGS: #voice #desacoplamento #projeto-independente #sob-demanda #torch #whisper #kokoro #systemd #uma-ferramenta-um-comando #level-4
---
## L53 — 2026-08-12 — cosca-voice desacoplado: projeto independente + sob demanda (cosca voice start/stop) — resolve travamento do sistema | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don: desktop travando, processador dispara → auditoria. Causa raiz: cosca-voice (systemd user, always-on com wake word) rodava há 1.5 dias com torch+faster-whisper+kokoro: 63% CPU, 2.1GB RAM + 1.8GB swap (pico 4.9GB) + opencode 3.7GB → sistema saturado, swap 3.8GB, WebKit do desktop morria ("WebProcess didn't exit"). Don: "podemos desacomplar o voice pra ser um projeto separado quem nem o cosca desktop ne". |
| **Technique** | Level 4 — **(1)** Movido scripts/voice/ → /home/cosca/Documents/projects/cosca-voice/ (voice_conversation.py 1066 linhas + voice_events.py 656 + lipsync/ + pronunciation.json + venv 5.4GB), git init + commit (8160966). **(2)** systemd user unit atualizado (caminho novo, WorkingDirectory novo) + `systemctl disable` (não sobe no boot — voice é sob demanda, não always-on). **(3)** `internal/cli/voice.go` — `cosca voice {start|stop|status}` via systemctl --user, admin command (fora da jaula). **(4)** `internal/tools` registro: voice = 3ª ferramenta (desktop, voice...). **(5)** E2E: start→active→stop→inactive validado. |
| **Level** | 4 |
| **Outcome** | success — Voice desacoplado e sob demanda. Sistema liberado: RAM livre 3.6GB→5.1GB (boot pós-stop), CPU ociosa 93.7%. `cosca voice start/stop/status` funcionando E2E. git rm do root (7 arquivos). cosca-voice commitado (8160966). Build+vet limpos, teste root command atualizado (voice na lista). |
| **Confidence** | 0.96 (validado: E2E start/stop/status, RAM/CPU medidas, projeto independente commitado) |
| **Tags** | #voice #desacoplamento #projeto-independente #sob-demanda #torch #whisper #kokoro #systemd #uma-ferramenta-um-comando #level-4 |
| **Related** | L209 (padrão cosca), L208 (desktop global), L202 (desktop fundação) |
| **Learned** | **(1) Serviços pesados always-on (wake word + torch + 5GB de modelos) são o câncer silencioso de um sistema dev: 63% CPU + 4GB por dias sem ninguém usar. A cura não é otimizar, é desacoplar + tornar sob demanda.** **(2) O desacoplamento segue o padrão cosca: projeto próprio em ~/Documents/projects/<nome> + registro em internal/tools + comando global (`cosca voice`) + admin bypass. A única diferença do desktop é que voice é serviço (systemd), não GUI.** **(3) `systemctl disable` + comando explícito é o modelo certo para serviços pesados de assistente: o Don liga quando quer falar, desliga quando não quer (libera CPU/RAM na hora).** **(4) Diagnóstico de travamento: olhar os 2-3 maiores consumidores de CPU/RAM ANTES de culpar o app — o desktop era vítima do voice+opencode, não culpado (WebKit morre por falta de recursos do sistema).** **(5) 5.4GB de venv (torch) não vão pro git — .gitignore venv/ no projeto novo.** |
| **Next** | (1) Commit + push (cosca: voice.go, tools, main, tests, L210, git rm voice; cosca-voice repo novo). (2) Considerar `cosca voice` com --record/--wake word toggle futuros. (3) Observar o sistema por 1 dia sem voice always-on — confirmar estabilidade. |
