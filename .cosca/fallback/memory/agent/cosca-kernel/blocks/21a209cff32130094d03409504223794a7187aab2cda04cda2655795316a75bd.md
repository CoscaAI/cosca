PREV: 7eecb4fab942eb7c20c488dda252116ec794425b65d3b65320b4290e702d2a3f
ID: L130
TIME: 2026-08-13
LEVEL: 2
TAGS: #git #gitignore #versionamento #desktop #runtime-data #wails #secrets #baseline #level-2
---
## L130 — 2026-08-13 — cosca-desktop versionado (git init + .gitignore fecha o gap do L187) | Level 2

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" — `git init` + primeiro commit do cosca-desktop (fechar a exposição sem versionamento pós-incidente L187). |
| **Technique** | Level 2 — **(1)** Atualizar `.gitignore` ANTES do commit: o arquivo original (6 linhas) não ignorava `.cosca/` (1.5MB de runtime — knowledge.db, trace.db, durable-events, memory) nem `frontend/wailsjs/` (bindings gerados). Adicionei ambos; `build/bin`, `node_modules`, `frontend/dist` já estavam cobertos. **(2)** Varredura de segredos antes de commitar (zero achados — sem `.env` no desktop; placeholders de `snippets.go` são exemplos, não credenciais). **(3)** `git init` + commit baseline (89 arquivos fonte) com mensagem descritiva das 10 correções de auditoria. **(4)** Validação pós-commit: `git ls-files` confirma zero runtime/artefatos (`grep .cosca|node_modules|dist|build/bin|wailsjs` = vazio); working tree limpo. |
| **Level** | 2 |
| **Outcome** | success — cosca-desktop agora versionado (commit `f39dfad`, 89 arquivos, branch master). `.cosca/`, `node_modules/`, `dist/`, `build/bin/`, `wailsjs/` corretamente excluídos. A lição do L187 (o que não está no commit não existe) foi aplicada ao produto. |
| **Confidence** | 0.98 (validado: git status limpo, ls-files sem runtime, secret scan vazio) |
| **Tags** | #git #gitignore #versionamento #desktop #runtime-data #wails #secrets #baseline #level-2 |
| **Related** | L228 (correções desktop), L187 (incidente — git perdido), L209 (padrão produto) |
| **Learned** | **(1) O `.gitignore` herdado de um scaffold Wails cobre `node_modules`/`dist`/`build/bin`, mas NÃO cobre o runtime do cosca (`.cosca/`) nem os bindings gerados (`frontend/wailsjs/`) — ao versionar um produto que embarca o engine, o runtime data (knowledge.db com corpus indexado, trace.db, memórias) é ESTADO LOCAL e deve ser excluído, senão vaza índice/vetores para o repo.** **(2) Antes de qualquer commit inicial: (a) checar .gitignore contra o que de fato existe no disco (du/sh + ls), (b) varrer segredos, (c) conferir pós-commit com `git ls-files` que nada de runtime/artefato entrou.** **(3) O padrão do L209 (todo produto = Wails + esteira + comando global) agora inclui um passo implícito: `git init` + .gitignore com `.cosca/` e `frontend/wailsjs/` — o produto nasce versionado, não depois. |
| **Next** | (1) Reconstruir o binário desktop (`wails build -tags webkit2_41`) para validar as correções Go no binário real. (2) Centralizar loadChatEnv. (3) CLI imprimir erro antes de os.Exit(code). (4) Achados LOW restantes. |
