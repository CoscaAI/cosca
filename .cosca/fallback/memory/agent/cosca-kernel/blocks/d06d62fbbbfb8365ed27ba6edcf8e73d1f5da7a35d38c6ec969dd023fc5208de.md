PREV: 6fb460dc427fcec6c344dcf5fc91615e68144af859e15e83bde549574588d0f6
ID: L43
TIME: 2026-08-11
LEVEL: 4
TAGS: #wails #desktop #cosca-desktop #react #webview #go #native-app #docker-desktop #command-center #esteira #level-4
---
## L43 — 2026-08-11 — cosca-desktop: Wails (Go + WebView ~15MB) — esteira real no frontend, internal-rule resolvida via module nesting | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ficou frustrado com o TUI do terminal ("ainda ta errado", "terminal de iniciante") e pediu algo melhor que React/web: "tem algo melhor que react ne, ja vi varias ferramentas leves que executa em tempo de execucao sem depender de um dom... exemplo o docker desktop, a gente poderia fazer o desktop nosso o que acha?" → "sim conserteza, faz o workflows perfeito pra rodar na esteira sem problema". |
| **Technique** | Level 4 — **(1)** Opções analisadas: Electron (Docker Desktop usa, 100-200MB, Chromium embutido) vs Wails (Go + WebView nativa, ~15MB) vs Tauri (Rust + WebView) vs Fyne (Go canvas puro, sem DOM). **Recomendação: Wails** — backend Go (nossa stack), WebView nativa (WebKitGTK/WebView2/WKWebView), leve (~15MB vs 200MB do Electron), cross-platform. **(2)** Implementação: /home/cosca/Documents/projects/cosca-desktop — Wails v2.14.0 + React 19 + Vite + TS. **DECISÃO DE ENGENHARIA-CHAVE:** um módulo separado com `replace` puro NÃO consegue importar os internal/* do cosca (a regra internal do Go é baseada no prefixo do import path, verificado empiricamente). Solução: o módulo desktop vive fora do repo cosca (padrão do Don) mas o path do módulo aninha sob o engine: `module github.com/CoscaAI/cosca/desktop` + `replace github.com/CoscaAI/cosca => /home/cosca/Documents/cosca`. Isso satisfaz a regra internal por prefixo de import path — internal/pipeline, internal/compute, internal/chat, internal/trace, internal/orchestration compilam direto, ZERO mudanças no engine (git status clean). **(3)** Backend bindings (todos não-bloqueantes): SendMessage(prompt) — emite RUN_STARTED, cria ctx cancelável, submete ao fabric agent pool (fabric.Submit em goroutine, fallback goroutine pura), retorno ~6µs; Cancel() — cancela o run ctx, emite RUN_CANCELLED; GetStatus() — provider/model/agent/pools fabric/uptime/tokens; GetModels(); GetAgents(); ListEvents() — ring buffer. engineHandle wirea a esteira REAL: chat.GetRegistry → RegisterChatProviders(registry, {"fabric": f}) → registry.Select → orchestration.NewFactory → pipeline.NewOrchAdapter → NewPlanner → NewStepRunner + compute.NewFabric(LoadFabricConfig).Start(ctx) + engines de memória/knowledge (degradação graciosa) + bootstrap .env/provider env. Todo evento do pipeline → trace.Event-shaped AppEvent → runtime.EventsEmit(ctx, "exec:event", ev) + channel in-process (testável). Offline a pipeline roda end-to-end mesmo assim (fallback determinístico none/ollama — sem deadlock). **(4)** Frontend: App.tsx (header COSCA branding + chips de modelo/agente + dot animado; split Chat ~60% / ExecutionTree ~40%; footer HUD com pools fabric), ChatPanel (react-markdown, input sempre focado, Enter-envia, Cancel), ExecutionTree (árvore aninhada por ParentEvent, expand/collapse, glifos ✓✗⠿○, clique → painel de detalhe), AnimatedStatus (pulse CSS + shimmer progress), useExecutionStream hook (EventsOn("exec:event")), tema petrol CSS (#092F33/#05090A/#4A5557/#72C9C6). |
| **Level** | 4 |
| **Outcome** | success — go build PASS, go vet PASS, go test 4/4 PASS (SendMessage não-bloqueante <2s, Cancel → RUN_CANCELLED, validação de input, status reflete engine), frontend npm install + build PASS, git status do engine cosca CLEAN (intocado). O `wails build` completo exige libs de sistema (dev headers webkit2gtk/gtk3) — a máquina tem runtime libwebkit2gtk-4.1-0, faltam os dev headers. Sudo command: `sudo apt install build-essential libgtk-3-dev libwebkit2gtk-4.1-dev` depois `wails build -tags webkit2_41` (ou variante 4.0). |
| **Confidence** | 0.93 (validado: go build/vet/test 4/4, frontend build, internal/* compilando via module nesting, git engine limpo; pendente wails build final com dev headers) |
| **Tags** | #wails #desktop #cosca-desktop #react #webview #go #native-app #docker-desktop #command-center #esteira #level-4 |
| **Related** | L201 (Enterprise Command Center — TUI real-time), L200 (terminal OpenCode mode), L174 (auditoria enterprise), L173 (L3 autônomo), L186 (chain git-anchored) |
| **Learned** | **(1) A regra internal do Go bloqueia import cross-module — o fix é aninhar o path do módulo (github.com/CoscaAI/cosca/desktop) para o prefixo do import path satisfazer a regra; diretório separado + replace directive. Este é o padrão para construir apps irmãos sobre o engine.** **(2) O Electron do Docker Desktop (200MB) é o benchmark errado — Wails entrega a mesma UX desktop a ~15MB com nosso backend Go.** **(3) Bindings não-bloqueantes + EventsEmit é o padrão de desktop certo — o Don pode conversar enquanto a esteira roda (o requisito central).** **(4) Determinismo offline importa: a pipeline precisa rodar sem API keys (fallback determinístico) para o desktop sempre mostrar vida.** |
| **Next** | (1) Don roda sudo para instalar dev libs webkit2gtk/gtk3, depois `wails build -tags webkit2_41` → bin/cosca-desktop. (2) Considerar cosca-desktop como projeto cliente (git init + commit). (3) Wirear prompts de permissão + cost tracking no desktop. (4) O TUI terminal continua como fallback CLI; o desktop é o cockpit premium. |
