PREV: e8904e5f8878ddddd2ff2de373e359abb13e127313ca9f95c9254af17d774da2
ID: L249
TIME: 2026-08-15
LEVEL: 5
TAGS: #cosca-trader #tier-1 #mineracao #engineclock #quantize #error-hierarchy #rate-limit #target-position #risk-rule #insight #go #fintech #level-5
---
## L249 — 2026-08-15 — Tier 1 da mineração IMPLEMENTADO: 7 padrões de engenharia no cosca-trader (EngineClock, Quantize, error hierarchy, rate limiter, TargetPosition, RiskRule, Insight) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Implementar o Tier 1 do roadmap do L247 (mineração de 8 projetos): os 7 padrões de maior alavancagem, mapeados das fontes (ccxt, Lean, barter-rs, OpenAlgo, StockSharp) para Go no cosca-trader. |
| **Technique** | Tradução de padrões cross-linguagem para Go idiomático, com teste por pacote novo + preservação da API pública existente (os testes antigos continuam verdes — refatoração sem quebra). (1) clock: interface Clock injetável (EngineClock do barter) — OMS e paper broker param de usar time.Now direto; clock.Fixed dá replay determinístico. (2) quantize: TruncateToStep/RoundToTick (decimal_to_precision do ccxt) — truncate p/ quantidade (nunca excede lote), round p/ preço; binance/info.go delega. (3) exchange: hierarquia de erro retryable/não-retryable (ErrorKind) + IsRetryable; binance classifica HTTP 429/418/5xx; o default de erro NÃO classificado é fail-closed (não retry). (4) ratelimit: TokenBucket + SlidingWindow (Throttler do ccxt) com custo ponderado, sem dependência externa. (5) OMS.SetTargetPosition: ordem delta-based idempotente (smart order do OpenAlgo) — mesmo target = mesma posição final; lock de rebalance serializa. (6) risk: refatoração para regras composáveis (IRiskRule do StockSharp) — Rule interface + Input + ExposureRule/TotalExposureRule/OpenOrdersRule plugáveis via Config.Rules; macro risk-off continua cortando exposição pela metade dentro das regras. (7) strategy: Signal com Magnitude + Confidence (Insight do Lean). |
| **Level** | 5 |
| **Outcome** | success — go build + go vet limpos; go test -race -count=1 ./... TODO verde (18 pacotes); 7 arquivos novos + 7 modificados; API pública do OMS/risk preservada (testes antigos intactos). |
| **Confidence** | 0.95 (evidência: go test -race verde; testes novos por pacote — quantize, ratelimit, errors, rules, rebalance — todos passando; vet limpo) |
| **Tags** | #cosca-trader #tier-1 #mineracao #engineclock #quantize #error-hierarchy #rate-limit #target-position #risk-rule #insight #go #fintech #level-5 |
| **Related** | L247 (mineração 8 projetos — o roadmap), L246 (auditoria), L199 (ordem sagrada: commit antes de assinar), doutrina Fintech (decimal) |
| **Learned** | (1) A paridade backtest/live NÃO exige reescrever o engine — basta trocar a fonte de TEMPO (Clock) e a fonte de EVENTOS. O Clock injetável é o primeiro passo barato; o feed genérico (replay do event store) é o passo seguinte (Tier 2). (2) Quantize: a distinção truncate(quantidade)/round(preço) é a diferença entre ordem aceita e rejeitada — e vale centralizar num pacote (não espalhar Floor()/Round() por adapter). (3) Error hierarchy: o default de erro não-classificado DEVE ser fail-closed (não retryable) — na dúvida, não martela a exchange. (4) TargetPosition delta-based é a idempotência CORRETA para webhook/LLM: reenviar o mesmo alvo produz a mesma posição (diferente de "comprar X" que duplica). (5) Regras de risco composáveis via interface + Config.Rules preservam a API e destravam extensão sem tocar o core. (6) Refatoração sem quebra: testes antigos são a rede de segurança — rodar go test -race ANTES e DEPOIS é o que garante que a tradução cross-language não mudou comportamento. |
| **Next** | (1) Tier 2: paridade backtest/live via feed genérico (replay do event store SQLite no mesmo canal que o live) + Advance(ctx,intentID) como máquina de estados reconciliada. (2) Wire o ratelimit no adapter binance (cada chamada REST passa por Wait(ctx,cost)). (3) Usar SetTargetPosition num endpoint /rebalance ou webhook. (4) Computar Magnitude/Confidence no scanner/seletor adaptativo para o ranking por convicção. |
