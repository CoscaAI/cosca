PREV: 2b9713e437423e487e6ee954126a082986145997e55168a98483cc598e22810a
ID: L102
TIME: 2026-08-13
LEVEL: 5
TAGS: #security #zero-trust #defense-in-depth #secure-by-design #fail-closed #least-privilege #agent-security #sandbox #secrets #supply-chain #threat-modeling #stack17 #matriz-cosca #level-5
---
## L102 — 2026-08-13 — Stack 17: Security Zero Trust / Defense in Depth / Secure by Design — o stack mais relevante pro Cosca | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: estudar Stack 17 (Security Engineering Zero Trust/Defense in Depth/Secure by Design — aprofundamento do Stack 09) e adicionar como padrão Cosca. |
| **Technique** | Level 5 — **(1)** Check de segurança antes de processar (protocolo L205): URLs legítimas, sem injeção/código mascarado — VEREDITO LIMPO reportado. **(2)** Consolidação no `knowledge/security/README.md` (Stack 09 + 17 num único doc da matriz): postura central (secure by design/default, zero trust, least privilege, fail closed, defense in depth), REGRA ZERO (todo input/rede/webhook/modelo/agente = UNTRUSTED), modelo central, authz matrix + object-level (IDOR/BOLA/BFLA), secrets lifecycle, supply chain (SBOM/SLSA/Sigstore), pipeline CI/CD de segurança, quality gate 20 itens, review levels 0-5, security debt/ADR, incident response. **(3)** Ênfase no COSCA-SPECÍFICO: trust model (user→UI→API→orchestrator→agent→skill→tool→runtime→sandbox→resource), permissão de ferramentas (allowlists por path/domínio/comando), sandbox (nunca "container = sandbox perfeito"), plugin security, invariantes de segurança. |
| **Level** | 5 |
| **Outcome** | success — knowledge/security consolidado (4fc69b7). O stack mais relevante para o Cosca: sua arquitetura de agente (bwrap jail, capability levels, tools) é literalmente o assunto. |
| **Confidence** | 0.95 (validado: prompt processado, doutrina consolidada no formato unificado) |
| **Tags** | #security #zero-trust #defense-in-depth #secure-by-design #fail-closed #least-privilege #agent-security #sandbox #secrets #supply-chain #threat-modeling #stack17 #matriz-cosca #level-5 |
| **Related** | L205 (matriz), L187 (incidente de segurança real — as lições batem: jail ausente, keys expostas, COSCA_ALLOW_NO_ROOT) |
| **Learned** | **(1) REGRA ZERO: nenhuma camada isolada basta e TODA camada pode falhar — input/rede/webhook/modelo/agente são UNTRUSTED por padrão.** **(2) Object-level authorization (IDOR/BOLA/BFLA) é risco crítico: nunca confiar em "está autenticado" — verificar CAN_USER_ACCESS_RESOURCE.** **(3) AuthN ≠ AuthZ; fail closed (falha → DENY, nunca ALLOW); security state machine VALIDATE→AUTHORIZE→EXECUTE→VERIFY→AUDIT.** **(4) O L187 (incidente real do Cosca) confirma a doutrina: rodar sem jail (COSCA_ALLOW_NO_ROOT=1), keys em texto puro, egress irrestrito — exatamente os anti-patterns que o Stack 17 criminaliza.** **(5) Agentes/ferramentas/plugins precisam de permissão declarada (NAME/PURPOSE/PERMISSIONS/RISK_LEVEL) com allowlists; container ≠ sandbox perfeito.** **(6) Invariantes de segurança (ex.: "input não confiável nunca vira comando shell") são o contrato que nunca deve quebrar.** **(7) A regra final honesta: nunca "100% seguro" — sempre ameaças/controles/evidências/riscos residuais/áreas a validar.** |
| **Next** | (1) Aplicar o trust model à arquitetura do próprio Cosca (auditoria: tools declararem permissões, jail/egress control). (2) Security quality gate no workflow de features. (3) Auditar o HornFit com os review levels. |
