PREV: 6aa9a7f1a2c48bbe6fde4614c86613cb358782628d930ce968bddaf61f5d5aac
ID: L108
TIME: 2026-08-13
LEVEL: 4
TAGS: #hornfit #cliente #monorepo #nestjs #nextjs #prisma #pnpm #turborepo #auth #jwt #rbac #docker #level-4
---
## L108 — 2026-08-13 — HornFit (projeto cliente): monorepo NestJS+Next.js+Prisma — F0+F1 entregues | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: criar projeto cliente HornFit (assistência técnica de equipamentos de academia) em `~/Documents/projects/clients/hornfit` — sistema único sem tenant, Next.js + NestJS + WebSockets + PostgreSQL + Redis + Docker + MinIO. Autonomia completa, entrega por fases (F0-F7). F0 (monorepo + infra + schema Prisma + seeds) e F1 (auth + RBAC + login enterprise) concluídas e commitadas. |
| **Technique** | Level 4 — **(1)** Monorepo pnpm 11 workspaces + Turborepo (`apps/{web,api}`, `packages/{shared,ui,config}`). **(2)** Decisão de localização: Prisma vive em `apps/api/prisma/` (NÃO na raiz) — o seed roda via ts-node no contexto de `apps/api` onde `@prisma/client` resolve; na raiz o import quebraria (module resolution sobe do arquivo, não do cwd). **(3)** Schema Prisma com 22 entidades (auth/RBAC, clientes, OS, contratos, faturamento, estoque, CRM, helpdesk, FAQ, CMS/page-builder, marketplace, observabilidade). **(4)** `onlyBuiltDependencies` no pnpm-workspace.yaml destrava os postinstall do prisma (pnpm 10+ bloqueia por padrão). **(5)** Auth: access JWT stateless + refresh token com hash SHA-256 em DB, rotação com revoke, e `jti: randomUUID()` no payload do refresh. **(6)** Guards globais via APP_GUARD (JwtAuthGuard + RolesGuard) + decorators @Public/@Roles/@CurrentUser. **(7)** Frontend: API client com refresh automático + retry em 401, AuthProvider (contexto), login enterprise split-screen, dashboard com sidebar. |
| **Level** | 4 |
| **Outcome** | success — F0: docker compose (postgres/redis/minio healthy), migration aplicada, seed 22 entidades, API + web bootam, commit `5126bbd`. F1: auth completo (login/register/refresh-rotation/logout) + RBAC (CLIENT→403 em /users, ADMIN→200) testados via curl, commit `bb3e369`. Build+typecheck verdes em api e web. |
| **Confidence** | 0.95 (validado: migration+seed reais, curl end-to-end de auth/RBAC, build turbo verde) |
| **Tags** | #hornfit #cliente #monorepo #nestjs #nextjs #prisma #pnpm #turborepo #auth #jwt #rbac #docker #level-4 |
| **Related** | L192 (projeto desacoplado), L195 (integração via contrato), L191 (wails/desktop) |
| **Learned** | **(1) Prisma em monorepo: colocar schema+seed dentro de `apps/api/` (não na raiz) — o seed em `ts-node` importa `@prisma/client`, que só resolve se o arquivo estiver sob o package que declara a dep.** **(2) Relações Prisma exigem o campo inverso nos DOIS lados; esqueci 7 back-references — `prisma validate` lista todas de uma vez, corrigir em lote.** **(3) `StockItem.orderItems` era relação ERRADA (OrderItem referencia Product, não StockItem) — remover, não "completar".** **(4) JWT refresh token DETERMINÍSTICO dentro do mesmo segundo (payload `{sub}` + `iat` em segundos → token idêntico → colisão de hash único no DB); fix padrão: `jti: randomUUID()`.** **(5) pnpm 10+ ignora postinstall (prisma engines) por segurança — `onlyBuiltDependencies` no pnpm-workspace.yaml é o destrave oficial.** **(6) NestJS query params chegam como string; usar DTO com `@Type(() => Number)` + ValidationPipe `transform:true` — senão Prisma estoura `take`/`skip` Int.** **(7) `strictPropertyInitialization: false` no tsconfig NestJS (DTOs são populados pelo framework, não pelo construtor).** **(8) Prisma `UserUpdateInput` (checked) NÃO expõe o scalar FK `clientId` em one-to-one — usar `UserUncheckedUpdateInput`.** **(9) `expiresIn` do @nestjs/jwt é tipado como `StringValue` (do ms) — cast para `JwtSignOptions['expiresIn']`.** **(10) Testar a esteira REAL no final de cada fase (build+seed+curl) pega bug que typecheck não vê — o bug do refresh só apareceu no curl.** |
| **Next** | (1) F2: CRUD completo de Clientes, OS, Contratos, Estoque. (2) F3: Faturamento + CRM + Helpdesk WebSocket. (3) F4: Portal do cliente + FAQ. (4) F5: Landing + Marketplace + page builder. (5) F6: Relatórios + rastreio de erros. (6) F7: CI/CD + docs + runbook. |
