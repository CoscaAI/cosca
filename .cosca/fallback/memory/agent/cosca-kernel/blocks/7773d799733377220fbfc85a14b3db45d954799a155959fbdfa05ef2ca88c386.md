PREV: bc8cc3b10bc6dddb12b7dce0165d1e17fb47419d88166b9a160ba3e23217521f
ID: L28
TIME: 2026-08-11
LEVEL: 4
TAGS: #jail #bubblewrap #golang #gomodcache #gocache #dns #resolv.conf #systemd-resolve #dev-in-jail #security #level-4
---
## L28 — 2026-08-11 — Jail dev-in-jail: GOMODCACHE/GOCACHE + DNS resolv.conf fix para programar dentro da jaula | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don asked two things: (1) what can the cosca inside the jail actually do? (2) fix so it can program inside the jail ("tipo o cosca dentro da jaula ajuda a programar?" → "sim"). |
| **Technique** | Level 4 — **(Audit result)** Jail isolates system from agent — workspace is rw root, system binds ro (/usr /lib /lib64 /etc /bin), --clearenv (only COSCA_* propagated), no /home /root /var /sys, no /etc/shadow, no SSH keys, network shared (--share-net), ping blocked (no cap_net_raw). Blocked: reading host secrets, escaping jail (root=workspace). Allowed: reading/writing workspace, running Go/Git/Make/Python/Docker, TCP network. **(Bottleneck)** Inside jail GOMODCACHE pointed to /go/pkg/mod (HOME=/) but host cache not mounted → go build/test failed for uncached deps. DNS broken: /etc/resolv.conf is a symlink to /run/systemd/resolve/stub-resolv.conf which wasn't mounted → dangling symlink → Go resolver fell back to [::1]:53 refused. **(Fix)** pkg/cosca/jail.go — binds: --ro-bind /home/cosca/go/pkg/mod /go/pkg/mod, --tmpfs /go-build-cache (private writable cache, host cache stays untouched — security), --ro-bind /run/systemd/resolve/stub-resolv.conf /run/systemd/resolve/stub-resolv.conf; env remap via jailGoToolchainEnv: GOMODCACHE=/go/pkg/mod, GOCACHE=/go-build-cache, GOPATH=/go, GOPROXY=https://proxy.golang.org,direct, GOTELEMETRY=off. BONUS FIX: ReexecInJail ended with os.Exit(0) which never runs deferred funcs → cleanupWorkspaceBinds was a no-op (left etc/ usr/ lib/ lib64/ pollution in workspace); replaced with explicit exit(code) closure so every exit path cleans up. |
| **Level** | 4 |
| **Outcome** | success — go test ./internal/integrity/... passes INSIDE the jail (0.151s), DNS resolves proxy.golang.org, full cycle validated: go mod init + go build + run binary inside jail. Known limitation: go build ./... from / cwd walks system binds and hits /etc/credstore permission denied (host root:700 dir, pre-existing); module cache read-only so NEW dep downloads fail with clear message (pre-provisioned deps work). |
| **Confidence** | 0.96 (validado: go test dentro da jaula, DNS resolvendo, ciclo build completo) |
| **Tags** | #jail #bubblewrap #golang #gomodcache #gocache #dns #resolv.conf #systemd-resolve #dev-in-jail #security #level-4 |
| **Related** | L181 (jail + religamento com jaula verificada), L179 (processo órfão / debug), L174 (segurança — jail) |
| **Learned** | Lesson: GOCACHE must be a private tmpfs (ro host bind breaks go test — Go always writes build artifacts; writable host bind would let jailed agent inject artifacts into host builds). resolv.conf symlinks require binding the real target. os.Exit never runs defer — always use explicit cleanup on all exit paths. |
| **Next** | provision new deps pre-jail (ro module cache) or add writable module overlay; audit other symlinked system files (/etc/...) that may need real-target binds. |
