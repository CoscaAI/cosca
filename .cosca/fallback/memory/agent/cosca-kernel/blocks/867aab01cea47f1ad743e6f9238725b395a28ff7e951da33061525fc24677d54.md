PREV: 3b43e93315c74ae6ebb77f7cd49843a654a3da6ef372da38da08d07bd0c43590
ID: L30
TIME: 2026-08-11
LEVEL: 4
TAGS: #audit #production #security #nestjs #nextjs #prisma #docker #snap-docker #jwt #rbac #webhook #pdf-ssrf #level-4
---
## L30 — 2026-08-11 — HornFit: auditoria de produção readiness — 111 achados corrigidos + guerra docker/snap resolvida | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ordered complete production readiness audit + fixes for the hornfit client project: "resolve tudo e deixa pronto pra producao". |
| **Technique** | Level 4 — Auditoria cross-agent: 4 agentes em paralelo (API quality, frontend, security, infra/git/docker) → 111 achados totais (9 CRITICAL, 26 HIGH, 42 MEDIUM, 34 LOW). Correção dos criticals + principais: (1) validação de `sub` do JWT (escalonamento CLIENT/TECHNICIAN→OWNER); (2) secrets JWT fracos/compartilhados; (3) PDF HTML injection→SSRF/RCE (`--no-sandbox` + HTML sem escape); (4) @Roles ausente (CLIENT lia dados de todos os tenants); (5) race de lost-update no estoque; (6) CI morto + sem remote + 133 arquivos sujos. |
| **Level** | 4 |
| **Outcome** | success — Full stack em modo produção: api healthy (/health ok), web 200, /docs 404 em prod, headers CSP/HSTS, exploit (JWT sem sub) bloqueado 401, senha fraca rejeitada 400, forte aceita. 5 containers healthy. Estrutura normalizada src/workflow/hornfit → src. Commits 9fd5ef1 (audit fixes, 303 files) + bd05b46 (baseline). Highlights: @Roles default-deny + filtro clientId p/ CLIENT; webhooks HMAC-SHA256 (Pagar.me + MercadoPago x-signature); seed com senhas aleatórias + sem PII real (LGPD); Helmet/CSP/HSTS; refresh token rotation; TenantCounter p/ números de OS/ORC (mata race 409); estoque atômico; paginação; proxy /api/proxy (bearer token nunca chega ao browser); middleware.ts proteção de rotas; Docker non-root; workspace .cosca/. |
| **Confidence** | 0.95 (validado: full stack em prod, exploit bloqueado, 5 containers healthy) |
| **Tags** | #audit #production #security #nestjs #nextjs #prisma #docker #snap-docker #jwt #rbac #webhook #pdf-ssrf #level-4 |
| **Related** | L176 (auditoria HornFit 25 bugs), L174 (auditoria enterprise + segurança), L179 (tema HornFit) |
| **Learned** | **(1) A GRANDE lição (infra): conflito snap docker vs docker nativo — runc "unable to signal init: permission denied" porque DOIS dockers (snap + nativo /usr/bin/dockerd) brigavam pelo /run/docker.sock; `snap remove` deixou socket fantasma (inode sem dono). Resolução: matar PIDs dos containers (sudo), abortar a change snap travada, `sudo snap remove docker --purge`, `systemctl restart docker.socket` → docker funciona SEM sudo (usuário já no grupo docker).** **(2) Dockerfile lição 1: `nest build` com tsconfig.tsbuildinfo stale emite dist PARCIAL (só pdf/) — `rm tsbuildinfo + dist` antes do build.** **(3) Dockerfile lição 2: `pnpm install --prod` remove o CLI do prisma (devDep), e `@prisma/client/default.js` faz `require('.prisma/client/default')` resolvido pelo virtual store do pnpm — o fix confiável é mover o CLI prisma para dependencies e rodar db:generate no stage de produção (COPY com glob falha: Docker NÃO expande wildcards no path destino, criando dirs literais '@prisma+client@*').** **(4) Dockerfile lição 3: compose env_file + interpolação de environment pode entregar secrets vazios silenciosamente — validar que os valores .env existem.** |
| **Next** | (1) Adicionar git remote para backup do hornfit. (2) Considerar backup cron (scripts/backup.sh já existe). (3) Monitorar docker sem snap. | |
