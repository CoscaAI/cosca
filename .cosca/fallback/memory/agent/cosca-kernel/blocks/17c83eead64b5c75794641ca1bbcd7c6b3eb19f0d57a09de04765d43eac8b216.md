PREV: 176d27df906c08613776c297a5ba7dc2291ecbb59dba1955b7cfbfddd72055c3
ID: L177
TIME: 2026-08-14
LEVEL: 5
TAGS: #cosca-trader #cards-no-canto #resizeobserver #width-explicito #widthprovider #legacy #incompativel #grid #level-5
---
## L177 — 2026-08-14 — cosca-trader: cards no canto resolvido — largura medida com ResizeObserver (WidthProvider legacy era de versão incompatível) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "acho que falta um valor inicial do drag, pois ainda continua tudo no canto" — os cards do painel continuam amontoados no canto esquerdo, mesmo após o L298 (WidthProvider). |
| **Technique** | Level 5 — **(1) DIAGNÓSTICO PROFUNDO**: o WidthProvider importado de `react-grid-layout/legacy` é do build ANTIGO (CommonJS), enquanto o GridLayout do ESM é o novo — os dois não conversam e a largura medida era 0/errada → cards no canto. **(2) SOLUÇÃO DEFINITIVA**: medir a largura NÓS MESMOS com ResizeObserver no container (gridMainRef) e passar `width={gridWidth}` EXPLÍCITO ao Grid — elimina o WidthProvider problemático por completo. **(3)** `width={gridWidth || undefined}` — o grid usa a largura real; sem ela (primeiro render), cai no comportamento interno até o observer medir. **(4)** CSS: `.grid-main` ganhou `width:100% + min-width:0` (o flex pai não esticava) e `.layout` `width:100%`. **(5)** Removida a declaração do módulo legacy (não mais usado). |
| **Level** | 5 |
| **Outcome** | success — commit. Build OK, HMR aplicado (App + CSS), painel 200. Os cards agora espalham pela largura real da tela. |
| **Confidence** | 0.95 (validado: build OK, ResizeObserver mede o clientWidth real, width explícito repassado) |
| **Tags** | #cosca-trader #cards-no-canto #resizeobserver #width-explicito #widthprovider #legacy #incompativel #grid #level-5 |
| **Related** | L299 (UX painel), L298 (tela branca — WidthProvider), L297 (painel arrastável) |
| **Learned** | **(1) Importar helper de subpath LEGACY para um componente ESM novo é armadilha: o WidthProvider antigo e o GridLayout novo não se integram — a largura media errada e os cards amontoavam. A solução robusta é medir você mesmo (ResizeObserver) e passar width explícito.** **(2) "Cards no canto" = grid sem largura: o grid de 12 colunas com width 0/errado coloca tudo no canto. Sempre verificar o width que o grid recebe ANTES de culpar o layout.** **(3) O padrão `width={measured || undefined}` é o contrato seguro: undefined até medir, largura real depois — o grid nunca fica com largura fantasma.** |
| **Next** | (1) O Don vê os cards espalhados. (2) Conectar divergência ao OMS. (3) Testnet. |
