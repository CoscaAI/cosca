PREV: 41906213bf5d02360d1b59879f73a7dbcabb36a0ce61c56ebd965f90f9fc48be
ID: L18
TIME: 2026-08-09
LEVEL: 4
TAGS: #facefusion #pipeline #processors #job-system #onnx #gpu #ffmpeg #state-management #patterns #level-4
---
## L18 — 2026-08-09 — Análise profunda FaceFusion: 10 patterns de pipeline e job system | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "achei ouro de novo https://github.com/facefusion/facefusion vasculha bem que acho que tem diamante" — análise profunda cross-agent da plataforma líder de manipulação facial |
| **Technique** | Level 4 — Clone + varredura + 2 agentes em paralelo (architecture + processors/testing) em 183 módulos Python. Extração de 10 patterns. |
| **Level** | 4 |
| **Outcome** | success — 10 patterns documentados em `internal/embed/cosca/knowledge/patterns/facefusion-pipeline-patterns.md`. INDEX atualizado (13 padrões). Destaques: (1) Processor Pipeline pipe-and-filter com contrato de 9 métodos — modelo para nossa esteira de agentes; (2) File-based job queue (JSON em diretórios de status) — zero dependências, atômico via mv; (3) Step/Job Key Split Registry — separação limpa de args globais vs por passo; (4) Dual-context state (cli/ui) com detecção automática; (5) ONNX multi-provider com override por processador — GPU CUDA/ROCm/CoreML/OpenVINO; (6) FFmpeg functional command builder com chain/concat; (7) 3-tier memory strategy (strict/moderate/tolerant); (8) Error code propagation 0-4 com short-circuit; (9) CRC32 model integrity + curl resume download; (10) argparse modular com parents composition. |
| **Confidence** | 0.93 (validado por leitura direta: 183 .py files, 47 tests, CI 3-OS matrix, 9 execution providers) |
| **Tags** | #facefusion #pipeline #processors #job-system #onnx #gpu #ffmpeg #state-management #patterns #level-4 |
| **Related** | L156 (CivitAI), L153 (esteira autônoma), L71 (gate transitions), L80 (GPU probe) |
| **Learned** | **(1) O padrão pipe-and-filter com contrato rígido de 9 métodos é o modelo canônico para processadores plugáveis — cada agente Cosca poderia implementar o mesmo contrato (pre_check, pre_process, process_task, post_process) e serem encadeados sequencialmente.** **(2) O job system baseado em arquivos (JSON em diretórios de status) é GENIAL pela simplicidade: zero banco de dados, atômico via mv, humano-legível, resumível após crash. Nossa esteira autônoma poderia adotar o mesmo para passos de workflow.** **(3) A separação step_keys vs job_keys resolve o problema de "quais args são do job e quais são do passo" — nossa execution plan do estimator (L47) tem o mesmo problema e poderia usar classificação similar.** **(4) O dual-context state (cli/ui) com detecção por stack frame é elegante mas frágil — para nós, o equivalente seria contexto por agente (kernel vs specialist vs chief), mas com detecção explícita, não stack inspection.** **(5) O FFmpeg functional command builder (chain + concat) é um padrão reutilizável para qualquer builder de CLI commands — nosso ffmpeg_builder ou equivalentes para git/docker/sqlite.** **(6) O override de inference provider por processador é o que nossa GPU probe (L80) não tem: detectar a GPU é o primeiro passo, mas permitir que cada agente escolha seu backend (CPU vs CUDA vs ROCm) é o segundo.**
| **Next** | (1) Implementar job queue file-based para workflow engine. (2) Contrato de processor para agentes Cosca (pre_check, process, post_process). (3) Step/Job key split no execution plan. (4) `make install` para embed sincronizar. |
