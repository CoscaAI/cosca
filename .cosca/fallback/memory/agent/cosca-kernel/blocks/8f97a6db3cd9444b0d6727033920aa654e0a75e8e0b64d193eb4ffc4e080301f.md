PREV: 88683afa7988dbada0248471a05dc1c670b2423ddb4d3017106f20fc921fbd42
ID: L35
TIME: 2026-08-11
LEVEL: 4
TAGS: #modelsdev #models #catalog #context-length #pricing #sync #provider-registry #mining #anomalyco #level-4
---
## L35 — 2026-08-11 — Mining models.dev (anomalyco): `cosca models` command + internal/models package — context/pricing catalog closes the stale config gap | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | After Epic Games (lore) mining, Don said "sim" to continue mining the anomalyco queue. Next mine: models.dev (anomalyco, MIT, 6.3K stars) — "open-source database of AI model specifications, pricing, and capabilities" used internally by OpenCode. It closes a REAL gap from our audit: hardcoded context_window in internal/config (was 200000 for deepseek, changed to 131072 for qwen — stale/wrong model context). |
| **Technique** | Level 4 — Mining: cloned models.dev (37MB). API at https://models.dev/api.json returns the full catalog. Model fields: id, name, context_length (or limit.context in current shape), architecture.input_modalities/output_modalities, pricing (or cost.* in current shape), supported_parameters, default_parameters, top_provider. Current live API has 4302 models / 179 providers, and the shape changed to provider-keyed with limit.context + cost.* (the README-documented context_length/pricing shape is legacy). Implementation: `cosca models` command group + internal/models/ package: (1) Model/ModelPricing types, atomic cache (temp+rename, 0600 file, 0700 dir) at ~/.config/cosca/models.json; (2) SyncFromAPI: 30s timeout, 64MB cap, tolerant parser (handles both current limit.context/cost.* AND legacy context_length/pricing shapes; plus flat models.json and catalog.json), collision resolution so canonical owner wins over reseller aliases; (3) Lookup (exact → suffix → name fuzzy), ContextWindowFor; (4) CLI: sync/list [query]/show <id>/info; (5) Integration: cosca-chat buildEngineWithMode uses ContextWindowFor(cache, model) → engine.NewBuilderContext (effective runtime context); config.Load enriched (explicit context_window in config wins); cache miss → hardcoded fallback (non-breaking, offline-safe); (6) cmd/cosca/main.go: models added to isAdminCommand + integrityGateBypass (sync needs network + ~/.config/cosca, not in jail). |
| **Level** | 4 |
| **Outcome** | success — 14 unit tests + 4 config integration tests PASS; E2E sync → 4302 models / 179 providers / 1.9MB cache; show deepseek/deepseek-v4-flash → context 1,000,000, prompt $0.14/1M, completion $0.28/1M; list qwen → 655 entries. Chain 34 blocks valid. |
| **Confidence** | 0.96 (validado: 18 testes pass, E2E sync 4302 modelos/179 providers, show/list corretos, chain 34 blocos) |
| **Tags** | #modelsdev #models #catalog #context-length #pricing #sync #provider-registry #mining #anomalyco #level-4 |
| **Related** | L192 (mining methodology), L191 (evals harness), L193 (mining Epic lore) |
| **Learned** | **(1) The mining queue (10x → harbor → lore → models.dev) shows a pattern: each mine closes a real gap — eval harness (10x/harbor), BLAKE3+parallel (lore), model metadata (models.dev). Mining without integration is wasted; each one must land as a native cosca feature.** **(2) External APIs change shape — the models.dev README documents context_length/pricing but the live API uses limit.context/cost.*; a tolerant parser handling both is essential.** **(3) Cache-as-enhancement, hardcoded-as-fallback is the safe integration pattern: never let an external dependency become a failure point.** **(4) Real model metadata (context 1M for deepseek-v4-flash) fixes silent correctness bugs — the cost tracker and context management were using stale hardcoded values.** |
| **Next** | (1) Wire pricing into the cost tracker (currently manual). (2) models sync cron/timer for freshness. (3) Keep mining: opentui (TUI patterns), sst (deploy), raddebugger (debugging), deadline (job queue). |
