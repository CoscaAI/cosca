PREV: 5664d0c4d6d6db6f6bd4aad96e8d2b54b57d9659aafe5730d6d2bc495318726a
ID: L306
TIME: 2026-08-16
LEVEL: 5
TAGS: #mineracao #auto-otimizacao #tvm #metaschedule #opentuner #halide #performance-brain #cost-model #bandit #algoritmo-vs-schedule #level-5
---
## L306 — 2026-08-16 — MINERAÇÃO Auto-otimização: TVM/MetaSchedule + OpenTuner + Halide — a escola de pilotagem do Performance Brain | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Mineração profunda dos 3 projetos de auto-otimização (prioridade máxima do Professor) para implementar o Cosca Performance Brain |
| **Technique** | Arqueologia arquitetural → o ciclo canônico gerar→medir→guardar→reutilizar; cost model prediz sem rodar, benchmark real decide |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.90 |
| **Tags** | #mineracao #auto-otimizacao #tvm #metaschedule #opentuner #halide #performance-brain #cost-model #bandit #algoritmo-vs-schedule #level-5 |
| **Related** | L305, L303, L250, L251 |
| **Learned** | (1) TVM/MetaSchedule: o loop gerar→medir→guardar→reutilizar como pipeline de componentes plugáveis: SpaceGenerator (search space via ScheduleRules) → SearchStrategy (EvolutionarySearch default) → CostModel (XGB/MLP prediz SEM rodar) → Builder/Runner (benchmark no hardware REAL) → Database (records append-only JSONL com hash estrutural da config) → TaskScheduler (aloca orçamento por peso da task). Warm-start: retomar tuning de seeds salvas. Reuso cross-workload por hash estrutural (module_equality strict vs anchor-block). Tuning seletivo por hot-spot (op_names). Two-tier: DLight rule-based cobre (0 custo), MetaSchedule refina hot spots. 200-500 trials/task. Speedup típico 2-10×. (2) OpenTuner: a joia é o ENSEMBLE + BANDIT — várias técnicas de busca em paralelo (DifferentialEvolution, Nelder-Mead, PSO, GreedyMutation), a metatécnica AUCBandit redistribui orçamento: quem acerta ganha trials, quem falha é desativado. Espaço de busca DECLARATIVO (ConfigurationManipulator + parâmetros tipados — vale mais que o tuner). Contrato mínimo run(config)→Result(time). Persistência total em SQL. Objetivos multi-métrica (Result.extra). Premissa: nenhum tuner único domina tudo. (3) Halide — o MAIS RELEVANTE para imagem: a separação FUNDAMENTAL algoritmo vs schedule (o "quê" imutável e puro, o "como" como lista de directives: split/reorder/tile/vectorize/unroll/parallel/compute_at/store_at) — o schedule É um vetor de parâmetros tunáveis (search space natural). Trade-off triplo memória×trabalho×paralelismo (números reais: sin calls 64→25, temp 25→10 floats). Bounds inference automática. Storage folding/circular buffers. JIT primeiro (realize()), AOT depois. Autoscheduler: 2016 (regras+bounds, sem tuning, segundos) → 2019 (beam search + cost model com features, ~2× mais rápido, primeiro a superar experts humanos). |
| **Next** | (1) Arquitetura do Cosca Performance Brain em Go: TASK (descrição canônica + shape) → SpaceGenerator (schedule params tipados) → Record DB (SQLite, hash estrutural da config) → Tuner Ensemble+Bandit (Go, math/rand + contador de budget) → CostModel (heurística/regressão simples primeiro, ML só se dados mostrarem ganho — P13) → Builder/Runner (benchmark real decide, fail-closed) → Dispatcher runtime (consulta DB por hash, heurística rule-based cobre, tuner async refina hot-spots, hot-swap atômico). (2) NÃO copiar: full compiler stack do TVM, XGB/MLP com centenas de milhares de programas, beam search 2019, SQLAlchemy. |
