PREV: addb12edd69ef95d94f1f23b03227975a73b25efb3473d7e58bcc7ece0230ea5
ID: L219
TIME: 2026-08-14
LEVEL: 5
TAGS: #arquivo-fantasma #rca #audit #sqlite #memory-dsn #privfile #teste-higiene #fix-na-raiz #level-5
---
## L219 — 2026-08-14 — O ARQUIVO FANTASMA (ordem do Don "não entendi aquele arquivo"): `:memory:?cache=shared` era DSN de memória virando arquivo físico — fix na raiz do audit.NewStore | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don viu no git um arquivo estranho: `api/rest/handler/:memory:?cache=shared` (0 bytes) e não entendeu. Investigar, explicar e eliminar. |
| **Technique** | Level 5 — **(1) RCA completo**: o artefato é criado por `audit.NewStore(":memory:?cache=shared")` (21 ocorrências em api/rest/handler tests). O DSN `:memory:` é VÁLIDO para o SQLite (DB em memória compartilhada), mas `NewStore` chama `privfile.EnsurePrivateDBFile(dbPath)` ANTES do `sql.Open` → `os.OpenFile(CREATE, 0600)` **materializa um arquivo físico de 0 bytes com o nome do DSN** no CWD do processo de teste, toda execução de `go test ./...`. O fallback do helper (temp file) nunca roda porque o `sql.Open` com `:memory:` não falha. **(2) Fix cirúrgico na raiz**: `audit.NewStore` agora pula `MkdirAll`+`EnsurePrivateDBFile` quando o path é um DSN de memória (`isMemoryDSN`: vazio ou prefixo `:memory:`). Produção nunca passa `:memory:` → comportamento de produção intocado. **(3) Verificação**: removi o artefato, rodei audit + handler tests (verdes), confirmei que o arquivo NÃO volta mais, e varri o repo por outros artefatos `:memory:*` (nenhum; secrets usa TempDir, auth não usa `:memory:`). |
| **Level** | 5 |
| **Outcome** | success — arquivo fantasma eliminado na raiz; `go test ./...` não suja mais o working tree. audit tests + handler tests verdes, build OK. |
| **Confidence** | 0.97 (validado: artefato removido, testes de ambos os pacotes verdes, ausência confirmada pós-execução, varredura limpa) |
| **Tags** | #arquivo-fantasma #rca #audit #sqlite #memory-dsn #privfile #teste-higiene #fix-na-raiz #level-5 |
| **Related** | L316 (fix do review parser — 4º bug do dia), memória do cosca-testing (documentava o `:memory:?cache=shared` compartilhado mas não o efeito colateral) |
| **Learned** | **(1) DSN de SQLite NÃO é caminho de arquivo — e `EnsurePrivateDBFile` não sabe a diferença: o padrão "garantir permissão 0600 criando o arquivo" é correto para DBs físicos e destrutivo para DSNs de memória; a guarda pertence a quem conhece o domínio (o store), não ao helper genérico.** **(2) Um artefato de teste que reaparece a cada `go test` é um sintoma de bug, não de sujeira — o Don viu o sintoma; o RCA levou à causa raiz em 3 níveis (teste → NewStore → privfile).** **(3) O fallback que nunca roda (temp file do helper) é dívida invisível: `:memory:` sempre funciona, então o caminho de erro estava morto e o lixo passava despercebido por todos.** **(4) Valor do olhar do Don: um arquivo de 0 bytes no git status que eu mesmo tinha removido e vi voltar foi o gatilho — a ordem "não entendi" produziu um fix de produção correto que nenhuma medição de cobertura teria motivado.** |
| **Next** | (1) Nada commitado — working tree à disposição. (2) Restam: 9 arquivos a 0% no pipeline + ~3,9 pts até o padrão de 70%. (3) Verificar se outros stores (memory.NewEngine, trace.NewStore) têm o mesmo risco com DSNs de memória — preventivo. |
