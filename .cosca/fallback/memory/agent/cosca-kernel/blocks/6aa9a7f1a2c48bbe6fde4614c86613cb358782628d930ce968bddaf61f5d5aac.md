PREV: a38ffff95662f907b2c96dfa2958dd603cdc33318aaba98e36a815d478129aad
ID: L107
TIME: 2026-08-13
LEVEL: 5
TAGS: #hornfit #sistema-completo #fullstack #websocket #portal-cliente #page-builder #relatorios #observabilidade #ci-cd #docker #level-5
---
## L107 — 2026-08-13 — HornFit completo: F2-F7 entregues — sistema de gestão full-stack do zero | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "FAZ COMO ACHAR MELHOR" (autonomia total) — concluir o HornFit (F2→F7) e entregar o sistema COMPLETO: CRUD de clientes/OS/contratos/estoque, faturamento, CRM, helpdesk WebSocket, portal do cliente, FAQ, landing + marketplace + page builder, relatórios + rastreio de erros, CI/CD + docs. Código em inglês, UI pt-BR, sistema único sem tenant. |
| **Technique** | Level 5 — **(1)** Envelope de resposta uniforme: `TransformInterceptor` sempre embrulha em `{ data }`; listas retornam `{ items, total }` (nunca `{ data, total }` — a heurística `'data' in data` causava inconsistência). **(2)** Numeração sequencial (`OS-0000NN`, `CT-`, `NF-`, `TC-`, `PD-`) via util `nextSequenceNumber` com pad à esquerda. **(3)** WebSocket: `HelpdeskGateway` com auth JWT no handshake; para empresa única, broadcast (`server.emit`) em vez de rooms — o frontend mapeia por `msg.ticketId`. **(4)** Gateway compartilhado entre módulos: `HelpdeskModule` EXPORTA o gateway e `PortalModule` IMPORTA o módulo (evita instância duplicada — gotcha clássico de NestJS). **(5)** JwtModule `global: true` para o gateway injetar JwtService sem re-registrar. **(6)** Portal do cliente: `@Roles(CLIENT)` + `getClientId(userId)` resolve o vínculo user→client via DB (o JWT só carrega sub/email/role). **(7)** Page builder: blocos como JSON livre; endpoint público por QUERY (`/pages/public?slug=/`) porque slug `/` quebra como path param. **(8)** Observabilidade: `HttpExceptionFilter` injeta PrismaService via `app.get()` e persiste 5xx no ErrorLog. **(9)** Docker monorepo: multi-stage com `pnpm install` na raiz, `prisma generate` antes do build, migrate deploy no CMD do runner. |
| **Level** | 5 |
| **Outcome** | success — Sistema entregue em 8 commits (F0→F7). Esteira verde: typecheck 3/3, testes 4/4, build 2/2. ~25 endpoints REST + 1 gateway WebSocket + ~20 telas. Testado ponta a ponta via curl (auth/RBAC/CRUD/transições/estoque/portal/relatórios) e WebSocket real validado com socket.io-client. |
| **Confidence** | 0.95 (validado: esteira completa verde, smoke test de KPIs, WebSocket real, 8 commits atômicos) |
| **Tags** | #hornfit #sistema-completo #fullstack #websocket #portal-cliente #page-builder #relatorios #observabilidade #ci-cd #docker #level-5 |
| **Related** | L201 (F0+F1), L192 (projeto desacoplado), L191 (desktop) |
| **Learned** | **(1) Envelope de API uniforme desde o início evita retrabalho — a heurística de "só embrulhar se não tem data" criou shapes divergentes (single vs list) que precisei padronizar na F2.** **(2) Numeração sequencial com max+1 é suficiente para empresa única; para concorrência real, usar sequence no Postgres.** **(3) WebSocket em monorepo: instalar `socket.io-client` no web; testar realtime com um script node que conecta, joina e posta via HTTP — pegou o fluxo inteiro.** **(4) Slug `/` não funciona como path param — usar query param para slugs especiais.** **(5) pnpm 10+ e Jest: config em `.js` precisa de `module.exports`, não objeto JSON solto (erro "Unexpected token ':'").** **(6) tsconfig de pacote dentro de `packages/*`: extends é `../config/` (um nível), NÃO `../../config/` — e lib DOM é necessária para tipos React.** **(7) turbo.json: não declarar `outputs` para tasks sem artefatos (warning + falha silenciosa).** **(8) Deploy: `prisma migrate deploy` no CMD do container API garante schema sempre aplicado sem passo manual.** |
| **Next** | (1) Subir em produção real (VPS + domínio) quando o Don ordenar. (2) Rotacionar as API keys pendentes (L187). (3) Testes e2e (Playwright) cobrindo a jornada completa login→OS→faturamento. (4) Upload de arquivos reais no MinIO (fotos de equipamento/NF). |
