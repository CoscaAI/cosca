PREV: 0c263e2105d817c5201e299548137f55cd4c3b326ed21949cda91f21f22884b6
ID: L214
TIME: 2026-08-14
LEVEL: 5
TAGS: #cobertura #pipeline #testes #flaky #race #seguranca #classificador #falso-positivo #falso-negativo #p13 #level-5
---
## L214 — 2026-08-14 — ATAQUE À COBERTURA DO PIPELINE (ordem do Don): 18,7% → 32,7%, teste flaky corrigido, 2 BUGS REAIS de segurança encontrados e corrigidos | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" (após a auditoria quantitativa L311): atacar a cobertura do internal/pipeline primeiro — a maior dívida (9.786 LOC de produção, ratio 0,18, teste flaky). |
| **Technique** | Level 5 — **(1) Diagnóstico por função**: `go tool cover -func` revelou 32 arquivos a 0% — priorizei os de lógica pura e auto-contidos (gate, op_security, budget, evidence, checkpoint, handoff, permission, interaction_policy, agent_router). **(2) 7 arquivos de teste novos** (~1.374 linhas) cobrindo caminhos felizes + erros + fronteiras + contratos. **(3) FIX do flaky**: `TestTaskQueueDeduplicates` enfileirava 3× com o worker ATIVO — se o worker pega a chave entre os Adds, o workqueue marca dirty e re-executa (semântica legítima de `Add` durante `Get` in-flight) → teste era o racy, não a fila; solução: enfileirar ANTES do `Start` (determinístico). **(4) P13 na prática**: os testes novos PEGARAM 2 bugs reais no classificador de segurança que a cobertura escondia. |
| **Level** | 5 |
| **Outcome** | success — pipeline: 18,7% → **32,7%** (+14,0 pts). Vet limpo, `-race` limpo, build OK, CLI (consumidor do SecurityGate) verde, flaky 5/5 estável. 2 BUGS corrigidos em op_security.go: **(A)** `"dd "` (disco) casava substring em `"git add ."` ("a**dd** ") → classificado como IRREVERSÍVEL (falso positivo grave — git add bloqueado sem override); **(B)** `"DROP "`/`"TRUNCATE "` em MAIÚSCULAS nunca casavam (a entrada é minúsculizada) → `DROP TABLE` classificado como READ (falso negativo — operação destrutiva passava sem confirmação). |
| **Confidence** | 0.95 (validado: teste-race + vet + build + suíte do consumidor; bugs confirmados por casos de teste que falhavam antes da correção e passam depois) |
| **Tags** | #cobertura #pipeline #testes #flaky #race #seguranca #classificador #falso-positivo #falso-negativo #p13 #level-5 |
| **Related** | L311 (auditoria — o baseline que revelou a dívida), L307 (read-only), P14, PIPELINE_TECNICO.md |
| **Learned** | **(1) Teste flaky ≠ código flaky: no workqueue estilo client-go, `Add` durante `Get` in-flight marca dirty e re-executa — o dedup cobre enfileiramento CONCORRENTE, não re-enfileiramento durante processamento; o teste que assumia o contrário era o bug.** **(2) Escrever testes de um componente de segurança é a forma mais barata de auditar: o par falso-positivo/falso-negativo do classificador (dd ↔ DROP) é exatamente o tipo de erro que nunca aparece em code review mas explode em produção.** **(3) O padrão de priorização funciona: funções puras e auto-contidas (budget, permission, policies, stores) rendem pontos de cobertura com risco zero; o que ficou de fora (exec externo: runBuild/runTests/runVet, benchmark, analytics, general_context, plugins_builtin) precisa de refactor para injeção de dependências — dívida futura.** **(4) gofmt revelou violações pré-existentes em ~20 arquivos do pipeline — reformatei só o que toquei (diferença mínima), o resto é dívida separada.** |
| **Next** | (1) Se o Don autorizar: Fase 2 do pipeline (benchmark, analytics, reconciler, history, general_context — os maiores blocos restantes). (2) Depois: CLI (27.425 LOC, 59,8%). (3) Decisão sobre as violações gofmt pré-existentes. |
