PREV: 7c6c8300dca8d59eb8add48f562131408c307b4802357749c2a97e46c2560cc1
ID: L136
TIME: 2026-08-13
LEVEL: 4
TAGS: #eval #oracle #black-box #descoberta #secret-tests #discovery-efficiency #metacognicao #spec #level-4
---
## L136 — 2026-08-13 — Spec de avaliação oracle-based (cosca eval v2): medir descoberta, não reprodução | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" (após análise da proposta de problem-oracle) — desenhar a spec concreta de avaliação oracle-based em cima do `cosca eval` (oráculo caixa-preta + testes secretos + métricas de descoberta). |
| **Technique** | Level 4 — **(1)** Análise da proposta: classificada como *oracle-based/black-box eval* (mesma metodologia de juízes competitivos/CASP), acerta o problema central (vazamento → mede reprodução, não descoberta) mas tem 4 buracos (oráculo finito ≠ correção; oráculo burro = explosão de busca; "impossível" vs "desisti"; verificação do verificador). **(2)** Grounding no código real: o `cosca eval` (internal/evals/) JÁ tem 80% — `verify`=oráculo público, `reward`=qualidade, canary anti-contaminação, runner com timeout. **(3)** Spec `internal/evals/ORACLE_SPEC.md`: schema estendido do `Case` (`problem_class` reproduction/discovery/open/existence + `oracle` mode/feedback/max_submissions/public_properties); store de segredos `.cosca/evals/secrets/<case-id>.yaml` (0600, fora do agente); `cosca eval oracle submit` (CLI+tool) com 4 níveis de feedback; instrumentação trace (`oracle.submit/result`, `hypothesis.record`); `DiscoveryMetrics` com **DiscoveryEfficiency = reward/(hipóteses+experimentos+1)**; 6 fases incrementais de implementação; validação do oráculo (self_check); limites honestos registrados. **(4)** Decisão-chave: **hipótese = submissão ao oráculo** (sinal primário confiável), redundada por registros de memória (mede aprendizado operacional). |
| **Level** | 4 |
| **Outcome** | success — Spec completa e buildável em `internal/evals/ORACLE_SPEC.md`. Conexão revelada: a spec avalia EXATAMENTE o que o cosca autônomo acabou de fazer (o `internal/ledger` = resultado do desafio "8 propriedades" do Don, L234) — é o instrumento para medir se o ledger foi DESCOBERTO ou REPRODUZIDO. |
| **Confidence** | 0.92 (validado: spec ancorada nos tipos reais do internal/evals/ — Case/Suite/VerifyRunner/Report/trace) |
| **Tags** | #eval #oracle #black-box #descoberta #secret-tests #discovery-efficiency #metacognicao #spec #level-4 |
| **Related** | L234 (ledger — o alvo da spec), L197/L191 (harness eval 10x/harbor), L231 (mineração k8s), L188 (o que não está no commit não existe) |
| **Learned** | **(1) O harness atual já implementa o padrão oráculo sem esse nome: `verify` (comandos shell) é o oráculo, `reward` é a qualidade, `canary` é anti-contaminação — a evolução para descoberta é INCREMENTAL (secret_verify fora do agente + instrumentação + DiscoveryEfficiency), não uma reescrita.** **(2) "Hipótese = submissão ao oráculo" resolve o problema de capturar o processo interno do LLM de forma confiável: cada `cosca eval oracle submit` é uma hipótese objetivamente registrável no trace, sem depender do LLM "logar" seu raciocínio.** **(3) A distinção crítica que a spec codifica: oráculo FINITO responde VALID/INVALID sobre um proxy da propriedade, não sobre a propriedade — overfit do oráculo é um modo de falha real, mitigado por public_properties formais + testes secretos independentes.** **(4) O modo existence (construir vs provar impossibilidade) é coNP-hard e exige verify_proof robusto, senão "impossível" degenera em "desisti" — distinção que a spec obriga a registrar.** **(5) Encontrei L234 (escrito pelo cosca autônomo, não por mim) no learnings.md — confirma que o runtime L3 está trabalhando em paralelo em outra sessão; a spec que desenhei é exatamente o instrumento para avaliar esse trabalho sem entregar o segredo. |
| **Next** | (1) Implementar F1 (secret_verify + store de segredos) e F2 (`cosca eval oracle submit`). (2) Montar os 3 casos A/B/C concretos (CRUD conhecido / otimização do índice / scheduling aberto) para testar o ledger como alvo. (3) Decidir com o Don o que fazer com `internal/ledger/` (commit/review/descarte). (4) Usar a spec para avaliar o ledger: foi descoberto ou reproduzido? |
