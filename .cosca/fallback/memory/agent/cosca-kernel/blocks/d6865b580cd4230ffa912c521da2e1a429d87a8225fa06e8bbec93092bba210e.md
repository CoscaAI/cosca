PREV: 7837ed9a6784b488efef8a2f7e2932ae7dc0b7695b8adf1bfb79becae069a98a
ID: L54
TIME: 2026-08-12
LEVEL: 4
TAGS: #erro #error-tracing #nextjs #error-boundary #digest #retry #global-error #unhandledrejection #crash #desktop #level-4
---
## L54 — 2026-08-12 — Rastro de erro estilo Next.js no cosca-desktop: ErrorBoundary com digest+retry, captura global, log no backend | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don: desktop travou com "null is not an object (evaluating 'M[0]')" (TypeError minificado de acesso [0] sem guarda) e pediu: "poderica criar rastro de erro pra debugar... e fazer que nem o nextjs faz, da uma analizada no projeto deles". |
| **Technique** | Level 4 — **(1)** Análise do padrão Next.js (docs oficiais): error.tsx com retry() + digest, boundaries ANINHADOS (granularidade por rota/segmento), global-error.tsx, logging via useEffect, boundaries NÃO capturam event handlers/async (precisam de captura global). **(2)** Fix do crash: `ExecutionTree.tsx` openRef() guard `refs.length === 0 || !refs[0]` + title com `?.`; audit completo de todos [0] (SidebarViews já tinha guard, App models[0]||'none' seguro, DockerPanel entries[0] seguro). **(3)** `errors.go` — binding `ReportFrontendError` (Message/Stack/Component/Digest/Time) com sanitize (msg≤2000, stack≤8000, component≤200), append em `~/.cosca/logs/desktop-errors.log` (COSCA_ERROR_LOG override p/ testes) + mirror zerolog, retorna nil sempre. **(4)** `errorTrace.ts` — genDigest() (ERR-<base36>-<seq>), reportError(), installGlobalErrorHandlers() (window error + unhandledrejection, component 'global'). **(5)** ErrorBoundary v2 Next-style: getDerivedStateFromError + componentDidCatch (reporta com digest) + fallback elegante (digest mono, mensagem, botões "Recarregar cockpit" e "Tentar novamente" = retry) + prop name p/ boundaries nomeados. **(6)** Boundaries ANINHADOS no App: outer global + ChatPanel/ExecutionTree/Editor/Sidebar — crash num painel mostra fallback localizado em vez de matar o cockpit. |
| **Level** | 4 |
| **Outcome** | success — 5 testes novos PASS (writes file, appends, truncates long message, weird input, sanitize). go vet+test limpos, tsc 0 erros, npm build OK, wails build 33s, desktop rodando a 17.9% CPU sem novos erros. |
| **Confidence** | 0.95 (validado: 5 testes, builds limpos, launch OK; captura real de crash depende de um próximo erro acontecer) |
| **Tags** | #erro #error-tracing #nextjs #error-boundary #digest #retry #global-error #unhandledrejection #crash #desktop #level-4 |
| **Related** | L207 (crash debug nil-slices), L210 (voice desacoplado), L202 (desktop fundação) |
| **Learned** | **(1) O padrão Next.js de erros tem 4 camadas que todo app rico deveria ter: boundaries aninhados com retry, digest de correlação, fallback elegante, e captura GLOBAL de async/handlers (window.onerror + unhandledrejection) — boundaries sozinhos não pegam event handlers.** **(2) "null is not an object (evaluating 'M[0]')" é a assinatura de acesso [0] sem guarda minificado — a auditoria de `foo[0].bar` (sem ?. ou length check) é item obrigatório em code review.** **(3) O erro de render do WebKit NÃO aparece no stdout do Wails — sem um pipeline de report explícito (binding Go + arquivo de log), crashes de UI são invisíveis para o operador. Esse é o gap que o Next.js resolve com logging no serviço.** **(4) Frontend input é untrusted: sanitize (truncar) mensagem/stack/component antes de gravar — um runaway nunca pode balloon o log.** **(5) Logging nunca deve quebrar o app: ReportFrontendError retorna nil sempre (fire-and-forget no front).** |
| **Next** | (1) Commit + push (desktop ~18 arquivos). (2) Se o crash reaparecer, o log ~/.cosca/logs/desktop-errors.log terá o digest + stack para debugar em 1 minuto. (3) Considerar rotacionar o log (size cap). |
