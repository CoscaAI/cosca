PREV: e8e59fe50cbf066f14e9ca24cb0f5f70733836c7ec579964e2dc70681168d3aa
ID: L92
TIME: 2026-08-13
LEVEL: 5
TAGS: #fintech #payment #ledger #idempotencia #pix #boleto #cartao #state-machine #refund #marketplace #hornfit #stack34 #level-5
---
## L92 — 2026-08-13 — Fintech integrada no HornFit: máquina de estados de pagamento no marketplace | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "integrar fintech no hornfit" — pagamento real no marketplace seguindo a doutrina do Stack 34. |
| **Technique** | Level 5 — **(1)** Schema: `PaymentStatus` (PENDING/CONFIRMED/FAILED/REFUNDED) + paymentDetails/confirmedAt/refundedAt; migration `add_payment_state_machine` criada e aplicada DENTRO do container da API (o postgres de prod não é exposto — migration executada via `docker exec` + `prisma migrate dev`, depois copiada de volta). **(2)** PaymentsModule: `initiate` (idempotente por pedido+método — reusa PENDING existente), `confirm` (callback PSP simulado), `refund` (admin, workflow reverso). **(3)** Provedor simulado: PIX copy-paste (hash determinístico), boleto, cartão — interface pronta para trocar por Stripe/PSP real. **(4)** `createOrder` agora gera fatura (type PRODUCT) vinculada → fluxo de pagamento completo. **(5)** Dinheiro em Decimal (nunca float); fix de sequência: NF usa o último número de FATURA (não de pedido — colisão com seed). **(6)** Frontend: modal de pagamento no marketplace (método → gerar → instruções → confirmar). |
| **Level** | 5 |
| **Outcome** | success — Fluxo validado em produção: PD-000003 → PIX → idempotência SIM → confirm → ordem/fatura PAID → refund OK. Commit e764927. |
| **Confidence** | 0.95 (validado: fluxo completo testado via curl no ambiente de produção) |
| **Tags** | #fintech #payment #ledger #idempotencia #pix #boleto #cartao #state-machine #refund #marketplace #hornfit #stack34 #level-5 |
| **Related** | L216 (doutrina fintech), L202 (HornFit completo), L211 (regra de libs) |
| **Learned** | **(1) Quando o banco de prod não é exposto, migration pode rodar DENTRO do container da API (schema copiado + `prisma migrate dev` + copiar migration de volta) — sem alterar o compose.** **(2) Bug real pego no teste: `@IsEnum(UserRole)` em vez de `@IsEnum(PaymentMethod)` no DTO — validava contra os valores errados; o teste end-to-end pega isso.** **(3) Sequência de NF deve usar o último número de FATURA, não de pedido (colisão com seed).** **(4) Idempotência por pedido+método reusando PENDING evita pagamento duplicado (3 cliques ≠ 3 cobranças — princípio fintech).** **(5) Confirmar de novo retorna alreadyConfirmed (idempotente).** **(6) Refund como workflow reverso (CONFIRMED → REFUNDED + fatura CANCELLED + ordem REFUNDED).** **(7) Provedor simulado com interface limpa permite trocar por Stripe/PSP real sem refatorar o fluxo. |** |
| **Next** | (1) Trocar o provedor simulado por Stripe/PSP real quando o Don fornecer as chaves (interface pronta). (2) Exibir status de pagamento no portal do cliente + botão "pagar fatura pendente". (3) Notificações de pagamento (PIX confirmado → notificação no sino). |
