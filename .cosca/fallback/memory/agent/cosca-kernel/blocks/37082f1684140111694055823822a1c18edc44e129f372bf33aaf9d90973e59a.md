PREV: cd35f1d8b03b3af23605b2f4634cca3f2fea99755f8cddc59fa3886ab29cbd59
ID: L179
TIME: 2026-08-14
LEVEL: 5
TAGS: #cosca-trader #tela-branca #widthprovider #react-grid-layout #legacy #exports-map #persistencia #level-5
---
## L179 — 2026-08-14 — cosca-trader: TELA BRANCA resolvida — WidthProvider do subpath /legacy (o ESM padrão do react-grid-layout não exporta) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Após o L297 (painel arrastável), o Don reportou: "sem persistenicia e foi todos pro canto" (cards amontoados = width fixo 1200) e depois TELA BRANCA: `Uncaught TypeError: GLPkg.WidthProvider is not a function` (App.tsx:14). |
| **Technique** | Level 5 — **(1) CARDS NO CANTO**: o GridLayout usava `width={1200}` fixo — a solução é o WidthProvider (mede a largura real do container). **(2) TELA BRANCA — causa raiz em 2 camadas**: (a) o ESM (`dist/index.mjs`, usado pelo Vite via module field) NÃO exporta WidthProvider — só o CommonJS (`index.js`) tem, via module.exports; (b) importar `dist/legacy.mjs` direto quebra no rollup (exports map do package bloqueia deep imports). **(3) FIX CORRETO**: importar de `react-grid-layout/legacy` (o subpath EXPORTADO no exports map) — o WidthProvider existe lá. **(4)** Declaração de módulo própria (react-grid-layout.d.ts) para o TS reconhecer o subpath sem types. **(5)** Persistência: onDragStop/onResizeStop salvam SÓ quando o usuário interage (não no mount — o onLayoutChange sobrescrevia o salvo com a versão compactada); compactType removido. |
| **Level** | 5 |
| **Outcome** | success — commits (fix). Build OK, Vite sem erros, painel 200. O WidthProvider espalha os cards na largura real; a persistência salva só na interação; a tela branca morreu. |
| **Confidence** | 0.95 (validado: build OK, HMR servindo o App atualizado com WidthProvider, log sem erros) |
| **Tags** | #cosca-trader #tela-branca #widthprovider #react-grid-layout #legacy #exports-map #persistencia #level-5 |
| **Related** | L297 (painel arrastável — onde o problema nasceu), L296 (gráfico), L295 (design) |
| **Learned** | **(1) O ESM e o CommonJS de um pacote podem exportar COISAS DIFERENTES: o react-grid-layout tem WidthProvider no CJS mas não no ESM — importar "o default" não garante acesso a tudo; o exports map do package.json é a fonte da verdade dos subpaths.** **(2) Tela branca = erro de runtime no mount, não de build: o build passava mas o componente quebrava ao montar — o erro só aparece no console do navegador (o Don viu antes de mim).** **(3) Persistência de layout: salvar no onLayoutChange dispara no mount (sobrescreve o salvo com a versão compactada) — o certo é salvar em onDragStop/onResizeStop (interação real do usuário).** |
| **Next** | (1) O Don reorganiza o painel com persistência real. (2) Conectar divergência ao OMS. (3) Testnet. |
