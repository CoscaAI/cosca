PREV: 454ad5623b44bfe0d0330ee2b5a00b3224051c91920d02832479f0c0be6b92e1
ID: L12
TIME: 2026-08-09
LEVEL: 4
TAGS: #vector-search #graph #segurança #jwt-secret #env #bwrap #jail #loadDotEnv #propagateProviderEnv #cosine #fts5 #level-4
---
## L12 — 2026-08-09 — Vector search + Graph ativados + correção de segurança .env | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's orders: "liga vector e graph" → "força rebuild" → "tira JWT_SECRET do .env". (1) Habilitar vector search e graph search no config (4 flags). (2) Corrigir wiring do graph builder (2 bugs: loadGraph depois do indexer + IndexerConfig BuildGraph=false). (3) Remover JWT_SECRET do .env do workspace — segredo fica só em serve.env (fora, 0600) e é injetado via bwrap --setenv. (4) Corrigir loadDotEnv e propagateProviderEnv para funcionar dentro da jaula. |
| **Technique** | Level 4 — (A) Vector: 4 flags no config.yaml → restart serve → busca semântica ativa (score cosseno 0.82 vs BM25 -4.09). (B) Graph: delegado cosca-backend → 2 bugs no knowledge.go → rebuild → cache 810KB (antes 23 bytes). (C) Segurança: JWT_SECRET removido do .env → agente dentro da jaula não lê segredo. (D) main.go: loadDotEnv sem guard InsideJail + propagateProviderEnv expandido com base URLs e embedding provider. |
| **Level** | 4 |
| **Outcome** | success — Vector search: score cosseno positivo (0.82) para "segurança sandbox jail" vs BM25 negativo (-4.09) antes. Graph: 810KB no cache, populado com nós e arestas. Segurança: .env sem JWT_SECRET, agente malicioso não vê nada. Serve: health ✅, 3 serviços green. Pipeline: 39 workflows definidos, execução funcional (-q), 20/20 testes PASS. |
| **Confidence** | 0.95 (validado: busca vetorial com score diferente, cache graph 810KB, serve health 200, git status limpo, 3 commits na sessão) |
| **Tags** | #vector-search #graph #segurança #jwt-secret #env #bwrap #jail #loadDotEnv #propagateProviderEnv #cosine #fts5 #level-4 |
| **Related** | L162 (ressuscitação ollama/voice/runtime), L161 (Bitcoin trading desk), L154 (Single Owner Model), L153 (esteira autônoma), L103 (runtime qwen), L68 (Fase B provider=none) |
| **Learned** | **(1) Vector search vs FTS5: mesma query "segurança sandbox jail" — FTS5 retorna score -4.09 (BM25, busca literal), vector retorna 0.82 (cosseno, busca semântica). O sistema entende que "jail" ≈ "sandbox" ≈ "segurança" sem matching exato de palavras.** **(2) O graph tinha 2 bugs de wiring: (a) loadGraph() era chamado DEPOIS do indexer — o indexer ficava com referência ao graph vazio; (b) IndexerConfig.BuildGraph nunca era true porque o CLI criava Config{} vazio. Fix: reordenar Init() + usar DefaultConfig como base.** **(3) O graph NÃO usa tabelas SQL nodes/edges — serializa JSON na tabela cache. 810KB de JSON com todos os nós e arestas. Carrega no boot em ms, salva no shutdown. Mas o saveGraph trava no shutdown (SIGTERM) — precisa de KILL pra matar o serve. Isso é um bug: o saveGraph serializa o graph inteiro em JSON de uma vez, o que pode travar com graphs grandes.** **(4) Segurança .env: JWT_SECRET no workspace é brecha — qualquer agente com code execution lê. Solução canônica: segredos em ~/.config/cosca/serve.env (0600, fora do workspace), bwrap injeta via --setenv, .env do workspace só tem config não-sensível (portas, modelos, feature flags).** **(5) loadDotEnv() precisa rodar DENTRO da jaula também: o bwrap faz --clearenv que limpa todas as env vars, e o workspace é bind-mounted como /, então /.env é acessível. Sem isso, provider env vars (OLLAMA_HOST, OPENAI_BASE_URL) são perdidas dentro da jaula.** **(6) propagateProviderEnv() agora cobre: API keys do chat provider + API keys do embedding provider (podem ser diferentes) + base URLs de ambos. OLLAMA_HOST é crítico: sem ele, o provider tenta localhost:11434 (default) e o TCP connect trava.** **(7) workflow run funciona mas o output quebra: com -q (quiet) funciona, sem -q o spinner/formatter causa exit 1. Bug cosmético — o motor de workflow (Manager, pipeline, fallback) está 100% funcional (20/20 testes).** |
| **Next** | (1) Corrigir saveGraph no shutdown (timeout ou async). (2) Corrigir output do workflow run (spinner/formatter). (3) Fazer o bwrap funcionar sem COSCA_ALLOW_NO_ROOT (investigar user namespaces). (4) Rebuild do cosca-chat com o fix do OLLAMA_HOST. |
