PREV: 835ab9d586f07a50
ID: L420
TIME: 2026-08-18
LEVEL: 5
TAGS: #merkle #bug #raiz #1-bloco #hex-ascii #cosca-merkle #integridade #fail-closed #level-5
---
## L420 — 2026-08-18 — BUG DO MERKLE: raiz de época com 1 bloco era hex-ASCII inválido (128 chars) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Corrigir raiz Merkle inválida detectada pelo Don ("tem um problema aí") |
| **Technique** | `merkleRoot()` com 1 leaf: o loop `for len(level) > 1` não executa e retornava `hex.EncodeToString([]byte(hash))` = hex do ASCII do hash (128 chars), não um SHA-256. Fix: `if len(leaves) == 1 { return leaves[0] }` — a raiz de época de 1 bloco É o próprio hash do bloco. O cosca-check detectou TAMPERED (fail-closed funcionando): a chain foi re-assinada após commit (ORDEM SAGRADA L199). O scan revelou o mesmo bug latente em 5 agentes (cosca-bootstrap, cosca-plugin, cosca-paradigm, cosca-context, cosca-specialist-documentation-writer) — merkle regenerado para todos. |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.97 |
| **Tags** | #merkle #bug #raiz #1-bloco #hex-ascii #cosca-merkle #integridade #fail-closed #level-5 |
| **Related** | L199 (ordem sagrada), L418 (raízes/neurônios), L417 (serve COSCA KERNEL), RECOVERY_PROTOCOL, L376 (fail-closed no provider) |
| **Learned** | (1) O Don viu o problema antes de mim — "tem um problema aí" era a raiz do epoch 13 vazando hex-ASCII. (2) O sistema funcionou como projetado: o cosca-check detectou os arquivos alterados (TAMPERED) e bloqueou o startup — fail-closed real. (3) O bug era latente: afetava QUALQUER época com 1 bloco (a primeira vez que um agente registrou exatamente 32k+1 blocos). (4) Fix correto: raiz de 1 leaf = o próprio leaf (convenção padrão de merkle). (5) A regra de ouro: após QUALQUER mudança em arquivos do embed (mesmo correção de bug), commit → re-sign. |
| **Next** | Adicionar teste unitário do merkleRoot com 1, 2, 3, 5 leaves (incluindo o caso de 1 bloco) para nunca regredir. Verificar se o cosca-merkle valida o comprimento das raízes (64 hex) como guarda. |
