PREV: a2e21f2ab3f07b5b9c0f1bfd380d3d39b94080459523d7f946464d49e50214bb
ID: L215
TIME: 2026-08-14
LEVEL: 5
TAGS: #cobertura #pipeline #fase-2 #analytics #reconciler #history #autonomy-score #bug-de-corretude #metricas #level-5
---
## L215 — 2026-08-14 — FASE 2 DA COBERTURA DO PIPELINE (ordem "marcha"): 32,7% → 41,9% + BUG REAL de corretude no AutonomyScore (RecoveriesSucceeded sempre 0) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "marcha" — continuar o ataque à cobertura do pipeline (Fase 2): analytics, history, perf_tracker, cost_tracker, reconciler, condensation — os blocos a 0% restantes de lógica pura. |
| **Technique** | Level 5 — **(1) 6 arquivos de teste novos** (~970 linhas): `analytics_test` (ComputeDerived com 5 casos + agregação ponderada + reconstrução por eventos), `history_test` (append/sequência/hash chain/VerifyIntegrity com corrupção real do JSONL/ReplayPlan/splitLines), `perf_tracker_test`, `cost_tracker_test` (pricing por modelo, fallback desconhecido, estimativa), `reconciler_test` (drift missing/failed, convergência, stale), `condensation_test` (limiares, resumo determinístico, compact). **(2) Acesso a campos privados do StepRunner** (`sr.history`, `sr.checkpoint`) direto por estar no mesmo pacote — sem refactor. **(3) P13 de novo**: o teste de reconstrução de analytics expôs um bug de corretude real. |
| **Level** | 5 |
| **Outcome** | success — pipeline: 32,7% → **41,9%** (+9,2 pts). vet/race/build/CLI verdes. **BUG corrigido em analytics.go**: `RecoveriesSucceeded` era SEMPRE 0 — o laço final exigia a interseção `taskRecovered ∩ taskFirstSuccess`, mas tasks completadas-após-retry são excluídas de `taskFirstSuccess` por design → interseção vazia → o AutonomyScore tratava tasks recuperadas como de primeira tentativa (1.0 em vez de 0.7), inflando a métrica de autonomia do quality gate. Fix: contagem via `taskCompleted` (tasks recuperadas E completadas). |
| **Confidence** | 0.95 (validado: teste que falhava antes do fix e passa depois; race limpo; suíte do consumidor — cli/gate — verde) |
| **Tags** | #cobertura #pipeline #fase-2 #analytics #reconciler #history #autonomy-score #bug-de-corretude #metricas #level-5 |
| **Related** | L312 (Fase 1: 18,7→32,7%), L311 (auditoria), P13 (verificação), gate.go (consumidor do AutonomyScore) |
| **Learned** | **(1) Métricas de autonomia são código de produção sensível: o bug `RecoveriesSucceeded=0` estava invisível porque ninguém testava a reconstrução por eventos — o AutonomyScore do quality gate estava inflado desde o início e nenhum teste pegava.** **(2) O padrão do reconciler (desired vs actual via ReplayPlan) é testável sem mocks complexos: basta um StepRunner com history real em temp dir + runner fake — acesso a campos privados do mesmo pacote evita refactor.** **(3) Contratos sutis descobertos: `Save→Load` do AnalyticsStore é PERDA (guarda só summary; reconstrução exige eventos de task), `PlanCompleted` zera total para done-only (plano com falha emite `PlanFailed`), e `Reconcile` retorna cedo SEM erro quando nada é corrigido no ciclo (o erro só aparece quando cada ciclo corrige ≥1 e o drift persiste).** **(4) Cobertura de storage/lógica pura (JSONL, pricing, limiares) rende pontos consistentes (~9 pts) com risco zero — o que resta (benchmark.go, general_context.go, plugins_builtin.go, subprocess.go) exige refactor de dependências ou é exec externo.** |
| **Next** | (1) Fase 3: benchmark.go (20 funcs), general_context.go (30 funcs), plugins_builtin.go (23 funcs) — exigem decisão sobre refactor vs testes de integração. (2) Depois CLI. (3) Se o Don quiser: corrigir o `TestAnalyticsStore` lossy documentado (fazer Save persistir campos estruturados). |
