PREV: 788402bca5054a2b1042f68c2659814af4efa77440345aebcf0e4fbb5835f35b
ID: L112
TIME: 2026-08-13
LEVEL: 2
TAGS: #api-rest #secao-24 #http #net-http #neural-link #level-2
---
## L112 — 2026-08-13 — Neural Link: API REST limpa (seção 24) — serve HTTP embutido no app Wails | Level 2

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "continue". Implementar a API REST limpa (seção 24) do Neural Link: `/experiments`, `/challenges`, `/hypotheses`, `/evidence`, `/agents`, `/knowledge`, `/memory`, `/discoveries`, `/verifications`, `/runs`, `/metrics`, `/audit` — todos com IDs, versionamento e proveniência. |
| **Technique** | Level 2 — **(1)** Serve HTTP embutido (`net/http` stdlib, sem dependência) em `http.go` (package main), iniciado no `startup` via goroutine. Porta `127.0.0.1:14124` (override `NEURAL_LINK_PORT`), alinhada à família de portas do Cosca (14120 serve, 14123 runtime). **(2)** Handlers reusam os DTOs já existentes do app (`CoscaStatus`, `LabStats`, `Graph`, `lastRun.Experiments/Results`) — a API REST é uma visão sobre o MESMO estado que o bind Wails expõe, sem duplicar dados. **(3)** `/metrics` agrega `Metrics` + `CognitiveScore` vetorial; `/audit` retorna eventos + checagem de integridade (`SafetyPolicy`). **(4)** Teste `http_test.go` no package main valida 200 em TODOS os 15 endpoints com um run real populando o estado. |
| **Level** | 2 |
| **Outcome** | success — 15 endpoints REST respondendo JSON; teste verde validando 200 em todos; build+vet limpos; commit `305ba1a`. |
| **Confidence** | 0.92 (validado: teste de endpoints, build/vet/testes) |
| **Tags** | #api-rest #secao-24 #http #net-http #neural-link #level-2 |
| **Related** | L196 (playback), L195 (integração CLI), README.md |
| **Learned** | **(1) Um app Wails (desktop) pode ter serve HTTP embutido — a API REST (seção 24) e o bind Wails são DUAS visões do mesmo estado, não fontes separadas; os handlers só reusam os DTOs existentes.** **(2) `net/http` stdlib cobre a API limpa sem gorilla/echo — para 15 endpoints de leitura JSON não há razão para dependência.** **(3) Porta 14124 segue a convenção da família (14120 serve, 14123 runtime) — coerência de endereçamento, não número arbitrário.** **(4) Teste de HTTP no package main funciona porque o embed `frontend/dist` existe após o build — `go test .` compila o binário embutido.** |
| **Next** | (1) React Flow (grafo interativo) — última peça da F5. (2) Rodar ablação --llm com budget/trials maiores para evidência estatística. (3) `go test ./...` já cobre 7 pacotes; considerar teste de contrato gRPC com o runtime real. |
