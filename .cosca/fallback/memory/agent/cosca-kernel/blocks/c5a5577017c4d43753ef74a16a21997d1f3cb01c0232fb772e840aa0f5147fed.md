PREV: 848740e704c0fc47380301d0e137870d567fcc6921a812174cfc91a6ceb2d6a0
ID: L99
TIME: 2026-08-13
LEVEL: 5
TAGS: #visual-media #cosca-media #rembg #u2net #pillow #svgo #sharp #poppler #raster-to-vector #tool #stack18 #level-5
---
## L99 — 2026-08-13 — Visual Media Engine EXECUTÁVEL: cosca-media — do conhecimento à ferramenta | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "procure padrões de código, conhecimento profundo sobre edição de imagem, analise profundamente os repositórios e adicione ferramentas únicas no cosca pra usar com sua capacidade". Transformar a doutrina do Stack 18 em ferramenta executável. |
| **Technique** | Level 5 — **(1)** Análise do ambiente: Pillow + poppler-utils disponíveis; sem sudo para apt → instalar via pip/npm isolados (~/.cosca/venv-media + scripts/node_modules). **(2)** Instalação real: `rembg[cpu]` (ONNX runtime) + Pillow no venv; svgo + sharp via npm local. **(3)** Construção do CLI único `cosca-media.py` com subcomandos: identify (formato por MAGIC header, nunca extensão), remove-bg (rembg/u2net), convert/resize (Pillow, formato-aware — RGBA→JPEG perde alpha conscientemente), pdf text/pages (pdftotext/pdftoppm), svg optimize (svgo) + svg to-png (sharp), vectorize (raster→vetor flat: compose em fundo branco → threshold → componentes conectados → SVG). **(4)** Testes reais: imagem gerada → remove-bg (modelo u2net 176MB baixado, 73% alpha) → convert WebP → resize → svg optimize/render → vectorize (círculo 203×203 correto) → validação Pillow. **(5)** Registro: binário global ~/.cosca/bin/cosca-media + skill `skills/visual-media/SKILL.md` + .gitignore para node_modules. Commit 627389a. |
| **Level** | 5 |
| **Outcome** | success — Ferramenta única do Cosca funcional e testada de ponta a ponta: detect → remove-bg → convert → resize → svg → vectorize. Commit 627389a. |
| **Confidence** | 0.95 (validado: todos os subcomandos testados com arquivos reais) |
| **Tags** | #visual-media #cosca-media #rembg #u2net #pillow #svgo #sharp #poppler #raster-to-vector #tool #stack18 #level-5 |
| **Related** | L209 (Stack 18 definitivo), L208 (draft), L205 (matriz) |
| **Learned** | **(1) Sem sudo, o caminho é ferramentas ISOLADAS: venv pip + node_modules local — rembg/ONNX funciona em CPU sem GPU.** **(2) rembg CLI exige extra `[cli]`; a API Python (`from rembg import remove`) funciona direto — usar API no wrapper.** **(3) sharp é biblioteca, não tem .bin — checar node_modules/sharp e `require` por caminho absoluto.** **(4) Detectar formato por MAGIC (header) é barato e correto — nunca confiar na extensão.** **(5) Conversão formato-aware: RGBA→JPEG perde alpha — avisar/decidir conscientemente (pilha do Stack 18).** **(6) Raster→vetor flat exige compor sobre fundo branco primeiro (transparência) e manter regiões ESCURAS (logo em fundo claro) — primeiro teste inverteu o fundo.** **(7) Disciplina confirmada: o Don abortou a instalação até o prompt definitivo chegar — conhecimento/planejamento primeiro, implementação depois.** |
| **Next** | (1) Extensões: OCR (tesseract), restauração/upscale (Real-ESRGAN), matting refinado (BiRefNet). (2) Integrar `cosca-media` aos agentes (skills) para uso em tarefas reais (tirar fundo do cliente HornFit, converter PDF). (3) Testar em imagens reais (foto de produto, retrato com cabelo). |
