PREV: e47ce347f1031b66d9e90c93e6629de0a73c5ca5b1d6ad98969e08b32012f290
ID: L170
TIME: 2026-08-14
LEVEL: 5
TAGS: #cosca-code #read-only #execguard #bloqueio #ordem-do-don #p14 #seguranca #level-5
---
## L170 — 2026-08-14 — cosca-code: READ-ONLY TOTAL na UI — execGuard bloqueia TODA execução (ordem do Don), leitura livre | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "bloqueia qualquer execucao por enquando somente leitura, e continue ta indo muito bem" — o Don quer a UI 100% read-only: nada de executar agentes/times/workflows/plan/test/save/engines. |
| **Technique** | Level 5 — **(1) VARREDURA (P13)**: grep revelou execuções espalhadas no frontend — PipelinePanel (agent/team/workflow run), Editor (save), InlineAI (inline), TD3D/Cinema/Scientific/CrossProduct/CoscaCanvas (engine runs). **(2) execGuard.ts**: um guard centralizado — isExecPath bloqueia POST para qualquer path de execução (lista explícita + fallback por sufixo /run /design /save /approve /reject /generate); guardedFetch retorna 403 "read-only" para execução e libera GET/leitura. **(3)** READONLY_MODE=true + aplicação nos 8 componentes: PipelinePanel mostra o bloqueio, Editor não salva, InlineAI só explica (não edita), engines passam pelo guard. **(4) TESTE LÓGICO**: node simulou isExecPath — agent/run bloqueado ✓, intel/symbols livre ✓, tree livre ✓, sci/design bloqueado ✓. **(5)** Chat (POST /api/ai/chat) mantido — não altera estado persistente, é leitura de resposta. |
| **Level** | 5 |
| **Outcome** | success — commit (read-only total). Build OK. A ordem do Don está cumprida: a UI não executa NADA; execução mostra "read-only bloqueado"; leitura (símbolos, tree, git, models, memory, skills) continua livre. Backend INTOCADO. |
| **Confidence** | 0.96 (validado: teste lógico do guard, build OK, varrdura completa dos POST, backend intacto) |
| **Tags** | #cosca-code #read-only #execguard #bloqueio #ordem-do-don #p14 #seguranca #level-5 |
| **Related** | L306 (Project Map), L305 (Control Room), L304 (Command Center), L303 (P14), L302 (Trust Center) |
| **Learned** | **(1) O guard centralizado é melhor que editar cada componente: isExecPath + guardedFetch em UM módulo cobre TODOS os endpoints de execução — e o fallback por sufixo (/run /save /design) pega futuros endpoints sem listar um a um.** **(2) A regra de leitura vs execução: GET livre, POST para path mutável bloqueado, chat mantido (não persiste) — o critério é "altera estado persistente?", não "é POST?".** **(3) Testar o guard com node (lógica pura) antes do build: a verificação da política de segurança merece prova, não suposição (P13).** **(4) O Don dirige a segurança da casa: a fase visual em read-only total protege o backend enquanto a UX amadurece — e a execução será liberada gradualmente (READ → SIMULATE → PREVIEW → APPROVAL → EXECUTE, do P14).** |
| **Next** | (1) Aguardar o "conhecimento bem legal" que o Don vai mostrar. (2) Fases 2-4 do workbench (floating, layouts, IA monta). (3) Endpoint de leitura de agentes para o pilar 5. |
