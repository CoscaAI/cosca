PREV: 2c69793d48dee15cd2ad5f899e9998271c15492447d73f32ecc8785051140b8f
ID: L90
TIME: 2026-08-13
LEVEL: 5
TAGS: #asset #content-addressable #registry #sha256 #dedup #non-destructive #derivatives #provenance #fase-1 #cosca-engine #level-5
---
## L90 — 2026-08-13 — Fase 1.2: Asset Registry content-addressable — nunca duplicar, nunca destruir | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuação do Cosca Creative Ecosystem (L218): etapa 1.2 do roadmap — Asset Registry unificado (imagem/vídeo/áudio/3D/fonte/texto/dados/modelo/material/script/documento) com content-addressable storage. |
| **Technique** | Level 5 — **(1)** Novo package `internal/asset`: `Asset` (ID=sha256 do conteúdo, Type, Hash, Size, Metadata, Dependencies, Version, Source, Preview, Derivatives, AddedAt) + `Registry` content-addressable em `<projeto>/.cosca/assets/` (objects/ por hash + derivatives/ + index.yaml). **(2)** Content-addressable = a identidade É o hash — o mesmo conteúdo nunca duplica (Add retorna o asset existente); edição NÃO-destrutiva (ORIGINAL + OPS do Stack 18): derivatives nunca tocam o original. **(3)** Dedup real: adicionar o mesmo arquivo 2x → mesmo ID, Count=1 (teste confirma). **(4)** Reuso: `crypto/sha256` do próprio filesystem; padrão YAML do projeto. **(5)** CLI `cosca asset add/list/info`: `--type` (11 tipos), `--source` (provenance §33/§35), info por prefixo de hash (min 12 chars, ambiguidade detectada), `--json`. **(6)** `asset` roda fora da jaula (como `project` — precisa ler arquivos arbitrários do usuário); findProjectRoot sobe até achar `.cosca/`. |
| **Level** | 5 |
| **Outcome** | success — Registry + CLI implementados, 8 testes core + 4 CLI verdes, esteira completa verde, testado ponta a ponta (add→dedup→list→info→json→disco). |
| **Confidence** | 0.95 (validado: dedup por identidade, persistência entre reopens, estrutura no disco conferida) |
| **Tags** | #asset #content-addressable #registry #sha256 #dedup #non-destructive #derivatives #provenance #fase-1 #cosca-engine #level-5 |
| **Related** | L218 (Fase 0 + 1.1), L210 (cosca-media — o derivado será gerado por ele), L207 (security — parsers sandbox) |
| **Learned** | **(1) Content-addressable storage (padrão git/IPFS) resolve o "nunca duplicar assets desnecessariamente" do §2 de forma natural: hash = identidade = dedup gratuito.** **(2) A separação objects/ (blobs imutáveis por hash) + derivatives/ (editáveis, derivados) + index.yaml materializa a edição NÃO-destrutiva: o original nunca é tocado, o derivado é um novo caminho vinculado ao hash.** **(3) O Source (provenance) é campo de primeira classe desde o dia 1 — §33/§35 exigem saber de onde cada asset veio.** **(4) CLI ergonômico: aceitar prefixo de hash (min 12) com detecção de ambiguidade — o usuário não digita 64 chars.** **(5) Comandos que leem arquivos arbitrários do usuário (asset add) devem rodar FORA da jaula — padrão do project.** **(6) findProjectRoot sobe de subdiretórios até achar .cosca/ — o asset pode ser adicionado de qualquer pasta do projeto.** |
| **Next** | (1) Etapa 1.3: AI Task Engine (18 tipos canônicos do manifesto §4). (2) Gerar derivatives reais (thumbnail/transcode) via cosca-media no próximo ciclo. (3) Integrar Asset Registry ao Project Manifest (assets: [ids] no project.yaml). |
