PREV: 06257ddd563d30d6a8faf9a3f1bc2c904cc51ba02decf9ee713bc9b1425dbc43
ID: L166
TIME: 2026-08-13
LEVEL: 4
TAGS: #cosca-trader #auditoria #fintech #decimal #event-sourcing #seguranca #pnl #level-4
---
## L166 — 2026-08-13 — cosca-trader: auditoria completa (Fintech + segurança + arquitetura) — 7 P0 encontrados | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "quero auditoria completa" após a atualização do cosca (Stack Catalog + Fintech #34). Auditar o cosca-trader contra a doutrina Fintech + segurança + arquitetura. |
| **Technique** | Level 4 — **(1)** Coleta de dados reais: `go test -race` (limpo), `go test -cover` (43.3% total; binance 24.8%, stream 0%), 3044 LOC. **(2)** 3 agentes `general` em PARALELO (auditoria independente): fintech (caminho do dinheiro), segurança/concorrência (mutex/secrets/signing), arquitetura/testes (camadas/event-sourcing/dead-code). **(3)** Cada agente leu a doutrina Fintech (`knowledge/fintech/README.md`) e todos os `.go`. **(4)** Síntese consolidada por prioridade P0/P1/P2. |
| **Level** | 4 |
| **Outcome** | success — 7 achados P0, ~8 P1, ~10 P2. Violações críticas de corretude (dinheiro float64, PnL zerado no reabrir, fee ignorado, fill não-idempotente) + resiliência (sem replay, sem reconciliação) + segurança (listenKey vaza). |
| **Confidence** | 0.9 (validado: 3 auditores independentes convergentes + dados reais de race/coverage; leitura de código com citação arquivo:linha) |
| **Tags** | #cosca-trader #auditoria #fintech #decimal #event-sourcing #seguranca #pnl #level-4 |
| **Related** | L263 (F2 OMS), L262 (F1), STACK-CATALOG.md, knowledge/fintech/README.md |
| **Learned** | **(1) A doutrina Fintech (#34) pegou a violação #1 UNIVERSAL do meu próprio código: "nunca float para dinheiro" — eu usei float64 em 29 pontos (domain+binance+SQLite REAL). A auditoria confirmou que a Binance manda preço como STRING justamente pra preservar precisão.** **(2) Bug de corretude real encontrado: `applyFill` zera `RealizedPnL` quando `Quantity==0` no reabrir símbolo (o map é indexado só por símbolo) → PnL realizado acumulado é APAGADO. O teste de flip não cobria reabertura pós-close.** **(3) Fill não-idempotente: `ApplyTrade` não deduplica por `Trade.ID` → retransmissão de executionReport dobra posição/PnL.** **(4) Security crítico: o `listenKey` (token de sessão do user stream) vaza no event store + SSE quando o dial falha (erro embute a URL → OnStatus → Emit → SQLite + /timeline + /events com CORS *).** **(5) O OMS não reconstrói estado após restart: `db.AllEvents()` existe mas nunca é chamado por produção — event sourcing SEM replay. E o user stream da Binance não faz replay pós-desconexão → deriva de estado sem reconciliação (Broker.OpenOrders/Balances estão mortos).** **(6) O padrão de auditoria "3 agentes paralelos + dados reais" (PATTERN-001) funcionou: os 3 auditores convergiram independentemente nos mesmos P0 (decimal, replay, listenKey).** |
| **Next** | (1) Fase corretiva P0-dinheiro: Decimal (shopspring) no caminho do dinheiro + fix PnL reset + fee no PnL + dedup de fill. (2) Fase P0-resiliência: replay do OMS + redação do listenKey + reconciliação REST. (3) Fase P1: double-entry ledger + validação + auth/CORS + testes de stream. |
