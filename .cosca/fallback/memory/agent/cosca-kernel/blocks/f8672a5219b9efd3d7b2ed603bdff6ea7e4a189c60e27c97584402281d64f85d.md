PREV: e34cfa2e0295fac467060d8f40478fec74f00d8cfed29a48d057ed1b374fab5a
ID: L37
TIME: 2026-08-11
LEVEL: 4
TAGS: #openai #evals #metrics #bootstrap #precision #recall #f1 #matthews #confidence #benchmark #level-4
---
## L37 — 2026-08-11 — Mining OpenAI evals: bootstrap confidence + 2-layer YAML + model-graded verification → internal/evals metrics (pure-Go stats) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don said "continue, carro ta ficando perfeito" — continued the mining queue. Next: OpenAI evals (19.1K stars, MIT, "framework for evaluating LLMs and LLM systems, open-source registry of benchmarks"). |
| **Technique** | Level 4 — **(1) metrics.py** — get_accuracy, get_bootstrap_accuracy_std (1000 samples bootstrap std), get_confusion_matrix, compute_matthew_corr, compute_precision/recall/f_score/averaged_f_score; higher_is_better flag per eval. **(2) YAML eval format in 2 layers** — `name:` with id (e.g. decrypt-caesar-cipher.dev.v0) + metrics, then `<id>:` with class (evals.elsuite.basic.match:Match) + args.samples_jsonl. **(3) samples JSONL** — N examples per eval, not one prompt. **(4) elsuite verification types** — basic.match, basic.includes, modelgraded (LLM as grader). **(5) modular solvers** (providers, nested, memory). **(6) record.py Event recording** — record_match with correct/expected/picked, record_sampling, record_function_call. Implementation: internal/evals/metrics.go — pure-Go stats (no numpy): ConfusionMatrix{TP,FP,TN,FN}, ComputeConfusionMatrix (positive = passed || reward>=0.5; every case is expected positive so FP/TN=0), Accuracy, Precision, Recall (0 on zero denom), FScore (1+β²)PR/(β²P+R), MatthewCorr (0 on degenerate), BootstrapStd (std of 1000 resampled means, math/rand/v2 PCG, fixed seed 20260811 for reproducibility). Report.Metrics EvalMetrics (JSON-tagged, old reports → zero). RunSuite computes after aggregateRewards. CLI summary renders: "Metrics: accuracy 0.67 | precision 1.00 | recall 0.67 | F1 0.80 | MCC 0.00 | ±0.47 (bootstrap, n=1000)". |
| **Level** | 4 |
| **Outcome** | success — 7 new tests PASS; go build/vet PASS; E2E smoke shows metrics line + JSON has metrics object; backward compat (legacy report → zero metrics). Note: pre-existing TestConfigValidateCommand_WithValidConfigFile fails only under broad -run filter — reproduces on unmodified baseline (parallel-test interference), passes in isolation. Chain 40 blocks valid. |
| **Confidence** | 0.96 (validado: 7 testes pass, build/vet limpos, E2E smoke com linha de métricas + JSON, backward compat, chain 40 blocos) |
| **Tags** | #openai #evals #metrics #bootstrap #precision #recall #f1 #matthews #confidence #benchmark #level-4 |
| **Related** | L195 (mining OpenAI codex), L194 (mining models.dev), L192 (mining harbor → evals patterns), L191 (eval harness) |
| **Learned** | **(1) Bootstrap confidence transforms a benchmark from "pass/fail" to a statistically solid measurement — the ±std answers "is this score reliable?"** **(2) OpenAI's 2-layer YAML (name→id versioned→class+samples) is a better format than our single-case prompts — versioned eval IDs (dev.v0) and N samples per eval.** **(3) Model-graded verification (LLM as grader) is the semantic-evaluation pattern we lack (our verify is binary commands).** **(4) The metrics are intentionally simple in OpenAI's impl — the value is the bootstrap std, not exotic math.** |
| **Next** | (1) Consider samples JSONL (N per case) + versioned eval IDs in cosca eval. (2) Consider model-graded verify. (3) tiktoken for real token counting. (4) Keep mining: codex-security, tunnel-client (Go MCP tunnel). | |
