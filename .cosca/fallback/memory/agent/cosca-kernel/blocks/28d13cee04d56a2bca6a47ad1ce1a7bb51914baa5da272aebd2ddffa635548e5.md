PREV: bfd8e4ecb95457b64b78f6ab0185df5be9401ad133d2966234067ae9f1b15b3f
ID: L59
TIME: 2026-08-12
LEVEL: 5
TAGS: #kernel-isolado #L2 #semantica #ollama #jaula #ia-interna #vulkan #fail-closed #level-5 #segurança #local-first
---
## L59 — 2026-08-12 — O CORPO DA JAULA: validação semântica L2 conectada ao Ollama local (qwen2.5-coder:14b via Vulkan RX 6700 XT) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Conectar a L2 semântica ao corpo da jaula: o Kernel isolado confirma a verdade do pensamento com IA interna local (Ollama), não com IA externa. |
| **Technique** | Level 5 — **(1)** Ollama 0.32.9 ressuscitado do zero: binário direto de ollama.com (tar.zst, 1.4GB), sem sudo, sem docker (ordem do Don), em ~/bin. **(2)** Segurança: OLLAMA_HOST=127.0.0.1:11434 (só localhost), ~/.ollama chmod 700, processo via setsid (desacoplado do shell — nohup sozinho morre com a sessão). **(3)** GPU AMD RX 6700 XT via Vulkan (12GB VRAM, modelo 14B cabe). **(4)** `internal/proposal/semantic.go`: OllamaValidator fala SÓ com /api/chat (nunca create/pull/push — proteção contra exfiltração via Modelfile), temperature 0 (determinismo), num_predict 40 (velocidade), prompt restrito a VALIDAR/REJEITAR+motivo. **(5)** Latência: 0.4-1.4s com modelo quente — dentro do timeout de 3s. **(6)** Fail-closed provado: jaula fora do ar → DENY; resposta ambígua → erro → DENY. **(7)** O corpo PEGOU a contradição real: "apagar o banco de produção para aumentar a segurança" → REJEITAR com motivo. |
| **Level** | 5 |
| **Outcome** | success — L2 semântica viva: `cosca propose --semantic` usa o corpo da jaula. Demo real: proposta coerente → APPROVE (1.1s); proposta incoerente → REVIEW com motivo da jaula; jaula offline → DENY fail-closed. 4 testes semânticos verdes (incluindo integração real com Ollama). Suite completa do pacote verde. |
| **Confidence** | 0.98 (validado: integração real com GPU + 4 testes semânticos + demos CLI) |
| **Tags** | #kernel-isolado #L2 #semantica #ollama #jaula #ia-interna #vulkan #fail-closed #level-5 #segurança #local-first |
| **Related** | L215 (Kernel isolado), L170 (ollama local-first), L162 (ressuscitação ollama), L187 (forense), internal/proposal/semantic.go |
| **Learned** | **(1) O corpo da jaula = IA interna local, isolada, que confirma a verdade do pensamento — comunicação mínima: VALIDAR/REJEITAR.** **(2) Segurança do Ollama local: só 127.0.0.1, ~/.ollama 700, endpoints perigosos (create/pull/push) jamais expostos — o /api/create lê arquivos do usuário (exfiltração de segredos).** **(3) Determinismo: temperature 0 — julgamento não pode variar por sorte.** **(4) Latência real: modelo quente na VRAM = 0.4s; primeira carga = ~20s (aceitável, uma vez).** **(5) setsid desacopla o processo do shell — nohup sozinho morre com a sessão.** **(6) A jaula PEGA a alma mentirosa: contradição ação×motivo é detectada pelo corpo — a alma pode ser sequestrada, o corpo não.** |
| **Next** | (1) Persistir o AuditLog em .cosca (SQLite) — forense durável. (2) Integrar o Kernel isolado ao pipeline de orquestração (toda execução passa pela validação). (3) Script de boot do Ollama (systemd user ou entrypoint do projeto) para o corpo nascer com a jaula. (4) Canários: perguntas com resposta conhecida no batch de validação para detectar alma sequestrada. |
