PREV: 81aa6984f781b41e835d19889d7b6e530cba66ff9f5ab6e0db476e2b8111633f
ID: L151
TIME: 2026-08-13
LEVEL: 4
TAGS: #cosca-code #cosca-studio #ide #ai-native #pesquisa #arquitetura #fase-1 #rope #lsp #gopls #level-4
---
## L151 — 2026-08-13 — COSCA CODE Fase 1: pesquisa (21 repos) → arquitetura original → 7 engines de pé | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: construir o COSCA CODE (codename COSCA STUDIO) — spec de 100 seções, "plataforma de engenharia de software AI-native" (editor + IA + agentes + debugger + terminal + git + tests + project intelligence + memory + models + providers + tools + workflows). Ordem explícita: "Comece pela pesquisa. Não implemente imediatamente. Primeiro o mapa de conhecimento, depois a arquitetura, depois implemente por fases e valide cada fase." |
| **Technique** | Level 4 — **(1) Pesquisa em 5 frentes paralelas (subagents) + 1 feita à mão**: editor/IDE (vscode, monaco, theia, zed, lapce), AI agents (opencode, codex, claude-code, cline, continue, roo-code), MCP (modelcontextprotocol + servers + inspector), code intelligence (tree-sitter, ast-grep, ripgrep, go-tree-sitter), debugging (vscode-js-debug, debugpy, DAP). Cada frente extraiu padrões no formato SOURCE→WHAT/HOW/WHY/STRENGTHS/WEAKNESSES/PATTERN/COSCA INTERPRETATION/ORIGINAL IMPLEMENTATION. **(2)** 12 padrões transversais extraídos (sintaxe×semântica, core headless + clientes finos, rope imutável, Document/View/Provider, workspace por RPC, extensibilidade dual, tool=contrato declarativo, allow/ask/deny + sandbox, contexto orçado, edição por diff reversível, broker/adaptador LSP/DAP, event bus nativo). **(3)** Arquitetura original: core Go headless orientado a eventos + 16 engines, UI (React/Vite/Wails) como cliente fino via RPC. **(4)** Fase 1 em 7 commits incrementais: Event Bus → Editor (rope) → Workspace → Frontend → Git → LSP → Terminal. **(5)** O Don corrigiu o prompt no meio (mandou COSCA NEURAL LINK "errado", depois COSCA CODE) — respeitei a ordem de pesquisa-primeiro. |
| **Level** | 4 |
| **Outcome** | success — Fase 1 completa: 7 engines backend de pé (Event Bus, Editor rope+undo, Workspace com detecção, Git, LSP cliente, Terminal PTY) + frontend (workbench + explorer + editor CodeMirror 6). ~25 testes verdes, gofmt/vet limpos. Prova real (não "funciona na minha máquina"): LSP end-to-end com gopls v0.23.0 (hover+definition+diagnostics), terminal echo em PTY, git contra o próprio repo, rope estresse 1000 edições × 2,35M runes. Projeto em ~/Documents/projects/cosca-code (git local, sem remote ainda). |
| **Confidence** | 0.92 (validado: 25 testes verdes, LSP real, PTY real, git real, rope estresse) |
| **Tags** | #cosca-code #cosca-studio #ide #ai-native #pesquisa #arquitetura #fase-1 #rope #lsp #gopls #level-4 |
| **Related** | L192 (Neural Link F0 — mesmo padrão de projeto independente), L195 (contrato CLI/internal), L247 (COSCA NODE) |
| **Learned** | **(1) Persistência estrutural em rope é traiçoeira: as rotações AVL mutavam nós COMPARTILHADOS e corrompiam o undo silenciosamente (teste de estresse pegou). A regra de ouro: rotações devem ser COPY-ON-WRITE (criam nós novos), nunca mutar o que é compartilhado — é o que separa um buffer imutável correto de um bug que só aparece em escala.** **(2) O padrão "pesquisa primeiro, implementa depois" (imposto pelo Don) evitou re-trabalho: os 12 padrões extraídos viraram decisões de arquitetura diretas (core headless, rope, LSP/DAP como contrato, event bus).** **(3) LSP não se reimplementa: um cliente JSON-RPC/stdio de ~200 linhas (framing Content-Length + request/notify + dispatch) já entrega hover/definition/diagnostics reais via gopls. O valor está no contrato, não no parser.** **(4) Detecção de projeto por "arquivos marcadores" (go.mod→go, package.json→js, etc.) é robusta e zero-config — a UI não precisa configurar nada para o Cosca saber o que é o projeto.** **(5) Delegar pesquisa a subagents em paralelo acelera muito, mas UM subagent travou (pkg.go.dev) — refiz à mão com webfetch direto no GitHub (fonte primária). Subagents precisam de instruções de "não acesse domínio X".** |
| **Next** | (1) Costura da Fase 1 no frontend: terminal via WebSocket + painel na UI, LSP hover/definition/diagnostics no CodeMirror, painel Git (diff inline/stage/commit). (2) Wails desktop (empacotar o frontend no binário Go, como o cosca-neural-link L192). (3) Fase 2 (spec §92): Search, Index, Project Intelligence, Debugger, Tests, Problems, Output. |
