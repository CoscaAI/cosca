PREV: 1a6152ea5798ab9597ddf5b3637819b17b9e496c0cf4132ad8d7bb205d133455
ID: L67
TIME: 2026-08-13
LEVEL: 5
TAGS: #diffusion #stable-diffusion #sd15 #rocm #gfx1030 #rx6700xt #image-generation #torch #diffusers #geracao-ia #level-5
---
## L67 — 2026-08-13 — GERAÇÃO DE IMAGEM POR IA: SD 1.5 + ROCm na RX 6700 XT — diffusion no editor (§7) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "eu queria geracao de video/imagem por ia diffusion" — instalar diffusion e integrar a geração de imagem ao editor. |
| **Technique** | Level 5 — **(1)** Instalação coordenada com o Don (L211): torch 2.13.0+rocm7.2 (cp312, o wheel certo para gfx1030/RDNA2) + diffusers 0.39 + transformers + accelerate no venv-media; SD 1.5 baixado (36 arquivos). **(2)** PROVA da GPU: `torch.cuda.is_available()=True`, RX 6700 XT 12.9GB, matmul OK. **(3)** `internal/diffexec` no cosca-code: executor de `image_generation` (task que já existia no aitask!) — Go orquestra, generate.py (venv) processa na GPU; cache por assinatura (§23). **(4)** Endpoint `POST /api/engine/image/generate` + xexec roteia para diffexec (§30). **(5)** E2E: "heroi espacial em traje de corrida futurista" → PNG 512x512 real em **15.3s** (primeiro 104s — modelo frio, depois cache). |
| **Level** | 5 |
| **Outcome** | success — geração de imagem por IA funcionando na GPU da família: prompt → PNG real. 4 testes diffexec verdes, esteira 34 packages OK. |
| **Confidence** | 0.95 (validado: 2 imagens geradas (neon + heroi), PNGs reais conferidos por file) |
| **Tags** | #diffusion #stable-diffusion #sd15 #rocm #gfx1030 #rx6700xt #image-generation #torch #diffusers #geracao-ia #level-5 |
| **Related** | L222 (GPU probe — o scheduler apontava para local-gpu, agora executa), L220 (aitask — image_generation já era task), L229 (Fase 3 imagem — agora com geração real), L211 (regra de libs — o Don aprovou) |
| **Learned** | **(1) O wheel certo muda tudo: torch 2.13.0+rocm7.2 roda na gfx1030 (RDNA2) — verificar `download.pytorch.org/whl/rocm7.2/` antes de instalar.** **(2) O padrão Go-orquestra/Python-processa (L211) escala para diffusion: o executor Go chama o generate.py via subprocesso com a GPU — sem importar torch no Go.** **(3) A task `image_generation` do aitask (L220) e o scheduler local-gpu (L222) já previam isto — a arquitetura estava pronta, só faltava o executor.** **(4) Primeira geração é lenta (modelo frio ~104s); depois ~15s (cache do modelo na RAM/VRAM).** **(5) SD 1.5 em fp16 cabe na 12.9GB confortável; attention slicing para segurança. |** |
| **Next** | (1) Botão de geração no painel Image do editor (UI). (2) Video generation (texto→vídeo) — mais pesado, avaliar modelo. (3) Registro da geração na provenance (§33: model/prompt/seed). (4) Upscale da imagem gerada (Real-ESRGAN) como nó seguinte do grafo. |
