PREV: 20f22dcfb15627c1c092c94dc76a253addaddc69e1896052dde0b958581f59f3
ID: L23
TIME: 2026-08-10
LEVEL: 3
TAGS: #monitoramento #frontend #live-debug #puppeteer #react-child #ratelimit #throttle #billing-path #equipamentos #faturamento #e2e #level-3
---
## L23 — 2026-08-10 — HornFit: monitoração live do frontend + 3 bugs reais corrigidos (React child, rate-limit global, path billing) | Level 3

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "monitora o logs do frontend, ja captura e vai consertando enquanto vou navegando". Criar um vigia automático do frontend em loop e corrigir o que aparecer. |
| **Technique** | Level 3 — **(1)** Script vigia (puppeteer headless) que loga no app, varre 15 páginas em ciclo de ~40s, captura pageerror/console error/requestfailed/overflow-X com timestamps em /tmp/opencode/frontend-errors.log, pausa 20s entre ciclos, roda em daemon via `(setsid node ... &)`. **(2)** Análise do log revelou 3 bugs reais: (a) PAGEERROR "Objects are not valid as a React child (found: object with keys {name})" em /equipamentos — API retorna category/location como objeto `{name}` mas o front tipava string e renderizava direto; (b) 429 em /orcamentos/new — ThrottlerModule GLOBAL 10req/60s estourava com navegação normal (16 páginas × 3-6 chamadas API); (c) 404 em /faturamento — front chamava GET /invoices, API expõe /billing/invoices. **(3)** Fixes: normalização `typeof x === "string" ? x : x?.name` em equipment-list/detail/form + tipos atualizados (category: string | {name}); throttle global 10→300/60s mantendo @Throttle 5/60s só no auth; paths /invoices→/billing/invoices e /charges→/billing/charges. **(4)** Distinguir artefatos do monitor (ERR_ABORTED de navegação rápida, ERR_CONNECTION_REFUSED durante rebuild da API, rota fantasma /relatorios sem página) de bugs reais. |
| **Level** | 3 |
| **Outcome** | success — Ciclo limpo: 16 páginas, 0 erros reais (o Σ=1 restante é artefato do login do próprio monitor). Verificação dedicada: /equipamentos 7 rows sem erro, /faturamento 1 row sem erro, /orcamentos/new sem 429, /estoque 3 rows. 12 chamadas API rápidas: 100% 200. Login segue rígido (4×401→429). Typecheck web 0 erros. API rebuildada e reiniciada (dist antigo tinha causado 404s silenciosos — conferir timestamp dist vs migration). |
| **Confidence** | 0.95 (validado: ciclo monitor limpo + verificação dedicada das páginas quebradas + API 200) |
| **Tags** | #monitoramento #frontend #live-debug #puppeteer #react-child #ratelimit #throttle #billing-path #equipamentos #faturamento #e2e #level-3 |
| **Related** | L177 (settings enterprise + header), L176 (redesign), L175 (CSS 404), sessão 2026-08-10 HornFit |
| **Learned** | **(1) Monitoração live = script headless em loop com timestamps + daemon setsid; padrão comprovado: logar no app, varrer as 15 páginas-chave, capturar pageerror/console/requestfailed, reportar diffs por página.** **(2) "Objects are not valid as a React child (found: object with keys {name})" = API retornando relação aninhada {name} onde o front tipou string — padrão comum em Prisma include; fix canônico: tipo `string | {name}` + helper `typeof x === "string" ? x : x?.name`.** **(3) Rate limit GLOBAL de API é armadilha: 10req/60s estoura com navegação normal de app rico (cada página = 3-6 chamadas). Regra: limite global folgado (300/60s = anti-abuso) + @Throttle estrito SÓ em auth (5/60s).** **(4) Path mismatch front/API: endpoints sob @Controller('billing') exigem /billing/... — auditoria de paths é item obrigatório.** **(5) Artefatos de monitor vs bugs reais: ERR_ABORTED (navegação rápida cancela requests anteriores), ERR_CONNECTION_REFUSED (janela de rebuild da API), rota fantasma sem página — NÃO são bugs; o sinal real é pageerror + console 4xx/5xx + requestfailed com URL da API.** |
| **Next** | (1) Vigia fica rodando em background até o Don encerrar a sessão de navegação. (2) Commit do trabalho no git (pendente, aguardando ordem). (3) Rotação de refresh token. (4) Paginação nas listagens da API. |
