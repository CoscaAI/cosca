PREV: 57eafd64cb3a11f5953de4cccb286798fac0886b0694a971293910fe4579dfac
ID: L406
TIME: 2026-08-17
LEVEL: 5
TAGS: #auditoria #agents #incidente #crash-loop #apparmor #userns #serve #jaula #sysctl #boot #chain #dna #memory #skills-catalog #level-5
---
## L406 — 2026-08-17 — Auditoria geral dos agents + incidente: crash-loop do serve pós-reboot (apparmor userns) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Auditoria geral dos agents + diagnóstico de crash-loop do serve |
| **Technique** | AUDIT_PROTOCOL (6 fases), isolamento de variáveis (env vs jaula), reprodução manual do bwrap, journalctl --list-boots, P13 em cada conclusão |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.97 |
| **Tags** | #auditoria #agents #crash-loop #apparmor #userns #serve #jaula #sysctl #chain #dna |
| **Related** | L288, L290, L296, L362, L364, L376, L399, L57, L254, L62, L218 |
| **Learned** | 1) **INCIDENTE P1 (crash-loop do serve)**: reboot da máquina às 19:40 (boot -1→0) subiu kernel 7.0.0-28 com `kernel.apparmor_restrict_unprivileged_userns=1` (Ubuntu) → bwrap falha "setting up uid map: Permission denied" / "loopback: Failed RTM_NEWADDR" → auto-jail fail-closed → serve exit 1 mudo (stderr engolido pela jaula, L62). O fix já existia no código (bwrapFailureHint, jail.go:334): `sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0` + persistir em /etc/sysctl.d/99-cosca-userns.conf. Aplicado pelo Don → serve UP na jaula, HTTP 200. 2) **Diagnóstico por isolamento**: TESTE A (COSCA_JAILED=1, sem serve.env) sobe; TESTE B (auto-jail + serve.env) falha → problema é a jaula, não envs/binário. 3) **A reinstalação do binário às 19:42 (outra sessão) foi inócua** — md5 idêntico ao ./bin/cosca; o crash começou 19:40 ANTES dela. 4) **Estado real dos agents**: 55 agents (INDEX+PROMPT), 55 memórias, 55 capability-profiles, 661 blocks, 249 learnings nos agents não-kernel (formato `###`), 404 no kernel (`## L`). 5) **DIVERGÊNCIA DE FORMATO**: agents usam `### data` / `## L1-SEED` / `## TASK` vs kernel `## LXXX | data |...` — 4 agents (critic, memory-chief, monitoring, paradigm) com blocks mas sem gatilhos indexados no padrão. 6) **AGENT_DNA defasado**: 55/55 PROMPT.md SEM os 28 campos (0 com ROLE/MISSION/QUALITY GATES); o contrato real virou frontmatter mínimo (agent/type/version/description) — o DNA v3.0 é letra morta ou a implementação divergiu. 7) **chain.dat por agente ausente**: 54/55 sem ledger próprio (só o kernel tem); a family chain cobre o embed inteiro (mitigação), mas a camada "memory blockchain" do MEMORY_ACCESS §4 não existe para 54 agents. 8) **SKILLS_CATALOG desatualizado**: 89 arquivos .md reais vs 30 linhas no catálogo (cobertura ~34%), 0 links quebrados. 9) Vuln GO-2026-5932 = falso positivo conhecido (L296, openpgp deprecated, Cosca usa bcrypt). |
| **Next** | Correções aprovadas pelo Don (se/quando ordenadas): padronizar formato learnings dos agents; sync SKILLS_CATALOG com os 89 arquivos; decidir se chain.dat por agente é necessário (ou documentar que family chain é a camada única); rever AGENT_DNA (atualizar para o formato PROMPT.md real ou fazer os PROMPT.md cumprirem os 28 campos); monitorar latência serve pós-51k vetores. |
