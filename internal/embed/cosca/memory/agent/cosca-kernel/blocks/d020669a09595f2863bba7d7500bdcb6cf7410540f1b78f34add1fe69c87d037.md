PREV: 5d228ee42acfa6ec931978a178c4667529040d31741dcb08498f4595e6562399
ID: L292
TIME: 2026-08-16
LEVEL: 4
TAGS: #mineracao #vercel-ai #provider #model #registry #openai-compatible #llm #integração #padroes #level-4
---
## L292 — 2026-08-16 — Mineração vercel/ai: Provider/Model/Registry + openai-compatible | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ordenou minerar o vercel/ai ANTES de integrar providers/modelos no header do cosca-trader ("tem bastante coisa pra integrar modelos providers"). |
| **Technique** | Level 4 — **(1)** Minerou os pacotes core do vercel/ai: `packages/provider` (abstração) + `packages/ai` (registry) + `packages/openai-compatible` (wrapper genérico). **(2)** Extraiu 5 padrões: ProviderV4 (fábrica por modalidade: `languageModel(id)`/`embeddingModel(id)`, lança NoSuchModelError), LanguageModelV4 (interface MÍNIMA: `specificationVersion` + `provider` + `modelId` + `doGenerate`/`doStream`), createProviderRegistry (resolve `"provider:modelo"` → modelo + middleware), metadados de capacidade (usage: tokens in/out, cache, reasoning), openai-compatible (um cliente pra QUALQUER API compatível com OpenAI). **(3)** Aplicou no cosca-trader: reescreveu `internal/cognitive/` com Provider/Model/Registry e um único `openAICompat` servindo ollama + openai + deepseek. |
| **Level** | 4 |
| **Outcome** | success — cognitive refatorado (o antigo tinha 2 clientes separados ollama/openai; agora é 1 openAICompat), endpoints /models + /models/active com latência medida. |
| **Confidence** | 0.95 (validado: build + vet + testes verdes, código aplicado no cosca-trader) |
| **Tags** | #mineracao #vercel-ai #provider #model #registry #openai-compatible #llm #integração #padroes #level-4 |
| **Related** | L35 (cosca models), L38 (Agent Skills spec), L248 (mineração de prompt patterns), L88 (Model Registry), cosca-trader internal/cognitive |
| **Learned** | **(1) A abstração de Provider é uma FÁBRICA por modalidade: `languageModel(id)`, `embeddingModel(id)`, `imageModel(id)` — cada uma devolve a interface certa ou lança NoSuchModelError (fail-closed).** **(2) A identidade de um modelo = `provider + modelId` (a string "provider:modelo"); interfaces VERSIONADAS (v2/v3/v4) permitem evoluir sem quebrar.** **(3) OLLAMA, OPENAI E DEEPSEEK FALAM O MESMO PROTOCOLO (/chat/completions) — um único cliente openai-compatible serve os três, só mudando base URL + API key. Elimina código duplicado.** **(4) O registry resolve strings "provider:modelo" e aplica middleware em TODOS os modelos de uma vez (transform/wrap).** |
| **Next** | (1) Usar o padrão Provider/Model/Registry em TODO projeto que integrar LLMs. (2) Considerar middleware de modelo (rate-limit, retry, telemetria) no registry do trader. |
