PREV: 065aae65832a09e2bbe2fb01cf9bf44dddaf64875d47801f8da7271f11cabe7b
ID: L211
TIME: 2026-08-14
LEVEL: 5
TAGS: #esteira #flakiness #finalizer-gc #fd-reuse #auditoria #seguranca #idor #sandbox #tar-plugin #heartbeat #race #ledger #fail-open #patrón-001 #level-5
---
## L211 — 2026-08-14 — Esteira 100% verde + AUDITORIA GERAL: 10 bugs P0/P1 com evidência — o loop da família | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "resolve tudo e depois faca uma auditoria atras de bugs em todo o projeto com evidencia". Duas frentes: (1) esteira do pkg/cosca estava VERMELHA por flakiness; (2) auditoria de bugs no projeto inteiro. |
| **Technique** | Level 5 — **(1) ESTEIRA**: diagnósticou a flakiness empiricamente (nunca teoria): `go test -count=N` → isolado passa, paralelo falha; GOMAXPROCS=2 → 4 falhas vs default → 8; `-parallel=1` → 0. CAUSA RAIZ: **finalizer do GC fechando fd reutilizado** — `createMemfd` retornava `os.NewFile(uintptr(fd))` e o cleanup fazia `unix.Close(fd)`; quando o GC rodava o finalizer, fechava o fd que o SO JÁ tinha reatribuído (socket HTTP de outro teste) → EBADF/EOF/refused espalhados. Fix: `runtime.SetFinalizer(f, nil)` + cleanup via `f.Close()` (idempotente). Ainda: retry de POST quebrado (body consumido — `req.GetBody()` por tentativa) e porta efêmera reciclada pelo SO sob carga (porta fixa 20000-24999 fora do range efêmero 32768+). **(2) AUDITORIA**: 3 agentes general em PARALELO (PATTERN-001) — segurança/concorrência/corretude — + verificação de 1ª mão de cada achado + staticcheck como evidência de máquina. Resultado: 10 bugs P0/P1 corrigidos (IDOR gRPC na memória, escape de sandbox GlobTool `..`, symlink para dir bloqueado, tar plugin symlink absoluto/hardlink, SDK heartbeat double-close+morto, TUI send-on-closed-channel, races HotReload/Watcher, ledger crash window, auth fail-open). |
| **Level** | 5 |
| **Outcome** | success — esteira 123 packages OK/0 FAIL (5×count=10 = 13.250 execuções zero falhas), 2 commits (3a24a61 esteira + 8dd5df7 auditoria), 19 arquivos, 459 inserções, 10 bugs corrigidos com testes de regressão (ledger, plugin tar, rails). |
| **Confidence** | 0.95 (validado: -race limpo, go vet limpo, estática + 3 auditores independentes convergentes) |
| **Tags** | #esteira #flakiness #finalizer-gc #fd-reuse #auditoria #seguranca #idor #sandbox #tar-plugin #heartbeat #race #ledger #fail-open #patrón-001 #level-5 |
| **Related** | L265 (auditoria trader — o mesmo PATTERN-001), L264 (padrão de auditoria), L246 (rebuild — o bug do memfd já era o mesmo padrão de fd), P12 (nunca pkill) |
| **Learned** | **(1) O finalizer do GC é uma armadilha silenciosa: `os.NewFile(uintptr(fd))` + `unix.Close` manual = o GC fecha o fd DEPOIS de reatribuído (double-close via finalizer corrompe I/O alheio) — SEMPRE `SetFinalizer(f,nil)` + cleanup idempotente.** **(2) Flakiness de teste exige isolamento empírico: `-count=N` + GOMAXPROCS + `-parallel` + `-skip` para isolar a causa raiz — nunca adivinhar; a porta efêmera reciclada sob carga é o flake clássico de httptest paralelo (fix: porta fixa fora do range efêmero).** **(3) O padrão PATTERN-001 (3 auditores paralelos + evidência arquivo:linha + verificação de 1ª mão + estática) é o método de auditoria da casa — convergência independente confirma achados (IDOR foi pego por 2 auditores).** **(4) Fail-open de auth é duplamente perigoso: sem middleware + HMAC aceita chave vazia — SEMPRE aplicar o middleware e rejeitar no middleware.** **(5) Symlinks e tar são as superfícies clássicas de escape: o check "Join + prefix" engana (absoluto vira relativo) — validar o caminho RESOLVIDO e o Linkname CRU.** **(6) O loop completo funciona: esteira vermelha → auditoria → fixes → esteira verde → memória → a família fica mais forte a cada ciclo. |** |
| **Next** | (1) P2 da auditoria (IDs fracos randomHex sleep-1ns, telemetry goroutine leak, signal.Notify leak no jail, API keys sempre admin, metrics secret logado, websocket drop) quando o Don aprovar. (2) Adicionar testes de encadeamento diffusion→remove_bg (L245). (3) Subir `cosca session index`. |
