PREV: 8f97a6db3cd9444b0d6727033920aa654e0a75e8e0b64d193eb4ffc4e080301f
ID: L36
TIME: 2026-08-11
LEVEL: 4
TAGS: #openai #codex #execpolicy #hardening #security #policy-engine #process-hardening #seccomp #mining #apache-2.0 #level-4
---
## L36 — 2026-08-11 — Mining OpenAI (268 repos): codex execpolicy + process-hardening → internal/execpolicy + internal/hardening | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ordered mining https://github.com/openai (268 repos). Analysis identified the gold: codex (105K stars, Rust, Apache-2.0, "lightweight coding agent"), codex-security (9.6K), evals (19.1K), tiktoken (19K), tunnel-client (Go), whisper (107K). Don: "sim, verifica tudo" → deep-mined the codex (91MB, 112 Rust crates). |
| **Technique** | Level 4 — **(1) execpolicy** — declarative command policy engine with prefix rules, decision=allow/prompt/forbidden, match/not_match examples as embedded unit tests, basename fallback for host executables. **(2) process-hardening** — pre_main_hardening(): RLIMIT_CORE=0, disable ptrace (PR_SET_DUMPABLE), remove dangerous env (LD_PRELOAD, DYLD_*). **(3) linux-sandbox refinements** — bwrap read-only by default (--ro-bind / /), .git re-applied as read-only even in writable roots, seccomp network filter + PR_SET_NO_NEW_PRIVS, bundled bwrap fallback. **(4) apply-patch** — streaming patch parser (streaming_parser.rs, seek_sequence.rs, file_update.rs). **(5)** skills/memories/context-fragments as separate modules (validates our architecture). **(6) AGENTS.md** — engineering standards (modules <500 LoC, RPITIT traits, no codex-core bloat). Implementation (2 patterns, Don: "sim"): (1) internal/execpolicy/ — Policy engine: Rule{Pattern [][]string with alternatives per position, Decision, Justification, Match/NotMatch validated on Load}, Load/LoadYAML, Evaluate (first-match wins, prefix matching blocks git reset --hard --force too, decision defaults allow), ResolveHostExecutables basename fallback, Tokenize shell-like. YAML format. Integrated into internal/chat/tool/shell.go via variadic NewShellTool(ws, sb, opts...): forbidden → error with justification, prompt → warn-log + run, no policy → unchanged (non-breaking). config exec_policy (optional). (2) internal/hardening/ — PreMain(): RLIMIT_CORE=0, PR_SET_DUMPABLE=0, PR_SET_NO_NEW_PRIVS=1 (unix) + RemoveDangerousEnv (LD_PRELOAD, LD_LIBRARY_PATH, DYLD_*); wired into cmd/cosca/main.go + cmd/cosca-chat/main.go at top of main(). hardening_unix.go / hardening_other.go (platform fallback). |
| **Level** | 4 |
| **Outcome** | success — 14 tests (execpolicy 8 required cases + shell_policy tests) PASS; go build/vet PASS; E2E: git reset --hard → forbidden, ls -l → allow, cp → prompt, /usr/bin/git reset --hard → forbidden (basename), echo → allow; hardening E2E: RLIMIT_CORE 0/0, PR_GET_DUMPABLE 0, PR_GET_NO_NEW_PRIVS 1, LD_PRELOAD unset. Chain 37 blocks valid. jail.go/integrity untouched. bwrap auto-jail reexec compatible with PR_SET_NO_NEW_PRIVS (bwrap non-setuid). |
| **Confidence** | 0.96 (validado: 14 testes pass, build/vet limpos, E2E execpolicy 5 casos + hardening 4 checks, chain 37 blocos) |
| **Tags** | #openai #codex #execpolicy #hardening #security #policy-engine #process-hardening #seccomp #mining #apache-2.0 #level-4 |
| **Related** | L194 (mining models.dev), L193 (mining lore → BLAKE3), L192 (mining harbor → evals patterns), L191 (eval harness), L174 (auditoria enterprise) |
| **Learned** | **(1) The execpolicy match/not_match examples-as-tests pattern is brilliant — policies are self-testing at load time.** **(2) PR_SET_NO_NEW_PRIVS is cheap and high-value hardening (blocks privilege escalation via exec).** **(3) Non-breaking integration (no policy = current behavior) is the safe pattern for security upgrades.** **(4) The codex confirms our sandbox direction (bwrap) but starts read-only by default — a future refinement for our jail (currently workspace rw).** **(5) Mining methodology validated again: clone → read crates → extract design → implement natively.** |
| **Next** | (1) Consider the read-only-default sandbox refinement (codex does --ro-bind / /). (2) Wire execpolicy "prompt" decisions into the TUI approval flow (currently warn+run). (3) tiktoken for real token counting. (4) Keep mining: evals (complement cosca eval), codex-security.
