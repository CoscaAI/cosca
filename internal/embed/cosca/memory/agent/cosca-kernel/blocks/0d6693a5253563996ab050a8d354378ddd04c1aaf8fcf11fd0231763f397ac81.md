PREV: 38e1d24362ca602debab887257f93d217bc89a0696c6dc1baefda318b8b3eda9
ID: L114
TIME: 2026-08-13
LEVEL: 4
TAGS: #neural-link #ablacao #metacognicao #llmsolver #contrato-cli #internal-package #integracao #nao-duplicar #level-4
---
## L114 — 2026-08-13 — Neural Link × ablação do Cosca: descoberta do LLMSolver existente + integração via contrato CLI | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "já fiz commit, continue e commit sempre". Antes de "plugar o LLM" no Neural Link, descobri (via `git log` do workspace) que o Cosca JÁ tem o sujeito e o instrumento: `internal/evals/llm_solver.go` (LLMSolver + Proposer desacoplado), `internal/evals/ablation.go` (harness alone vs managed com estatística mediana/IQR) e o CLI `cosca eval ablation` (`--llm`, `--trials`, `--budget`, `--json`). A decisão passou de "reimplementar solver LLM" para "integrar o Neural Link ao experimento real via contrato". |
| **Technique** | Level 4 — **(1)** Forense de contexto: `git log --oneline` revelou os commits `c0eb403` (LLMSolver), `85291e1` (ablação), `714815b` (HYPOTHESIS/CANDIDATE estruturado). **(2)** Verificação empírica: `./bin/cosca eval ablation` (demo determinístico) → "alone 0% solved (H1→H1→H1), managed 100% (42 tentativas), managed won"; `--json` devolve o `evals.Compare` completo (alone/managed com trials+attempts). **(3)** Descoberta da barreira Go: `internal/` de um módulo NÃO é importável por outro módulo — o Neural Link (módulo separado) não pode importar `internal/evals` mesmo com `replace`. **(4)** Decisão: contrato CLI — `contracts.RunAblation` executa `cosca eval ablation --json` como subprocess e parseia; resolução do binário por `COSCA_BIN` → `~/.cosca/bin/cosca` → `./bin/cosca` → PATH; timeout 120s com kill do processo. **(5)** Estruturas de parse espelham o JSON do Cosca (nomes de campo capitalizados por serem marshals default do Go; `Elapsed`/`TimeToSolve` como int64 nanossegundos → ms no DTO). **(6)** Teste de integração roda o demo real do Cosca e valida `managed_wins`. |
| **Level** | 4 |
| **Outcome** | success — Neural Link agora observa o Cosca de verdade: `RunAblation` (demo + `--llm`) exposto no app.go + seção "Metacognition Ablation" no frontend. Teste de integração verde (invoca `cosca eval ablation` real, managed vence). ADR-006 registra a decisão. Build+vet+testes limpos, commit `557ebe4`. Aprendizado: o movimento certo diante de sobreposição é INTEGRAR via contrato, não duplicar. |
| **Confidence** | 0.95 (validado: comando real do Cosca, teste de integração, build/vet/testes verdes, JSON parseado) |
| **Tags** | #neural-link #ablacao #metacognicao #llmsolver #contrato-cli #internal-package #integracao #nao-duplicar #level-4 |
| **Related** | L194 (F6), L193 (F2-F4), commits c0eb403/85291e1/714815b, ABLATION_PROTOCOL.md |
| **Learned** | **(1) Antes de "plugar X", verificar se X já existe no ecossistema — `git log` do workspace revelou LLMSolver+ablação prontos; reimplementar seria duplicação (violaria o spec).** **(2) Barreira Go: `internal/` não atravessa fronteira de módulo — um módulo desacoplado NUNCA importa `internal/` de outro, mesmo com `replace`; o contrato (CLI/gRPC/HTTP) é a única via limpa.** **(3) Subprocess + `--json` é um contrato CLI de primeira classe: o protocolo (markdown) é compartilhável, o código não; resolve a integração sem tocar no kernel.** **(4) O harness de ablação do Cosca é mais geral que o PolynomialSolver do Neural Link: candidato string + oracle string (não função float64) — a ablação mede metacognição (memória alone vs managed), o Neural Link fornece o laboratório (UI, epistemic states, graph, provenance). São camadas complementares, não rivais.** **(5) Marshal JSON default do Go capitaliza os campos (`Solved`, `AttemptCount`); o parse do lado consumidor deve usar as MESMAS tags — e `time.Duration` vira int64 nanossegundos.** **(6) "commit sempre" = disciplina: cada incremento funcional vira um commit com mensagem descritiva, sem esperar o fim da sessão.** |
| **Next** | (1) Rodar a ablação `--llm` real (via Ollama) e observar o resultado no dashboard. (2) F5: API REST + React Flow (grafo interativo) + playback temporal dos attempts da ablação (seção 23 — o JSON já traz Elapsed por attempt). (3) Plugar solver LLM próprio no Neural Link SE for necessário além do contrato CLI (avaliar). (4) Human baseline comparando solução LLM vs humana na ablação. |
