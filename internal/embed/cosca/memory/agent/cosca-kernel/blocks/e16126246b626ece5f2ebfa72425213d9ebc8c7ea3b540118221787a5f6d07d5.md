PREV: 1b4df3b0367af4a139737bc7cbe2d717106981c87a705661125cab43054ae768
ID: L308
TIME: 2026-08-16
LEVEL: 5
TAGS: #mineracao #node-graph #comfyui #blender #bevy #geonodes #depsgraph #dirty-flags #ecs #lazy #fields #paralelismo #level-5
---
## L308 — 2026-08-16 — MINERAÇÃO Node Graph/Procedural/ECS: ComfyUI + Blender + Bevy + geonodes — como executar grafos, lazy, dirty e paralelo | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Mineração de 4 projetos-referência de execução de grafos/ECS para melhorar o node graph do Cosca |
| **Technique** | Arqueologia arquitetural → grafo é dado puro; execução derivada, lazy, cacheada e paralelizável pelas arestas |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.90 |
| **Tags** | #mineracao #node-graph #comfyui #blender #bevy #geonodes #depsgraph #dirty-flags #ecs #lazy #fields #paralelismo #level-5 |
| **Related** | L86, L306, L307, L305 |
| **Learned** | (1) ComfyUI: grafo = serialização pura, execução derivada (já é nosso P1); TOPOLOGICAL DISSOLVE incrementável (blockCount/blocking permite re-adicionar nós staged — essencial p/ lazy inputs); cache com write-back no touch; LAZY INPUTS por declaração de tipo (nó declara inputs opcionais, scheduler não bloqueia neles — short-circuit de custo altíssimo); nós efêmeros (grafo dinâmico em runtime); erro de ciclo com NÓ CULPADO reportável; scheduling orientado a UX (outputs/async primeiro); big outputs por referência + previews streamados. (2) Blender: GEOMETRY NODES com 3 categorias (data-flow/function/input); FIELDS = funções lazy com contexto (field não é avaliado até um data-flow node consumi-lo; avaliado contra (geometria, domínio)); Capture Attribute = materializar+cachear; DEPSGRAPH — dirty flags propagados por relations (mudou upstream → tag dirty propaga → só re-avalia o tagged → reuso do avaliado) — a máquina de "não recomputar o que não mudou"; instancing compartilhando dados (Object = transform + referência, não o dado); baking (congelar subárvores caras); profiling por nó. (3) Bevy ECS: o DAG é a ordem — arestas = únicos constraints (paralelismo derivado só das dependências); conflito detectado por ACESSO DECLARADO (read/write sets — dois sistemas só rodam juntos se disjuntos); archetypes/contiguidade (data locality); deferred commands com sync points; change detection por tick; Extract/Prepare/Queue (separar custo do core da borda). Números: sprite 8k→100k, GLTF 40s→0.2s. (4) geonodes: builder DSL TIPADO por socket (classe-por-socket + chaining — segurança em analyze-time); inputs por NOME (robusto a reordenação); context scopes para subgrafos; naming canônico determinístico. |
| **Next** | (1) A1: dirty tracking no executor (hoje Run chama Signature para TODO nó, recursivo O(depth) — com dirty: só recomputa assinaturas cujos ancestrais mudaram; cache hit vira O(1)). (2) C1: executor PARALELO — DAG já é o schedule; worker pool + ready-set (blockCount) em vez de loop serial; determinismo no desempate (P13). (3) C2: conflito por acesso declarado (read/write sets em Go, verificação runtime). (4) B1/B2/B3: validar tipo contra registry, erro de ciclo com nó culpado, proteger recursão da assinatura. (5) A3: lazy inputs no Executor (ConsumedInputs). (6) D4/D3: dirty propagation no scene graph + instancing compartilhando computação. (7) E1: builder tipado no SDK. NÃO copiar: monolito Python do ComfyUI, Depsgraph completo do Blender, unsafe do Bevy, APIs geradas gigantes do geonodes. |
