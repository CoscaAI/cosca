PREV: 3fabd3a39907864c4e13aee0751ad5a5e413a269880a0ef9f7303a83e3dea21c
ID: L85
TIME: 2026-08-13
LEVEL: 5
TAGS: #render-engine #quality #preview #draft #final #deterministic #resumable #checkpoint #crash-recovery #job-key #seed #fase-1 #cosca-engine #level-5
---
## L85 — 2026-08-13 — Fase 1.7: Render Engine — deterministic, cacheable, resumable (§22/§40) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuação do Cosca Creative Ecosystem (L218-223): etapa 1.7 — Render Engine: renderização DETERMINISTIC · CACHEABLE · RESUMABLE · PARALLEL · OBSERVABLE com QUALIDADE preview/draft/final (§22) e crash recovery (§40). |
| **Technique** | Level 5 — **(1)** Novo package `internal/render` que ESTENDE o nodegraph (1.6): `Quality` (Preview < Draft < Final com Rank/AtLeast), `Job` (Graph + Quality + Seed + Cache + CheckpointPath) e `JobKey` (sha256 do grafo JSON + qualidade + seed — identidade canônica do render). **(2)** `Renderer.Render`: executor wrapper injeta RenderParams (quality+seed) via context — DETERMINISMO: mesmo seed + mesmo grafo = mesmo resultado (§22). **(3)** Cache por assinatura do nó reusado — não re-renderiza o inalterado. **(4)** RESUMIBILIDADE (§40): checkpoint JSON em disco (job_key + completed[nodeID→sig] + results[sig→valor]); na retomada o cache é reconstruído do checkpoint → nós completados viram `resumed` e NUNCA re-executam. Provado por teste: crash no 2º nó → retomada com 0 re-execuções; e ao vivo: 2º render mesmo seed → Executed=0, Resumed=6, Hits=6. **(5)** CLI `cosca render <workflow.json> [--quality] [--seed] [--cache] [--checkpoint] [--json]`. **(6)** Observable: Result com Executed/CachedHits/Resumed/DurationMs. |
| **Level** | 5 |
| **Outcome** | success — Render Engine implementado, 8 testes verdes, CLI E2E (draft→final, checkpoint resume ao vivo com 6/6 resumed, JSON), esteira completa verde. |
| **Confidence** | 0.95 (validado: determinismo via JobKey estável, cache hit total na 2ª execução, resume com 0 re-execuções testado e ao vivo) |
| **Tags** | #render-engine #quality #preview #draft #final #deterministic #resumable #checkpoint #crash-recovery #job-key #seed #fase-1 #cosca-engine #level-5 |
| **Related** | L223 (nodegraph — o render ESTENDE o grafo), L222 (scheduler — executor decidirá onde cada nó roda), L221 (modelreg), L220 (aitask) |
| **Learned** | **(1) O Render Engine não é um sistema separado — é o NODE GRAPH com contrato de qualidade, determinismo e resumibilidade em cima. Reuso é a jogada (Regra 38).** **(2) Determinismo exige identidade canônica: JobKey = hash(grafo + qualidade + seed) — qualquer mudança em qualquer um invalida o render.** **(3) Checkpoint = estado derivado do cache: completados[node→sig] + results[sig→valor] permite reconstruir o cache na retomada sem re-executar nada — crash recovery real (§40).** **(4) Qualidade é contrato de primeira classe (preview < draft < final) — o executor lê do context e decide o esforço (resolução/passos).** **(5) 2ª execução com mesmo seed + checkpoint = 0 trabalho — isso é o "não re-renderizar partes inalteradas" do §22 em ação.** |
| **Next** | (1) Etapa 1.8: Media Engine estendida (vídeo via ffmpeg pipeline + áudio + 3D import). (2) Etapa 1.9: Durable Execution (workflow resumível via replay + activities com retry — padrão Temporal). (3) Executores REAIS: ligar render+ngraph a tasks/modelreg/scheduler. (4) Etapa 1.10: Provenance + Licenças (§32-35). |
