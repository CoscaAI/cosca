# L353 — Implementacao do filtro T1-T4 (prevencao, comportamento-futuro)

## O que foi implementado (aprovado pelo Don com travas)
- internal/chunker/trivial.go: IsTrivial(content) (bool, Rule) — regras
  T1 vazio / T2 separador / T3 zero-alfanumerico / T4 estrutura vazia
  (mesmas do dry-run L352, 0 FP estruturais). NENHUMA regra usa comprimento.
- TrivialFilter: contadores atomicos filtered_total, filtered_t1..t4,
  by_source (section_type), by_document (document_id) — apenas contagens,
  NUNCA conteudo sensivel.
- Chunker.FilterTrivial (config, default TRUE): aplicado na saida do
  ChunkDocument — comportamento-FUTURO apenas. Nada historico tocado.
- Chunker.TrivialStats() para observabilidade.

## Travas respeitadas
- Nada apagado · sem dedup · sem re-embed (48.621 orfaos intocados) · sem
  correcao do chunker JSON · oracle/scoreParallel/int8/int16 intocados ·
  contagens absolutas nao comparadas entre fases sem snapshot (db MUTANTE:
  14.006->14.119 vetores — EVIDENCE de instabilidade do instrumento).

## Validacao
- Unit IsTrivial: 28 casos (removidos + preservados: "Go 1.26" "v1.2.3"
  "RAG" "API" "SQL" "404" "C++17" "--- # Introducao" '"scripts": {').
- ChunkDocument filtra + contadores OK; flag desligada preserva o
  comportamento antigo (reversibilidade testada).
- Teste existente TestChunkDocument_EmptyAST atualizado: doc vazio com
  filtro ativo -> 0 chunks (o chunk vazio e o lixo T1); com filtro
  desligado -> 1 chunk (contrato antigo preservado).
- Repro L354 (TestChunkerJSONFragmentsRepro): JSON como texto puro grande
  -> 61 chunks, 2 triviais filtrados — documenta o problema do chunker
  (JSON-as-text fragmentado; 251 fragmentos de 1 char no corpus real,
  section_type=text) para investigacao futura. NAO corrigido.

## Medidas
- build ./... OK · vet OK · chunker OK · knowledge OK · vector OK
- dry-run re-executavel (campaign_dryrun_test.go, L352) intocado

## Classificacao
MEASURED filtro implementado e testado; 251 fragmentos JSON text no corpus
EVIDENCE 0 FP no dry-run; doc vazio agora gera 0 chunks (comportamento novo)
INFERRED chunks novos triviais nao entram mais no corpus
HYPOTHESIS reducao do lixo futuro reduz a taxa de duplicacao ao longo do tempo
UNKNOWN causa exata dos fragmentos JSON de 1 char (L354); impacto no NDCG
DECISION filtro ATIVO por default (reversivel); implementacao para aqui —
         proxima fase (L354) = investigar chunker JSON, somente apos
         autorizacao do Don.
