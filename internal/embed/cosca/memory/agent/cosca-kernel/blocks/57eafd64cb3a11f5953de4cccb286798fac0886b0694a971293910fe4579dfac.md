# L405 — @PROJECT cosca-code executado via registry (tarefa 4, PASS)

## status: DECISION (ordem do Don: "@PROJECT cosca-code" — após erro de digitação)

## Execução (contrato @PROJECT v1, read-only)
1. ORDEM: "@PROJECT cosca-code" (3 tok) — resolução determinística via registry
2. SEQUÊNCIA: o Don digitou "@PROJCT code" → ERROR: UNKNOWN_COMMAND (sem
   chute, PROMPT_PROTOCOL §5) → sugestões do resolver → confirmação do Don
3. PROJECT_REPORT:
   - manifest: type editor, v0.1.0, updated 2026-08-16T22:15Z
   - isolamento: .cosca/project.yaml próprio (135B) — sem chain da família
   - repo: 9e7c5b7 (doutrina ADR-008/009/010 + undo/redo + cosca doctor)
   - protocolo: ATIVO (escopo isolado)
4. TASK_SUCCESS: PASS
5. AMBIGUIDADE: 1 (o erro @PROJCT — RESOLVIDA por confirmação do Don, não crítica)
6. EVIDENCIA: project manifest + ls .cosca/ + git log

## Lição registrada
O resolver NÃO chutou: erro explícito + sugestões + confirmação. O custo da
ambiguidade foi 2 tokens extras (o erro + a confirmação) — o custo de um chute
errado seria uma execução fora do escopo. Determinismo paga.

## Classificacao
MEASURED 3 tok vs ~60 natural (95% com erro+confirmação)
EVIDENCE PROJECT_REPORT emitido e registrado na campanha (tarefa 4)
INFERRED o contrato @PROJECT cobre o fluxo completo (erro → sugestão → execução)
UNKNOWN fidelidade nas próximas tarefas
DECISION campanha ativa; PARAR só na conclusão ou ordem do Don
