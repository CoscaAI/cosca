# L362 — int16 PROMOVIDO a default de producao (decisao do Don)

## O que mudou
- SQLiteVecConfig.EnableInt16 -> DisableInt16 (default false = int16 ATIVO,
  consistente com DisableInt8). Selecao: int16 > int8 > float32.
- Testes que esperavam o caminho exato (oracle float32) atualizados:
  idxStore/idxConcurrentStore/StoreNormalized desligam int16 explicitamente
  (SetInt16Enabled(false) / DisableInt16: true).
- Binario recompilado e instalado (~/.cosca/bin/cosca), serve reiniciado
  (graceful, kill -TERM). Doctor 26/26 ✓.

## Validacao
- Suite vector verde (44.8s); busca real via CLI funcionando (10 resultados).
- TestDefaultIsInt16: store com config padrao -> int16Enabled = true
  (default ATIVO confirmado empiricamente).

## Estado de producao
O Cosca busca com int16 (jaccard 0.989 / recall@10 0.995 no corpus limpo,
L361) — quase-oracle — a ~26 Mvec/s (2.18x float32). O int8 fica como
fallback (0.934) e o float32 como oracle exato (DisableInt16/SetInt16Enabled).

## Classificacao
MEASURED default int16 ativo; doctor 26/26; suite verde
EVIDENCE int16 jaccard 0.989 no indice real (L361); busca CLI funcional
INFERRED scores de producao agora sao int16 (aproximacao quase-exata);
         RAM do snapshot +2B/elem (int16 14.9MB no corpus de 9.7k)
HYPOTHESIS qualidade percebida do RAG melhora (recall ~0.995 vs 0.954)
UNKNOWN impacto de longo prazo (a monitorar)
DECISION int16 = DEFAULT de producao (reversivel via DisableInt16 /
         SetInt16Enabled(false)); int8 fallback; float32 oracle. Proximas
         decisoes em aberto: re-embed (provider real), determinismo do
         top-K (tie-breaker), governanca (L358).
