PREV: 9ef4e3f37e064b7f5f13e90bd0670bde493f742f3d66ebf5656e7a91988d7b57
ID: L42
TIME: 2026-08-11
LEVEL: 4
TAGS: #terminal #operations-mode #treeview #fabric #background-execution #concurrency #animations #trace #real-time #command-center #level-4
---
## L42 — 2026-08-11 — Enterprise Command Center: Operations Mode (TUI em tempo real + chat não-bloqueante + Fabric wired) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don asked for the cosca terminal to be an enterprise command center — monitor everything in real time, right panel showing the real execution tree (who's talking to whom, clickable, real-time), and the ability to CHAT WHILE WORKING (no blocking). He also asked for interactive animations and requested an audit of the Fabric computer. "tem animacoes interativas?" → "ok, gostei pode fazer!" |
| **Technique** | Level 4 — Audit findings (this session): **(1)** The interactive TUI was a single-turn LLM shell — planner/stepRunner/recoveryLoop were stored in the Model constructor (model.go:76-81) but NEVER used; the real esteira (planner→stepRunner→recovery→DoD) ran only in headless mode; Tasks panel always empty; HUD progress/tokens/cost FABRICATED (0.1→0.3→0.7 constants); palette rendered below the fold (Ctrl+P invisible); diff viewer dead code; permission prompts dead; syntax highlighting (chroma) never attached; welcome said "Phase 1"; emoji chat prefixes; wrong key hints; "ugly" = symptom of "empty". **(2)** The Compute Fabric (internal/compute, 535 lines) EXISTS with the perfect API — Submit/FanOut/SubmitGPU, 5 worker pools (agent/tool/index/io/sandbox), circuit breaker + rate limiter + memory budget, hardware probe — but was NOT connected to the terminal or runtime. **(3)** trace.Event{TraceID, ParentEvent, Actor, Action, Timestamp, Result, Details} + CausalGraph already exist — the data source for the execution tree (ParentEvent IS the tree structure). Implementation (Operations Mode): **(1)** treeview.go — interactive TreeView (pure lipgloss): TreeNode{ID,Label,Detail,Status}, SetRoot/Upsert/Find(DFS)/Flatten(respects expanded)/MoveUp/Down/ToggleExpand/Selected, ANSI-width-aware Render with ▸/▾ toggle, status glyphs (⠿ running/✓ done/✗ failed/○ pending/● idle), th.Selection highlight, ... truncation, detail footer. **(2)** operations.go — OperationsPanel: AddEvent feeds a real trace-event ledger, BuildTreeFromEvents nests by ParentEvent (grouping by TraceID under run roots), Render = "EXECUÇÃO" header + event/active counts + animated progress bar + tree + detail footer. **(3)** animations.go — SpinnerByOp (agent=Globe, tool=Pulse, build=Meter, test=Moon, index=Points), StatusDot(status,frame) 4-frame animation, AnimatedProgress with pulsing leading edge. **(4)** trace_mapping.go — MapPipelineEvent maps pipeline.RunEventType (tool_start/build_start/error/done...) + high-level IDs to canonical actions (PLAN_CREATED/TASK_STARTED/TOOL_STARTED/BUILD_STARTED/TEST_STARTED/EXECUTION_DONE/EXECUTION_FAILED). **(5)** model.go integration: operations panel added (Ctrl+O toggles, also Tab/4), frame ticker (100ms, only while busy/background — zero CPU idle), EVERY event → operations.AddEvent + hud.Progress now REAL step counts, INPUT NEVER BLURS (the Don's #1 requirement — runPipelineStream spawns goroutine, returns immediately; new prompts QUEUE while busy and auto-run when pipelineDoneMsg fires), Fabric wired (terminal.go creates compute.NewFabric + Start, passes via ModelConfig.Fabric; fabric.Submit to agent pool with goroutine fallback), palette fixed (centered overlay, viewport shrinks, NOT below fold), welcome upgraded (ASCII COSCA + shortcut grid + system status row, no more "Phase 1"). Panel switching allowed while busy (removed !m.busy guards). |
| **Level** | 4 |
| **Outcome** | success — Validation: 23 new tests PASS (treeview/operations/animations/trace_mapping/model incl input-stays-focused test with mock runner); go build/vet PASS; make install OK; E2E under real PTY: terminal launches and stays up (EXIT 124 on 5s timeout = running normally), stderr 0 bytes clean. Chain 55 blocks valid. |
| **Confidence** | 0.95 (validado: 23 testes novos, build/vet limpos, E2E PTY sem stderr, chain 55 blocos) |
| **Tags** | #terminal #operations-mode #treeview #fabric #background-execution #concurrency #animations #trace #real-time #command-center #level-4 |
| **Related** | L200 (terminal fora da jaula — OpenCode mode), L199 (mining adk-go), L186 (chain git-anchored), L174 (auditoria enterprise), L168 (trace OTel-aligned) |
| **Learned** | **(1) The Don's core need is CONVERSATIONAL CONCURRENCY — typing while the agent works. The fix is architectural: never blur the input, queue prompts, run work in goroutines/fabric.** **(2) "Ugly" TUI was a symptom of empty data — wiring real events into the tree/HUD fills the screen with life; polish follows data.** **(3) The audit pattern (read every file, trace the flow, find dead code) is what revealed that the esteira existed but wasn't connected — the code was 90% there, only glue missing.** **(4) Fabric was built but orphaned — a common fate of infrastructure built ahead of its consumer; connecting it to the terminal activates its purpose (background pools, FanOut parallelism).** **(5) trace.Event with ParentEvent is literally a tree structure — the flight recorder was always meant to drive a live UI.** |
| **Next** | (1) Wire permission prompts (RenderPermissionPrompt exists, not wired). (2) Render the diff viewer (DiffPanelView dead code). (3) Wire cost/tokens from costTracker into HUD. (4) Fix pre-existing ModelRouter nil-provider panic in headless --task. (5) Make the operations tree clickable via mouse (bubbletea mouse support). | |
