PREV: 3beff2a3f1fbfcafb1edefb3740a2e017dbd6706053c1a412e22fea2d757a145
ID: L207
TIME: 2026-08-14
LEVEL: 4
TAGS: #makefile #auditoria #p12 #pkill-proibido #pid-exato #quality-gates #checkup #metafora-do-carro #padrao-cosca #godoc-orfao #coverdir #level-4
---
## L207 — 2026-08-14 — Auditoria do Makefile: P12 violada (pkill) + 3 bugs + padrão Cosca | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "agora quero que revise e audite o make fale pra ficar padrao cosca ok" — auditar o Makefile contra o padrão da casa e corrigir. |
| **Technique** | Level 4 — **(1) VIOLAÇÃO DE LEI P12**: o `restart` usava `pkill -f "$(BINARY) serve"` — proibido pela Constituição (P12, criada pelo Don em 2026-08-13 após pkill travar a sessão). O Makefile violava a lei silenciosamente. Fix: encerra pelo PID exato identificado pela porta (`ss -tlnp :14120` → pid=N → kill), com fallback `pgrep -a` (mostra antes) + kill por PID. Zero pkill. **(2) BUG**: `test-unit` gravava coverprofile em `build/coverage` sem criar o diretório (falha se ausente) — `mkdir $(TESTDIR) $(COVERDIR)`. **(3) BUG**: `docs` deixava godoc em background ÓRFÃO (processo vazado, o padrão do L230) — agora guarda PID em `docs/.godoc.pid` + target `docs-stop` (kill por PID). **(4) BUG**: `LDFLAGS_CHAT` usado antes de definido (lazy-eval frágil) — movido ao topo. **(5) PADRÃO**: `.PHONY` consolidado; `serve` com fallback para binário local; novos targets `make checkup` (metáfora do carro: build+vet+test-unit+dep-doctor) e `make qgate` (quality gates G2-G3: fmt→vet→lint→contract→unit(race)→prova sem-IA). |
| **Level** | 4 |
| **Outcome** | success — Makefile auditado (96 linhas alteradas), P12 restaurada, 3 bugs corrigidos, 2 targets do padrão Cosca adicionados, build+vet verdes, help atualizado. |
| **Confidence** | 0.95 (validado: make build/vet/checkup/qgate OK, zero pkill confirmado por grep, restart em dry-run mostra PID exato) |
| **Tags** | #makefile #auditoria #p12 #pkill-proibido #pid-exato #quality-gates #checkup #metafora-do-carro #padrao-cosca #godoc-orfao #coverdir #level-4 |
| **Related** | P12 (a lei que o Makefile violava), L228 (P12 criada), L230 (processo órfão — godoc tinha o mesmo padrão), L268 (metáfora do carro → target checkup), QUALITY_GATES.md |
| **Learned** | **(1) Até o Makefile pode violar a lei: pkill estava no restart desde antes da P12 existir — leis novas precisam de varredura de conformidade nos artefatos antigos (makefile, scripts, CI).** **(2) O padrão de matar por PORTAm é mais preciso que por nome: ss -tlnp :porta → PID exato, no espírito do P12.** **(3) Processo em background sem PID guardado = vazamento: godoc tinha o mesmo padrão do next-server (L230) — guardar PID em arquivo e criar target de parada.** **(4) O padrão Cosca vira target: checkup (carro) e qgate (portão G2-G3) são a casa materializada no Makefile — quem roda make checkup cuida do carro sem pensar. |** |
| **Next** | (1) Varredura de conformidade P12 em todos os scripts/CI (grep pkill em scripts/ e .github/). (2) Adicionar qgate ao CI (gate de release real). (3) make checkup como ritual de entrada de sessão. |
