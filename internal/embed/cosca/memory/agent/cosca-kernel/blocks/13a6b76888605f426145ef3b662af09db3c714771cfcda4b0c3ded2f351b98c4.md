PREV: 5bb0cef74f5669eed32c680e796e1ee2572460e1009f0892a8bb4a74d7984b4a
ID: L184
TIME: 2026-08-14
LEVEL: 5
TAGS: #cosca-trader #grafico #historico #candles #endpoint #klines #sse #level-5
---
## L184 — 2026-08-14 — cosca-trader: gráfico com HISTÓRICO real — endpoint /candles + frontend carrega 300 velas ao abrir | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "ta sem historico no grafico" — o gráfico do painel só desenhava candles que chegavam AO VIVO pelo SSE; ao abrir, a tela estava vazia (sem histórico). |
| **Technique** | Level 5 — **(1) DIAGNÓSTICO**: o frontend alimentava o gráfico SÓ via SSE (CandleClosed ao vivo) — zero histórico inicial. **(2)** Endpoint novo `/candles` no serve.go: busca da Binance via Klines público (sem chave), com fallback para o rastro persistido; query symbol/interval/bars (max 1000). **(3)** Frontend: método candles() no api.ts + efeito dedicado que carrega 300 velas de 1h UMA VEZ ao montar/mudar token (não no polling de 3s — a Binance é consultada 1x; o SSE alimenta por cima). **(4)** Proxy /candles no vite.config. **(5)** Prova real: /candles devolveu 300 velas reais (02/08→14/08, último close 63004.48), via proxy HTTP 200. |
| **Level** | 5 |
| **Outcome** | success — commit. Build OK. O gráfico agora tem histórico real ao abrir + candles ao vivo por cima. |
| **Confidence** | 0.97 (validado: /candles com dados reais da Binance, proxy 200, build OK) |
| **Tags** | #cosca-trader #grafico #historico #candles #endpoint #klines #sse #level-5 |
| **Related** | L292 (bug do painel), L291 (porta 5273), L289 (frontend completo) |
| **Learned** | **(1) Gráfico vazio na abertura é UX quebrada: o SSE é para o AO VIVO, o histórico precisa de um endpoint próprio — o painel carrega 300 velas e o SSE incrementa.** **(2) Separar o carregamento do histórico (1x) do polling das tabelas (3s): buscar candles a cada refresh seria bater na Binance 20x/min — desperdício e risco de rate limit (a preocupação recorrente do Don).** **(3) Reutilizar o Klines público (já validado na L280/L286) para o gráfico — zero código novo de coleta, só o endpoint.** |
| **Next** | (1) Seletor de símbolo/intervalo no gráfico (hoje fixo BTCUSDT 1h). (2) Conectar divergência ao OMS. (3) Testnet. |
