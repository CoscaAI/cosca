PREV: 5818627ad3231af588645f2a867e63a60184e333752e9e5268c53e13d3b6292c
ID: L48
TIME: 2026-08-12
LEVEL: 4
TAGS: #google #skills #agent-plugins #plugin #marketplace #mcp #mcp-servers #plugin.json #marketplace.json #codeload #mining #ecosystem #level-4
---
## L48 — 2026-08-12 — Mining Google skills parte 2: Agent Plugins Spec v1.0.0 + marketplace.json → cosca skill plugin/marketplace (plugin = skills + MCP servers como unidade) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don: "continue https://github.com/skills" (google/skills). Continuar a mineração do repo após L204 (canal GitHub). O ouro restante: o repo distribui plugins como UNIDADE (skills + MCP servers) via Agent Plugins Spec v1.0.0 (agentplugins/agent-plugins-spec, 918 stars) e marketplace.json (Claude Code/Codex format: `plugins[]` com source github/git-subdir + ref pinado). plugins/cloud/data-agent-kit/ usa git submodules pinados (alloydb@0.2.0 etc.). skills.sh cataloga 118 skills da Google com contagens de instalação (gke-basics 9.1K, cloud-run-basics 11.8K). |
| **Technique** | Level 4 — **(1)** `internal/skills/plugins.go` — Plugin = `plugin.json` (obrigatório: name slug; opcionais: version/description/repository/license) + `skills/*/SKILL.md` (Agent Skills standard) + opcional `mcp.json` (mcpServers: stdio{command,args,env,cwd} | streamable-http{url,headers} | sse{url,headers} — validado por tipo, stdio exige command). `InstallPlugin(source, allowRemote)`: local dir OU GitHub ref (reusa downloadAndExtractRepo/singleTopLevelDir do install_remote.go); `locatePluginRoot` procura plugin.json em .claude-plugin/ OU .codex-plugin/ OU root (deviation real: os plugins google colocam plugin.json em .claude-plugin/ mas skills/ na raiz — payload base = raiz); persiste em `coscaDir/plugins/<name>/` (copyPluginDir pula .git/.github/README/evals/test), instala cada skill via installStandardSkill existente (containment resolveWithinRoot), grava mcp.json. Registry em memória + `loadPluginsFromDir` rehidrata do disco no NewManager (deviation: sem isso, plugin list/info vazio em processo novo). `RemovePlugin` remove dir (resolveWithinRoot) + skills via SkillNames. **(2)** `internal/skills/marketplace.go` — Marketplace = `{name, plugins[{name, description, source{source: github|git-subdir, repo, ref, url, path}}]}` (encoding/json). `ListMarketplace(ref, allowRemote)` baixa o repo e procura marketplace.json em 3 locais (.claude-plugin/, .codex-plugin/, raiz). `InstallPluginFromMarketplace(ref, pluginName, allowRemote)`: github → `InstallPlugin(owner/repo[@ref])`; git-subdir → `InstallPlugin(url/path[@ref])`; "all" instala todos. **(3)** CLI — `cosca skill plugin {list,info,install,remove}` + `cosca skill marketplace {list,install}` (--allow-remote fail-closed igual skill install). **(4)** E2E REAL com rede: marketplace list google/skills → 16 plugins; marketplace install google/skills google-cloud-storage → plugin v1.0.0 com skill gcs-security-assessment; plugin info/list/remove validados. |
| **Level** | 4 |
| **Outcome** | success — 29 novos testes PASS (-race): plugins_test.go 17 (parsePluginManifest, parseMCPConfig stdio/http/erros, InstallPlugin local dir E2E com mcp.json, traversal/symlink reject, RemovePlugin, allow-remote fail-closed) + marketplace_test.go 12 (parseMarketplace github/git-subdir, loadMarketplaceFromDir local, InstallPluginFromMarketplace via Marketplace construído — sem rede). CLI: 16 testes novos (plugin install no-args, install local, remove nonexistent, marketplace remote requer --allow-remote). Total suíte skills+chat: 137 PASS. Build + vet limpos. E2E real: google/skills marketplace listado (16 plugins), plugin google-cloud-storage instalado via marketplace (v1.0.0, skill gcs-security-assessment), list/info/remove OK. `git-subdir` db-context-engineering: repo contém symlink (dev-plugin/plugin/CLAUDE.md) rejeitado pelo extractTarGz fail-closed existente — comportamento correto, não bug. |
| **Confidence** | 0.96 (validado: 137 testes -race, E2E real marketplace→install→info→remove, build+vet limpos) |
| **Tags** | #google #skills #agent-plugins #plugin #marketplace #mcp #mcp-servers #plugin.json #marketplace.json #codeload #mining #ecosystem #level-4 |
| **Related** | L204 (canal GitHub skills), L203 (AWS durable), L199 (mining adk-go), L198 (mining osv-scanner), L192 (mining methodology) |
| **Learned** | **(1) O ecossistema evoluiu de "skill solta" para "plugin = skills + MCP servers" — Agent Plugins Spec v1.0.0 é o padrão vendor-neutral (agent-plugins.org) que VS Code, Cursor, GitHub Copilot, Claude Code, Codex e Antigravity já consomem; plugin.json mínimo é `{name}` + skills/ + opcional mcp.json.** **(2) Os repos google distribuem plugin.json DENTRO de .claude-plugin/ ou .codex-plugin/ (harness-specific) com skills/ na raiz — o installer precisa procurar o manifest nos 3 locais mas usar a raiz como base do payload, senão as skills nunca são achadas.** **(3) Marketplace.json é o padrão de distribuição multi-plugin: manifest enxuto (nome/descrição/source) + refs pinados — instalar por nome `@marketplace` é o fluxo que o Don espera (igual `claude plugin install <plugin>@<marketplace>`); source git-subdir cobre repos onde o plugin vive em subdir (url+path).** **(4) Registro em memória NÃO basta para CLI — sem rehidratar plugins do disco no NewManager (loadPluginsFromDir, espelhando loadFromDir de skills), list/info/remove ficam vazios em processo novo.** **(5) Symlinks dentro de plugin repos (ex: CLAUDE.md dev-plugin) são legítimos no GitHub mas o extractTarGz fail-closed os rejeita — postura de segurança correta, documentar como limitação do canal.** **(6) O padrão de instalação em camadas consolida: GitHub ref → sandbox temp (zip-slip reject) → pipeline installStandardSkill (containment) — qualquer fonte nova (repo, marketplace, plugin) reusa o MESMO pipeline validado; nunca escrever um quarto caminho de instalação. |
| **Next** | (1) `cosca skill search <query>` contra skills.sh catalog / GitHub indexes. (2) Batch-select interativo (listar skills de um repo, Don escolhe por número). (3) Wire do mcp.json no runtime MCP (internal/chat/mcp) — hoje o config é registrado e persistido, mas não auto-conecta servidores. (4) Curated bundle das WAF pillars no catálogo embutido. |
