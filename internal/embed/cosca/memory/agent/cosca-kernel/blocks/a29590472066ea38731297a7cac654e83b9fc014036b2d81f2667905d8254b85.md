PREV: 51dee8b25997d1679f30216f558270f69549a2eec1f6735b53839b4456c57b59
ID: L134
TIME: 2026-08-13
LEVEL: 4
TAGS: #workqueue #taskqueue #runplan #dag #esteira #wiring #recovery-loop #pipeline-run #level-4
---
## L134 — 2026-08-13 — TaskQueue wireado na esteira real: RunPlan (DAG) + RunPlanWithQueue + `cosca pipeline run` | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" — wirear o TaskQueue na esteira real (usá-lo no fluxo de execução de planos, em vez de deixá-lo órfão). |
| **Technique** | Level 4 — **(1)** `TaskQueue.RunPlan(ctx, plan, onProgress)`: executor DAG self-contained via workqueue — constrói grafo (taskMap/dependents/remaining), enfileira tasks sem dependência, workers fazem `Get → RunStep → recover/retry → finish`, `finish` decrementa `remaining` dos dependentes e enfileira quando a última dependência termina (sucesso OU falha — mesma semântica do RunPlanParallel), `close(doneCh)` quando pending==0. **(2)** Camadas: workqueue (agendamento dedup+backoff) + RecoveryLoop (fix inteligente antes do retry) + RunStep (execução). **(3)** Skip de tasks já `TaskCompleted`/`TaskSkipped` (durable `--resume`) — pre-marca como finished e desbloqueia dependentes. **(4)** Workqueue plano-escopado (não o `q.queue` compartilhado) → RunPlan é seguro sob chamadas concorrentes. **(5)** `StepRunner.RunPlanWithQueue` (delega ao TaskQueue com `s.recoveryLoop`). **(6)** Wireado: `cosca pipeline run` (pipeline.go) trocou `RunPlanParallel` → `RunPlanWithQueue`. **(7)** 4 testes novos (ordem de dependência, independentes, retry, skip completed) + race detector limpo. |
| **Level** | 4 |
| **Outcome** | success — A workqueue agora roda em produção (`cosca pipeline run`). DAG com dedup+backoff+recovery. 15 testes no pacote pipeline, `go build ./...` + `go test -race` limpos. RunPlanParallel preservado (workflow run/REST/eval seguem nele) — evolução aditiva (L199). |
| **Confidence** | 0.94 (validado: 4 testes novos de RunPlan + 11 de workqueue/taskqueue + race detector + build completo) |
| **Tags** | #workqueue #taskqueue #runplan #dag #esteira #wiring #recovery-loop #pipeline-run #level-4 |
| **Related** | L232 (workqueue), L231 (mineração k8s), L199 (evolução aditiva), L190 (esteira real) |
| **Learned** | **(1) A camada certa é: workqueue (QUANDO executar — dedup+backoff) + RecoveryLoop (COMO corrigir — fix inteligente) + RunStep (O QUE executar) — o retry com backoff não substitui o fix, ele o complementa: tenta o fix primeiro, e só faz requeue com backoff se o fix falhar.** **(2) A semântica de dependência do RunPlanParallel (dependente roda quando a dep TERMINA, sucesso ou falha) foi preservada — mudar isso seria uma regressão de comportamento.** **(3) O skip de tasks `TaskCompleted` é ESSENCIAL para o `--resume` durável: sem ele, o RunPlan re-executaria passos já completados.** **(4) Workqueue plano-escopado (não o compartilhado) elimina a classe inteira de race de estado compartilhado — cada RunPlan é uma unidade isolada.** **(5) Evolução aditiva (novo RunPlanWithQueue + trocar SÓ o `cosca pipeline run`, deixando workflow/REST/eval no RunPlanParallel) é o caminho seguro para provar um engine novo em produção sem arriscar todos os caminhos de uma vez. |
| **Next** | (1) Trocar os caminhos restantes (workflow run, REST, eval) para RunPlanWithQueue após validar em produção. (2) Extension points + factory na esteira (hooks por etapa). (3) `resourceVersion` + optimistic concurrency no knowledge.db. (4) Admission chain nos gates. |
