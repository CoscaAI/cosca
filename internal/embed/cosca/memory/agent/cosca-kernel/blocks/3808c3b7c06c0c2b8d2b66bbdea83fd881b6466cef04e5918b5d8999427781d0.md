PREV: 1a04de78529de65ea61a200cb730348ee8cd735f150a3bb333638dfb68346e30
ID: L329
TIME: 2026-08-16
LEVEL: 5
TAGS: #mineracao #padroes-de-codigo #padrao-cosca #mlt #kdenlive #shotcut #comfyui #gstreamer #ardour #ladspa #lv2 #filter-complex #timeline #node-executor #content-addressed-cache #caps-negotiation #ghost-pads #proxy #level-5
---
## L329 — 2026-08-16 — MINERAÇÃO de PADRÕES DE CÓDIGO (ordem do Don): MLT/Kdenlive + ComfyUI + GStreamer + Ardour/LADSPA + FFmpeg filter_complex — como os projetos sérios estruturam editores e engines, mapeado para o PADRÃO COSCA | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Buscar no GitHub padrões de código de engines de edição/geração multimídia (vídeo, imagem, áudio) para construir da melhor forma no padrão Cosca |
| **Technique** | Mineração 5 frentes: MLT/Kdenlive/Shotcut (NLE), ComfyUI (node executor), GStreamer (pipeline), Ardour/LADSPA/LV2 (áudio), FFmpeg filter_complex (composição programática) |
| **Level** | 5 |
| **Outcome** | success — 8 padrões de código extraídos, todos com mapeamento direto para o que a casa já tem (ngraph, media engine, asset registry) |
| **Confidence** | 0.90 |
| **Tags** | #mineracao #padroes-de-codigo #padrao-cosca #mlt #kdenlive #shotcut #comfyui #gstreamer #ardour #ladspa #lv2 #filter-complex #timeline #node-executor #content-addressed-cache #caps-negotiation #ghost-pads #proxy |
| **Related** | L328 (Visual F7), L327 (Knowledge Fabric), L86 (node graph), L66 (canvas), L90 (asset registry), L85 (render engine), L80 (cosca-image), L79 (cosca-cinema), L78 (cosca-music), L308 (mineração node graph), L52/L295 (padrão cosca) |
| **Learned** | **P1 — Timeline como DADO, não objeto** (MLT/Kdenlive): a timeline é um documento serializável (trilhas+clips+efeitos+transições) que o engine consome; UI é só controller (MVC). Kdenlive: MLT no core, efeitos plugáveis (frei0r), nested sequences. Shotcut: proxy generation + filtros empilháveis + keyframes em parâmetros. **P2 — Tudo é NÓ com contrato tipado** (ComfyUI, 110k★): node = inputs tipados + outputs tipados + função; registry pattern; workflow JSON = reprodutibilidade total (o grafo É o programa). **P3 — Executor com cache content-addressed** (ComfyUI): hash = sha256(tipo+params+hashes dos inputs); só executa o que mudou; invalidate_downstream via BFS; LRU para VRAM; ordenação topológica via Kahn (detecta ciclo e processa em camadas). **P4 — Pipeline como CONTRATO NEGOCIADO** (GStreamer): elements com pads (sink/source), caps negotiation (fail cedo se incompatível — falha na negociação, não no meio do fluxo), bins reutilizáveis com GHOST PADS (escondem complexidade interna), bus para eventos/erros, queues criam threads (isolam ramos lentos). Padrão essencial: dynamic pads (pad-added → inspeciona caps → linka ramo certo). **P5 — Composição via filter_complex programático** (FFmpeg): labels únicos [0:v], split/concat/overlay/amix/xfade/adelay/acrossfade, validação ANTES de codificar com `-f null -`, debug com graph2dot. O filtergraph é o "assembly" da composição de mídia. **P6 — Plugins de áudio por PADRÃO ABERTO** (Ardour/LADSPA/LV2): não reinventar host DSP — LADSPA é API mínima (f32), LV2 é o padrão moderno; FAUST gera DSP compilado; engine separado da UI, session model, roteamento qualquer-para-qualquer. **P7 — Engine separado da UI** (Kdenlive MVC/Ardour): o core não sabe que existe UI — headless primeiro. **P8 — Qualidade contratual + proxy** (Shotcut/Kdenlive): preview/draft/final com proxy generation — a casa JÁ TEM esse contrato no internal/media (Quality: preview→crf32 ultrafast, draft→crf23, final→crf18). |
| **Next** | A síntese para o padrão cosca: (1) timeline JSON serializável no cosca-cinema (P1) + compilador timeline→filter_complex (P5) com validação `-f null -` antes do render (fail-closed); (2) ngraph já tem topo sort e cache por assinatura (L86) — alinhar com o executor ComfyUI (invalidate_downstream BFS + LRU VRAM) (P2/P3); (3) internal/media: adicionar validação de capacidade antes de montar pipeline (probe → check codec/encoders disponíveis → fail cedo) estilo caps negotiation (P4); (4) áudio: LADSPA/LV2 como formato de plugins DSP, FAUST para gerar DSP próprio (P6); (5) manter engine headless, canvas como view (P7) — o padrão cosca já é esse. Fica como proposta — execução só com aprovação do Don. |