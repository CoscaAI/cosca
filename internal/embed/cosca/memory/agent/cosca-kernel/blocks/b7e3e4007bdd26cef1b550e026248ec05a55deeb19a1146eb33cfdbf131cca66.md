PREV: 27e61292cf827ee26232c0d44506285dda5211477c681adf5e18e746fbea65ec
ID: L254
TIME: 2026-08-16
LEVEL: 4
TAGS: #knowledge #vector-store #dangling-vectors #orphan #verify-fix #tool-gap #integridade #cofre #fix #level-4
---
## L254 — 2026-08-16 — Knowledge: `verify --fix` tinha falso sucesso (não limpava vetores órfãos) — corrigido com CleanupDanglingVectors | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Ordem do Don: "resolve da melhor forma possível" após o checkup "carro?" revelar 1 warning de integridade no knowledge (`vector count mismatch: 22257 vectors vs 21998 chunks`) + 15 HIGH no security scan. O `cosca knowledge verify --fix` reportou "Repair complete" MAS o mismatch persistia idêntico após re-verificação. |
| **Technique** | Forense de causa raiz via sqlite3 read-only: 259 vetores órfãos de UM único documento deletado (índice antigo do learnings.md, doc 80423fc1). O `RepairOrphanVectors` só trata "chunks sem vetor" (LEFT JOIN chunks→vectors, re-embute); o caso real era o INVERSO — "vetores sem chunk" (dangling). Corrigi adicionando `Engine.CleanupDanglingVectors()` (DELETE FROM vectors WHERE chunk_id NOT IN chunks) + método no adapter + wire no CLI --fix (chama cleanup + repair + re-verifica com relatório honesto) + 3 testes unitários. |
| **Level** | 4 |
| **Outcome** | success — `go build ./...` verde; `go test ./internal/knowledge` e `./internal/cli` verdes; verify 0 issues após fix manual (backup de 214MB via `.backup` WAL-safe). |
| **Confidence** | 0.90 (evidência: teste unitário cobre dangling/keeps-valid/no-db; verify real voltou a 0 issues; vetores 22257→21998 batendo com chunks) |
| **Tags** | #knowledge #vector-store #dangling-vectors #orphan #verify-fix #tool-gap #integridade #cofre #fix #level-4 |
| **Related** | L126 (cofre de conhecimento — ingestão), L211 (auditoria geral 10 bugs P0/P1), L219 (arquivo fantasma RCA), L253 (media executor) |
| **Learned** | (1) "Repair complete" NÃO garante reparo — mensagem incondicional de sucesso esconde que nada foi feito. (2) Vetor órfão (dangling) nasce de re-ingestão que deleta doc+chunks mas deixa os vetores. (3) Condição correta é `chunk_id NOT IN chunks` — NÃO incluir document_id: vetor com chunk vivo mas doc órfão é "orphaned chunks" (outro problema). (4) DELETE cirúrgico em DB exige backup WAL-safe antes (`.backup`). (5) `go mod why` revelou que os 15 HIGH do security scan vêm do PRÓPRIO osv-scanner (docker/buildkit transitivos, não usados pelo runtime). |
| **Next** | (1) Atualizar chain.dat + merkle (cosca-merkle) — o ledger da memória está atrasado em L179. (2) Auditar por que a re-ingestão do learnings.md deleta chunks sem limpar vetores (causa raiz do dangling). (3) `go get osv-scanner@v2.5.0` + `go mod tidy` para zerar os docker/buildkit transitivos. |
