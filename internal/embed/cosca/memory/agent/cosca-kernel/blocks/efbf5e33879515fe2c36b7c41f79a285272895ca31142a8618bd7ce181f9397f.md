# L387 — CAMPANHA DE PERFORMANCE READ-ONLY (Fase 6-8 do professor): baseline + 15 achados

## status: EVIDENCE (medido, nada implementado — a campanha é read-only)

## Baseline medido (Fase 8 — item 1)
- Busca real (TestRealBenchmark): cache quente 0.045ms / frio 0.696ms / 11.058 qps
- Breakdown da busca (L368): FTS 46% + ranking/merge 29% + vector 25% + embed 0.2%
- Kernel AVX2 (da campanha): int8 52.66 Mvec/s (bandwidth DDR4 saturada), int16 26.28
- Startup do serve: ~0.55s + 206MB (wrapper); init frio CLI ~6s (loadGraph — knowledge.go:294)
- Indexacao (db de teste, SEM embed): 4.404s/doc; re-indexacao de arquivo alterado: 4.475s (o MESMO custo — FTS rebuild + graph por documento)
- Embed real: ~133 chunks/s (nomic via ollama, L364)

## Os 15 achados (Fase 7 — trabalho duplicado, agente explore + confirmacao minha)
ALTO:
1. dedup DEPOIS do embed + batch sem cache — re-indexar re-embeda TUDO (indexer.go:240 vs storeDocument 714-766; provider.go:383-408 batch nunca consulta cache)
2. FTS rebuild COMPLETO a cada IndexDirectory (indexer.go:415-423) — as funcoes incrementais existem e sao codigo morto (fts.go:264-291)
3. busca REST/gRPC SEM o caminho bounded — o FTS acha candidatos mas o vetor varre NxD (search.go:202-230 — CandidateIDs nunca preenchido; o layered/bounded so no CLI)
4. dedup check por chunk = scan full sem indice (indexer.go:720-724; coluna hash existe mas sem indice no schema)
MEDIO-ALTO:
5. saveGraph completo (DELETE+re-INSERT de ~13k entidades) a cada evento do watcher (knowledge.go:1951-1960)
MEDIO:
6. 5 COUNT(*) MATCH descartados por busca (search.go:324, fts.go:199-204)
7. 3 representacoes do indice em memoria (~3.5x — slab float32 retida, ~184MB em 60k) (index.go:576-628)
8. sync le + hasheia arquivo 2x (knowledge.go:1468-1472 + indexer.go:206-212)
9. init frio ~6s por invocacao CLI (loadGraph deserializa o grafo inteiro — graph.go:658-681)
10. fallback SQL do score sem a janela bounded (sqlite_vec.go:662-686) — o in-memory ja resolveu (index.go:188-194)
BAIXO:
11. cache de busca case-sensitive (knowledge.go:1970-1988)
12. string(content) 2x por doc (indexer.go:212/224)
13. eviction do embedding cache O(n) (provider.go:592-604)
14. 3x json.Marshal do resultado por cache miss (knowledge.go:766, cache.go:241/488)
15. PRAGMA chunksHaveDedupCols por documento (indexer.go:711)

## Ranking por impacto estimado (item 3)
1. #1+#2 (re-indexacao: 4.4s+embed -> ~0.1s — 40-100x no pipeline)
2. #3 (busca: scan NxD -> ~100-250 linhas via FTS candidates — 5-10x na latencia fria)
3. #4 (indice em chunks(hash) — elimina os scans full do dedup)
4. #5 (debounce do watcher — 13k entidades por evento -> 1x por batch)
5. #9 (lazy-load do grafo — CLI ~6s -> ~0.5s)
6. #7 (memoria: -184MB no daemon 24/7)

## Experimento recomendado (item 7) — a rodar na proxima fase
#3 (busca bounded no serve): medir recall@10 do caminho bounded vs full scan
com o corpus real (ja temos o recall oracle: int16 0.995, tie-breaker).
Se o recall nao degrada, o bounded e seguro. Metrica: latencia fria + recall@10.
#1+#2 (re-indexacao): IndexDocument de arquivo alterado — N de chamadas de
embed (deve ser 0 se nada mudou) + tempo total.

## Invariantes a preservar (item 9 — Fase 6 do professor)
identidade do embedding (digest nomic + fail-closed 3 niveis) · provider
correto (ollama) · provenance (chain/merkle) · recall (0.995 int16) ·
ranking (score desc, id asc) · memoria (ordem sagrada) · recovery (o 28o) ·
determinismo (tie-breaker + digest ambiental) · dedup preventivo.

## O que NAO vale a pena (item 10)
kernel AVX2 (bandwidth saturada — limite fisico) · int16 vs int8 (ja e o
default, recall 0.995) · cache case-sensitive (busca fria ja e 0.696ms) ·
3x marshal · PRAGMA por documento · eviction O(n) do cache de embed.

## Classificacao
MEASURED baseline (busca, startup, indexacao, kernel) + achados confirmados
EVIDENCE os 15 achados com arquivo:linha (agente explore + confirmacao)
INFERRED ganhos estimados (40-100x re-indexacao, 5-10x busca) — a TESTAR
HYPOTHESIS bounded nao degrada recall (experimento recomendado)
UNKNOWN o custo real do embed por chunk no fluxo padrao de producao
DECISION relatorio entregue (Fase 8); nada implementado; experimentos na
proxima fase (com o Don)
