PREV: e7729b4e7c2869ea413c633e6e77eb5f23cee0e5a11a4f73bb10d296a077fdf0
ID: L121
TIME: 2026-08-13
LEVEL: 4
TAGS: #git #chain #ancora-git #sign-auto #identidade #chave-publica #open64 #anti-hijack #versionamento #level-4
---
## L121 — 2026-08-13 — PRIMEIRA ÂNCORA DA FAMÍLIA: git init + commit baseline + bloco 1 git-anchored + identidade pública conferível (open64) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ordenou "crie o primeiro ancora e esconde a chave criptogravada em root 0600". Executado: git init, baseline commitado, bloco 1 da chain git-anchored, chave pública restaurada no workspace, esconderijo da chave privada preparado em ~/.config/cosca/keys (0700), identidade pública entregue em base64 (open64) pra conferência externa na camada segura do Don. |
| **Technique** | Level 4 — **(1)** git init -b main; identidade global Henrique <henryalflen@gmail.com>; .gitignore pré-existente cobre runtime (`.cosca/*` fora exceto framework/), `*.env`, `bin/`, `*.db`; 2.376 arquivos / ~546K linhas staged; varredura de segredos limpa (kernel_public.key é PÚBLICA Ed25519, zero privadas, zero .env). **(2)** Commit a321eb5 = âncora 1; chain: `./bin/cosca-check --sign-auto` — SignAuto é 100% keyless: a assinatura É o hash do commit (L186). Bloco 1: GIT_COMMIT + GIT_TREE do tree do `internal/embed/cosca` (`git rev-parse HEAD:internal/embed/cosca` — NÃO o tree do commit inteiro), GIT_ANCHORED: true, 1284 arquivos. **(3)** Check() exige a cópia da chave pública no workspace (.cosca/keys/kernel_public.key) — sem ela FATAL "chain exists but public key is missing"; restaurada via cp da versionada, 0600, dir 0700 → Chain valid. **(4)** kernelKeyDir() prefere ~/.config/cosca/keys (config root) → esconderijo criado 0700. A chave privada criptogravada (AES-256-GCM, derivada iterada) só nasce com passphrase via --passphrase-stdin; --init RECUSA com kernel_public.key versionada presente (anti-hijack da identidade original). **(5)** Open64: chave pública em DER base64 + fingerprint SHA-256 + GIT_COMMIT/GIT_TREE/BLOCO — comparável externamente pelo Don (openssl pkey -pubin -outform DER | base64). |
| **Level** | 4 |
| **Outcome** | success — Repo versionado (a321eb5), chain criada e VALIDADA (`Chain valid: 1 blocks, 1284 files`), identidade pública conferível, esconderijo da chave pronto. Pendência única: a kernel_private.key criptogravada (passphrase do Don — caminho A: restaurar chave original; caminho B: nova identidade, exige remover a pública versionada). |
| **Confidence** | 0.95 (validado ao vivo: commit + bloco + Check green; único trecho não executado = nascimento da chave privada, que depende do Don) |
| **Tags** | #git #chain #ancora-git #sign-auto #identidade #chave-publica #open64 #anti-hijack #versionamento #level-4 |
| **Related** | L186 (chain auto-sign no hash), L219 (fix aplicado e commitado), internal/integrity/git_anchor.go, sign.go (kernelKeyDir, InitChain anti-hijack), .cosca/family_chain.dat |
| **Learned** | **(1) Âncora git é forja-impossível sem o commit: o hash É a prova — sem passphrase, sem chave, sem estado extra.** **(2) Check() valida o ESTADO do workspace (precisa da cópia da pública em .cosca/keys) — restauração da chave é parte do procedimento de recovery, não opcional.** **(3) A recusa do --init com a pública versionada é anti-hijack DELIBERADO: resetar identidade é uma decisão do Don (ele compara as camadas externamente), não um flag de conveniência.** **(4) Open64 (gíria do Don) = identidade PÚBLICA em DER base64 — diferente do PEM; precisa do openssl DER pra conferência exata entre camadas.** |
| **Next** | (1) AGUARDANDO DON: caminho A (restaurar kernel_private.key original da camada segura → ~/.config/cosca/keys 0600) ou caminho B (remover pública versionada + `--init --passphrase-stdin` → identidade nova). (2) stderr do preso visível (io.MultiWriter) — pendência L219. (3) Substituir binário de produção + religar serviço. (4) --sign-auto no próximo commit → bloco 2 da chain. |
