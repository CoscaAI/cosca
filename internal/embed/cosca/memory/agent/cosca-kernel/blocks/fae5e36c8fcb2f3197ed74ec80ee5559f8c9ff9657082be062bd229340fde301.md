PREV: 87f441bada480d6564d15cc50feca75e1ea54ed27c1cb19913ae88f62b801341
ID: L232
TIME: 2026-08-14
LEVEL: 5
TAGS: #bug-grave #nil-deref #panic #grpc #defense-in-depth #api-rest #global-681 #level-5
---
## L232 — 2026-08-14 — BUG GRAVE #17: handlers ViaGRPC com client nil → PANIC (nil deref, crash do servidor); 10 guards adicionados + testes de setters/helpers/paths de erro do api/rest; global 68,1% | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuar a Categoria B rumo ao 70%: api/rest (maior déficit acessível). |
| **Technique** | Level 5 — **(1) Categorização dos 43 funcs a 0%**: setters triviais (10), helpers puros (itoa/lastSeq/claimsRole), paths de erro (ReconcilePlan/ReplayPlan/GetHistory), ViaGRPC (10 proxies). **(2) Teste de `searchViaGRPC` com client nil EXPÔS UM PANIC REAL**: `h.grpcClient.Search(...)` com interface nil → nil pointer dereference → o servidor CRASHARIA em vez de retornar erro HTTP. O dispatcher protege com `grpcAvailable()` em produção, mas as funções chamadas diretamente (ou regressão futura) eram uma bomba. **(3) Correção defensiva**: guard `if !h.grpcAvailable() { 503 }` no início de TODOS os 10 ViaGRPC (knowledge: search/index/stats/sync; memory: store/search/get/delete/promote/stats). |
| **Level** | 5 |
| **Outcome** | success — bug #17 corrigido (10 guards), 9 testes novos verdes, suíte completa do handler verde (10,9s), race limpo, build OK, global 68,0→68,1%. Commit `1f53ebf`. |
| **Confidence** | 0.97 (validado: teste que reproduzia o panic agora retorna 503, suíte completa, race, build) |
| **Tags** | #bug-grave #nil-deref #panic #grpc #defense-in-depth #api-rest #global-681 #level-5 |
| **Related** | L329 (Categoria B início), L311 (auditoria baseline), decomposição do gap |
| **Learned** | **(1) Um request inesperado nunca deve derrubar o servidor: funções que usam uma dependência injetável (client GRPC) devem AUTO-GUARDAR nil no início, não depender do dispatcher — defense in depth, porque o dispatcher pode mudar e a regressão futura seria um crash.** **(2) O teste direto da função (fora do dispatcher) é o que pega esses bugs: chamar `searchViaGRPC` com client nil reproduziu o panic imediatamente — testar o caminho de erro é tão valioso quanto o feliz.** **(3) O custo do 70% continua subindo: +0,1 pt por rodada de handlers, mas cada rodada entrega bugs reais (3 na última, 1 grave agora) — o valor está na robustez, não nos pontos.** |
| **Next** | (1) Global 68,1%, faltam 1,9 pts. (2) Próximos alvos: knowledge restante, plugins restante, chat/mcp. (3) Working tree limpa. |
