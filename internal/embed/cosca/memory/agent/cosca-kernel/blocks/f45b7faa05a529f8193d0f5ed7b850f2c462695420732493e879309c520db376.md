# L369 — RECALL REAL (experimento 2 da revisao): provider nomic no fluxo

## status: DECISION (experimento 2 concluido, read-only + config validada)

## O que foi feito
1. Registrado o provider ollama de embeddings (ollama.Register — o go test
   nao roda o initProviders do cmd/cosca) + Config EmbeddingProvider=ollama,
   EmbeddingModel=nomic-embed-text.
2. Self-match (caminho COMPLETO: texto -> embed nomic -> busca -> top-1):
   originais (n=3): 0.9998 (3/3 hits >0.9) · novos nomic L364 (n=30):
   1.0000 (30/30). O indice e NOMIC-COERENTE — sem mistura visivel.

## ERRATA PARCIAL da L368
O mismatch "queries TF-IDF vs indice nomic" era do AMBIENTE DE TESTE: o go
test nao carrega o config.yaml -> auto-detect -> provider "local". EM
PRODUCAO, o config.yaml JA tem embedding.provider=ollama + model
nomic-embed-text -> o serve usa o nomic (com o ollama.Register do
initProviders). A descoberta REAL permanece: o DEFAULT "auto" cai no local
(nao-semantico) — um RISCO DE CONFIGURACAO (se o config.yaml for perdido
ou alterado, as queries voltam ao local). O self-match ~1.0 confirma que o
fluxo com o nomic e coerente com o indice.

## Classificacao
MEASURED self-match originais 0.9998 / novos 1.0000 (fluxo nomic completo)
EVIDENCE provider ollama selecionado no fluxo; config.yaml ja correto
INFERRED em producao o serve usa o nomic (config + initProviders); o risco
         e o default "auto" (cai no local)
HYPOTHESIS o recall real de producao (top-K de conteudo) esta no nivel
           medido (self-match ~1.0) — a validar com queries de usuario
UNKNOWN recall real com queries reais de usuario (nao self-match)
DECISION config validada (nenhuma mudanca necessaria — ja correta);
         experimento 2 concluido. Revisao L367 fechada: 2 de 4 experimentos
         executados (L368 breakdown + L369 recall real). Restam: dedup
         preventivo (risco de re-sujar) e guard ativo — decisoes do Don.
