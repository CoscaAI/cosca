PREV: 2ddda7af87b1df8b391447e75f9d1a933e101abc407079a5c66198e0ee3eff79
ID: L119
TIME: 2026-08-13
LEVEL: 2
TAGS: #servicos #reload #restart #serve #ollama #bwrap #jaula #health #provider #level-2 #manutencao
---
## L119 — 2026-08-13 — Reload limpo dos serviços: serve+ollama parados e religados sem systemd | Level 2

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "se tiver algum rodando pare e inice" — parar o que estivesse rodando e iniciar (reload limpo) do ambiente de serviços. |
| **Technique** | Level 2 — **(1)** Mapeamento do estado: unidades systemd user do cosca NÃO existem (destruídas no incidente L187 — `~/.config/systemd/user` só tem tracker services), portanto serve/ollama rodam como processos diretos com setsid/nohup, não via systemctl. **(2)** Identificação via ps+ss: `cosca serve` (porta 14120, com jaula bwrap ativa — COSCA_ENABLE_REGISTRATION=false, --unshare-all) e `ollama serve` (porta 11434, provider ativo). Runtime (14123) já estava parado. **(3)** Parada limpa: `kill -TERM` no PID do serve primeiro, depois ollama; confirmação por ss (portas liberadas) e ps (zero processos cosca/ollama restantes). **(4)** Religada na ordem correta de dependência: ollama (provider) PRIMEIRO, depois cosca serve; `setsid nohup` + log em `~/.cosca-serve.log` e `~/.cosca-ollama.log` (padrão do `make restart`). **(5)** Validação pós-subida: ss (portas 11434/14120 escutando), `curl /health` → `{"healthy":true}`, jaula bwrap ativa como filho do serve, `cosca capability level` → L2-raciocínio com provider ollama. |
| **Level** | 2 |
| **Outcome** | success — Serve (14120, jaula ativa) e ollama (11434) de pé, health OK, L2 com provider ollama. Nenhum serviço systemd envolvido — padrão atual é processo direto (units destruídas no incidente ainda não recriadas). |
| **Confidence** | 0.97 (validado: health check HTTP, portas escutando, processos corretos, capability L2) |
| **Tags** | #servicos #reload #restart #serve #ollama #bwrap #jaula #health #provider #level-2 #manutencao |
| **Related** | L187 (incidente — units systemd destruídas), L174 (4/4 green via systemd), L173 (jaula/capability), L188 (identidade embutida) |
| **Learned** | **(1) Após o incidente L187, o ambiente perdeu as units systemd user (cosca-serve/runtime/voice) — o padrão de operação voltou a ser processo direto com setsid/nohup; o `make restart` cobre isso automaticamente (systemd inativo → pkill + setsid).** **(2) Ordem de religada importa: provider (ollama) ANTES do serve — o serve depende do provider para o nível cognitivo (L2 com ollama).** **(3) A jaula bwrap está ativa no serve (`--unshare-all`, COSCA_ENABLE_REGISTRATION=false) — a lição de segurança do L187 foi incorporada ao startup padrão.** **(4) Validação de reload: health check HTTP + portas + capability level é o trio suficiente; não precisa reiniciar o sistema.** **(5) Não rodar build/restart de serviços em paralelo com dev do frontend — mesma lição do L175 aplicada ao ecossistema. |
| **Next** | (1) Recriar units systemd user (cosca-serve/runtime/voice) com `make install-service` para daemon 24/7 + linger. (2) Avaliar religar o runtime gRPC (14123) para subir para L3 (`cosca capability level` aponta runtime desabilitado). |
