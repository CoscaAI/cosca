PREV: d6056c59e035e32f29837d53834ee1619c951c233804ac9fa21840b1504b57d5
ID: L150
TIME: 2026-08-13
LEVEL: 2
TAGS: #checkup #fail2ban #jail-local #duplicacao #seguranca #incidente-l187 #nftables #level-2
---
## L150 — 2026-08-13 — Checkup completo pré-desafio: fail2ban caído 15h por jail.local com seções duplicadas (resíduo L187) | Level 2

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "faz um checkup completo" (sala/garagem/carro/gasolina/pneus/água) antes do desafio grande. Varredura de segurança + saúde de todos os subsistemas. |
| **Technique** | Level 2 — **(1)** Varredura em 5 frentes paralelas: serviços/portas (ss), processos+jaula (ps --forest), segurança (fail2ban/ufw/keys/perms), dados (knowledge.db/backups/cv/integrity), recursos+git+binários. **(2)** Achado crítico: `systemctl is-active fail2ban` = failed há 15h. Causa raiz: `/etc/fail2ban/jail.local` tinha um bloco INTEIRO de config APPENDADO por engano no final — `[DEFAULT]` duplicado (linha 984) + `[sshd]` + `[nginx-http-auth]` + `[nginx-botsearch]` + `[apache-auth]` duplicados (as originais estão nas linhas 274/376/389/302) — re-declarando `bantime` e `[sshd]`, o parser do fail2ban explodiu. **(3)** O resíduo veio do incidente L187 (alguém/script de "harden" fez `cat >>` sem verificar duplicação). **(4)** Correção: remover o bloco duplicado inteiro (`sed -i '981,$d'`) — o `[monitorix]` original (linha 978) ficou como último jail; o `enabled = true` do sshd já vem do `/etc/fail2ban/jail.d/defaults-debian.conf` (padrão debian), NÃO precisava ser re-declarado. **(5)** Validação: `fail2ban-client -t` limpo → `systemctl restart` → jail `sshd` started com banaction nftables. |
| **Level** | 2 |
| **Outcome** | success — fail2ban ATIVO (running) + jail `sshd` rodando com `nftables`. Superfície confirmada blindada: TODAS as portas em loopback (nada exposto), porta 8501 (vuln L187) sumiu, SSH parado (22 não escuta), auth.json com API keys removido, configs 0600/dirs 0700, knowledge.db 97.9MB + backup auto 11:20, memory integrity OK, 0 zumbis, disco 41%/RAM 19GB. O comando de correção precisou do sudo do Don (sudo -n falha; jail.local é root:root). |
| **Confidence** | 0.95 (validado: systemctl active, log "Jail 'sshd' started", fail2ban-client -t limpo, seções duplicadas confirmadas via grep) |
| **Tags** | #checkup #fail2ban #jail-local #duplicacao #seguranca #incidente-l187 #nftables #level-2 |
| **Related** | L248 (falso negativo status), L187 (incidente — resíduo do jail.local), L189 (reload serve) |
| **Learned** | **(1) "Appendar" config de segurança sem verificar duplicação quebra o fail2ban em fail-closed SILENCIOSO: o serviço morre (exit 255) mas nada alerta — ficou 15h caído sem ninguém notar. Config de daemon de segurança precisa de `fail2ban-client -t` (test) após QUALQUER edição.** **(2) O `defaults-debian.conf` em jail.d/ JÁ ativa o `[sshd] enabled = true` e define `banaction = nftables` + `backend = systemd` — re-declarar `[sshd]`/`[DEFAULT]` no jail.local é REDUNDANTE e foi exatamente o que quebrou. Overrides devem ir em jail.d/, nunca duplicando seções do jail.conf.** **(3) Checkup de segurança começa pelo estado ATUAL (portas escutando + processos + service status), não pela config — foi o `ss` + `ps --forest` que revelou a verdade, não a config em si.** **(4) `systemctl is-active` + o log de boot do daemon ("Jail 'X' started") são a prova de vida definitiva; `fail2ban-client status` sem root dá "Permission denied" (o socket é do root) — não confundir com serviço parado.** **(5) O cofre está limpo: auth.json com API keys foi removido (pendência L187 resolvida), configs 0600, dirs 0700 — a exposição de keys do incidente foi mitigada.** |
| **Next** | (1) `cosca cv snapshot` — criar baseline cognitivo antes do desafio (não precisa de sudo). (2) Commit da L248 + L249 (4+1 arquivos) e push dos 3 commits pendentes (L246/L247/L248-L249). (3) Avaliar ativar outros jails (nginx/apache) SE os serviços voltarem a rodar. |
