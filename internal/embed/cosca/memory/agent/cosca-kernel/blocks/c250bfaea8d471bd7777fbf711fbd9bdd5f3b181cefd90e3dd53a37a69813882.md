# L376 — Fase 1: pinagem do digest + fail-closed no provider (com teste)

## status: DECISION (implementada e validada; re-embed NAO executado)

## O que entrou (a ordem do professor: uma mudanca -> uma validacao)
1. embeddings.Config.Digest + ProviderRegistryConfig.Digest + propagacao
   (createProvider passa o digest ao provider).
2. FAIL-CLOSED no Select (provider.go): com Primary EXPLICITO, o "local" NAO
   e mais o ultimo candidato (fallback silencioso removido). Erros de
   IDENTIDADE (ErrEmbeddingIdentityMismatch, sentinel) sao FATAIS no Select.
3. ollama.New: quando cfg.Digest != "", consulta /api/tags e REFUSA operar
   se o digest do modelo carregado nao bater ("REFUSING OPERATION (L376)").
4. knowledge.Init: erro de identidade e FATAL (errors.Is) — NAO cai em
   auto-detect (o segundo fallback silencioso que anulava o primeiro).
5. Config do Cosca: embedding.digest (config.yaml) -> CLI -> bootstrap ->
   knowledge.Config -> Select.

## DESCOBERTA durante a implementacao
O conhecimento tinha um SEGUNDO nivel de fallback silencioso: quando o
provider explicito falhava, re-tentava com auto-detect (-> local),
anulando o fail-closed. A correcao: erros de IDENTIDADE sao fatais; apenas
indisponibilidade cai no fallback historico.

## Validacao
- TestFailClosed: Primary explicito + provider falha -> Select ERRO (nao local).
- TestDigestPassed: digest propagado ao provider.
- TestEmbeddingDigestPin (integracao ollama real): digest errado ->
  "embedding identity mismatch ... REFUSING OPERATION" e o Init FALHA.
- TestEmbeddingDigestOK: digest correto (0a109f...) -> Init OK.
- Suites: embeddings/ollama/cli/knowledge OK. Serve reinstalado (doctor 26/26).
- Limpeza: removido um /tmp/.cosca artefato (08-16) que quebrava um teste.

## Classificacao
MEASURED digest errado -> Init falha; digest certo -> OK; fallback removido
EVIDENCE o fail-closed cobre os 2 niveis (Select + conhecimento)
INFERRED a reconstrucao agora tem a identidade protegida (digest + fail)
HYPOTHESIS com o digest pinado no config, o bootstrap/re-embed nao cai mais
           no local silenciosamente
UNKNOWN impacto do config sem digest (a producao atual nao tem o digest —
        a adicionar na fase 2 do plano do professor)
DECISION Fase 1 CONCLUIDA. Proximas (ordem do professor): Fase 2 (destruir
         ambiente) -> Fase 3 (bootstrap) -> Fase 4 (verificar digest) ->
         Fase 5 (re-embed) -> Fase 6 (repetir L375). Aguardando o Don.
