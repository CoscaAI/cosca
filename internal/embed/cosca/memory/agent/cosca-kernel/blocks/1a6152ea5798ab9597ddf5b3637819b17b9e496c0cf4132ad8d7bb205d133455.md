PREV: 4c846d86f5a3a07812fa0cbf62317dbd998adc6c6eba5474925e750b111517cf
ID: L66
TIME: 2026-08-13
LEVEL: 5
TAGS: #cosca-canvas #comfyui #node-editor #visual #drag-drop #sockets #bezier #palette #react #svg #level-5
---
## L66 — 2026-08-13 — COSCA CANVAS: node editor visual estilo ComfyUI, próprio (§21) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "poderia criar um frontend tipo esse https://github.com/comfy-org/comfyui" — o node editor visual. |
| **Technique** | Level 5 — **(1)** `CoscaCanvas.tsx`: node editor visual PRÓPRIO em React + SVG puro (zero lib de node graph — o manifesto: entender princípios, não copiar). **(2)** Paleta de 17 nós por domínio (§30) com cor de domínio: diffusion (image_generation, roxo), imagem (verde), vídeo (vermelho), áudio (âmbar), 3D (ciano), jogo (verde), científico (roxo). **(3)** Nós arrastáveis + sockets conectáveis por drag (curvas bezier) + seleção + inspector de params + deletar + limpar. **(4)** Executa no orquestrador cross-product (/api/engine/x/run §30) com resultados por nó + cache hits (§23). **(5)** Item 'canvas' (⬡) como painel principal de tela cheia. |
| **Level** | 5 |
| **Outcome** | success — canvas visual implementado (556 linhas), build + 5 vitest verdes, rodando na 5173. |
| **Confidence** | 0.94 (validado: build OK, paleta completa, execução ligada ao x/run) |
| **Tags** | #cosca-canvas #comfyui #node-editor #visual #drag-drop #sockets #bezier #palette #react #svg #level-5 |
| **Related** | L242 (diffusion — o nó image_generation está na paleta!), L223 (nodegraph — o canvas serializa para o mesmo JSON), L235 (cross-product — executa via x/run), L218 (ComfyUI era a referência da Fase 0) |
| **Learned** | **(1) O ComfyUI é INSPIRAÇÃO, não cópia: React + SVG puro bastam para um node editor (nós, sockets, bezier, drag) — sem react-flow/lib pesada.** **(2) O canvas serializa para o MESMO JSON do nodegraph (§21) — o visual é uma VIEW dos dados puros; execução via cross-product reusa tudo.** **(3) Diffusion na paleta: image_generation já é um nó arrastável — o ciclo prompt→imagem vira visual.** **(4) A paleta por domínio com cores é o mapa visual do ecossistema §30. |** |
| **Next** | (1) Botão de preview da imagem gerada no nó diffusion. (2) Zoom/pan no canvas. (3) Salvar/carregar grafos do canvas. (4) Nó whisper/legendas quando o Don aprovar. |
