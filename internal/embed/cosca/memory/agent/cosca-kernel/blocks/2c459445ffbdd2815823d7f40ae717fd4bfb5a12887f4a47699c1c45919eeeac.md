# L378 — Fase 2-4 do plano do professor: ambiente limpo + bootstrap + identidade

## status: DECISION (Fases 2-4 concluidas; re-embed NAO executado)

## O que foi verificado (ambiente limpo = clone + HOME virtual + config)
- FASE 2: ambiente recriado do zero (clone sem .cosca, HOME virtual).
- FASE 3: bootstrap OK (memory integrity init + doctor: Memory/Knowledge ✓).
- FASE 4 POSITIVO: config com provider=ollama/model=nomic/digest=0a109f...
  -> o conhecimento do clone aceita (identidade verificada).
- FASE 4 NEGATIVO (o essencial): digest ERRADO no config -> o conhecimento
  do CLONE RECUSA: "embedding identity mismatch ... REFUSING OPERATION" — a
  pinagem esta ativa no ambiente limpo (nao so no ambiente de producao).
- FASE 4b (fail-closed no USO): provider explicito fora (baseURL errado) ->
  erro "all embedding providers failed" — NAO cai no local. Teste limpo no
  pacote embeddings (TestFailClosedInUsage); a tentativa no pacote knowledge
  foi contaminada pelo singleton global do registry (artefato de teste).

## Descoberta adicional
O GenerateEmbedding do registry tem fallback NO USO (r.fallbacks) — mas com
Primary explicito (sem o local nos candidatos), os fallbacks ficam VAZIOS e
o erro do provider e retornado. O fail-closed cobre os 3 pontos: Select
(sem o local), criacao (digest), uso (sem fallbacks).

## Classificacao
MEASURED digest errado recusado no clone; uso com provider fora -> erro
EVIDENCE a Fase 1 esta ativa no ambiente limpo; os 3 niveis do fail-closed
INFERRED um re-embed no ambiente limpo (com config + digest correto) usara
         a identidade verificada e nao caira no local
HYPOTHESIS o re-embed reproduzira o espaco (a confirmar na Fase 6)
UNKNOWN nada bloqueia a Fase 5 alem da autorizacao do Don
DECISION Fases 2-4 OK. Fase 5 (re-embed definitivo) = transformacao que
         requer a autorizacao do Don. Fase 6 (repetir L375) depois.
