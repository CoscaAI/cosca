# L361 — FASE A implementada: slab int16 no indice (validado no corpus limpo)

## O que entrou (decisao do consigliere, "faz o que achar melhor")
1. Serve reiniciado (graceful, kill -TERM) — producao agora reflete o corpus
   limpo (9.706 vetores); doctor OK (Runtime, Health, Knowledge Engine ✓).
2. Slab vecs16 no indexGeneration (FASE A): quantize16 (grade 2^10, pad 32),
   score16/scoreRange16/scoreParallel16 via dot16AVX2 (kernel L347).
   Selecao: int16Enabled (default OFF) > int8Enabled > float32. Config:
   SQLiteVecConfig.EnableInt16 + SetInt16Enabled. RAM do snapshot: +2B/elem
   (9.706 vetores: int16 14.9MB — folga no L3).
3. Re-embed dos orfaos: ADIADO (precisa provider real — test-deterministic
   nao e o nomic; arriscado sem o provider certo).

## Validacao (INDICE REAL, corpus limpo, 100 queries)
int8:  jaccard 0.9335 · recall@10 0.9540
int16: jaccard 0.9890 · recall@10 0.9950   ← quase-oracle CONFIRMADO no
                                            indice real (Go puro previa 0.990)
Suites verdes (vector 43s, knowledge campanha+indice 3s), build/vet OK.

## Classificacao
MEASURED int16 no indice real: jaccard 0.989 / recall@10 0.995
EVIDENCE o int16 e quase-oracle no corpus limpo (bate com L360 Go puro)
INFERRED com o int16 ativo, o contrato de scores muda (aproximacao melhor
         que int8 — recall ~0.995 vs 0.954); RAM +2B/elem
HYPOTHESIS velocidade int16 no L3 (~2 bytes/elem) mantem >40 Mvec/s no
          corpus de 9.7k (a medir)
UNKNOWN impacto de producao do int16 como default (decisao do Don);
         custo do re-embed com provider real
DECISION slab int16 DISPONIVEL (default OFF — promocao e decisao do Don
         com os numeros do indice real); serve reiniciado; re-embed adiado.
         Proxima decisao na mesa: int16 como default?
