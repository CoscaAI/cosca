PREV: e54db808671865056e955c00c99e9b0140d3dd1c22956c9e4a6e9b6cc5e5faad
ID: L226
TIME: 2026-08-14
LEVEL: 5
TAGS: #chaos #avancado #crash-runtime #eventos-atrasados #isolamento #restart #chatWithRetry #level-5
---
## L226 — 2026-08-14 — chaos avançado: crashes de runtime, eventos atrasados, isolamento, restart do orchestrator — achado real (chatWithRetry não é não-cooperativo) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Executar as dimensões que faltavam ao chaos: crashes de runtime, eventos atrasados, contexto conflitante, memória parcial, restart do orchestrator (métricas: deadlocks=0, state_inconsistencies=0). |
| **Technique** | Level 5 — 4 cenários novos (5 testes): crashes de runtime (erro FATAL não-transiente na 1ª chamada — 30 sessões, todas terminais, zero retries); eventos atrasados (provider dorme 200ms ignorando ctx com attempt de 30ms); isolamento de contexto sob carga (25 sessões concorrentes — cross-contamination=0); restart do orchestrator (2 crashes + retomada idempotente via durable). ACHADO REAL no cenário 2: `chatWithRetry` NÃO é não-cooperativo — chama `provider.Chat(attemptCtx)` direto; se o provider ignora ctx e demora, o worker fica preso até responder. O watchdog não-cooperativo (goroutine+select) existe no stallwatch mas NÃO está integrado no executor — dívida registrada. |
| **Level** | 5 |
| **Outcome** | success — 5 testes verdes, race limpo, suítes completas, build OK. Comportamento documentado: crash fatal termina sem retry; resposta atrasada aceita (completed, hung=0); isolamento = 0 contaminação; restart idempotente. Commit `76187d0`. |
| **Confidence** | 0.96 (race limpo com goroutines; suítes completas; build; achado verificado no código — provider.Chat direto em executor.go) |
| **Tags** | #chaos #avancado #crash-runtime #eventos-atrasados #isolamento #restart #chatWithRetry #level-5 |
| **Related** | L323 (chaos escala), L322 (chaos v1), L320 (stallwatch) |
| **Learned** | **(1) O chaos é uma máquina de achados: o cenário de eventos atrasados revelou que o chatWithRetry não é não-cooperativo — o watchdog (goroutine+select) existe no stallwatch mas não foi integrado ao executor; mesma classe de bug do incidente original (provider que ignora ctx), agora com prova e local exatos.** **(2) Erro fatal não-transiente é tratado corretamente: isTransientError rejeita "process crashed" → exatamente 1 chamada, zero retries.** **(3) Isolamento de contexto sob carga: 25 sessões concorrentes → 0 contaminação.** **(4) Restart idempotente com durable: 2 crashes e cada task executa exatamente 1× — o log é a fonte da verdade, o replay nunca re-executa.** **(5) Import cycle pipeline↔orchestration no teste forçou a separação correta de camadas.** |
| **Next** | (1) Dívida: integrar o watchdog não-cooperativo no chatWithRetry. (2) Categoria B (~2,2 pts). (3) Working tree limpa. |
