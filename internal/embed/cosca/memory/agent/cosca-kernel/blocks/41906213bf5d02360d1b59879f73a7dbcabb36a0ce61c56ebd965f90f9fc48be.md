PREV: faf9e2e727361f0dad47ef800991b03591204d266c66be740d1ed453243c10b1
ID: L17
TIME: 2026-08-09
LEVEL: 4
TAGS: #lipsync #kokoro #facefusion #audio-processing #mel-spectrogram #sliding-window #onnx #tts #level-4
---
## L17 — 2026-08-09 — Lip Sync Pipeline: FaceFusion extraído e integrado ao kokoro TTS | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "sim quero" — implementar pipeline de lip sync extraído do FaceFusion integrado ao kokoro TTS |
| **Technique** | Level 4 — Extração direta da arquitetura FaceFusion + implementação Python: (1) AudioProcessor com log compression (100dB→8 unidades), Mel spectrogram (80 bandas), sliding window (16 passos de áudio por frame de vídeo); (2) VoiceExtractor com ONNX opcional (Kim Vocal, UVR-MDXNet); (3) LipSyncPipeline integrado ao kokoro TTS; (4) Fallback resample adaptativo (48kHz/24kHz/16kHz); (5) Adaptive STFT window para áudio curto. |
| **Level** | 4 |
| **Outcome** | success — Módulo `scripts/voice/lipsync/` criado (3 arquivos, ~550 linhas Python). Teste E2E: kokoro TTS "Olá Don, o sistema Cosca está operacional" → 3.90s de áudio → 117 frames de vídeo → espectrograma 80×235 → sliding windows 80×16 por frame. ONNX Runtime 1.28 instalado (CPU). Estrutura pronta para modelos ONNX de lip sync visual (EdTalk/Wav2Lip). |
| **Confidence** | 0.90 (validado: processamento de áudio funciona com tom de teste 440Hz e kokoro TTS real; ONNX Runtime disponível; modelos visuais pendentes de download) |
| **Tags** | #lipsync #kokoro #facefusion #audio-processing #mel-spectrogram #sliding-window #onnx #tts #level-4 |
| **Related** | L157 (FaceFusion patterns), L73 (voz modo rádio), L79 (aquisição adaptativa + voz), L65 (wake word), L61 (Ollama local) |
| **Learned** | **(1) O pipeline FaceFusion é diretamente implementável: extração de voz → espectrograma Mel → sliding window → ONNX inference → paste back. A parte de áudio (nossa implementação) funciona independentemente da parte visual (ONNX).** **(2) kokoro gera áudio float32 a 24kHz mono — precisa de conversão para int16 WAV antes do processamento. O resample 24kHz→48kHz→16kHz introduz artefatos; melhor trabalhar na sample rate nativa.** **(3) O sliding window (80 bandas Mel × 16 passos por frame) é o coração do alinhamento áudio-visual: cada frame do vídeo "ouve" 16 momentos de áudio, criando uma representação temporal densa o suficiente para o ONNX inferir a forma dos lábios.** **(4) A log compression (exp(-5·ln(10)) → log10·1.6+3.2 → clip(-4,4)) preserva sussurros E gritos na mesma escala — essencial para STT robusto e lip sync natural. Nossa pipeline de STT (whisper) pode se beneficiar imediatamente dessa normalização.** **(5) O ONNX Runtime CPU é suficiente para prototipagem; GPU ROCm via onnxruntime-rocm habilitaria inferência em tempo real. 9 providers disponíveis no FaceFusion (CUDA, TensorRT, ROCm, CoreML, OpenVINO, DirectML, MIGraphX, QNN, CPU) — nossa GPU probe (L80) já detecta ROCm, o próximo passo é instalar onnxruntime-rocm.**
| **Next** | (1) Baixar modelos ONNX (EdTalk 256, Wav2Lip GAN 96) dos assets FaceFusion. (2) Instalar onnxruntime-rocm para GPU. (3) Integrar face detection (OpenCV/ONNX) para pipeline visual completo. (4) Criar comando `cosca lipsync` no CLI. (5) `make install` para embed sincronizar. |
