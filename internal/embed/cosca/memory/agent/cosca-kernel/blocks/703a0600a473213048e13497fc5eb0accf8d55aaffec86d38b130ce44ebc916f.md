PREV: 926c8c7df99708bb6e85b7f233acef5a40c5c7878a6d45eb06daf4ba363c25d4
ID: L227
TIME: 2026-08-14
LEVEL: 5
TAGS: #chaos #v4 #restart #continuidade #durable #retomada #zero-perda #level-5
---
## L227 — 2026-08-14 — chaos V4: restart de pipeline em 10/25/50/75/90% do progresso — continuidade, não só resistência à carga | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Testar CONTINUIDADE da execução (não só carga): restart em momentos diferentes do progresso com invariantes duros (zero perda, zero duplicação, zero estado inválido, retomada do ponto correto, retry finito, zero hang). |
| **Technique** | Level 5 — runner de progresso GLOBAL: a 1ª tentativa (crashAfter local) falhou (processo que retoma 10 tasks com crashAfter=15 nunca crashava); correção com `progressCrashRunner` com contador global atômico (o processo morre quando o pipeline atinge X% do progresso). 5 restarts em pontos globais de 20 tasks + processo final. Descoberta: failed events = 50 (não 5) — o recovery loop retenta a task do crash dentro do run (retry legítimo, não duplicação); o invariante duro é `completed == 20`. Teste BEFORE/AFTER: processo 1 completa 3 tasks e morre; após restart, o novo processo completa 10 tasks executando só 7 (t1-t3 replayed, nunca re-executados). |
| **Level** | 5 |
| **Outcome** | success — 2 testes verdes (restart em 5 pontos + equivalência), race limpo, suíte completa, build OK, chain do durable íntegra após 5 restarts (VerifyChain). Commit `12817c0`. |
| **Confidence** | 0.96 (race limpo; VerifyChain após 5 restarts; suíte completa; build) |
| **Tags** | #chaos #v4 #restart #continuidade #durable #retomada #zero-perda #level-5 |
| **Related** | L324 (v3), L323 (v2 escala), L322 (v1) |
| **Learned** | **(1) Testar restart exige contador de progresso GLOBAL: um runner local não simula crash em X% (processos que retomam executam menos tasks); o contador atômico torna o ponto de crash determinístico em relação ao progresso.** **(2) O recovery loop retenta a task do crash dentro do run — é design (falha → recovery → retry até orçamento); o invariante de duplicação aplica-se a COMPLETADAS (exatamente 1 por task), não a falhas.** **(3) BEFORE/AFTER equivalence é o teste mais forte: provar que o estado lógico antes do restart é preservado após (tasks replayed, não re-executadas).** **(4) O valor está em provar SEMÂNTICA sob restart, não em números — invariantes > volume.** |
| **Next** | (1) Escada de carga (V5: 1k+). (2) Dívida: watchdog não-cooperativo no chatWithRetry. (3) Categoria B (~2,2 pts). (4) Working tree limpa. |
