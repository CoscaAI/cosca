PREV: 02a68d896aaea322423a7ac5b53834effefc7ef02ef7463f29fc718a8ffda41f
ID: L415
TIME: 2026-08-17
LEVEL: 5
TAGS: #performance #cpu #serve #rebuild #embedding #gc #knowledge #gargalo #diagnostico #level-5
---
## L415 — 2026-08-17 — Diagnóstico de CPU alta do serve: 9.680 embeddings + 460GB alocados (re-indexação) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Descobrir por que o serve consumiu ~145-160% de CPU e por que afeta tanto a máquina (ordem do Don) |
| **Technique** | Medição multi-fonte (ps/top/uptime, /proc/PID/stat ticks, ss, metrics Prometheus 14121), isolamento de hipóteses (fabric ticker, circadian, watcher, semantic router, RepairOrphanVectors, IndexEntityVectors, provider fallback), contabilidade cruzada (embeddings vs entidades vs vetores vs GC vs HTTP) |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.82 (diagnóstico forte; o gatilho EXATO do rebuild não foi isolado — stderr engolido pela jaula, journal mudo) |
| **Tags** | #performance #cpu #serve #rebuild #embedding #gc #knowledge #gargalo #diagnostico |
| **Related** | L368, L364, L387, L396, L376, L362, L335, L254, L62, L149 |
| **Learned** | 1) **O serve consumiu ~145-160% de CPU sustentado** (load 1.65→3.81) desde que subiu 20:12:43. Morto com kill -TERM (L56, sem pkill) + systemctl stop (impede auto-restart). 2) **Números que fecham (P13)**: 9.680 embedding_requests · 30.655 vetores (vs 51.122 do L364 → -20.467) · 9.110 entidades no grafo · 7.972 GCs · 460GB alocados em 51min (~150MB/s) · apenas 2 requests HTTP (só health) · ollama PARADO. 3) **Veredito**: o serve executou uma **re-indexação em massa** no boot — 9.680 embeddings (≈ entidades + órfãos) com o provider **'local' (TF-IDF esparso, em memória)** — porque o ollama está parado (L376: sem primário → 'local' é candidato no auto-detect). O custo dominante NÃO foi a rede (embedding local é CPU/memória) — foram as **460GB de alocações** que o **GC Go** processou, mantendo a CPU alta mesmo após o embedding terminar. O L387/L396 já provou que rebuild/embedding é o custo dominante da re-indexação. 4) **Por que afeta tanto**: re-indexação inteira + TF-IDF esparso em memória + GC = ~150MB/s de alocação = 1.5+ cores. 5) **O gatilho exato não isolado**: o stderr do processo interno é engolido pela jaula (L62) e o journal do systemd está mudo; nenhum dos suspeitos do código (fabric ticker 5s, circadian restcycle, watcher, semantic router, RepairOrphanVectors via CLI, IndexEntityVectors via CLI) é chamado no Init do serve. Hipóteses remanescentes: algum processo externo chamou /v1/knowledge/index ou sync antes de eu medir; ou o Init invoca rebuild condicional não mapeado. **O conhecimento do banco está PRESERVADO** (473MB, estável após a morte). 6) **Errata parcial**: a correlação 9.680≈9.110 entidades sugere IndexEntityVectors, mas a chamada só existe no CLI — marcada como HIPÓTESE, não fato (P13: conclusão não excede a evidência, L373). |
| **Next** | (a) Religar o serve com stderr visível (COSCA_JAILED=1 temporário) e capturar o log do boot para isolar o gatilho do rebuild; (b) religar o ollama (provider primário) para o índice voltar ao nomic e o 'local' sair do caminho; (c) considerar COSCA_DEV_MODE=false; (d) se o rebuild no boot for confirmado, avaliar desativá-lo (o L364 já deixou o índice pronto — rebuild não era esperado). |
