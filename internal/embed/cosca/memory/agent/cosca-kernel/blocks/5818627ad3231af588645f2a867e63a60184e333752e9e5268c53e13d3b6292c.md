PREV: 48b0eb8791e72011f9eeb38227243fbbea1f34ff9f4ce9c742c34ebe6996edaf
ID: L47
TIME: 2026-08-12
LEVEL: 4
TAGS: #google #skills #agent-skills #github-install #codeload #tarball #zip-slip #mining #skill-registry #ecosystem #level-4
---
## L47 — 2026-08-12 — Mining Google skills: instalador GitHub nativo (owner/repo[/path][@ref]) via codeload tarball → cosca skill install/remove | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don found https://github.com/google/skills (17.8K stars, Apache-2.0) — the official Agent Skills repo for Google products (100+ skills: GKE, BigQuery, Gemini, Cloud Run, WAF pillars). Confirmed the ecosystem (agentskills.io) is the SAME standard already adopted from Anthropic, and that distribution happens via `npx skills add google/skills` and plugin marketplaces (Claude Code / Codex). Don: "minera isso aqui" → implemented the missing channel: installing skills straight from a GitHub repo. |
| **Technique** | Level 4 — **(1)** `internal/skills/install_remote.go` — `InstallFromGitHub(ref, allowRemote)`: parses `owner/repo[/path][@ref]` (codeload tarball https://codeload.github.com/..., branch default HEAD), 15s timeout, 3-redirect cap, 100 MiB cap; extraction into private temp sandbox with zip-slip rejection (absolute paths, ../, symlink/hardlink entries); `singleTopLevelDir` unwraps the `<repo>-<ref>` codeload prefix; `findSkillDirsRecursive` walks NESTED category trees (skills/cloud/gke-basics/ — the old installFromDir only scanned ONE level) and skips dirs that fail the slug check with warnings; every skill installs through the same validated `installStandardSkill` pipeline (containment via resolveWithinRoot). Fail-closed: remote fetch requires explicit opt-in. **(2)** CLI — `cosca skill install <source>` (local dir single/repo OR GitHub ref; --allow-remote flag for network) + `cosca skill remove <name>` (Manager.Remove: deletes from disk via resolveWithinRoot, rejects embedded). **(3)** Local source name derived from base directory, not the raw path (slug validation). |
| **Level** | 4 |
| **Outcome** | success — 13 new tests PASS (-race): parseGitHubRef (valid/invalid refs), extractTarGz (round-trip, traversal rejection, symlink rejection), findSkillDirsRecursive (google/skills nested layout → 3 candidates, invalid slug skipped with warning), InstallFromGitHub requires --allow-remote, installStandardSkill E2E (License/resources/progressive-disclosure GetResource/persistence), Remove (disk + embedded rejection), single-skill dir install, skills-repo install. CLI tests: install no-args error, GitHub requires --allow-remote, local dir install, remove nonexistent. E2E REAL: `cosca skill install google/skills/skills/cloud/gke-basics --allow-remote` downloaded the repo, installed 1 skill, listed with full description and 3130-char instructions. |
| **Confidence** | 0.96 (validado: 13 testes -race, E2E real com google/skills, build + vet limpos) |
| **Tags** | #google #skills #agent-skills #github-install #codeload #tarball #zip-slip #mining #skill-registry #ecosystem #level-4 |
| **Related** | L203 (AWS durable), L199 (mining adk-go), L198 (mining osv-scanner), L192 (mining methodology), L-anthropic-skills (Agent Skills standard adoption) |
| **Learned** | **(1) The Agent Skills standard is now a DE FACTO industry standard — both Anthropic and Google distribute skills in the exact same skill-name/SKILL.md layout (agentskills.io), and the Google repo even bundles plugins = skills + MCP servers. Being compatible with the standard makes cosca a citizen of BOTH ecosystems.** **(2) Distribution over GitHub refs (owner/repo[/path][@ref]) is the ecosystem's install channel (npx skills add) — native support means the Don can pull 100+ Google SRE/cloud skills without leaving cosca.** **(3) Nested category trees (skills/cloud/<skill>/) need a RECURSIVE SKILL.md walk, not the flat one-level scan — the original installFromDir would have missed the entire google/skills layout.** **(4) Tarball extraction must be hardened: zip-slip, absolute paths, symlinks — the archive is untrusted input from the internet.** **(5) Fail-closed opt-in (--allow-remote) keeps the manager's SSRF posture while enabling the network channel explicitly.** |
| **Next** | (1) Plugin format parity: skills + MCP servers as a single installable unit (Claude Code plugin marketplace pattern). (2) `cosca skill search <query>` against GitHub skill indexes / skills.sh catalog. (3) Batch-select install (list available skills in a repo, let the Don pick). (4) Consider bundling a curated Google set (GKE basics + WAF pillars) into the cosca embedded catalog. |
