PREV: 11c8c328067ab772bc2ebdd619d2c3669353de3db0ee2a8488265637e120bfe6
ID: L153
TIME: 2026-08-13
LEVEL: 3
TAGS: #cosca-node #cosca-connector #grpc #eventos-reais #realidade #level-3
---
## L153 — 2026-08-13 — COSCA NODE: Cosca Connector (eventos REAIS via gRPC) — REALIDADE antes de tudo | Level 3

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" — plugar o Cosca Connector (a prioridade nº1 do spec: REALIDADE > CLAREZA). O cérebro deve mostrar o Cosca REAL, não simulação. |
| **Technique** | Level 3 — **(1)** `internal/connector/connector.go`: conecta ao runtime REAL do Cosca (gRPC 14123, `insecure.NewCredentials`), cria `RuntimeServiceClient`/`KnowledgeServiceClient`/`MemoryServiceClient` (mesmos stubs `api/grpc/pb` que o Neural Link usa — L192). **(2)** Poll a cada 3s: `Status` (estado/health), `Knowledge.Stats` (document_count) e `Memory.Stats` (record_count por layer) — detecta MUDANÇAS e emite eventos reais: `runtime_connected`, `runtime_status`, `runtime_offline` (falha real), `knowledge_indexed` (+N docs), `memory_stored` (+N records). **(3)** `metadata.simulation=false` EXPLÍCITO em todo evento real — nunca se mistura com simulação (seção 36). **(4)** `-mode real` flag no main; a simulação e o connector compartilham o MESMO `emit` (apply → store → publish). **(5)** Verificado: modo real emite `runtime_status (running, healthy)` com `sim=False` contra o runtime real do Cosca. |
| **Level** | 3 |
| **Outcome** | success — O COSCA NODE agora mostra o Cosca REAL: conecta ao runtime gRPC, observa status/conhecimento/memória e emite eventos reais (simulation=false). A prioridade do spec (REALIDADE) está atendida — o cérebro reflete a atividade real, não uma ilusão. |
| **Confidence** | 0.90 (validado: build Go limpo, modo real emite runtime_status real com sim=False contra o runtime de pé) |
| **Tags** | #cosca-node #cosca-connector #grpc #eventos-reais #realidade #level-3 |
| **Related** | L251 (Time Travel), L247 (fundação), L192 (Neural Link — mesmos stubs gRPC), spec seção 36/51 |
| **Learned** | **(1) O padrão "contrato gRPC compartilhado" (L192) reaparece: o Cosca Node importa `api/grpc/pb` (não internal/) com `replace` no go.mod — o MESMO cliente que o Neural Link usa, zero duplicação, zero mudança no kernel.** **(2) `metadata.simulation=false` explícito (não implícito) é o que garante a separação REAL vs SIMULAÇÃO — o frontend decide o selo pelo campo, nunca por ausência.** **(3) O connector é um observador passivo: poll de stats + detecção de mudança (delta de counts) — sem watch do Cosca, o poll é o contrato mínimo viável para eventos reais.** **(4) `go mod tidy` removeu os requires do grpc/cosca antes do connector existir (nada importava) — ao adicionar o connector, o `go mod tidy` os re-adicionou; o ciclo build→tidy→build é a sequência certa ao introduzir uma nova dependência de módulo.** **(5) Detalhe operacional recorrente (L251): `pkill` + restart no MESMO comando estoura o timeout do shell e mata o daemon; subir num comando dedicado e verificar noutro.** |
| **Next** | (1) Fase 3: Path Tracing + Causal Explorer (seções 23/24). (2) Wails desktop. (3) Performance (instancing/culling). (4) Trigger real: rodar `cosca run`/`eval` e ver os eventos reais de EXECUÇÃO (não só stats) percorrerem o cérebro — o "teste definitivo" da seção 51. |
