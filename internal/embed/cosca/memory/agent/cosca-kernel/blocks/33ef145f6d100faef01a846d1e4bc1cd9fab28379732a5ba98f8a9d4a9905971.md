# L397 — E-007: FTS incremental vs rebuild — EQUIVALENTE (VERDICT PASS)

## status: CONFIRMED (read-only; autorizado: "AUTORIZO E-007 — READ_ONLY")

## A pergunta: "O FTS incremental produz resultado equivalente ao rebuild
## completo?" — RESPONDIDA: SIM (com evidencia)

## Metodo (E-007 — o contrato da campanha)
3 operacoes (insert/update/delete) x 2 abordagens (rebuild completo vs
incremental — o MESMO SQL das funcoes fts.go:264-310 — IndexDocument/
IndexChunk/RemoveDocument), db de teste (copia), as mesmas condicoes.
Equivalencia: contagem (4 tabelas FTS), conteudo (token achavel), ranking
(query do baseline 'vector' — top-5) e tempo.

## Resultado (MEDIDO)
| operacao | rebuild | incremental | speedup | counts | token | ranking |
| insert   | 701ms   | 10.2ms      | 69x     | =      | =     | 5/5     |
| update   | 676ms   | 7.1ms       | 95x     | =      | =     | 5/5     |
| delete   | 698ms   | 121us       | 5769x   | =      | =     | 5/5     |
O incremental produz o MESMO indice (conteudo, contagem, ranking) em uma
fracao do tempo. O rebuild de ~700ms por operacao vira ~ms.

## VERDICT: PASS
A equivalencia e demonstrada nas 3 operacoes do comportamento suportado.
O FTS incremental e uma transformacao candidata com evidencia forte.

## RISK (a registrar — o guardiao da transformacao)
- o teste usou o SQL direto (equivalente ao das funcoes) — a integracao
  real (o indexer chamando as funcoes no storeDocument) e a transformacao
- o recall COMPLETO (as 20 queries do baseline no indice incremental vs
  rebuild) deve ser a verificacao pos-transformacao (o guardiao ANTES/DEPOIS)
- as funcoes incrementais nunca foram usadas em producao (codigo morto) —
  o caminho de integracao precisa do teste de regressao
- a nao ser que o Don autorize, NAO implementar

## NEXT DECISION
TRANSFORMACAO CANDIDATA registrada na campanha: FTS incremental no
storeDocument + eliminar o RebuildIndex do IndexDirectory (o custo
dominante da re-indexacao — E-006). Aguardando AUTORIZACAO do Don.
O guardiao: recall@10 das 20 queries do baseline ANTES/DEPOIS + counts +
ranking — REJECT se qualquer invariant quebrar.

## Classificacao
MEASURED tempos (69x/95x/5769x) + equivalencia (counts/token/ranking 5/5)
EVIDENCE as 3 operacoes identicas entre rebuild e incremental
INFERRED o FTS incremental elimina ~95-99% do custo da re-indexacao
HYPOTHESIS o recall completo (20 queries) permanece apos a integracao (a
  verificar na transformacao — o guardiao)
UNKNOWN o caminho de integracao real (o indexer chamando as funcoes)
DECISION candidata registrada; NAO implementar sem autorizacao
