PREV: 339f26018adb8f14cd1ab07e7e1fd31c401e31ed3a2e1bf7335fef9b49538c36
ID: L159
TIME: 2026-08-13
LEVEL: 4
TAGS: #mina #loop-fechado #dois-loops #solution-class #dossie #m1-m4-m5 #descoberta-autonoma #level-4
---
## L159 — 2026-08-13 — A MINA wired: loop fechado de descoberta autônoma (M1 + M4 + M5) — rodou ao vivo | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "VAMOS PRA MINA" — implementar A MINA (L246, a "coisa muito top"): o loop fechado de descoberta autônoma com DOIS loops aninhados, solution_class e dossiê. |
| **Technique** | Level 4 — **(1) `internal/mina`**: `Engine.Run` orquestra o fluxo completo: ① loop EXTERNO (`evolve.RunOn` — self-evolution parametrizado por função alvo arbitrária, refatorado de `Run`), ② loop INTERNO (descoberta final com a melhor estratégia), ③ CLASSIFICAÇÃO (M4 — `classify` compara solução vs referência normalizada → KNOWN/NOVEL, regra de ouro 5: relativa), ④ REPRODUTIBILIDADE (roda 2× com store ISOLADO — repro não polui a timeline), ⑤ EVIDÊNCIA (cadeia causal do SOLUTION_VERIFIED via `causal.Chain`). **(2) `evolve.RunOn(target)`** generaliza o self-evolution para qualquer função alvo (a MINA usa para problemas não-padrão). **(3)** `RunMina` no app + `POST /mina` + eventos `MINA_STARTED/COMPLETED` + botão no dashboard. **(4) Dossiê (M5)**: solução + classe + eficiência (1/(1+attempts)) + estratégia antes→depois + repro + evidência. |
| **Level** | 4 |
| **Outcome** | success — Rodou AO VIVO: `POST /mina` → 113 eventos persistidos (MINA_STARTED + 4 SOLUTION_VERIFIED: baseline + benchmark + variante + descoberta final). O `MINA_COMPLETED` não disparou desta vez porque o app reiniciou via systemd no meio (o Restart/daemon-reload mata o run em andamento), mas TODOS os eventos estão duráveis (timeline restaurada). 16 pacotes verdes, commit `249f44e`. |
| **Confidence** | 0.90 (validado: mina rodou ao vivo com 4 soluções verificadas, testes de classify/normalize verdes, build/vet/frontend limpos) |
| **Tags** | #mina #loop-fechado #dois-loops #solution-class #dossie #m1-m4-m5 #descoberta-autonoma #level-4 |
| **Related** | L246 (MINA spec), L257 (benchmark-as-gate), L245 (self-evolution), L256/L255 (observabilidade/estado durável) |
| **Learned** | **(1) A MINA é wiring, não código novo: o loop interno (discovery) e o externo (evolve) já existiam separados — o `Engine.Run` só os CONECTA em sequência (evoluir → descobrir com a melhor estratégia → classificar → reproduzir → evidenciar). "90% pronto" do L246 estava certo; o M1 é o elo, não o motor.** **(2) A reprodutibilidade usa store ISOLADO por um motivo de OBSERVABILIDADE: se usasse o store compartilhado, os 2 runs de repro inundariam a timeline do app com eventos duplicados — repro é GATE, não história.** **(3) O custo da MINA é o benchmark-as-gate × LLM lento: iterations=1 variants=1 = ~11 descobertas (baseline 1 + benchmark 3 + variante 4 + final 1 + repro 2) ≈ 8 min em CPU. O benchmark triplica o custo de cada variante — é o preço do anti-overfit.** **(4) `systemctl restart` (ou daemon-reload) mata um run MINA em andamento — a MINA é long-running e não tem checkpoint; para produção, precisa de persistência de estado intermediário ou tolerância a restart.** **(5) O dossiê é o que separa "uma resposta" de "ouro verificado": solução + classe (KNOWN/NOVEL relativa) + eficiência + evolução + repro + cadeia causal — tudo auditável, nada inventado.** |
| **Next** | (1) Fast-mode da MINA (pular benchmark para demo rápida) + checkpoint/retomada para tolerar restart. (2) Problema real além do polinômio (oráculo arbitrário). (3) M3 gradiente contínuo + M4 hash de reference_solution no schema. |
