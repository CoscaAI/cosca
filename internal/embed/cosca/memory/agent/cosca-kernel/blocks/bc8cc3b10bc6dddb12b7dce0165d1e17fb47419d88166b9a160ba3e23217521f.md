PREV: c1717345573f7fe24cf832311ee800e89c30cc810a60bf56fdd075c3b5f3ed9f
ID: L27
TIME: 2026-08-11
LEVEL: 4
TAGS: #integrity #git-anchor #auto-sign #family-chain #git #blockchain #signauto #no-passphrase #level-4
---
## L27 — 2026-08-11 — Family Chain: auto-sign ancorado no hash de commit do git elimina passphrase | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don requested auto-signing of the family chain using git hash — "cada commit usar hash de assinatura anterior e valida externa no git se mudar ja identifica que nao valida o commit". |
| **Technique** | Git-Anchored Chain — new block fields GIT_COMMIT/GIT_TREE/GIT_ANCHORED/GIT_AUTHOR; SignAuto() signs WITHOUT passphrase (the git commit hash IS the cryptographic proof, cannot be forged); SignAfterLearning now passphrase-free; Check() validates: latest block commit pin (GIT COMMIT MISMATCH), tree pin (GIT TREE MISMATCH), historical commit existence via git cat-file (GIT COMMIT REWRITTEN/REBASED); watchdog reports GitAnchor status. |
| **Level** | 4 |
| **Outcome** | success — chain now 8 blocks (4 Ed25519 + 4 git-anchored), 1282 files, valid. Tests 8/8 pass. Flow validated end-to-end: edit file → check detects change → --sign-auto creates new block → chain valid without passphrase. make install done. |
| **Confidence** | 0.97 (validado: 8/8 testes, E2E de --sign-auto, chain 8 blocos verificada) |
| **Tags** | #integrity #git-anchor #auto-sign #family-chain #git #blockchain #signauto #no-passphrase #level-4 |
| **Related** | L181 (assinatura Go nativa), L174 (auditoria enterprise — segurança) |
| **Learned** | Root cause solved: every learning registration edited learnings.md and broke the Ed25519 chain (needed Don's passphrase, AES-256-GCM encrypted key) → startup blocked (TAMPERED, fail-closed) → crash loop (7350 restarts). Lesson: the git repo is a natural blockchain (chained commit hashes, immutable, remote witness). Using the commit hash as the signature removes the passphrase bottleneck while keeping cryptographic strength. The remote (only the Don pushes) remains the ultimate authority. |
| **Next** | commit the new embed + code to git; consider auto-commit after SignAuto (or rely on Don's commit flow). |
