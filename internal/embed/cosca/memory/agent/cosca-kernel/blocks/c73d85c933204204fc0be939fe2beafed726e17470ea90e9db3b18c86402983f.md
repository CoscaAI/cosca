PREV: 03423671bd133c219d83afecc6251622819fbdb4d03dfa15f22d8cbebb52a5c9
ID: L222
TIME: 2026-08-14
LEVEL: 5
TAGS: #stallwatch #endurecimento #nao-cooperativo #jitter #backoff-cap #retryable #level-5
---
## L222 — 2026-08-14 — endurecimento do stallwatch: operação não-cooperativa, jitter+cap, retryable predicate — 3 cenários A/B mantidos verdes | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Endurecer o watchdog sem quebrar os 3 comportamentos provados (saudável→0 falso positivo; recuperável→recuperação automática; morto→falha em tempo finito), mantendo o A/B como régua de regressão. |
| **Technique** | Level 5 — auditoria das fraquezas reais da v1 (mudanças cirúrgicas): (a) `Watch` chamava `fn(attemptCtx)` direto — provider que ignora cancelamento de ctx travava o watchdog; (b) backoff exponencial sem cap/jitter (thundering herd); (c) executor registrava `Provider: "provider"` hardcoded; (d) sem `Retryable` — erro permanente queimava retries. Endurecimento: `runAttempt` executa fn em goroutine + select no deadline (recupera o controle mesmo se a fn nunca retorna); `backoffWithJitter`; `WatchSpec.Retryable`; `MaxBackoff`; executor passa nome/modelo reais do provider. |
| **Level** | 5 |
| **Outcome** | success — A/B original 3/3 verdes, stallwatch 92,5% de cobertura, race limpo, Global Suite 130 ok/0 fail. Commit `f4fe3f9`. |
| **Confidence** | 0.96 (A/B original re-executado verde; race limpo; Global Suite; build) |
| **Tags** | #stallwatch #endurecimento #nao-cooperativo #jitter #backoff-cap #retryable #level-5 |
| **Related** | L319 (watchdog v1 + E2E), L318 |
| **Learned** | **(1) A falha real da v1 era depender do cooperativo: provider que ignora ctx trava qualquer implementação que espera a fn retornar — a solução é goroutine + select no deadline.** **(2) Race detector pegou o efeito colateral: a goroutine vazada continua rodando após o retorno — teste precisa de atomic no contador compartilhado (custo aceitável documentado).** **(3) Retryable predicate é anti-waste: erro permanente falha em 1 tentativa em vez de queimar retries.** |
| **Next** | (1) Anexar Collector na execução real e registrar Report (A/B definitivo com números de produção). (2) Working tree limpa. |
