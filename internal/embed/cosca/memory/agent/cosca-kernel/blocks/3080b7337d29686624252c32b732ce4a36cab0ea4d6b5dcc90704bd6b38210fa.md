PREV: e16126246b626ece5f2ebfa72425213d9ebc8c7ea3b540118221787a5f6d07d5
ID: L309
TIME: 2026-08-16
LEVEL: 5
TAGS: #mineracao #imagem #video #gpu #opencv #ffmpeg #vulkan #wgpu #mat #t-api #filtergraph #hal #tracker #level-5
---
## L309 — 2026-08-16 — MINERAÇÃO Imagem/Vídeo/GPU: OpenCV + FFmpeg + Vulkan + wgpu — Mat COW, filtergraph, T-API e HAL em 3 camadas | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Mineração de 4 projetos de imagem/vídeo/GPU para o Visual Engine do Cosca (RX 6700 XT, ROCm/Vulkan) |
| **Technique** | Arqueologia arquitetural → dado canônico COW, grafo de filtros, dispatch transparente CPU/GPU, HAL em camadas |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.88 |
| **Tags** | #mineracao #imagem #video #gpu #opencv #ffmpeg #vulkan #wgpu #mat #t-api #filtergraph #hal #tracker #level-5 |
| **Related** | L306, L307, L308, L250, L251 |
| **Learned** | (1) OpenCV: Mat = header (dims/dtype/strides/refcount) + ponteiro dados — copiar Mat copia só header, clone() copia dados; ROI sem cópia; WRITABILITY/COW (escrever só quando refcount==1, make_writable duplica on-demand) — FUNDAMENTAL para o node graph não clonar frames; T-API (Transparent API) — dispatch runtime escolhe CPU/SIMD/OpenCL com fallback silencioso (o desacoplamento exato do Professor); UMat (memória heterogênea sem ponteiro host acessível — transferências explícitas, fail-closed); cv::Algorithm (factory+params); G-API (grafo declarativo + backends por nó, otimização em nível de pipeline: 1.5GB→750KB). Algoritmos: SIFT/ORB, Canny, GrabCut/watershed, Lucas-Kanade/Farneback. (2) FFmpeg: FILTERGRAPH = o irmão do nosso node graph (AVFilter=nós, AVFilterLink=arestas tipadas, AVFilterPad=portas); format negotiation por referência compartilhada (interseção in-place, retentável EAGAIN); frame scheduling por estado (filter_frame push / request_frame pull / activate() com NOT_READY); FIFOs limitadas = backpressure natural; pipeline de estágios com tipos distintos (demux→dec→filter→enc→mux; packets vs frames). (3) Vulkan: command buffers + queues + submit; state objects pré-compilados (pipelines/descriptors); barreiras por estágio de pipeline (srcStage cedo, dstStage tarde — barreira cirúrgica = +13% frame time vs flush total); aliasing de memória (reuso intra-frame); frames-in-flight (nunca WaitIdle); specialization constants; multi-threaded recording com secondary buffers. (4) wgpu: HAL em 3 CAMADAS — wgpu-hal (fino, unsafe, por-backend) → wgpu-core (validação bullet-proof + lifetime via refcount + TRACKER que gera barriers automaticamente no submit — o usuário NUNCA toca em barrier) → API idiomática (device/queue/buffer/texture/bind group/pipeline/pass — vocabulário WebGPU). Boas práticas: 1-5 submits/frame, recursos coalescidos, staging pool reciclado, bind groups por frequência, middleware pattern new()/prepare()/render(pass externo). |
| **Next** | (1) Dado canônico de imagem Go: Image{Header{Dims,Format,Stride,RefCount} + Data []byte} com COW (MakeWritable duplica no 1º escritor) — resolve memória do procgen sem cópias. (2) Node graph = filtergraph: pads tipados, format negotiation por interseção, activate() por estado, FIFOs limitadas como backpressure. (3) T-API para backend desacoplado: dispatcher por op escolhe CPU→SIMD/ROCm→Vulkan com fallback silencioso; kernel/shader cache. (4) Camada GPU em 3 camadas estilo wgpu (hal fino → core com Tracker → API idiomática) sobre Vulkan RADV (ganha ROCm de graça); não Vulkan direto na cara do node graph (sync manual = 80% do código). (5) Frame loop: pools de command buffer, 2-3 frames in-flight, 1-5 submits, staging pool. (6) Mídia via libav (demux→dec→ops→enc→mux), frames como *Image COW. NÃO copiar: milhões de LOC em C/C++, sync manual do Vulkan, reinventar codecs. |
