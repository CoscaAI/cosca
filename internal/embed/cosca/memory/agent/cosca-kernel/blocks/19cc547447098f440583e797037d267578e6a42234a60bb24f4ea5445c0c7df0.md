# L396 — E-006: decompor o custo da re-indexacao (perfil de CPU) — CONFIRMADO

## status: CONFIRMED (read-only; dentro do contrato da campanha PERF-2026-001 —
## mode READ_ONLY, experimentos autonomos pelo CAMPAIGN_PROTOCOL §10)

## A pergunta (E-006): "onde exatamente os ~6.5s da re-indexacao sao gastos?"

## Metodo (E-006)
pprof de CPU no IndexDocument (re-indexacao de documento ALTERADO — 1 linha,
o cenario recorrente), db de teste, provider nomic, 6.52s de parede.

## Resultado (MEDIDO — o perfil)
- re-indexacao: 6.52s (o mesmo custo da fria — confirma E-005)
- SQLite/VDBE (_sqlite3VdbeExec): 98.20% cum — o custo e QUASE TODO do motor
  SQLite (o FTS rebuild + o graph persist + os INSERTs)
- Syscall6 (I/O do db): 38.96% flat
- _sqlite3BtreeNext (scan das arvores B): 44.37% cum
- _moveToLeftmost (deslocamento do scan): 41.44% cum
- _readDbPage (leituras de paginas): 36.49% cum
- o embed (ollama/HTTP) NAO aparece no perfil da CPU (o tempo de espera) —
  o custo de parede do embed (~1.8s, E-005) e separado

## VEREDITO: CONFIRMED
O FTS rebuild (a re-tokenizacao de 62k chunks — RebuildIndex) + o graph
persist sao o custo DOMINANTE da re-indexacao — ~98% do tempo de CPU e o
motor SQLite processando o rebuild (scans + I/O + reescrita). O achado #2
do L387 confirmado com perfil de CPU.

## Conclusao limitada
- a decomposicao: SQLite/FTS rebuild ~98% (CPU) + embed ~1.8s (parede)
- o proximo passo (E-007): testar se o FTS incremental (as funcoes existem —
  fts.go:264-291, codigo morto) produz resultado EQUIVALENTE ao rebuild
  completo — a transformacao candidata com o guardiao de regressao

## Classificacao
MEASURED 6.52s; VDBE 98.2% cum; BtreeNext 44%; syscalls 39%; readDbPage 36%
EVIDENCE o perfil de CPU (cpu-e006.prof) — o FTS rebuild domina
INFERRED o FTS incremental eliminaria ~90% do tempo da re-indexacao
HYPOTHESIS o FTS incremental e equivalente ao rebuild (a testar em E-007)
UNKNOWN o custo exato do graph persist vs o FTS rebuild (sub-decompor se
  necessario)
DECISION E-006 CONFIRMED; E-007 (FTS incremental — equivalencia) proposto
