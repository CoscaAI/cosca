PREV: 7993da71c4084a7454a615229c25bc6084d865cec39f87661fdc23f446b61e00
ID: L116
TIME: 2026-08-13
LEVEL: 4
TAGS: #neural-link #knowledge-graph #multi-agent #cognitive-manager #cognitive-score #provenance #epistemic #f2-f3-f4 #level-4
---
## L116 — 2026-08-13 — Neural Link F2+F3+F4: knowledge graph, multi-agent, cognitive manager, score vetorial | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "continue" — avançar o Neural Link além da F0/F1 para as fases seguintes do roadmap: F2 (knowledge graph + novelty detection), F3 (multi-agent + cognitive manager + blind mode + multiple runs), F4 (métricas completas + cognitive score). |
| **Technique** | Level 4 — **(1)** Refatoração do engine para produzir `Experiment` e `Result` como entidades de primeira classe (seção 6) no `RunResult` — pré-requisito para o grafo de proveniência. **(2)** F2: pacote `internal/graph` com nós/arestas tipados (12 `LinkType`), `ChainOf(id)` que reconstrói a cadeia de proveniência via DFS reverso sobre arestas de origem (SUPPORTS/DERIVED_FROM/TESTED_BY/GENERATED/CAUSED), e `Contradictions()` que detecta evidências conflitantes; builder `loop.BuildGraph(run)` no package loop (evita import circular graph→loop). **(3)** F3: `loop/team.go` (`Team` roda N solvers independentes e compara — `IndependentEvidence` só é true se todos convergiram E cada um se auto-verificou; codifica "consenso sem evidência independente NÃO é prova" da seção 10) + `loop/manager.go` (`Manager.Observe` emite orientações de processo — repetição, desperdício, falta de verificação, conclusão prematura, excesso de confiança — sem entregar a solução). **(4)** F4: `loop/metrics.go` com métricas completas (seção 17: tempo, rejeição, refutação, alucinação, cobertura, reprodutibilidade) + `CognitiveScore` como VETOR de 5 dimensões (não uma nota única). **(5)** Exposição no app.go: `RunTeam`, `Graph`, `ManagerOrientations` + `cognitive_score` no `DiscoveryResult`. **(6)** Frontend: score bars, graph nodes/edges (pills por tipo), orientações do manager. |
| **Level** | 4 |
| **Outcome** | success — F2/F3/F4 integradas e testadas: smoke test mostra single-agent (descobre f(x)=x² VERIFIED), knowledge graph (22 nós/arestas + cadeia de proveniência reconstruída: experiment→result→hypothesis), cognitive manager (0 orientações = processo saudável). 4 pacotes de teste verdes (domain, eventstore, graph, loop — incl. `TestTeamConvergesIndependently` com 3 agentes convergindo com evidência independente). Binário 21MB rebuildado, gofmt limpo, go vet limpo. |
| **Confidence** | 0.94 (validado: smoke test completo, testes verdes, build+vet+gofmt limpos, frontend builda sem erro de tipo) |
| **Tags** | #neural-link #knowledge-graph #multi-agent #cognitive-manager #cognitive-score #provenance #epistemic #f2-f3-f4 #level-4 |
| **Related** | L192 (Neural Link F0+F1), L191 (wails), L188 (identidade=texto), README.md/ADR.md do projeto |
| **Learned** | **(1) Import circular Go: graph↔loop — a solução é o builder viver no package que conhece os TIPOS de runtime (loop) e o graph ser só tipos puros (Graph/ChainOf/Contradictions); `loop.BuildGraph(res) *graph.Graph`.** **(2) Proveniência navegável exige entidades de primeira classe: Experiment/Result com ID estável no RunResult (não só eventos) — só assim as arestas TESTED_BY/GENERATED/SUPPORTS conectam nós reais.** **(3) `ChainOf` = DFS reverso sobre arestas incoming de origem, com `visited` para quebrar ciclos (o grafo hipótese↔experimento↔resultado é naturalmente cíclico).** **(4) "Consenso sem evidência independente NÃO é prova" vira código: `Comparison.IndependentEvidence = converged && allVerified` — se 2 agentes concordam mas só 1 verificou, o consenso é marcado fraco.** **(5) Cognitive Manager é observador, não resolvedor: emite orientações de processo (repetição/desperdício/falta-verificação) sem tocar na solução — mantém o agente honesto sem dar a resposta.** **(6) Cognitive score é vetorial (5 dimensões independentes 0..1), nunca um escalar — reduzir inteligência a uma nota é erro de design (seção 17).** **(7) gofmt -w antes de entregar: alinhamento de structs com campos de nomes diferentes (TimeToFirstValidHypothesis etc.) ficou desalinhado no write manual — o gofmt corrige e revela o que o olho não vê.** |
| **Next** | (1) F5: API limpa REST (`/experiments`, `/challenges`, `/hypotheses`, ...) + React Flow para o neural graph interativo + playback temporal dos eventos. (2) F6: safety DETECT/ALERT/HALT/AUDIT + replay de experimento (seção 16). (3) Plugar solver LLM (provider do Cosca) no lugar do PolynomialSolver. (4) Human baseline (seção 20): registrar solução humana de referência e comparar. (5) git init + commit inicial (aguardando ordem do Don). |
