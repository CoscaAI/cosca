PREV: 381723116173dcab
ID: L426
TIME: 2026-08-18
LEVEL: 5
TAGS: #oraculo #gate #serve #integracao #validacao #fronteira #cofre #level-5
---
## L426 — 2026-08-18 | O ORÁCULO NO SERVE: Gate valida toda request na fronteira do Cofre | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Fechar o segundo GAP da auditoria: ORACLE (Gate) existia com 18 testes mas o serve não o usava — nenhuma request era validada semanticamente |
| **Technique** | (1) `RunHandler.oracleGate` inicializado no construtor (`oracle.NewGate()`). (2) `Execute()` avalia `SemanticPackage{Intent: "responder ao prompt", Result: req.Prompt, Source: "external", Context: "request via API"}` ANTES de qualquer execução — o Oráculo na fronteira do Cofre (§0). (3) REJECT → HTTP 400 com motivo + audit log; ACCEPT/CAVEAT → fluxo normal. (4) Teste `TestRunHandler_HasOracleGate`: request normal passa (nunca REJECT), pacote vazio é bloqueado. (5) BUG de teste: token JWT com espaços no JSON do header era rejeitado (401) — JWT precisa ser compacto (separators=(',',':')); o secret rotacionado (L421) também invalidou tokens antigos. |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.95 |
| **Tags** | #oraculo #gate #serve #integracao #validacao #fronteira #cofre #level-5 |
| **Related** | L425 (SEARCH conectado), L422/L423 (ORACLE), L421 (rotacao JWT), ORACLE_PROTOCOL §0/§14, auditoria da casa (GAP: serve sem validação) |
| **Learned** | (1) Os dois GAPs da auditoria estão fechados: SEARCH conectado à busca (L425) e ORACLE conectado ao serve (L426). (2) O Gate no serve é fail-closed mas não bloqueia requests legítimas: prompt normal → ACCEPT_WITH_CAVEAT (confiança não declarada), nunca REJECT. (3) O Oráculo agora é a fronteira real do Cofre: toda request que entra passa pela validação semântica antes de tocar o pipeline. (4) Lição de teste: JWT deve ser compacto (sem espaços) — headers com espaço são inválidos e dão 401 silencioso. (5) A cadeia completa: SEARCH (busca sem lixo) → ORACLE (validação semântica) → serve (fronteira). |
| **Next** | Conectar o filtro ao `cosca knowledge search` (knowledge.go tem fluxo próprio, separado do search.go). Considerar o MemoryCheck real do Gate (contra chain/merkle verificados). Atualizar o mapa de 32→34 no COSCA_PROTOCOL. |
