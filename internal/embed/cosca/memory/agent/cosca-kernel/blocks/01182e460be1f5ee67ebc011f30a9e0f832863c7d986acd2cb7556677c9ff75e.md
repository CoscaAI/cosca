PREV: d6865b580cd4230ffa912c521da2e1a429d87a8225fa06e8bbec93092bba210e
ID: L55
TIME: 2026-08-12
LEVEL: 4
TAGS: #desktop #dev-mode #wails-dev #hot-reload #devtools #upx #producao-vs-dev #level-3
---
## L55 — 2026-08-12 — `cosca desktop --dev`: modo desenvolvimento (wails dev) com hot-reload + devtools — build de produção vs dev explicado | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don mostrou a config do wails build (production: obfuscated=false, compress=false, devtools=false) e perguntou: "essa config ta pra producao e nos estamos em developer ainda certo? quer que instale o upx?" → expliquei produção vs dev, recomendei NÃO instalar upx agora (dev: descompressão em RAM + stack trace turvo = contra o rastro de erro L211; upx é para release) e propus `cosca desktop --dev`. Don: "sim". |
| **Technique** | Level 4 — **(1)** `internal/cli/desktop.go`: flag `--dev` no comando desktop → `runDesktopDev()` executa `wails dev -tags webkit2_41` em FOREGROUND no root do projeto (Ctrl+C para parar), com COSCA_WORKDIR apontando para a pasta alvo (o dev-server do Wails também honra a env no engine). Modo produção (default) continua: auto-build + binário instalado + setsid. **(2)** Teste: TestDesktopDevFlagRegistered (flag existe, default false = produção). **(3)** E2E: `cosca desktop --dev` → "hot-reload ativo" + Wails v2.14 + go mod tidy + bindings + frontend deps + tsc && vite build (exit 124 = timeout do teste, esperado — o processo é foreground interativo). |
| **Level** | 3 |
| **Outcome** | success — Build+vet limpos, teste PASS, E2E validado (wails dev sobe completo). `make install` feito. Comando instalado: `cosca desktop --dev`. |
| **Confidence** | 0.95 (validado: E2E wails dev completo, teste da flag, build limpo) |
| **Tags** | #desktop #dev-mode #wails-dev #hot-reload #devtools #upx #producao-vs-dev #level-3 |
| **Related** | L211 (rastro de erro), L208 (desktop global), L207 (fixes desktop) |
| **Learned** | **(1) wails build = produção (sem hot-reload, sem devtools, 40s por mudança); wails dev = desenvolvimento (hot-reload, devtools do WebKit, recarga na hora) — o fluxo dev NUNCA deve rebuildar produção.** **(2) UPX é para a hora do release, não do dev: comprime 94MB→35MB mas descomprime em RAM a cada start (mais lento) e pode turvar stack traces — contraproducente enquanto se caça crash; instalar só quando distribuir.** **(3) `wails dev` é interativo/foreground (Ctrl+C para parar) — um launcher `--dev` não deve dar setsid/detach; o Don roda no terminal e fica nele.** **(4) O fluxo "produção por default, --dev explícito" é o padrão certo: `cosca desktop` abre o cockpit real, `cosca desktop --dev` abre o laboratório.** |
| **Next** | (1) Commit + push (desktop ~18 arquivos + L211/L212 no cosca). (2) Quando for distribuir o desktop: sudo apt install upx + wails build -upx -clean. (3) Observar se o crash M[0] reaparece — o log ~/.cosca/logs/desktop-errors.log vai contar a história. |
