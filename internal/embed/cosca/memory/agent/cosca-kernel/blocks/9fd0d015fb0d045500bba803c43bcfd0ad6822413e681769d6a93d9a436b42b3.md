# L356 — Implementacao da correcao A (parser markdown, CommonMark)

## O que foi implementado (aprovado pelo Don)
internal/markdown/markdown.go: Parser.GroupConsecutiveParagraphLines (flag
REVERSIVEL, default true) — no default: do loop, agrupa linhas de paragrafo
consecutivas sem blank line em UM no (padrao CommonMark). A funcao
looksLikeMarkdownBlock impede absorver heading/code/table/list/blockquote.
Corrige a fragmentacao por linha (L354) na raiz (a fabrica).

## Validacao (OLD vs NEW)
Caso critico JSON 30KB indentado:
  parser: OLD=1.802 nos -> NEW=1 no
  CHUNKER (pipeline completo): OLD=1.500 chunks (1.200 <=25 chars) ->
  NEW=9 chunks (0 <=25) — exatamente o alvo da L355 (~9).
Casos de seguranca (11): nenhum conteudo perdido; fronteiras preservadas
(heading/code/lista/blank lines); markdown normal sem regressao.
Regressao real: json-indent 8->1, texto-tecnico 8->1, md-paragraphs 7->5,
md-misto 6->5 (code/lista/heading preservados); missing=0, tipos-perdidos=[].
Suite existente: markdown/chunker/knowledge (incl. campanha) VERDE; build e
vet VERDES.

## Critérios de sucesso do Don — TODOS atendidos
- nenhum conteudo perdido ✓ (normalizeLine: semantica, nao marcadores)
- fronteiras estruturais preservadas ✓ (h/c/l intactos; blank lines separam)
- markdown normal sem regressao relevante ✓ (md-paragraphs 7->5, correto)
- JSON deixa de fragmentar por linha ✓ (1.500 -> 9)
- reducao de fragmentacao confirmada ✓ (99.4%)
- suite existente verde ✓ · build/vet verde ✓

## Nao feito (ordem do Don: corrigir a fabrica, limpar o estoque DEPOIS)
Nao limpar os 251 fragmentos historicos · nao remover os 8.730 candidatos ·
nao re-embedar os 48.621 orfaos.

## Classificacao
MEASURED caso critico 1.500->9; casos de seguranca e regressao real
EVIDENCE agrupamento correto sem perda; splitLongParagraph ja agrupava
         (9 chunks de ~2.550 chars) — o parser era o gargalo
INFERRED chunks futuros de JSON/texto comeca a ser blocos semanticos;
         menos embeddings inuteis entrando no indice
HYPOTHESIS impacto no indice futuro (menos vetores de fragmentos) e no
          retrieval (menos ruido) — a medir apos a limpeza historica
UNKNOWN comportamento em TODOS os docs reais (amostra pequena); bordas
         exoticas do markdown
DECISION correcao ATIVA (flag reversivel); PARAR aqui — limpeza historica
         e campanha propria, aguardar autorizacao
