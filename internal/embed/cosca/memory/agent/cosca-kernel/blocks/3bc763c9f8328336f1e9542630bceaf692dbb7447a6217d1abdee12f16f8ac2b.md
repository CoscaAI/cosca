PREV: 168119243fe19d687cc71397d7a00090ece97d0dee6f884e0648a9b226b1c26c
ID: L109
TIME: 2026-08-13
LEVEL: 4
TAGS: #cliente #iguaracy #blogger-migration #nextjs #react-leaflet #nominatim #geocoding #diretorio-local #pwa #whatsapp #scraping-feed #level-4
---
## L109 — 2026-08-13 — Cliente Iguaracy.com: migração Blogger→Next.js + protótipo com dados reais (fichas, GPS, WhatsApp) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: cliente iguaracy.com (diretório de comércio local de Iguaracy-PE). "ir pra stack propria e novas funcionalidades exclusivas pra atrair clientes locais... montar workflow e executar... mostrar pra ele como fica, utiliza as fotos existentes". Projeto `iguaracy` na pasta `clients/`. |
| **Technique** | Level 4 — **(1)** Reconhecimento do alvo: site é Blogger/Blogspot (blogID 343519614652387150) com domínio próprio, 26 fichas de negócio (mai–ago/2026), AdSense, ~30 labels. **(2)** Extração de dados via feed JSON (`/feeds/posts/default?alt=json&max-results=30`) — expõe `content` (HTML do card), `georss$point` (GPS), `media$thumbnail`, `category` (labels) e links `api.whatsapp.com` + `instagram.com` embutidos no HTML. **(3)** Parse por entidade: tagline (token antes de 📍), endereço (Endereço OU Atendimento), horário, serviços (split por `|`), WhatsApp, Instagram, slug, imagem em resolução cheia (`=s72-c`→`=s1280`). **(4)** Geocodificação: `georss$point` de TODAS as fichas = centro da cidade (mesma coord) → Nominatim resolveu 11/26 por rua real; os 15 restantes espalhados deterministicamente num raio ~70–130m do centro (todos são "Centro Iguaracy-PE" — honesto). **(5)** Stack: Next.js 14.2.35 + Tailwind + TypeScript + react-leaflet v4 (Leaflet/OSM, sem chave de API). **(6)** Features exclusivas: mapa interativo, busca instantânea, filtro por categoria, badge "aberto agora" (parser best-effort de horário em pt-BR), WhatsApp direto (wa.me real), Instagram, vitrine "Destaque" premium (monetização), PWA + manifest. **(7)** Screenshots via `google-chrome --headless --screenshot` (desktop 1440 + mobile 390). |
| **Level** | 4 |
| **Outcome** | success — Protótipo rodando (build + prod server :3210), 26 fichas reais renderizadas (31 `<img>`, imagens 200), commit `b6f6d3c`. Dados completos: 26/26 WhatsApp, 26/26 Instagram, 26/26 imagens, 25+ endereços, 11 geocodificados. |
| **Confidence** | 0.9 (validado: build Next.js limpo, prod server 200, HTML render check com títulos reais, imagens servindo) |
| **Tags** | #cliente #iguaracy #blogger-migration #nextjs #react-leaflet #nominatim #geocoding #diretorio-local #pwa #whatsapp #scraping-feed #level-4 |
| **Related** | L192 (projeto novo desacoplado), L191 (desktop), L187 (projetos cliente perdidos — lição de versionar) |
| **Learned** | **(1) Migrar site Blogger = extrair o feed JSON, não raspar HTML**: `/feeds/posts/default?alt=json` entrega content completo + `georss$point` + `media$thumbnail` + labels + links wa.me/instagram embutidos — é a fonte canônica, sem parser frágil de HTML renderizado. **(2) `georss$point` do Blogger é o CENTRO da cidade para todas as fichas (o editor marca um ponto só) — para mapa real por negócio, geocodificar o endereço via Nominatim (resolvi 11/26; o resto fica no centro com jitter honesto, pois são todos "Centro").** **(3) react-leaflet v4 em Next.js App Router exige `dynamic(..., {ssr:false})` + `divIcon` custom (os ícones padrão do Leaflet quebram com bundler porque o path das imagens some) — o pin é HTML puro com `transform:rotate(-45deg)`. **(4) "Aberto agora" a partir de horário livre em pt-BR ("Seg à Sab: 06:00h às 19:30h", "Dom: fechado") só é confiável com parser que RETORNA null quando ambíguo — nunca chutar aberto/fechado. **(5) Stack para diretório local de cliente = Next.js (SSG/SEO é o valor do negócio — ser achado no Google), não Vite; Leaflet/OSM em vez de Google Maps para zero custo de API. **(6) Screenshot headless com `google-chrome --headless --screenshot --window-size --virtual-time-budget` cobre a validação visual sem browser GUI — e o kernel (modelo sem visão) valida por HTML/log e entrega os PNGs pro Don ver. |
| **Next** | (1) Mostrar os screenshots ao Don (/tmp/shots/*.png). (2) Se aprovado: página de detalhe por negócio (rota dinâmica `/[slug]`), admin/painel do comerciante (cadastro de ficha), busca por geolocalização do usuário. (3) Deploy de demonstração (Vercel/Netlify) pro cliente abrir no celular. (4) Rodar `integrity.SignAfterLearning()` (chain) quando o binário local voltar a ter bit de execução. |
