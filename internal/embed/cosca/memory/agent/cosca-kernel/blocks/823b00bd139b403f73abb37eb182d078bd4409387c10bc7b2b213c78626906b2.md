PREV: dfcd2ea3d1e323be51260b7ca1d06a79c037bd2433991fd37df6ee00176b6d86
ID: L122
TIME: 2026-08-13
LEVEL: 5
TAGS: #lixo #trash #chave-original #recuperacao #forense #anti-edicao #tampered #chain #bloco-2 #level-5
---
## L122 — 2026-08-13 — CHAVE ORIGINAL RECUPERADA DO LIXO: o ~/.config/cosca INTEIRO foi pro Trash no incidente; casa provou anti-edição ao vivo (TAMPERED → bloco 2) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don escolheu o Caminho A (restaurar kernel_private.key original). Forense achou a chave no Trash: `/home/cosca/.local/share/Trash/files/cosca/keys/kernel_private.key` (212 B, 0600, armadura COSCA válida) — o diretório `~/.config/cosca` ORIGINAL inteiro foi despejado no lixo em 11/08 (data do incidente). Chave copiada (não movida) pro esconderijo `~/.config/cosca/keys/` 0600; lixo preservado até o par ser validado. |
| **Technique** | Level 5 — **(1)** find em /mnt /media /opt /srv /Downloads /Trash achou `Trash/files/cosca/keys/kernel_private.key`; o lixo guarda o config ROOT original completo: config.yaml (ORIGINAL, hash ≠ do atual recriado pós-incidente), serve.env (credenciais, 334 B), security.log + security.log.1, models.json, cosca-chat binário, data/, scripts/. **(2)** Verificador check-pair standalone (lógica fiel da casa: decrypt AES-256-GCM, deriveKey iterado 1000x, PKCS#8→SPKI, compara com a versionada) em /tmp/opencode/check-pair — passphrase via stdin (EOF no ambiente tool = nunca vaza). **(3)** A casa provou o anti-edição AO VIVO: L220 registrada sem ancorar → `cosca-check` = `🔴 FAMILY CHAIN BREACH DETECTED — Block 1: TAMPERED — learnings.md` → commit + `--sign-auto` → bloco 2 `0ca2686…` (prev 4338d3…) → `Chain valid: 2 blocks`. **(4)** security.log do lixo: warning do bug L218 VIVO em produção em 11/08 13:51 ("bwrap failed" + COSCA_ALLOW_NO_ROOT=1) — o incidente e o bug são a MESMA linha do tempo. |
| **Level** | 5 |
| **Outcome** | success — chave original restaurada no esconderijo (0600), chain 2 blocos válidos, anti-edição provado (TAMPERED → ancoragem → valid), forense do lixo mapeada. Pendências: check-pair (passphrase do Don — via stdin no terminal dele), purga do lixo (decisão do Don), binário de produção + religar serviço. |
| **Confidence** | 0.96 (validado: armadura da chave + permissões + hashes + chain 2 blocos green; único passo não executado = decrypt com passphrase do Don) |
| **Tags** | #lixo #trash #chave-original #recuperacao #forense #anti-edicao #tampered #chain #bloco-2 #level-5 |
| **Related** | L220 (primeira âncora), L219 (fix L218 — o warning de 11/08 no security.log é a assinatura do bug), L218 (bug), internal/embed/cosca/keys/kernel_public.key, ~/.config/cosca/keys/ |
| **Learned** | **(1) O Trash é o último refúgio forense: o atacante (ou agente) moveu pro lixo em vez de apagar — permissões 0600 e metadados preservados; camada "segura" do Don pode ter sido só o lixo do usuário.** **(2) Lixo NÃO é camada segura: serve.env com credenciais + config.yaml original expostos a qualquer processo do usuário — purgar após validar o par.** **(3) O anti-edição funciona de verdade: edição não ancorada de learnings.md = TAMPERED no Check — o ciclo casa é REGISTRAR→COMMIT→SIGN-AUTO, sem exceção.** **(4) security.log original é testemunha: o bug L218 (falso jail unavailable) estava ativo em produção no dia do incidente — a correção L219 fecha essa linha do tempo.** |
| **Next** | (1) Don roda `COSCA_PRIV_KEY=~/.config/cosca/keys/kernel_private.key /tmp/opencode/check-pair/check-pair` no terminal dele — validação do par (PAR FIEL = identidade original; DIVERGENTE = parar tudo). (2) Purgar o Trash do cosca (decisão do Don — serve.env, config.yaml original, security.logs). (3) Ancorar esta L221 (bloco 3). (4) Binário de produção + religar serviço. |
