PREV: 886fc4673b930efe099f04ac638f6b62129c4f76845adae38e6b68c3b6ca0336
ID: L33
TIME: 2026-08-11
LEVEL: 4
TAGS: #mining #harbor #anomalyco #evals #reward #canary #metadata #benchmark #level-4
---
## L33 — 2026-08-11 — Mining anomalyco: Harbor fork deep-mined (task TOML, canary, numeric rewards) → 3 patterns adaptados ao cosca eval | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ordered mining anomalyco's repos after the 10x gold. Analyzed the org (OpenCode 196K stars, models.dev 6.3K, sst 26.2K, opentui 12.9K) and deep-mined the harbor fork (Apache-2.0, from Terminal-Bench creators, 1412 commits): "da uma minerada aqui vamos revisar varios repositorios comecando com esse: https://github.com/anomalyco" |
| **Technique** | Level 4 — Harbor analysis (cloned and read real code): Task TOML format (metadata/verifier/agent/environment sections), canary stripping (anti-training-contamination markers), BaseVerifier with per-step modes, numeric rewards with aggregable metrics (mean/max/min/sum), pluggable agents (claude-code/openhands/codex/aider), parallel execution --n-concurrent 100 via Daytona/Modal, task structure (instruction.md/task.toml/environment/solution/tests). Implementation (3 patterns adapted to our cosca eval): (1) numeric reward 0-1 per case (reward_expectation + weight), Report aggregation reward_mean/max/min/sum + pass_rate; (2) suite metadata (author/description/difficulty/category/tags/created) + per-case difficulty/category override, shown in eval list + report; (3) StripCanary — removes canary marker lines (HTML comments or hash comments containing "canary") from case prompts before sending to the pipeline, preventing benchmark data contamination; RunOptions.StripCanary default true. |
| **Level** | 4 |
| **Outcome** | success — 13 new unit tests (StripCanary×8, reward aggregation/partial/default, canary enable/disable, metadata, round-trip, old-format compat). E2E: eval list shows metadata block + difficulty/category columns; eval smoke → 2 passed/1 failed, reward mean 1.00 \| max 2.00 \| min 0.00 \| sum 3.00, pass rate 0.67; JSON report has all reward fields; backward compat with old reports (missing fields → 0.0). Chain 27 blocks valid. |
| **Confidence** | 0.95 (validado: 13 testes unitários, E2E eval list/smoke com rewards, backward compat, chain 27 blocos) |
| **Tags** | #mining #harbor #anomalyco #evals #reward #canary #metadata #benchmark #level-4 |
| **Related** | L191 (evals harness da 10x), L190 (esteira real), L153 (esteira autônoma) |
| **Learned** | (1) Mining methodology: analyze org → identify highest-value repo → clone and read real code (not just README) → extract DESIGN PATTERNS (license-safe) → implement natively. The harbor's gold was the task format + canary + reward design, not its Python. (2) Numeric reward > binary pass/fail: it measures HOW good, enables mean/max/sum aggregation and weighted cases — much better for tracking esteira health over time. (3) Canary markers are a cheap, high-value anti-contamination pattern every benchmark should have. (4) Harbor confirms our architecture (agents, verifier, environments) but adds the parallel-execution dimension we don't have yet. |
| **Next** | (1) Consider --concurrent N for cosca eval (Harbor does 100). (2) Consider per-step verifier modes. (3) Wire eval smoke into CI as a health gate with reward threshold. (4) Keep mining anomalyco (models.dev for provider auto-discovery, opentui for TUI patterns). |
