PREV: 27b4237796119fac741f089ddbeb1a559280284f37ec6c300cc034eebf7ba599
ID: L230
TIME: 2026-08-14
LEVEL: 5
TAGS: #divida-fechada #l324 #nao-cooperativo #chatWithRetry #goroutine #select #race #provider-thread-safe #level-5
---
## L230 — 2026-08-14 — dívida L324 fechada: chatWithRetry não-cooperativo — provider que ignora ctx não bloqueia mais o worker; régua completa (E2E + chaos V1-V6) verde; race revelou contrato (provider thread-safe) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Fechar a dívida do L324: o chatWithRetry chamava provider.Chat direto — um provider que ignora o cancelamento do ctx bloqueava o worker até responder, ignorando o timeout do attempt. |
| **Technique** | Level 5 — `runChatAttempt`: executa provider.Chat em goroutine + select no deadline (channel buffered 1). O worker retoma o controle no deadline e trata como stall (ErrAttemptTimedOut). BUG REAL da integração: ErrAttemptTimedOut não era reconhecido como transiente pelo isTransientError (não contém "deadline exceeded") → stalls não retryam; corrigido com `errors.Is`. RACE REAL revelado pelo -race: a goroutine vazada pode chamar o provider concorrentemente com a próxima tentativa → o mock (contador não-atômico) quebrou; o provider real é thread-safe (http.Client) — mock alinhado ao contrato (atomic). |
| **Level** | 5 |
| **Outcome** | success — provider que dorme 200ms com timeout 30ms: ANTES 20 sessões esperavam os 200ms; AGORA 0 completas + 20 erro em 0,06s. Régua completa verde (E2E stall + chaos V1-V6). Race limpo, build OK. Commit `04a8829`. |
| **Confidence** | 0.97 (régua completa re-executada; race limpo; Global Suite; comportamento antes/depois documentado no teste) |
| **Tags** | #divida-fechada #l324 #nao-cooperativo #chatWithRetry #goroutine #select #race #provider-thread-safe #level-5 |
| **Related** | L324 (achado original), L320 (stallwatch) |
| **Learned** | **(1) Fechar dívida com a régua existente como critério funciona: o teste que documentava o comportamento antigo virou a prova do novo (0 completas, terminal em 0,06s).** **(2) Erros próprios do watchdog precisam de reconhecimento explícito no retry (errors.Is), não por texto — isTransientError por markers não pegou ErrAttemptTimedOut e a régua E2E falhou imediatamente.** **(3) Execução não-cooperativa muda o contrato de chamada do provider: ele pode ser invocado concorrentemente (goroutine vazada + próxima tentativa) — -race provou que mocks não-atômicos quebram; providers reais já são thread-safe.** **(4) O trade-off da goroutine vazada permanece (provider que nunca retorna deixa a goroutine viva), mas o worker não fica preso — o requisito "nenhuma chamada espera indefinidamente" está cumprido no nível do worker.** |
| **Next** | (1) 100k para completar a curva. (2) Categoria B (~2,2 pts). (3) Working tree limpa. |
