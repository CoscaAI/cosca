PREV: 28d13cee04d56a2bca6a47ad1ce1a7bb51914baa5da272aebd2ddffa635548e5
ID: L60
TIME: 2026-08-12
LEVEL: 5
TAGS: #kernel-isolado #auditoria #teste-extremo #fail-closed #protocolo-exato #anti-mutação #snapshot #segurança #level-5
---
## L60 — 2026-08-12 — AUDITORIA DO TESTE EXTREMO (19 fases): 6 achados reais corrigidos — protocolo exato do juiz, blindagem anti-mutação e testes empíricos | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don ordenou auditar o TestExtremeAudit (COGNITIVE → GATE → JUDGE → JAIL → EXECUTION) detalhe por detalhe. Resultado ao vivo: 46 PASS / 3 FAIL (gaps assumidos). A auditoria encontrou 6 achados: A1 (F6 com ramo de código morto — aprovaria se a injeção VENCESSE), A2 (HasPrefix "VALIDAR" normalizava sufixo malicioso em aprovação — burlava o protocolo estrito), A3 (auditoria in-memory some no crash — forense volátil), A4 (INV-1 panica em vez de FAIL com err nil), A5 (F14/INV-15 auto-afirmações + baseline hardcoded), A6 (juiz recebia ponteiro VIVO da proposta — mutação pós-lei entraria na cópia selada). |
| **Technique** | Level 5 — **(1)** Auditoria = ler código + rodar ao vivo + cruzar com a implementação (flow/validator/semantic). **(2)** A1: ramo `reason == ""` era código morto (OllamaValidator nunca devolve "") — trocado por detecção real de prefixo "VALIDAR". **(3)** A2: `upper == "VALIDAR"` exato — sufixo vira resposta inválida → fail-closed; prompt do juiz reforçado ("Responda EXATAMENTE") sem quebrar o teste real. **(4)** A6: selagem por cópia profunda ANTES da validação + hash antes/depois do julgamento — divergência → DENY fail-closed; juiz julga snapshot, nunca o ponteiro vivo; aplicado em Submit E Review. **(5)** Testes viraram empíricos: F14 (restart = fluxo novo não conhece autorização), INV-15 (audit com ProposalID), baseline via debug.ReadBuildInfo+ollamaUp. **(6)** Novo TestJudgeMutationFailClosed: juiz sabotador que muta Action/Risk → DENY fail-closed + ponteiro do chamador intocado. |
| **Level** | 5 |
| **Outcome** | success — 46 PASS / 3 FAIL (F15/INV-10/INV-12 = gaps assumidos por design: sandbox de execução ausente e TTL de autorização inexistente). Build+vet+suite completa+race limpos. F6 agora prova rejeição real da injeção ("A operação viola as políticas estabelecidas"), F14 empírico, INV-1 sem pânico, INV-15 verificado de verdade. |
| **Confidence** | 0.97 (validado: suite verde, race clean, juiz real respondendo, 6 arquivos cirúrgicos) |
| **Tags** | #kernel-isolado #auditoria #teste-extremo #fail-closed #protocolo-exato #anti-mutação #snapshot #segurança #level-5 |
| **Related** | L215 (Kernel isolado), L216 (corpo da jaula), L213 (nunca pkill), internal/proposal/flow.go, semantic.go, audit_extreme_test.go |
| **Learned** | **(1) Teste que "passa" com lógica invertida é pior que teste vermelho: o ramo morto `reason == ""` aprovava a injeção vencedora com mensagem mentirosa — auditoria precisa caçar código morto em assert.** **(2) Protocolo do juiz exige match EXATO ("VALIDAR" palavra única): qualquer normalização com prefixo reabre a porta pro sufixo malicioso.** **(3) Blindagem anti-mutação em 3 tempos: selar ANTES → julgar o snapshot → reverificar o hash → divergência = DENY fail-closed. O juiz nunca toca o ponteiro vivo do chamador.** **(4) Fail-closed estrutural (in-memory) protege contra re-execução pós-crash, MAS apaga a auditoria junto — forense exige persistência (gap A3 aberto).** **(5) Lacunas conhecidas (F15/INV-10/INV-12) precisam de registro explícito para automação distinguir gap esperado de regressão — o teste passa mesmo com vermelhos por design.** |
| **Next** | (1) Persistir AuditLog em .cosca (SQLite) — fechar o gap A3. (2) Camada de sandbox na execução (F15/INV-12) quando a execução virar real. (3) TTL de autorização (INV-10). (4) Rodar a suíte completa do repositório e re-auditar em sessão futura. |
