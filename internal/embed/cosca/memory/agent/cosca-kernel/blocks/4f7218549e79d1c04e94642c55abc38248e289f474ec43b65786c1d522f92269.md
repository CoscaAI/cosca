PREV: f9c5610a2b5c71b76a6874d1b7a9e8dae4b29106e0a3910a47f77304889bf200
ID: L200
TIME: 2026-08-14
LEVEL: 5
TAGS: #cosca-trader #fase-4 #risk-manager #exposicao #drawdown #fail-open #fail-closed #rate-limit #level-5 #implementacao-propria
---
## L200 — 2026-08-14 — cosca-trader Fase 4: RISK MANAGER (exposição, drawdown, rate limit) — e a descoberta de um FAIL-OPEN no caminho | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "sim" (após "ficou pensando muito" + "depois que le o style.css ele pensa muito") — executar a Fase 4 (camada de risco) do cosca-trader. O kernel MUDOU A TÁTICA: os agentes delegados estavam afundando em contexto ao ler o frontend inteiro (App.tsx 586 linhas + style.css 501 linhas) e eram cancelados. O kernel implementou TUDO na mão: pacote risk + integração OMS + main.go + serve.go + frontend. |
| **Technique** | Level 5 — **(1) LIÇÃO DE PROCESSO: quando a delegação falha 2x pelo MESMO padrão (agente lê frontend inteiro → pensa demais → cancelado), o kernel muda a tática e faz na mão, cirúrgico — P13: a verificação é sempre do kernel, mas a implementação pode ser dele quando a delegação é mais lenta que o caminho direto.** **(2)** Criou internal/risk/risk.go: Manager com Check(notional, positions, openOrders) fail-closed (sem equity → bloqueia), exposição por símbolo + total, drawdown (pico → Halt → RiskBreach), rate limit deslizante de 1min, ordens abertas, OnTick() que consulta o provider. **(3)** Integração: WithRiskManager no OMS, Check ANTES da saga de intent (fail rápido, sem gastar client_order_id), RegisterOrder no pós-sucesso, OnTick alimentado pelos ticks/position updates no main. **(4)** Endpoint /risk + card no frontend (drawdown %, banner vermelho "TRADING PAUSADO"). **(5)** Smoke tests reais do kernel: ordem 0.3 BTC @60000 = 34% do equity → BLOQUEADA por exposição; 0.1 BTC = 11.5% → passou; /risk mostrando equity/pico/drawdown/ordens_min. |
| **Level** | 5 |
| **Outcome** | success — 1 commit (Fase 4A), 12 arquivos, 842 inserções. Build/vet/test/race verdes (13 pacotes). **FAIL-OPEN DESCOBERTO E CORRIGIDO durante a verificação**: ordem market sem preço de referência era medida como exposição ZERO (furo — o risco desligava silenciosamente); agora bloqueia. Testes de integração OMS+risk provaram exposição e drawdown de ponta a ponta. |
| **Confidence** | 0.95 (validado: smoke test real do kernel — ordem 34% bloqueada, 11.5% passou, /risk respondendo; testes unitários + integração verdes) |
| **Tags** | #cosca-trader #fase-4 #risk-manager #exposicao #drawdown #fail-open #fail-closed #rate-limit #level-5 #implementacao-propria |
| **Related** | L276 (Fase 3), L275 (Fase 2), L274 (Fase 1), L273 (análise), fintech/README.md (doutrina) |
| **Learned** | **(1) Quando a delegação falha 2x pelo MESMO padrão, o problema é o processo, não o agente: prompt gigante + frontend inteiro no contexto = afundar. O kernel fez na mão e entregou em minutos.** **(2) Fail-open é o pior bug de risco: exposição medida como ZERO quando não há preço é silenciosamente perigoso — a correção é fail-closed (sem preço, bloqueia). A verificação ativa (P13) pegou o que testes iniciais não pegariam.** **(3) O Check de risco deve rodar ANTES da saga de intent: violou regra → rejeita sem gastar client_order_id nem tocar a exchange.** **(4) Rate limit deslizante de 1 min (janela de timestamps) é simples e testável — melhor que contadores fixos.** |
| **Next** | (1) Fase 4B pendente: take-profit/trailing stop por posição + sizing por risco (o agente original faria, mas o kernel pode fazer na mão se o Don aprovar). (2) Testnet com chaves do Don. (3) F6 assistente IA + F7 B3/Coinbase/CoinEx. |
