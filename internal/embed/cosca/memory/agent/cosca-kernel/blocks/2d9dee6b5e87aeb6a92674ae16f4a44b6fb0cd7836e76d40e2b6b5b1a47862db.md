PREV: 38fe52744f39d2cafb5ccf574711e953bbb94effbd0b1808b9f4f45bdf7a5bec
ID: L87
TIME: 2026-08-13
LEVEL: 5
TAGS: #gpu-engine #gpu-probe #scheduler #rocm #vaapi #opencl #vulkan #gfx1030 #rx6700xt #hardware-class #section28 #fase-1 #cosca-engine #level-5
---
## L87 — 2026-08-13 — Fase 1.5: GPU Engine — probe ROCm/VAAPI/OpenCL ampliado + scheduler CPU/GPU/remote (§17/§28) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuação do Cosca Creative Ecosystem (L218-221): etapa 1.5 — GPU Engine: programatizar o §17 (detectar NVIDIA/AMD/INTEL, VRAM, drivers, ROCm, Vulkan, OpenCL, video accel) + scheduler do §28 (CPU/GPU/remote por performance/custo/disponibilidade/qualidade). |
| **Technique** | Level 5 — **(1)** Reuso do `ProbeGPU()` existente (internal/compute — já detectava vendor/VRAM/ROCm/Vulkan/CUDA/gfx target) e AMPLIAÇÃO com os campos do §17: HasVAAPI (aceleração de vídeo via vainfo/Mesa DRI radeonsi-amdgpu), HasOpenCL (clinfo/ICD vendors), ComputeUnits e MaxClockMHz do rocminfo. **(2) BUG REAL de parser pego no teste real da máquina**: cusFromRocminfo/clockFromRocminfo pegavam a seção do CPU (Agent 1 vem primeiro no rocminfo) — CUs=16/clock=4151 (do Ryzen 5700X3D) em vez de CUs=40/clock=2855 (da RX 6700 XT). Correção: `gpuSection()` isola a partir de "Device Type: GPU" — agora reporta os valores REAIS da GPU. **(3)** Novo package `internal/sched`: Scheduler puro e determinístico (testável) que cruza a task (aitask.HardwareClass) com o probe GPU e decide Target (local-cpu/local-gpu/remote) + Reason (gpu-present, no-gpu, gpu-low-vram, task-wants-remote...). Decisão §28: task GPU sem GPU→CPU; VRAM insuficiente→CPU; task remote sem modelo local→remote; task CPU leve com GPU+modelo local→GPU (custo/performance). **(4)** CLI `cosca gpu probe` (tabela completa §17) + `cosca gpu plan <task> [--vram]` (decisão ao vivo). **(5)** A RX 6700 XT da família agora é detectada PROGRAMATICAMENTE: 12 GiB, gfx1030, ROCm 7.2.4, Vulkan, VA-API, 40 CUs, 2855 MHz — e o scheduler decide com esses dados. |
| **Level** | 5 |
| **Outcome** | success — GPU Engine + Scheduler implementados, 9 testes sched + testes compute ampliados verdes, esteira completa verde, decisões ao vivo testadas (segmentation→local-gpu, image_gen 20GiB em 12GiB→local-cpu). |
| **Confidence** | 0.95 (validado: probe reporta os valores REAIS da GPU da máquina, scheduler determinístico testado, CLI E2E) |
| **Tags** | #gpu-engine #gpu-probe #scheduler #rocm #vaapi #opencl #vulkan #gfx1030 #rx6700xt #hardware-class #section28 #fase-1 #cosca-engine #level-5 |
| **Related** | L221 (modelreg — VRAM do modelo alimenta o scheduler), L220 (aitask — hardware class por task), L218 (check-up: descoberta manual da GPU virou probe programático), L207 (security) |
| **Learned** | **(1) Parser de saída de ferramenta (rocminfo) tem armadilha de SEÇÃO: o CPU vem antes da GPU no Agent 1 — sem isolar por "Device Type: GPU", o probe mente (CUs=16 vs 40). Teste real da máquina é obrigatório quando o parser consome output de hardware.** **(2) O scheduler de decisão deve ser PURO (injeção do probe) para ser testável — cruza task×hardware e devolve Target+Reason determinísticos; a leitura real acontece no CLI.** **(3) A decisão §28 tem nuances: task GPU sem GPU→CPU (fallback), VRAM insuficiente→CPU (não prometer o que a placa não aguenta), task remota sem modelo local→remote, e tarefa CPU leve NÃO acorda a GPU se não houver modelo local (custo).** **(4) O GPU Probe é a fundação do GPU Engine: scheduler, capacidade do sistema (gpu_exec/gpu_tts) e logs de observabilidade dependem dele — detectar a verdade (AMD ROCm) e não o que se espera (NVIDIA CUDA) é o princípio.** |
| **Next** | (1) Etapa 1.6: Node Graph v1 (grafo serializável + cache por assinatura de inputs — P1 do ComfyUI). (2) Integrar scheduler ao orchestrator/Node Graph (cada nó decide onde roda). (3) GPU Probe alimentando o system capability (gpu_exec) e o Model Registry (VRAM real). |
