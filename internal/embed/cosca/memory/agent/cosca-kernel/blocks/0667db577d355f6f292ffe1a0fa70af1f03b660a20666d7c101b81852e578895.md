# L351 — Plano de correcao do chunker + deduplicacao (ESPECIFICACAO SO)

## Escopo (ordem do Don)
PROPOSTA somente. Zero codigo, zero execucao, zero alteracao. Orfaos
intocados. Preservar L349/L350 como evidencia historica.

## Decisoes centrais
1. Filtros de triviais T1-T4 (vazio, whitespace/separadores puros, sem
   alfanumericos, estrutura vazia) — NUNCA por comprimento bruto (preserva
   "Go 1.26", "v1.2.3", siglas). Cada filtro com condicao exata + auditoria
   de FP (100 amostras) no dry-run.
2. Dedup EXATA por SHA-256 do texto BRUTO (zero FP) + chave normalizada so
   para agrupamento/analise. Nada deletado: dedup_of (referencia -> canonico),
   IDs/metadata/document_id preservados. NAO deduplicar por vetor (CAT5=155
   sao conteudos distintos — UNKNOWN a colisao). Quase-duplicatas FORA do
   escopo (exato != semantico).
3. Orfaos: classificar (duplicados 48.9%, triviais 18.8%, reais, sem-origem)
   — re-embed SOMENTE apos filtro+dedup+validacao.
4. Determinismo: tie-breaker (score desc, id asc) no insertScored/merge —
   especificado, NAO implementado. Testes: N runs -> ranking byte-identico.
5. Dry-run em memoria (SELECT + analise): ANTES/DEPOIS simulado com slabs
   int8/int16 vs budget 80MB.
6. Validacao: oracle float32 ANTES vs POS (nao usar so int8); recall@1/5/10,
   Jaccard@50, NDCG@10, diversidade, estabilidade.
7. Rollback: nada deletado (flags/dedup_of); backup antes; indice derivado.

## Impacto estimado (INFERRED/HYPOTHESIS)
Indice ~9.273 vetores (simulacao L349, 33.8% menor); slab int8 7.1MB /
int16 14.2MB << 80MB; latencia ~66%; diversidade 4.91 -> ~10 (HYPOTHESIS);
NDCG pos-dedup UNKNOWN.

## Ordem futura (cada fase com aprovacao do Don)
filtros -> dry-run -> backup+flags -> dedup -> determinismo -> validacao RAG
-> decidir representacao -> re-embed. Criterios de aprovacao/rejeicao
definidos (FP 0 na amostra; NDCG pos >= pre - 0.02; estabilidade 100%).

## Regras da casa reforçadas
Implementacao = consequencia de evidencia, nunca recompensa. Errata P13
mantida (HYPOTHESIS != MEASURED). Fronteira EXPERIMENTAL/PRODUCAO rigida.
