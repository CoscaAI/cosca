PREV: 67992f75325356c6f8c1223add327071956f12239dce5d7a32d682a39b074d07
ID: L167
TIME: 2026-08-13
LEVEL: 4
TAGS: #cosca-trader #auditoria-resolvida #decimal #ledger #replay #reconciliacao #seguranca #level-4
---
## L167 — 2026-08-13 — cosca-trader: auditoria RESOLVIDA em 4 fases (Fase A-D, 4 commits) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "resolve tudo" — corrigir TODOS os achados da auditoria (L264): 7 P0 + P1 + P2. |
| **Technique** | Level 4 — Execução em 4 fases, cada uma com commit: **Fase A (dinheiro)** → Decimal em todo o caminho (domain/oms/binance/SQLite TEXT), PnL preservado no reabrir, fees rastreados, fill idempotente (dedup por Trade.ID), posições por symbol+exchange. **Fase B (resiliência/segurança)** → OMS.Replay (event sourcing sem perda em restart) + Reconcile (REST no startup/reconexão) + redação do listenKey e da signature dos erros. **Fase C (P1)** → ledger double-entry (débito/crédito por conta), engine.Emit loga erro de persistência, /orders com token + CORS fechado + /ledger endpoint. **Fase D (P2)** → flip sinaliza close+open, recvWindow, timeout de escrita, DELETE /orders, teste de integração, cobertura. |
| **Level** | 4 |
| **Outcome** | success — 4 commits (`eadb271`, `dd34116`, `568c758`, `1bf13cb`). 40+ testes verdes, -race limpo, gofmt/vet limpos. Cobertura: ledger 100%, stream 100%, oms 85.1%, domain 86.7%, engine 87.5%. Smoke test do binário ok. |
| **Confidence** | 0.9 (validado: testes verdes + race + smoke test; a execução live com chaves reais continua dependendo do Don fornecer as credenciais) |
| **Tags** | #cosca-trader #auditoria-resolvida #decimal #ledger #replay #reconciliacao #seguranca #level-4 |
| **Related** | L264 (auditoria), L263 (F2), L262 (F1 gotcha json), knowledge/fintech/README.md |
| **Learned** | **(1) shopspring/decimal serializa como STRING no JSON ("63406.18") e aceita string+número na leitura — exatamente o padrão da Binance; trailing zeros são perdidos mas o VALOR é exato. decimal.Decimal é struct (não funciona com omitempty).** **(2) O fix do PnL reset foi PRESERVAR RealizedPnL no branch de reabertura (`pos.Quantity.IsZero()`) — o map é indexado por símbolo+exchange, então o acumulador sobrevive ao ciclo fechar→reabrir.** **(3) Replay do OMS exige SUPRIMIR a emissão (flag `replaying`) — senão reemite eventos que já estão no rastro e duplica. E `decodePayload` trata payload como struct em memória OU json.RawMessage do disco (JSON round-trip unificado).** **(4) Ledger double-entry por ativo: cada fee vira débito `fee:<asset>` + crédito `asset:<asset>` (balanceado); o saldo da conta é débito−crédito. Fluxo de base/quote exige metadata de instrumento (deferido).** **(5) Segurança de segredo em erro: o erro de dial do websocket e do http.Do embutem a URL (com listenKey/signature) — redigir retornando erro genérico, nunca propagar o erro bruto.** **(6) Executar correção em FASES com commit por fase (A/B/C/D) mantém o git rastreável e permite reverter uma fase sem perder as outras — o Don viu o progresso incremental.** |
| **Next** | (1) F3: gráficos (KLineChart/lightweight-charts) + indicadores (cinar/indicator). (2) Metadata de instrumento (exchangeInfo) para o fluxo base/quote do ledger. (3) Paper trading (broker simulado). (4) Chaves reais/testnet do Don para validar execução live. |
