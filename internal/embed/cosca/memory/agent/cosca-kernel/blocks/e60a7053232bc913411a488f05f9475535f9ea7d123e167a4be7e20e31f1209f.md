# L370 — Estados da investigacao formalizados + L368 registrada como REFUTADA

## status: DECISION (protecao arquitetural do professor aplicada)

## O que entrou
GOVERNANCE_PROTOCOL.md §5.1 — Estados da investigacao:
OBSERVED -> HYPOTHESIS -> TESTED -> CONFIRMED/REFUTED -> DECIDED.
Regras: (1) uma investigacao pode terminar SEM codigo alterado
(NO_CHANGE_JUSTIFIED — nao e falha, e decisao); (2) hipotese refutada e
registrada como refutada, NUNCA apagada (e conhecimento); (3) a regra de
ouro: "o Cosca ganha por descobrir que a hipotese estava errada, nao por
acertar a primeira".

## Cadeia de proveniencia formalizada (L368 -> L369 -> L370)
L368  OBSERVED/HYPOTHESIS: "queries TF-IDF vs indice semantico = mismatch"
L369  TESTED (fluxo completo real) + REFUTADO parcial: a causa era o
      ambiente de TESTE (go test sem config.yaml -> provider local);
      producao validada (ollama + nomic); self-match 0.9998/1.0000.
      ACHADO VALIDO preservado: default "auto" cai no local (risco).
L370  DECIDED: NO_CHANGE_JUSTIFIED (nenhuma alteracao de producao).

## Classificacao
MEASURED estados formalizados no protocolo; cadeia registrada
EVIDENCE L368 REFUTED com causa + achado valido + producao validada
INFERRED investigacoes futuras seguem os 5 estados; hipoteses refutadas
         viram conhecimento reutilizavel (ex.: "falso positivo veio do
         ambiente de teste sem config")
HYPOTHESIS a memoria de engenharia experimental melhora decisoes futuras
UNKNOWN (nenhum)
DECISION protocolo atualizado; cadeia registrada; proximas decisoes do Don:
         dedup preventivo (L370b) e guard ativo (L371).
