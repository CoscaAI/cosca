PREV: fd860926ad6ed963afa068c434f1b5bf78b4e710518b3b8a51a55d151801d4c2
ID: L144
TIME: 2026-08-13
LEVEL: 4
TAGS: #caca-a-bugs #parser #precedencia #unary-minus #gradiente-discreto #cobertura #oraculo-burro #level-4
---
## L144 — 2026-08-13 — Caça-a-bugs do parser: bug real `-x^2` encontrado+corrigido; LLM explora a área certa mas não acha o gatilho (gradiente discreto) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" — montar o "investigue a confiabilidade do parser" como tarefa de descoberta de lacuna de cobertura (o degrau entre "descobre x²" e "descobre o que nem eu previ"). |
| **Technique** | Level 4 — **(1) Bug real encontrado via sugestão da conversa**: o parser tinha um bug de PRECEDÊNCIA — unário menos ligava mais forte que potência, então `-x^2` = `(-x)^2` (errado; correto é `-(x^2)`). A conversa sugeriu testar `-2x^2` (que por acaso funciona, pois `-2` é número + mult implícita) mas NÃO `-x^2` (variável + potência par), que é o gatilho. Confirmei: `-x^2` em x=3 dava 9 (errado), correto -9. **Fix**: reordenar a gramática (unary envolve power, não o contrário) — `parseUnary` chama `parsePower`, `parsePower` chama `parseAtom` com expoente `parseUnary` (para `x^-2`). **(2) Exportei `ParseExpression` + criei `ParseExpressionBuggy` (fixture do bug) + `HuntOracle` (compara buggy vs correto em x=3) + `cmd/bughunt`. **(3) Rodada real do caça-bugs (budget 10 e 20): o LLM explorou a ÁREA certa (precedência: `-2^3`, `2^-3`, `8/2+3`...) mas NÃO achou o gatilho (`-x^2`/`-2^2` com potência PAR). `-2^3` (ímpar) coincide por acaso (-8=-8), então o LLM não viu sinal e derivou pra outras precedências. |
| **Level** | 4 |
| **Outcome** | success — Bug real encontrado e corrigido (o parser agora está correto). Caça-a-bugs construído e rodado. Resultado honesto: o LLM explora a área certa mas não acha o gatilho discreto — MATCH/MISMATCH é gradiente BINÁRIO (discreto), sem subida de colina. É a CONFIRMAÇÃO FINAL da tese: descoberta exige gradiente, e bug-hunting com oráculo binário é intratável para o LLM. |
| **Confidence** | 0.92 (validado: bug reproduzido por teste, fix testado, caça-a-bugs rodado 2x com LLM real) |
| **Tags** | #caca-a-bugs #parser #precedencia #unary-minus #gradiente-discreto #cobertura #oraculo-burro #level-4 |
| **Related** | L242 (gradiente → descobre x²), L241 (sem gradiente → não descobre), conversa do Don (níveis 1-6, ambiguidade, lacuna de cobertura) |
| **Learned** | **(1) A sugestão da conversa (`-2x^2`) NÃO pegava o bug porque `-2` é número + mult implícita (funciona); o gatilho real é `-x^2` (variável + potência PAR) — prova viva de que casos de teste "perto" não garantem cobertura, e que a LACUNA real é sutil.** **(2) O bug de precedência unário-menos-vs-potência é o clássico: `-x^2` deve ser `-(x^2)`, não `(-x)^2`. A gramática correta é unary ENVOLVENDO power (unary → '-' unary | power; power → atom ('^' unary)?), e o expoente usa unary para permitir `x^-2`.** **(3) CONFIRMAÇÃO FINAL da tese do gradiente: bug-hunting com MATCH/MISMATCH é gradiente DISCRETO (binário), sem subida de colina — o LLM explora a área certa mas não consegue convergir para o gatilho exato. `-2^3` (ímpar) coincide por acaso (-8=-8) e o LLM perde o sinal. Descoberta de bug exige gradiente CONTÍNUO (ex.: trace da avaliação, ou valores em múltiplos pontos que revelem a assimetria par/ímpar).** **(4) O ciclo completo está fechado: função-discoberta (gradiente contínuo de amostras) → LLM DESCOBRE; bug-hunt (gradiente binário) → LLM EXPLORA MAS NÃO ACHA. A régua do "oráculo burro" vale para TODO tipo de descoberta.** **(5) "Investigue o parser" é a tarefa certa para testar "descobre lacuna ou só executa" — e a resposta empírica foi: explora (descobre a ÁREA), mas não o GATILHO discreto, sem gradiente. |
| **Next** | (1) Dar gradiente CONTÍNUO ao caça-a-bugs (ex.: revelar o valor em múltiplos x — a assimetria par/ímpar aparece como diferença que cresce, não binária) e medir se o LLM então acha o bug. (2) F4/F5 (solution_class, existence). (3) A "coisa muito top": o desenho final precisa de oráculo com gradiente CONTÍNUO, não só discreto. |
