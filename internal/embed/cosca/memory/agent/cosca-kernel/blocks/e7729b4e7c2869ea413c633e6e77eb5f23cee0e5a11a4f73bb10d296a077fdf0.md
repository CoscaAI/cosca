PREV: 59d1ba0487d37713b5f5a03f0d276c4a2530273dbbecc3d896f549b52af2772c
ID: L120
TIME: 2026-08-13
LEVEL: 2
TAGS: #runtime #grpc #L3 #autonomo #compute-fabric #jaula #bwrap #capability #level-2
---
## L120 — 2026-08-13 — Runtime religado: L2 → L3 autônomo (gRPC 14123 + jaula) | Level 2

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "runtime?" → "sim" — religar o runtime daemon (porta gRPC 14123) para habilitar execução autônoma e subir o nível cognitivo de L2 para L3. |
| **Technique** | Level 2 — **(1)** Verificação de pré-requisitos: unit systemd `cosca-runtime.service` existe só como template em `deploy/` (não instalada — destruída no incidente L187); `~/.config/cosca/serve.env` ausente (EnvironmentFile do template não existe); binário local suporta `cosca runtime start` (compose do engine completo: knowledge, memory, runtime, compute fabric; graceful shutdown SIGINT/SIGTERM). **(2)** Subida no padrão pós-incidente (processo direto, sem systemd): `setsid nohup ./bin/cosca runtime start >> ~/.cosca-runtime.log`. **(3)** Validação: porta 14123 escutando; `cosca capability level` → **L3-autônomo** com `autonomous_execution ✓ disponível` (capability check detecta o runtime via TCP dial em 14123 — mesmo mecanismo do L173, funciona com jaula ativa); serve /health OK; runtime também sobe DENTRO da jaula bwrap. |
| **Level** | 2 |
| **Outcome** | success — Runtime gRPC de pé (14123, em jaula), sistema em **L3-autônomo** (planeja → propõe → valida → executa → testa → observa → aprende). Serve continua healthy. Padrão de operação: 3 processos diretos (ollama 11434, serve 14120, runtime 14123), todos com jaula bwrap ativa. |
| **Confidence** | 0.97 (validado: porta 14123, capability L3 com autonomous_execution disponível, health serve OK, processos em jaula) |
| **Tags** | #runtime #grpc #L3 #autonomo #compute-fabric #jaula #bwrap #capability #level-2 |
| **Related** | L189 (reload serve+ollama), L187 (incidente — units destruídas), L173 (detecção de capacidade via TCP dial), L174 (4/4 green) |
| **Learned** | **(1) O runtime NÃO depende das units systemd nem do serve.env para operar: `cosca runtime start` compõe tudo sozinho e o capability check o detecta por TCP dial em 14123 (localhost atravessa a jaula bwrap — L173).** **(2) Cadeia de subida para L3 completa: ollama (provider) → serve (14120) → runtime (14123), todos processos diretos setsid/nohup com jaula ativa.** **(3) `~/.cosca/bin/cosca` (instalação global) NÃO existe mais — o ambiente opera com o binário local `./bin/cosca`; o template systemd aponta para `%h/.cosca/bin/cosca` e falharia hoje se instalado sem `make install` antes.** |
| **Next** | (1) `make install` para restaurar `~/.cosca/bin/cosca` (pré-requisito do template systemd). (2) Recriar units systemd user (serve/runtime/voice) + linger para daemon 24/7. (3) Restaurar `~/.config/cosca/serve.env` se houver necessidade de envs adicionais. |
