# L374 — Auditoria de determinismo do pipeline de embeddings (read-only)

## status: DECISION — INCONCLUSIVE, NAO AUTORIZAR TRANSFORMACAO

## O que a auditoria mapeou (FACT/MEASURED)
- chunking: DefaultConfig sempre (deterministico pela versao do codigo);
  hash do chunk = sha256(content) (ordem irrelevante)
- provider: Select com "local" SEMPRE como ultimo candidato (fallback
  silencioso no bootstrap/reindex — o mesmo risco da L369)
- modelo: nomic-embed-text:latest (digest 0a109f422b47... — AMBIENTAL,
  nao pinado); 768 dims; sem normalizacao no provider (o indice normaliza)
- config: producao ~/.config/cosca/config.yaml (ollama/nomic); testes e
  bootstrap sem config -> "auto" -> local (medido: ambiente limpo gerou
  4.492 vetores LOCAIS)

## Contrato de reproducao
OBRIGATORIO: provider, modelo, dimensionalidade, cosine
AMBIENTAL: digest do modelo (:latest muda)
DERIVADO: chunker/parser (versao), indice, schema
IRRELEVANTE: normalizacao (o score normaliza), ordem/hash

## Determinismo (definicao arquitetural)
SEMANTICAMENTE COMPATIVEL: mesmo provider/modelo/digest + mesmo chunking +
mesma fonte -> ranking/recall reproduziveis. Nao byte-identical (o score
normalizado torna variacoes numericas irrelevantes para o ranking).

## config.default.yaml: INSUFICIENTE sozinho
Resolve provider/modelo nominal, mas NAO pina o digest (:latest) e NAO
impede o fallback silencioso para o local. Necessario, nao suficiente.

## Experimento minimo (read-only, reversivel)
200 docs nos 2 ambientes (atual + limpo com config explicita): self-match,
cosine dos vetores novos vs antigos, recall, digest, deteccao de mistura.
PASS: self-match ~1.0 + recall >= limiar + mesmo digest + sem mistura.

## Classificacao
MEASURED digest 0a109f...; 768 dims; ambiente limpo -> local; L369 -> local
EVIDENCE fallback silencioso no Select (local sempre ultimo candidato)
INFERRED re-embed em ambiente limpo NAO e garantido reproduzir o espaco
HYPOTHESIS o digest do :latest e estavel (a verificar); o cosine dos
           vetores reconstruidos bate (a verificar)
UNKNOWN digest estavel?; cosine/recall pos-re-embed
DECISION INCONCLUSIVE — NAO AUTORIZAR TRANSFORMACAO ate o experimento PASS
         + fallback controlado + digest pinado.
