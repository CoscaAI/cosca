PREV: 8166fd92eadbc28a48673be4b22d30affa21df429d843d9b379874876173bc2e
ID: L84
TIME: 2026-08-13
LEVEL: 5
TAGS: #media-engine #ffmpeg #ffprobe #video #audio #transcode #extract-audio #extract-frame #validate #p5 #h264 #aac #wav #fase-1 #cosca-engine #level-5
---
## L84 — 2026-08-13 — Fase 1.8: Media Engine estendida — vídeo/áudio via ffmpeg (§16) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuação do Cosca Creative Ecosystem (L218-224): etapa 1.8 — Media Engine universal: estender além de imagem/PDF/SVG (cosca-media) para VÍDEO e ÁUDIO, pipeline DECODE→PROCESS→AI→FILTER→ENCODE→VALIDATE (§16). |
| **Technique** | Level 5 — **(1)** Novo package `internal/media` em Go (P5 do Blueprint: "I/O e codecs sempre via FFmpeg — nunca reescrever parsing"): `Probe` via ffprobe JSON (Streams com codec_type/name/width/height/duration/bitrate/channels/samplerate + Format container/duration/size + conveniências HasVideo/HasAudio/VideoStream/AudioStream/DurationSeconds), `ExtractAudio` (wav pcm_s16le p/ ASR, mp3 libmp3lame, aac default — pipeline VIDEO→EXTRACT_AUDIO→WHISPER do §21), `Transcode` (crf por QUALIDADE preview=32/draft=23/final=18 + preset — contrato §22), `ExtractFrame` (still em t), `Validate` (probe + streams presentes). **(2)** HWAccel flag preparada (VA-API auto quando GPU Probe 1.5 detectar). **(3)** Integra com o nodegraph: cada operação é um NÓ (extract_audio, transcode, extract_frame) para os workflows do §21. **(4)** CLI `cosca media probe/extract-audio/frame/transcode/validate`. **(5)** UX: stderr do ffmpeg CAPTURADO e exposto só em erro — o banner não polui a CLI (correção após ver o ffmpeg vazar o banner). **(6)** Aprendizado de teste: `head` fecha o pipe cedo → SIGPIPE mata o ffmpeg antes de criar o arquivo (falso negativo) — rodar sem truncar a saída. |
| **Level** | 5 |
| **Outcome** | success — Media Engine de vídeo/áudio implementada, 7 testes verdes (ffmpeg real: probe video+audio, non-media, extração wav sem video, frame, transcode, corrupted validate), CLI E2E (probe→extract→frame→transcode→validate com arquivos reais), esteira completa verde. |
| **Confidence** | 0.95 (validado: pipelines reais com ffmpeg 6.1, saída limpa, integridade validada) |
| **Tags** | #media-engine #ffmpeg #ffprobe #video #audio #transcode #extract-audio #extract-frame #validate #p5 #h264 #aac #wav #fase-1 #cosca-engine #level-5 |
| **Related** | L224 (render — media alimenta os nós), L223 (nodegraph — operações viram nós), L222 (scheduler — VA-API quando GPU), L210 (cosca-media — imagem/PDF/SVG em Python; esta é a extensão Go de vídeo/áudio) |
| **Learned** | **(1) P5 é lei: ffmpeg/ffprobe fazem o parsing de mídia — escrever pipeline próprio de codec é reinventar roda e criar CVE.** **(2) Qualidade vira CRF: preview(32,ultrafast) < draft(23,medium) < final(18,slow) — o contrato §22 do render flui até o codec.** **(3) WAV pcm_s16le é o formato de troca para ASR (whisper consome direto); mp3/aac para distribuição.** **(4) Capturar stderr do ffmpeg (expor só em erro) é essencial para UX de CLI — o banner de 3 linhas polui toda saída.** **(5) Em testes E2E com ffmpeg: nunca truncar a saída com head (SIGPIPE mata o processo antes de gravar) — redirecionar para arquivo e conferir o exit code.** |
| **Next** | (1) Etapa 1.9: Durable Execution (replay + activities com retry — padrão Temporal, workflow resumível de verdade). (2) Etapa 1.10: Provenance + Licenças (§32-35). (3) Executores REAIS: ligar media/tasks/models ao ngraph. (4) Whisper/ASR no pipeline (exige aprovação do Don — modelo ~1.5GB). |
