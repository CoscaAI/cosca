PREV: 17e53dbf0c0583af530c8b3153d539821b4173f46e7a02bffeae0d386a9f2584
ID: L137
TIME: 2026-08-13
LEVEL: 4
TAGS: #oracle #eval #black-box #secret-tests #discovery-metrics #submission-log #spec-implementation #level-4
---
## L137 — 2026-08-13 — Oracle-based eval implementado (F1-F3): schema + submit + DiscoveryMetrics | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "quero que vc implemente no codigo" — implementar a spec oracle-based (ORACLE_SPEC.md) em cima do `cosca eval`. |
| **Technique** | Level 4 — **(1)** `internal/evals/oracle.go` (novo): `Oracle` (mode/feedback/max_submissions/public_properties), `ProblemClass` (reproduction/discovery/open/existence), `SecretCase` (secret_verify + reference_solution + acceptance_criteria + feedback), `SubmitToOracle` (caixa-preta: roda secret_verify, retorna sinal mínimo por feedback, **nunca vaza o comando nem o output**), `SubmissionLog` (append-only JSONL 0600 em `.cosca/evals/submissions/<id>.jsonl`), `ComputeDiscoveryMetrics` (**DiscoveryEfficiency = reward/(hipóteses+experimentos+1)**). **(2)** `suite.go`: `Case.ProblemClass` + `Case.Oracle`. **(3)** `report.go`: `CaseResult.Discovery` + `CaseResult.SolutionClass` (known/novel). **(4)** `internal/cli/eval_oracle.go`: `cosca eval oracle submit <case-id>` (carrega secret, submete, registra no log, imprime VALID/INVALID ou t1=PASS t2=FAIL). **(5)** 6 testes (valid_invalid, pass_fail anônimo sem vazar comando, log append/load 0600, discovery metrics, load secret missing/parse). Smoke test E2E: INVALID sem solução, VALID com solução, JSON output. |
| **Level** | 4 |
| **Outcome** | success — Núcleo F1-F3 implementado e testado. `cosca eval oracle submit` funcional end-to-end. A leak de comando foi pega e corrigida no teste (pass_fail NÃO pode expor `Command`/`OutputTail`). `go build ./...` + vet + 6 testes limpos. |
| **Confidence** | 0.95 (validado: 6 testes + smoke E2E (INVALID/VALID/JSON) + build/vet limpos) |
| **Tags** | #oracle #eval #black-box #secret-tests #discovery-metrics #submission-log #spec-implementation #level-4 |
| **Related** | L235 (spec), L234 (ledger — alvo), L197/L191 (harness), L226 (memory integrity gate em dir externo) |
| **Learned** | **(1) O oráculo DEVE anonimizar o resultado por comando: no pass_fail, expor o `Command`/`OutputTail` do VerifyResult vazaria o segredo — o teste `TestSubmitToOraclePassFail` pegou isso e a correção foi construir `VerifyResult{OK: vr.OK}` (só o bool), nunca o struct cheio.** **(2) O store de segredos é self-contained (`SecretCase` carrega feedback + verify + reference), então `cosca eval oracle submit <id>` não precisa da suíte — o oráculo é definido inteiramente no segredo.** **(3) A "hipótese = submissão" virou o SubmissionLog JSONL append-only: cada submit é uma linha imutável, e o DiscoveryMetrics é derivado do log (não de estado em memória) — auditável e replayável.** **(4) O memory integrity gate (L226) bloqueia o comando em diretório externo não-inicializado — confirmado de novo no smoke test; o fluxo de eval em projeto novo exige `cosca memory integrity init` antes.** **(5) F4 (solution_class known/novel via hash do reference_solution) e F5 (existence + verify_proof) ficaram como Next — o schema já tem os campos (`reference_solution`, `verify_proof`), falta a comparação e o checker. |
| **Next** | (1) F4: comparar a solução do agente com `reference_solution` → `solution_class: known/novel`. (2) F5: modo existence + `verify_proof`. (3) Montar os 3 casos A/B/C concretos contra o `internal/ledger` (avaliar descoberta vs reprodução). (4) Decidir com o Don o destino do `internal/ledger/`. |
