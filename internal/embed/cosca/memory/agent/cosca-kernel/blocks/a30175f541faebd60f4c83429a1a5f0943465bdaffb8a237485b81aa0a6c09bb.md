PREV: 4008fea3867d8f91a4d5f538de4fb30378a3c52988d9ad7f596ae5457f4e7aae
ID: L140
TIME: 2026-08-13
LEVEL: 4
TAGS: #ablacao-real #llm #gradiente #exploracao #repeticao #memoria #distinct-candidates #primeira-rodada #level-4
---
## L140 — 2026-08-13 — Primeira rodada real da ablação: memória muda exploração (alone repete, managed explora) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" — montar o problema real e rodar os trials de verdade da ablação (sujeito = LLM qwen2.5-coder). |
| **Technique** | Level 4 — **(1)** Extensão de gradiente: `Verifier` agora retorna `(valid, signal, err)` (o sinal anônimo t1=PASS/t2=FAIL), `Attempt.Signal` guarda o feedback, `buildPrompt` renderiza o gradiente (não só VALID/INVALID) — sem o gradiente, VALID/INVALID puro não tem subida de colina e NENHUM agente descobre (confirmou o ponto da conversa "oráculo burro = explosão de busca"). **(2)** `TrialResult.Attempts` virou o log bruto (evidence/audit — "guardar tudo") + `Distinct` (candidatos únicos) + `ConditionResult.MedianDistinct`. **(3)** Problema real: `invariantOracle` — n≡3 (mod 7), n primo, n>100 — com feedback de propriedade (gradiente). **(4)** Rodada real (N=3, budget 12): ALONE median distinct=3 (repete, sintoma H1→H1→H1), MANAGED median distinct=11 (explora quase todo o budget). |
| **Level** | 4 |
| **Outcome** | success — Ablação rodou de verdade com o LLM. Sinal real e honesto: **memória muda o comportamento de exploração** — alone fica preso repetindo (3 únicos em 12), managed explora (11 únicos em 12). Nenhum resolveu o problema (muito difícil pra 12 attempts), MAS a métrica de exploração capturou o efeito que a métrica de "solved" não captura. |
| **Confidence** | 0.90 (N=3 é fraco; o sinal é claro mas precisa de N≥5 + problema mais fácil para conclusão sobre "solved") |
| **Tags** | #ablacao-real #llm #gradiente #exploracao #repeticao #memoria #distinct-candidates #primeira-rodada #level-4 |
| **Related** | L238 (LLMSolver), L237 (harness), L236 (oracle), L234 (ledger) |
| **Learned** | **(1) O resultado MAIS valioso não é "solved rate" e sim EXPLORAÇÃO: o amnésico repete o mesmo candidato (distinct=3 em 12 = o sintoma H1→H1→H1 que a conversa descreveu), o managed explora (distinct=11). A métrica "distinct candidates" captura o efeito da memória MESMO quando nenhum resolve — o "solved rate" sozinho teria dado "inconclusivo" e perdido o sinal.** **(2) VALID/INVALID puro não tem gradiente → nenhum agente descobre; o feedback de propriedade (t1=PASS/t2=FAIL) é o que dá a subida de colina. A conversa acertou ("comece VALID/INVALID, depois propriedade") mas a ordem prática é o inverso para LLM: precisa do gradiente DESDE O INÍCIO.** **(3) O problema "primo≡3 mod 7 >100" é difícil demais pro LLM resolver em 12 attempts (aritmética modular + primalidade são fracos no modelo) — mas a exploração já mostra o efeito da memória; para medir "solved", o problema precisa ser resolvível em poucos passos.** **(4) Guardar o log bruto dos attempts (Attempts []Attempt) é essencial — sem ele eu não teria VISTO que o alone repetia '100' e o managed explorava; o número agregado (solved rate) escondia o fenômeno.** |
| **Next** | (1) Rodar N≥5 trials com problema MAIS FÁCIL (resolvível em ~5-10 passos) para obter o sinal de "solved rate" além de exploração. (2) Medir DiscoveryEfficiency (reward/recursos) nos dois braços. (3) F4 (solution_class) + F5 (existence). (4) Alvo final: o internal/ledger como caso B. |
