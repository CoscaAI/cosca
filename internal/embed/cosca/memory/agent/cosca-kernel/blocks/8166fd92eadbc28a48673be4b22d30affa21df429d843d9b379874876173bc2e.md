PREV: 85d47bffeeaa9bf55e9f196cf3937a07079060c5ba5369be762f181ee356d44a
ID: L83
TIME: 2026-08-13
LEVEL: 5
TAGS: #durable-execution #dflow #temporal #replay #activities #retry #backoff #history #crash-recovery #idempotency #p2 #fase-1 #cosca-engine #level-5
---
## L83 — 2026-08-13 — Fase 1.9: Durable Execution — replay determinístico + activities com retry (§40) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuação do Cosca Creative Ecosystem (L218-225): etapa 1.9 — Durable Execution no padrão Temporal (P2 do Blueprint): workflows PURAS determinísticos + Activities com retry + replay por histórico — qualquer tarefa longa resumível se o processo morrer (§40). |
| **Technique** | Level 5 — **(1)** Novo package `internal/dflow`: `Workflow` (função pura de orquestração — NUNCA faz I/O direto, só chama activities), `Activity`/`ActivityFunc` (efeitos colaterais reais com `RetryPolicy`: MaxAttempts/InitialInterval/MaxInterval/BackoffMultiplier + backoff exponencial com teto), `History` (estado append-only por ActivityKey — P2: state = history, nunca variável em memória). **(2)** `WorkflowCtx.ExecActivity`: IDEMPOTENTE — se o histórico já tem o resultado, devolve sem re-executar (replay); senão executa com retry e registra. **(3)** `Runner.Run(ctx, wf, acts, retries, opts)`: com `RunOptions.History` (retomada §40), os effects já feitos viram `replayed`, nunca re-executam. **(4)** ActivityCtx via context (Attempt/LastError) para backoff por tentativa. **(5)** Testes provam: replay = 0 execuções/3 retomadas; retry recupera falha transiente (3 tentativas); retry esgotado → ErrActivityFailed; crash recovery: extract nunca re-executa após crash, só whisper roda. **(6)** CLI `cosca flow [--history path] [--fail n] [--json]` — demo do pipeline §21 com retry e replay ao vivo. **(7)** `internal/durable` (ledger com fencing/lease) já existia — o dflow adiciona a camada de ORQUESTRAÇÃO por cima (workflow+activity+history). |
| **Level** | 5 |
| **Outcome** | success — Durable Workflow implementado, 8 testes verdes, CLI E2E (retry 2x ao vivo, replay ao vivo: executed=0 replayed=3), esteira completa verde. |
| **Confidence** | 0.95 (validado: idempotência via histórico, retry com backoff, crash recovery testado e ao vivo) |
| **Tags** | #durable-execution #dflow #temporal #replay #activities #retry #backoff #history #crash-recovery #idempotency #p2 #fase-1 #cosca-engine #level-5 |
| **Related** | L225 (media — extract_audio é activity real), L224 (render — checkpoint complementar), L223 (nodegraph — o workflow orquestra o grafo), L218 (Fase 0 — princípio Temporal da pesquisa) |
| **Learned** | **(1) A separação workflow-puro vs activity-com-efeito é a essência do durable execution: o workflow nunca faz I/O (senão o replay quebra), effects reais só nas activities com retry.** **(2) Idempotência por key no histórico = replay grátis: re-executar o workflow do zero com o histórico devolve os resultados gravados — o efeito real acontece UMA vez.** **(3) Retry com backoff exponencial com teto (InitialInterval * mult^(n-1), cap MaxInterval) é o contrato certo para tarefas de mídia/GPU (falha transiente ≠ falha permanente).** **(4) History é o estado de verdade (append-only) — persistível em JSON para retomada entre processos (§40 crash recovery).** **(5) O ledger (internal/durable) resolve concorrência/fencing; o dflow resolve ORQUESTRAÇÃO — camadas complementares, não concorrentes.** |
| **Next** | (1) Etapa 1.10: Provenance + Licenças (§32-35) — fecha a Fase 1. (2) Integrar dflow ao render/ngraph (tarefas longas resumíveis de verdade: render com checkpoint + workflow). (3) Integrar com o ledger durable existente (fencing/lease no runner). |
