# L398 — TRANSFORMACAO E-007 EXECUTADA (PASS) — FTS incremental no caminho real

## status: DECISION (autorizado formalmente; executado sob o CAMPAIGN_PROTOCOL v3)

## FASE 1 PRE_STATE: commit 466c139; backup knowledge-pre-e007-transform (398MB);
## corpus 63.100 chunks/51.250 vetores; verify 1 issue (mismatch por design)

## FASE 2 IMPLEMENTACAO (commit 88e593c)
- storeDocument: RemoveDocument do oldID antes da substituicao + indexDocumentFTS
  (IndexDocument/IndexChunk/IndexCodeBlock — rowids dos content tables)
- IndexDirectory: batch RebuildIndex removido (RebuildAll permanece)
- fts.go: IndexDocument INSERT OR REPLACE; IndexChunk delete-then-insert
- fora do escopo intacto (ranking/busca/embeddings/provider/digest/vector/
  graph/chunking/dedup/contratos/recovery/provenance)

## FASE 3 GUARDIAO (PASS)
- testes existentes verdes (indexer/sqlite/knowledge)
- caminho real insert/update/delete: PASS (a/b/c — sem stale, delete limpo)
- counts FTS: 63.107 = 63.107
- recall@10 (20 queries): 200/200 (0 perdidos)
- benchmark: IndexDirectory ~0s (antes ~700ms/rebuild)
- integridade: verify 1 issue (mismatch por design — inalterado)

## O que foi PROVADO
- o incremental no caminho REAL produz o MESMO indice que o rebuild
  (counts + recall@10 200/200 + insert/update/delete reais)
- o custo do rebuild eliminado do caminho normal (~700ms por IndexDirectory;
  69x-5769x por operacao — E-007)

## INFERRED / UNKNOWN / NOT TESTED
INFERRED o ganho em producao (o serve com o binario novo) — nao instalado
UNKNOWN o comportamento do serve em producao com o binario novo (a instalar
  com o Don — o CARRO_PROTOCOL)
NOT TESTED: outros caminhos de escrita do FTS fora do indexer (a revisar se
  existem); o RebuildAll intencional (nao exercitado nesta transformacao)

## Classificacao
MEASURED counts/recall/benchmark (acima)
EVIDENCE o caminho real a/b/c + 200/200 + counts iguais
INFERRED o ganho em producao (binario nao instalado)
UNKNOWN serve em producao com o binario novo
DECISION transformacao PASS; commit 88e593c + chain assinada; PARAR (o Don
  decide a instalacao do binario do serve)
