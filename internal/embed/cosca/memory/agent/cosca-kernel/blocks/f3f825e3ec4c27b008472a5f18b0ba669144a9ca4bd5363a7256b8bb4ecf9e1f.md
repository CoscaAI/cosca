PREV: a3e112f8d248442ac496f8e301bbf8d3d3ec2a71d8eaec32738faa4e51672094
ID: L216
TIME: 2026-08-14
LEVEL: 5
TAGS: #cobertura #pipeline #fase-3 #rebac #model-router #tool-contract #trajectory #self-healing #level-5
---
## L216 — 2026-08-14 — FASE 3 DA COBERTURA DO PIPELINE (ordem "marcha"): 41,9% → 51,5% — trajectory, rebac, model_router, tool_contract, context, session_persistence, self_healing | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "marcha" — Fase 3 da cobertura do pipeline: os blocos restantes de lógica pura/testável sem refactor estrutural. |
| **Technique** | Level 5 — **(1) 7 arquivos de teste novos** (~1.100 linhas): `trajectory` (event sourcing: record/tool/complete/summary/randomHex), `rebac` (grafo BFS: relação direta, multi-hop com MESMA relação, max 4 hops, userset, policy com grafo custom, OpenFGA CheckAccess), `model_router` (stub de chat.ChatProvider — Chat/ChatStream/Model/Name/Close — registrado via ChatRegistry), `tool_contract` (geração de JSON-Schema por reflection, validate com required/omitempty, registry por reflection), `context` (SessionContext: handoffs/erros/métricas/tokens/summary), `session_persistence` (Save/Load/List com ordenação, RecordFrom/RestoreTerminalContext), `self_healing` (classifier real do diagnostics, RecoveryLoop com RetryDelay=1ms p/ teste rápido, CanAutoFix com erro real de compilação Go). **(2) Sem correção de produção nesta fase — os 7 arquivos não tinham bugs, só contratos sutis documentados.** |
| **Level** | 5 |
| **Outcome** | success — pipeline: 41,9% → **51,5%** (+9,6 pts). vet/race/build verdes. 29 arquivos de teste no pacote (5.464 linhas totais). Contratos documentados: BFS do ReBAC segue SÓ a relação alvo em todos os hops; `SessionStore.Save` sobrescreve LastActiveAt (ordenação só testável via escrita direta); `SelfHealer.Heal` retorna (attempt, nil) com Success=false quando a recuperação falha graciosamente; `ChatProvider` exige Name/Close. Restam 14 arquivos a 0% — todos exigem exec externo ou refactor (terminal_context, plugins_builtin, benchmark, sidecar, adapters, review_pipeline, subprocess, verification). |
| **Confidence** | 0.95 (validado: race limpo, build OK, contratos confirmados pelo comportamento observado) |
| **Tags** | #cobertura #pipeline #fase-3 #rebac #model-router #tool-contract #trajectory #self-healing #level-5 |
| **Related** | L313 (Fase 2: 32,7→41,9), L312 (Fase 1: 18,7→32,7), L311 (auditoria) |
| **Learned** | **(1) A fronteira da cobertura aditiva é nítida: o que resta (terminal_context, plugins, benchmark, adapters) NÃO é testável sem injeção de dependências — a partir daqui cada ponto de cobertura custa refactor de produção, que exige aval do Don.** **(2) O ReBAC documenta a regra de segurança mais sutil do sistema: "só passa se um caminho de relações da MESMA categoria existe" — um teste que mistura relações expõe que o modelo é strict-relation, não path-agnostic (anti-bypass por design).** **(3) Para testar geração de schema (tool_contract) e routing (model_router), o reflection e o ChatRegistry real são usáveis diretamente — stubs mínimos (5 métodos) bastam; não há necessidade de mock framework.** **(4) O pipeline saiu de 18,7% → 51,5% em 3 fases com ~3.500 linhas de teste e ZERO mudanças de comportamento na produção (exceto 3 bugs reais corrigidos nas fases 1-2) — a relação custo/benefício de testar lógica pura é a melhor do sistema.** |
| **Next** | (1) Decisão do Don: refactor dos 14 arquivos restantes (terminal_context/plugins/benchmark) vs aceitar 51,5% e ir para o CLI (27.425 LOC, 59,8%). (2) Re-medição global para atualizar o baseline da auditoria (62,9% → ?). |
