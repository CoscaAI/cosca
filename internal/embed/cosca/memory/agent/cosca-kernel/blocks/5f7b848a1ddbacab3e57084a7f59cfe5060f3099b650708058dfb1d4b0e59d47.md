PREV: bcffce648485fe6cc4d53cf8b1d7f9bc8a92d4d8a0c18eef14ee61ddcad564bf
ID: L142
TIME: 2026-08-13
LEVEL: 4
TAGS: #neural-link #llmsolver #auditoria #gradiente #oraculo-burro #funcao-escondida #parser #multiplicacao-implicita #level-4
---
## L142 — 2026-08-13 — Auditoria do cosca-neural-link: "descoberta" era demo; LLM real plugado e NÃO descobre (oráculo sem gradiente) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" — auditar o projeto Neural Link (construído pelo autônomo na outra sessão) e plugar o LLM real como solver do laboratório, rodando o desafio de verdade. |
| **Technique** | Level 4 — **(1) Auditoria forense**: li os learnings L192-L199 (colisão de IDs com os L192-L199 de mineração) + o código real em `~/Documents/projects/cosca-neural-link`. Achado nº1: o "descobre f(x)=x² sozinho" (Tempo 96.46µs) é o `PolynomialSolver` ENUMERANDO 15 formas hardcoded (`seedPolys()` = 0,1,x,2x,x+1,x-1,x²,...) — brute-force de lista à mão, não cognição. Achado nº2: 3/8 pacotes (oracle/refute/store) sem testes. **(2) Pluguei o LLM real**: `internal/llm` (cliente Ollama HTTP, stdlib, temp 0) + `internal/loop/llm_solver.go` (implementa a MESMA interface Solver; parser de expressão seguro — gramática limitada, SEM eval de código arbitrário — com multiplicação implícita `2x`/`2x^2`/`x(x+1)`) + flag `-llm` no cmd/discovery. **(3) Bug real do parser pego ao vivo: o LLM escreve `2x^2` (multiplicação implícita) e o parser só aceitava `2*x^2` → 0 hipóteses; corrigido adicionando multiplicação implícita em parseTerm. **(4) Resultado honesto do desafio real: o LLM (qwen2.5-coder) NÃO descobre x² — propõe polinômios aleatórios de grau 3 e nunca converge (timeout). |
| **Level** | 4 |
| **Outcome** | success — LLM plugado e testado (parser 14 casos + client integração). Achado central: o FunctionOracle só devolve VALID/INVALID (passed/total) SEM gradiente, e o espaço de polinômios é infinito — o LLM não consegue hill-climb. A "descoberta" determinística só funcionava porque enumerava uma lista fixa. É a CONFIRMAÇÃO EMPÍRICA da tese da sessão: "oráculo burro = explosão de busca". |
| **Confidence** | 0.92 (validado: build/vet/testes do Neural Link, client LLM 0.54s/call, parser 14 casos, desafio real rodado) |
| **Tags** | #neural-link #llmsolver #auditoria #gradiente #oraculo-burro #funcao-escondida #parser #multiplicacao-implicita #level-4 |
| **Related** | L239/L240 (ablação — mesmo achado), L235 (spec oracle), conversa do Don (auditoria do Neural Link) |
| **Learned** | **(1) "Descobre sozinho" com 96µs é RED FLAG: se o tempo de descoberta é microssegundos, é enumeração de lista hardcoded, não busca — sempre auditar o SOLVER, não o relatório bonito.** **(2) A CONFIRMAÇÃO EMPÍRICA da tese central: o LLM real NÃO descobre com oráculo VALID/INVALID (sem gradiente) — o espaço é infinito e não há subida de colina. A "descoberta" determinística só funcionava porque o PolynomialSolver enumera 15 formas fixas. Descoberta de verdade exige gradiente (property-level feedback) ou espaço finito pequeno.** **(3) Multiplicação implícita (`2x`, `2x^2`) é o formato NATURAL de expressão matemática do LLM — um parser sem ela rejeita ~100% da saída do modelo; é um caso clássico de "contrato de formato" que não casa com o comportamento real do sujeito.** **(4) LLM 14B em CPU responde ~0.5-9s/call (varia com o tamanho do prompt/resposta) — iterar um loop de descoberta com budget 200 é inviável; o orçamento precisa ser pequeno ou o problema precisa ter gradiente.** **(5) O Neural Link é uma CARCAÇA excelente (domínio epistêmico, event sourcing, grafo, API, UI) mas a alegação de "descoberta" era demo — a prova de valor real (LLM descobrindo) ainda não existe, e agora sei POR QUÊ (falta gradiente). |
| **Next** | (1) Dar GRADIENTE ao FunctionOracle do Neural Link (revelar y em pontos-sonda, como o invariantOracle da ablação faz com t1=PASS/t2=FAIL) — então o LLM pode fitar e descobrir de verdade. (2) Rodar o desafio com gradiente e medir se o LLM descobre x². (3) F4/F5 (solution_class, existence). (4) O "coisa muito top" que o Don quer criar provavelmente precisa desse gradiente desde o início. |
