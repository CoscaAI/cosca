PREV: c16651fd89dd7a14b483fa552c17302ee5f406e4d5d640954b8492c47f38e044
ID: L158
TIME: 2026-08-13
LEVEL: 4
TAGS: #neural-link #time-heterogeneo #graph-fold #benchmark-as-gate #anti-overfit #systemd #linger #cobertura #level-4
---
## L158 — 2026-08-13 — Neural Link: fecha os itens restantes (time heterogêneo + graph fold + benchmark-as-gate + systemd) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "resolve" (segunda vez) — fechar os itens 🟡/🟢 que sobraram: LLM no time multi-agente, graph fold completo, benchmark-as-gate anti-overfit, unit systemd permanente, cobertura de testes. |
| **Technique** | Level 4 — **(1) Time heterogêneo**: `runTeamLocked(solvers, objective)` sob `runMu` + `RunTeamLLM` (2 PolynomialSolver + 1 LLMSolver); o oracle é gradiente (o determinístico ignora samples, o LLM usa) — um único oracle serve os dois. **(2) Graph fold**: `replay.SnapshotAt` agora reconstrói a LISTA de hipóteses exploradas (statement + estado refuted/confirmed) via fold — map `entity_id → índice` para atualizar estado sem duplicar; `EventExperimentStarted` cria "testing", `EventExperimentFinished` atualiza. **(3) Benchmark-as-gate**: `evolve.discover(client, prompt, hidden, events)` parametriza a função alvo; `benchmarkScore` roda a descoberta sobre 3 funções (x³, x²+1, 2x+1) com event store ISOLADO (gate não é observabilidade); o gate aceita variante só se `vScore > currentScore && vBench >= benchmarkBaseline` — impede "trapacear" hardcodando x² (L245). **(4) systemd permanente**: `deploy/cosca-neural-link.service` + `cp → ~/.config/systemd/user/` + `daemon-reload` + `enable --now` + `loginctl enable-linger` — daemon 24/7 real (o systemd-run anterior era transiente). **(5) Cobertura**: oracle 50→87.5% (modo estrito/CheckAt/DomainHint), replay 100%, causal 100%, refute 100%. |
| **Level** | 4 |
| **Outcome** | success — Serviço systemd permanente ativo (enabled+running, Main PID 1269888); `/timeline` restaura 31 eventos pós-restart; 13 pacotes verdes; oracle 87.5%, replay/causal/refute 100%. Commits `367c5ef`. |
| **Confidence** | 0.90 (validado: systemctl is-active=active, timeline restaurado, testes verdes, frontend builda) |
| **Tags** | #neural-link #time-heterogeneo #graph-fold #benchmark-as-gate #anti-overfit #systemd #linger #cobertura #level-4 |
| **Related** | L256 (3 críticos), L255 (observabilidade), L245 (self-evolution/overfit), L251/L252 (daemon morre) |
| **Learned** | **(1) Um único oracle gradiente serve time heterogêneo: o solver determinístico IGNORA as amostras e o LLM as USA — não precisa de dois oracles; a interface `Solver` é o ponto de composição (mesmo princípio do L192/L195).** **(2) Graph fold por `entity_id → índice` é o truque: para reconstruir uma lista de entidades com estado mutável a partir de um log append-only, mapeia-se o ID para a posição na slice e ATUALIZA no lugar — sem duplicar e sem reordenar.** **(3) Benchmark-as-gate resolve o overfit estruturalmente: o gate usa event store ISOLADO porque benchmark é GATE (não observabilidade) — se usasse o store compartilhado, a timeline do app seria inundada por runs de benchmark.** **(4) `systemd-run --user` é TRANSITÓRIO (some no reboot); o serviço permanente exige unit file + `enable --now` + `loginctl enable-linger` para rodar sem login ativo — a diferença entre "funciona agora" e "24/7".** **(5) Cobertura de testes por função pura é o caminho de maior retorno: testar `splitVariants`/`describePrompt`/`DomainHint`/`CheckAt` (sem LLM) é barato e fecha branches que a integração não cobre.** |
| **Next** | (1) Teste de integração do pipeline completo LLM (automatizado, hoje é manual via curl). (2) Observabilidade operacional (health do Ollama, métricas de runtime). (3) A MINA com gradiente contínuo + dossiê de saída (M3/M5 do L246). |
