PREV: 45c6b71e57406cc41d9924afff077499d3b20a2a5a4506c2a9eca3f29f17da52
ID: L409
TIME: 2026-08-17
LEVEL: 5
TAGS: #memoria #re-cadeamento #p15 #chain #merkle #integridade #blocks #agents #transformacao #level-5
---
## L409 — 2026-08-17 — Re-cadeamento P15 dos 54 agents: 258 blocks com sha256 REAL + chain.dat/merkle por agente | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Fechar achado #2 do L406: chain.dat por agente (54 agents sem ledger próprio) — e no caminho descobrir hashes falsos |
| **Technique** | Auditoria sha256 (nome vs conteúdo) → diagnóstico (dívida da migração P15, commit c765754, nunca re-cadeada como o kernel 6bc966f) → dry-run idempotente → re-cadeamento em 4 passos (ordem pela cadeia PREV, PREV novo = hash do anterior, renomear, gatilhos) → chain.dat por agente (formato kernel) → merkle por agente → validação cruzada chain↔blocks |
| **Level** | 5 |
| **Outcome** | success |
| **Confidence** | 0.98 |
| **Tags** | #memoria #re-cadeamento #p15 #chain #merkle #integridade #blocks #agents #transformacao |
| **Related** | L406, L407, L408, L57, L254, L255, L219, L199, L45 |
| **Learned** | 1) **Achado crítico**: 258/258 blocks dos 54 agents tinham nome ≠ sha256(conteúdo) — hash calculado ANTES de injetar o header (PREV/ID/TIME/LEVEL/TAGS) na migração P15 (c765754, 15/08). O kernel foi re-cadeado (6bc966f), os agents NUNCA. 2) **Re-cadeamento completo**: nome==hash REAL em 258/258; PREV novo = sha256 do bloco anterior (raiz = zeros); gatilhos dos 54 learnings.md atualizados (hash16 novo); chain.dat por agente no formato do kernel (54/54, cosca-api vazio); merkle por agente (53/53). 3) **Validação cruzada**: 0 chain→block órfãos, 0 blocks sem chain, 0 PREV órfãos, 1 raiz por agente. 4) **Cadeia da família**: 1956 arquivos assinados (era 1795) — 161 novos arquivos (chain.dat/merkle). 5) **Método seguro**: backup tar.gz + dry-run idempotente + git como rede de reversão; P13 (validar antes e depois, nunca confiar na mensagem). 6) O cosca-api (0 blocks) ganhou chain.dat vazio por consistência estrutural — todo agente tem ledger. |
| **Next** | Restam do L406: #3 (padronizar formato de learnings — os 4 agents com blocks sem gatilho no padrão `### 20` ainda têm formato L1-SEED/TASK) e #4 (SKILLS_CATALOG sync 30 vs 89). O formato de learnings dos agents segue `### {data}` — ok por design próprio (diferente do kernel `## L`); documentar se necessário. |
