PREV: c92af6c88b136bbaa929420aa981e1f4ff642fb719c5c7ac7dcd1d42e1a5133d
ID: L224
TIME: 2026-08-14
LEVEL: 5
TAGS: #p1 #p2 #chaos-test #replay-parcial #idempotencia #adversarial #zero-hang #level-5
---
## L224 — 2026-08-14 — testes P1+P2: replay parcial, idempotência ×3, tool extremo, adversarial, chaos cognitive test (zero hang, race limpo) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Continuar os testes de sistema: P1 (replay parcial, idempotência Resume×3, tool failures extremos, adversarial prompts) e P2 (chaos cognitive test). |
| **Technique** | Level 5 — recon (durable idempotent resume já existia; ReadTool não trunca — o gap era provar com 5MB); P1 (4 testes): replay parcial pós-crash (t3 nunca rodou → PENDING, nunca inventado), Resume×3 no durable (idempotência exata), tool extremo (read 5MB sem truncar, write vazio recusado, edit ambíguo → erro claro), adversarial prompts (vazio/espaços/10k palavras → fallback, nunca panic). P2 chaos (1 teste, -race): 20 sessões em goroutines contra `chaosProvider` (stall + falha transiente), timeout total 5s — invariante: toda sessão termina em tempo finito. |
| **Level** | 5 |
| **Outcome** | success — 8 testes verdes, race limpo (inclusive chaos com 20 goroutines), suítes completas, build OK, global 67,8%. Commit `d922986`. Chaos prova: mesmo com provider caótico, nenhuma chamada pendura (watchdog + retry + deadline total). |
| **Confidence** | 0.96 (race limpo no chaos; suítes completas; build; determinismo por seed fixo) |
| **Tags** | #p1 #p2 #chaos-test #replay-parcial #idempotencia #adversarial #zero-hang #level-5 |
| **Related** | L321 (P0), L320 (stallwatch — base do chaos) |
| **Learned** | **(1) O chaos test é o teste de sistema mais barato com o maior retorno: 20 goroutines + provider caótico + deadline provou em 0,07s que nenhuma chamada pendura sob instabilidade.** **(2) recovered=0 no chaos é artefato do perfil (stalls consecutivos esgotam retries), não falha — o invariante real (nunca pendura) foi o validado.** **(3) Replay parcial documentou o contrato: um passo que nunca rodou NUNCA é inventado (pending, não completed) — permite retomar pós-crash com segurança.** **(4) Planner adversarial: prompts vazio/espaços/gigantes produzem fallback sem panic, com intent determinístico.** **(5) Durable idempotência ×3: o log é a fonte da verdade; re-executar o mesmo plano não re-executa steps.** |
| **Next** | (1) Ampliar o chaos (crashes de runtime, eventos atrasados). (2) Categoria B para ~2,2 pts até 70%. (3) Working tree limpa. |
