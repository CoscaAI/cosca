PREV: 10707a28d16e81e6b94455c493bcf77bf673a138557d2a636e3b566b36bf953e
ID: L155
TIME: 2026-08-13
LEVEL: 4
TAGS: #auditoria #bug-fix #panic #promote #erro-silencioso #metrics-fake #oracle-gradiente #unsubscribe #dead-code #cobertura #level-4
---
## L155 — 2026-08-13 — Auditoria profunda + correção total do cosca-neural-link (bug→fix→verde) | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "o carro ta sem pintura ou foi pintado com pincel? resolve tudo ai" — auditoria completa (evidência, performance, erros, mocks, placeholders, manipulação de código, código desabilitado, delays, loops) do projeto `cosca-neural-link` e correção de TUDO que foi encontrado. |
| **Technique** | Level 4 — auditoria em 10 dimensões com evidência (grep/read/`go test -cover`/`-race`) + correção em lote: **(1)** panic latente `e.oracle.(*oracle.FunctionOracle)` sem comma-ok → comma-ok + evento INCIDENT. **(2)** `domain.Promote()` (regra dura, seção 2) era código morto — o engine atribuía `h.State` direto → helper `engine.promote()` aplica Promote em TODA transição. **(3)** `LLMSolver.SeedHypotheses/Refine` engoliam erro do LLM (`return nil`) → interface `Solver` agora retorna `(…, error)`; o engine distingue "LLM falhou" de "espaço vazio" (HALT+INCIDENT). **(4)** score cognitivo com 2/5 dimensões fake (HallucinationRate/EvidenceCoverage nunca calculadas → Robustness=0 sempre) → medidas reais no buildReport. **(5)** oracle vazava f(x) → `RevealSamples` + `NewHiddenOracle` (estrito) + `NewChallenge` estrito + `NewChallengeWithGradient` (LLM). **(6)** LLM não plugado no app → `RunDiscoveryLLM` + botão no frontend. **(7)** bug do `Subscribe` unsubscribe (`&f == &fn` sempre false) → nil-marker + `sync.Once`. **(8)** dead code (`DomainHint` descartava min/max; `/agents` listava "verifier" inexistente) → corrigidos. **(9)** erros ignorados no startup/persist → logging stderr. **(10)** HTTP server sem timeouts → `http.Server` com Read/Write/Idle. **(11)** cobertura 0% em refute/store → testes novos (refute 100%, store 73.8%). |
| **Level** | 4 |
| **Outcome** | success — 19 arquivos alterados + 2 testes novos; `go build`/`go vet`/`go test`/`go test -race` verdes; frontend builda; smoke test descobre f(x)=x² VERIFIED; cobertura refute 0→100%, store 0→73.8%, eventstore 67→81%. |
| **Confidence** | 0.92 (validado: build/vet/test/-race/frontend/smoke test verdes, gofmt limpo) |
| **Tags** | #auditoria #bug-fix #panic #promote #erro-silencioso #metrics-fake #oracle-gradiente #unsubscribe #dead-code #cobertura #level-4 |
| **Related** | L192-L199 (construção do neural-link), L186 (auto-sign git-anchored), AUTO_EVOLUTION_PROTOCOL |
| **Learned** | **(1) O padrão sistêmico do neural-link era "declarado mas não efetivado": Promote() testado mas não chamado, score com dimensões default 0, LLM existente mas não plugado, store que grava mas não lê. Auditoria não é só caçar bug — é achar o que PARECE feito mas não está.** **(2) Type assertion sem comma-ok + zero `recover()` no projeto = panic derruba o app Wails inteiro; `x.(T)` é bomba-relógio, `x, ok := x.(T)` é contrato.** **(3) Engolir erro de LLM (`return nil`) é PIOR que crashar: transforma "Ollama caiu" em "espaço de busca vazio" — o sistema mente sobre a causa. Retornar `(…, error)` e distinguir os dois é a única forma honesta.** **(4) "Métrica medida vs default": um score que exibe 5 dimensões mas calcula só 3 é manipulação de evidência — o Don vê número que não mede nada. Fix = computar do dado real ou remover a dimensão.** **(5) `&f == &fn` compara endereço de cópias locais → sempre false; unsubscribe por índice com nil-marker + `sync.Once` é o padrão robusto (funcs não são comparáveis em Go).** **(6) Oracle "hidden" que devolve amostras f(x) é uma relaxação que PRECISA ser explícita — escondê-la sob o nome "hidden" é dishonest; os modos gradiente/estrito separam os dois contratos.** **(7) `gofmt -l` pega drift de alinhamento que o olho não vê — rodar antes de entregar.** |
| **Next** | (1) Commit do neural-link (aguardando ordem do Don). (2) Persistência read-back (store grava mas não restaura no startup — gap restante documentado no roadmap). (3) Rodar `cmd/evolve` com mais iterações para evidência estatística do self-evolution. |
