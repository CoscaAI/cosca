PREV: 09d4c693aaed1f97bfe609fc475b8618f2820a686347f162dc9404e12e144982
ID: L80
TIME: 2026-08-13
LEVEL: 5
TAGS: #fase-3 #cosca-image #mediaexec #executor #node-graph #cosca-media #remove-bg #resize #convert #load-image #graph-run #webp #level-5
---
## L80 — 2026-08-13 — Fase 3: COSCA IMAGE — executor real de imagem no node graph (§7) | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "sim" — Fase 3 do Cosca Creative Ecosystem: COSCA IMAGE (gerar/editar/restaurar/upscale/segmentar/remover fundo/inpaint/colorir/compor/vetorizar) executando no editor. |
| **Technique** | Level 5 — **(1)** `internal/mediaexec` no cosca-code: `Executor` de imagem que implementa o contrato de executor do node graph (Fase 1.6) chamando a **cosca-media** (Python) como processo — remove_bg (model u2net + feather), convert (formato-aware), resize, identify (MAGIC), ocr, vectorize + **load_image** (nó de ENTRADA que valida path e devolve caminho). **(2)** Integração com cache por assinatura (§23): o grafo executa com o RunStats do nodegraph. **(3)** Bridge `pkg/engine` ampliado: Executor/ExecutorFunc/Cache/NewCache/RunStats/RunOptions/GraphRun — consumidores externos executam grafos. **(4)** Endpoint `POST /api/engine/graph/run`: executa o grafo de imagem E2E + evento `graph.executed` no bus. **(5)** NodeGraphPanel ganha botão **Executar** — mostra executados vs cache hits e artefatos. **(6)** Bug real pego: nó `identify` retorna JSON (string), não caminho — um nó terminal não pode alimentar outro; e nós de entrada precisam de `load_image` (param path), não "primeiro input". Correção: firstInput usa param path quando sem inputs. |
| **Level** | 5 |
| **Outcome** | success — executor real funcionando: pipeline load_image→resize→convert gerou **WebP real 32x32** no disco (provado E2E). 5 testes mediaexec verdes, esteira 25 packages OK cosca-code + bridge OK cosca. |
| **Confidence** | 0.95 (validado: cosca-media real invocada, artefato WebP conferido por bytes/file, cache por assinatura pronto) |
| **Tags** | #fase-3 #cosca-image #mediaexec #executor #node-graph #cosca-media #remove-bg #resize #convert #load-image #graph-run #webp #level-5 |
| **Related** | L228 (Fase 2 editor), L223 (nodegraph — executor contract), L210 (cosca-media visual engine), L207 (security — mídia é input não confiável) |
| **Learned** | **(1) Executor real = a ponte entre o grafo (dados) e a mídia (efeito): cada nó vira um comando da cosca-media — o §7 pipeline (PROMPT→MODEL→IMAGE→SEGMENT→EDIT→UPSCALE→EXPORT) é um grafo com executores, não código espaguete.** **(2) Nós de ENTRADA (load_image) e nós TERMINAIS (identify/ocr) têm papéis distintos — terminal produz observação (string), entrada produz caminho; misturar os dois quebra o pipeline (bug real pego).** **(3) O bridge pkg/engine precisa expor o EXECUTOR (não só dados): a execução de grafo é o que faz o editor agir, não só exibir.** **(4) A cosca-media (Python) é o executor de mídia real — Go orquestra, Python processa (divisão de trabalho L211).** |
| **Next** | (1) Fase 4: COSCA CINEMA (timeline + render). (2) remove_bg no pipeline visual (teste com imagem real de produto). (3) Ligar o modo do projeto (2.1) ao canvas: project_type=image → NodeGraphPanel default. (4) Gerar derivatives (thumbnail) no asset add. |
