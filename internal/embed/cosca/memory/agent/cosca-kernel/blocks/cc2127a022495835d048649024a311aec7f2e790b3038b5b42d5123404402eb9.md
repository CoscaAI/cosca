PREV: e0990515bce4f4bd6d7c3d616ba0be79d4d3397d2fd85b978e8d989217068efc
ID: L220
TIME: 2026-08-14
LEVEL: 5
TAGS: #cobertura #itens-a-d #benchmark #theme #chat-tool #providers #bug-real #pytest-parser #web-fetch-stub #level-5
---
## L220 — 2026-08-14 — ITENS A-D DA TABELA (ordem "sim"): benchmark 47%, theme 56%, chat/tool 49%, providers 14-81% + BUG REAL no parser de pytest (fail sempre 0) + WebFetchTool é STUB | Level 5

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order "sim" — atacar os itens de custo baixo/médio da tabela de cobertura: A (internal/benchmark), B (chat/ui/theme), C (internal/chat/tool), D (providers cloud bedrock/azure/ollama/google/groq/mistral). |
| **Technique** | Level 5 — **(1) A: benchmark** — Registry, CoscaSelfEval, LLMJudge com runner stub + parseJudgeOutput puro, Scorer (save/load/list/compare). **(2) B: theme** — styles renderizam com `lipgloss.SetColorProfile(termenv.TrueColor)` no init (sem TTY não há ANSI — lição de CI), fallback do background, RiskLevel bold, MarkdownRenderer real. **(3) C: chat/tool** — ShellTool com mock sandbox (chama `sh -c` no modo workspace) + exec policy (forbidden bloqueia antes do sandbox), GitTool whitelist + stash + modo read-only, parsers puros de test results, search parsers, WebFetchTool, WebSearchTool. **(4) D: providers** — embedding providers: config/getters/validação (azure/bedrock/google exigem credenciais → erro), ollama GenerateEmbeddings com httptest (detecção de dimensão), groq/mistral factories. |
| **Level** | 5 |
| **Outcome** | success — global **66,1% → 66,7%** [MEASURED]. Por pacote: benchmark 0→47,4%, theme 6→56,1%, chat/tool 31,1→48,7%, ollama 56,7%, groq 81,2%, mistral 75,0%, azure 25,2%, bedrock 14,4%, google 20,8%. **BUG real #5 corrigido em `parsePytestResults`**: usava `switch` com cases " passed"/" failed" MUTUAMENTE EXCLUSIVOS — a linha típica do pytest ("4 passed, 1 failed in 1.2s") cai no primeiro case e **fail era SEMPRE 0** (resultados de teste falseavam). Fix: ifs independentes. **Dívida documentada**: `WebFetchTool.fetch` é um STUB (TODO "Implement HTTP call to Jina Reader API") — a ferramenta web_fetch NÃO faz chamada HTTP real. |
| **Confidence** | 0.96 (validado: race limpo em todos, build OK, global re-medido, bug confirmado por teste que falhava antes do fix) |
| **Tags** | #cobertura #itens-a-d #benchmark #theme #chat-tool #providers #bug-real #pytest-parser #web-fetch-stub #level-5 |
| **Related** | L317 (fix do fantasma), L316 (Fase 4), L315 (tabela de cobertura), L311 (auditoria) |
| **Learned** | **(1) Testar UI lipgloss fora de TTY exige forçar o color profile no init — sem isso o render vem sem ANSI e qualquer asserção de cor falha (lição de CI).** **(2) O `switch` em parsers de output é uma armadilha clássica: quando dois contadores aparecem na MESMA linha, os cases se cancelam — pytest passava com 4, fail sempre 0, e ninguém percebia porque o parser "funcionava" para o caso simples.** **(3) Stubs com TODO no caminho feliz são dívida invisível: WebFetchTool.Execute retorna placeholder com sucesso — testes documentaram o contrato atual em vez de fingir que há integração.** **(4) O ajuste de cor do termenv (0x72→113, não 114) torna asserções hex exatas frágeis — testar propriedades estruturais (bold, background presente) em vez de valores exatos.** |
| **Next** | (1) Nada commitado. (2) Dívidas abertas: WebFetchTool stub (integrar Jina Reader), providers azure/bedrock/google abaixo de 30% (testes de GenerateEmbeddings com httptest), 9 arquivos do pipeline a 0%. (3) Global 66,7% — faltam ~3,3 pts para 70%. |
