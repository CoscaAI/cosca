PREV: c1da9eab73ecad349d6f041aa5d5bed590aea070b217e563f82299ffa49841ed
ID: L165
TIME: 2026-08-13
LEVEL: 4
TAGS: #cosca-trader #f2 #oms #ordens #posicoes #pnl #hmac #assinatura #user-data-stream #listenkey #level-4
---
## L165 — 2026-08-13 — cosca-trader F2: OMS (ordens/fills/posições/saldos) + execução autenticada Binance | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | F2 do cosca-trader: OMS completo (ordens market/limit/stop, fills, posições, saldos) + execução autenticada da Binance (REST assinado HMAC + user data stream). |
| **Technique** | Level 4 — **(1) `internal/oms`**: Order Management System — `PlaceOrder` (valida→broker→registra→emite OrderCreated), `CancelOrder`, `ApplyOrderUpdate` (merge preserva campos ausentes), `ApplyTrade` (atualiza posição), `ApplyBalance`; lógica de posição `applyFill` = aumento (média ponderada), redução/fechamento (PnL realizado proporcional ao preço médio), e FLIP quando o fill excede a posição (o excesso abre o lado oposto). **(2) `exchange.Broker`** separado de `exchange.Exchange` (market data) — execução exige credenciais e opera sobre a conta do usuário: riscos/contratos diferentes. **(3) Binance REST assinado**: HMAC-SHA256 do query string (com `timestamp`), header `X-MBX-APIKEY`; `url.Values.Encode()` ordena as chaves (exigência da Binance); GET/POST/DELETE todos com params no query string. **(4) User data stream**: `POST /api/v3/userDataStream` → listenKey → `wss://.../ws/{listenKey}`, keepalive PUT a cada 30min (expira 60), reconexão com novo listenKey. **(5) Testes com fake broker (sem rede) + payloads REAIS da Binance.** |
| **Level** | 4 |
| **Outcome** | success — 22 testes verdes, gofmt/vet limpos. Smoke test: modo observação (sem chaves) com `/orders` `/positions` `/balances` respondendo e `trading:false` no health. Commit `091983b`. |
| **Confidence** | 0.88 (validado: 22 testes incl. fake broker + payloads reais; a execução LIVE exige chaves que o Don ainda não forneceu — validada a lógica, não o round-trip de conta real) |
| **Tags** | #cosca-trader #f2 #oms #ordens #posicoes #pnl #hmac #assinatura #user-data-stream #listenkey #level-4 |
| **Related** | L262 (F1 gotcha json), L261 (F0), L256 (cosca-code Provider Engine) |
| **Learned** | **(1) O executionReport da Binance tem ~20 pares case-colliding (e/E, s/S, c/C, o/O, f/F, q/Q, p/P, x/X, i/I, l/L, z/Z, n/N, t/T, m/M) — a lição do L262 se amplia: declarar TODOS os campos com tipo certo é obrigatório, e o campo `N` (commission asset) pode vir null → usar `*string`.** **(2) O REST de ordens usa lowerCamelCase (symbol/orderId/origQty/executedQty), DIFERENTE do stream executionReport (letras únicas) — são DOIS formatos de resposta para a mesma entidade, precisa de dois structs + conversão.** **(3) A lógica de posição (aumento/redução/flip com PnL realizado) é o coração do OMS: long soma em compra e realiza PnL em venda; flip = o excesso do fill abre o lado oposto. Testar os 4 casos (abrir/aumentar/fechar/flip) separadamente.** **(4) `Broker` separado de `Exchange` é o design certo: market data é público e descredenciado, execução é autenticada e muta a conta — acoplar os dois forçaria o core de observação a carregar segredos.** **(5) nil pointer em `applyFill(pos, ...)` quando a posição não existe: o caller aloca `&domain.Position{}` antes — a função assume non-nil (contrato claro).** **(6) listenKey tem TTL de 60min — keepalive PUT a cada 30min é obrigatório ou o stream morre silenciosamente.** |
| **Next** | (1) F3: gráficos (KLineChart/lightweight-charts) + indicadores como plugins (cinar/indicator). (2) Paper trading (broker simulado) para validar OMS sem fundos reais. (3) Book de ofertas (depth stream) + sincronização inicial de ordens/posições via REST no startup. |
