PREV: fdd08a29288493146f7f9f10860c520f7b6fede453d38b8cf300dd63121c1758
ID: L98
TIME: 2026-08-13
LEVEL: 4
TAGS: #regra-don #libs #instalacao #tesseract #pymupdf #potrace #opencv #reportlab #pdf-enterprise #hornfit #cosca-media #level-4
---
## L98 — 2026-08-13 — Regra de libs do Don + cosca-media v2 + PDF enterprise do HornFit | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: (1) regra da família — "toda vez que precisar de lib e precisa ser instalado, não deixe de chamar o Don"; (2) instalar libs que faltam; (3) integrar na cosca-media; (4) gerar PDF enterprise do HornFit com o logo transparente. |
| **Technique** | Level 4 — **(1)** REGRA DA FAMÍLIA: nenhuma lib é instalada sem chamar o Don antes — ele decide entre pip/apt/ponderado (OCR pesado ~2GB exige aprovação). **(2)** Instalação coordenada: Don rodou apt (tesseract+por, imagemagick, potrace, librsvg2, ffmpeg); eu rodei pip no venv (pymupdf, pypdf, opencv-python-headless, pillow-avif-plugin) — apt para binários de sistema, pip isolado para libs Python. **(3)** cosca-media v2: ocr (tesseract, com PDF via render), pdf text/merge/split/extract-images (PyMuPDF — preserva objetos), vectorize com POTRACE real (Bézier) + fallback, remove-bg --feather (OpenCV GaussianBlur no alpha). **(4)** PDF enterprise: ReportLab (WeasyPrint precisa de libs de sistema, não disponível) — gerador reutilizável make-presentation-pdf.py + HornFit-Apresentacao.pdf (7 págs, logo embutido). |
| **Level** | 4 |
| **Outcome** | success — cosca-media v2 testada (OCR imagem/PDF 'HornFit 2026', merge/split, potrace SVG real, feather) + PDF enterprise do HornFit (7 páginas, 1.6MB). Commits em ambos os repos. |
| **Confidence** | 0.95 (validado: todos os comandos novos testados com arquivos reais; PDF validado por bytes/páginas) |
| **Tags** | #regra-don #libs #instalacao #tesseract #pymupdf #potrace #opencv #reportlab #pdf-enterprise #hornfit #cosca-media #level-4 |
| **Related** | L210 (cosca-media v1), L209 (Stack 18 definitivo), L187 (segurança) |
| **Learned** | **(1) REGRA OPERACIONAL: qualquer instalação de lib passa pelo Don — pip leve eu proponho, apt é ele quem roda (sudo exige senha), OCR/pesados exigem aprovação explícita (~2GB torch).** **(2) Divisão de trabalho: apt = binários de sistema (tesseract, potrace, ffmpeg, rsvg); pip no venv = libs Python (pymupdf, opencv, pillow-avif) — pip isola e não polui o sistema.** **(3) PyMuPDF: usar `import pymupdf` (fitz está deprecated); `insert_pdf` para merge preserva objetos (nunca rasterizar).** **(4) ReportLab para PDF enterprise quando WeasyPrint não tem libs de sistema; células de Table aceitam LISTA de flowables (não concatenar com +).** **(5) Potrace lê BMP (Pillow salva direto) — pipeline raster→vetor real: compose branco → threshold → BMP → potrace -s → SVG.** **(6) O Don valoriza coordenar a instalação (perguntar antes), não apenas executar. |** |
| **Next** | (1) Usar o logo transparente no HornFit (header/landing/login — trocar o 'H' genérico). (2) Estender cosca-media: restauração/upscale (Real-ESRGAN) quando o Don aprovar. (3) OCR em massa (processar PDFs escaneados do cliente). |
