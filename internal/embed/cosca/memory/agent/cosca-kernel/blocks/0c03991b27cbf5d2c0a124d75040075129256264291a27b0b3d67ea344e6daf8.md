# L372 — DEDUP PREVENTIVO implementado (decisao do Don apos L371)

## status: DECISION (implementacao autorizada + validada)

## O que entrou (internal/indexer/indexer.go)
- storeDocument: dedup preventivo na ingestao — para cada chunk novo, se o
  conteudo ja existe (chunk com vetor), marca dedup_of=<canonico> e NAO cria
  vetor (proveniencia preservada; o estoque nao re-suja).
- Fail-safe: verifica a coluna dedup_of via PRAGMA (chunksHaveDedupCols) —
  sem a coluna (db antigo/schema sem migracao), comportamento historico.
- Cache por hash do chunk (uma consulta por conteudo unico no batch).
- schema.go NAO foi alterado (o checksum da V1 nao pode mudar — drift);
  as colunas existem no db real (migracao L360) e em dbs com ela.

## Validacao
- TestCampaignDedupPreventive (db temp isolado = copia do real, com
  colunas): vetores 51.240 -> 51.240 (0 novos); chunk criado com
  dedup_of="3d5d1d42..." (preenchido) — a hipotese original (re-suja) foi
  REFUTADA pela implementacao (a protecao agora existe).
- Suites: indexer OK, knowledge OK, build OK. Serve reinstalado + reiniciado
  (doctor 26/26).
- ARMADILHA registrada: alterar o schema.go quebra o checksum da V1
  (migration drift) — a correcao foi restaurar o schema e usar o PRAGMA.

## Classificacao
MEASURED 0 vetores novos no teste; dedup_of preenchido; suites verdes
EVIDENCE o dedup preventivo funciona no db real (colunas existem); fail-safe
         preserva dbs sem colunas
INFERRED o estoque limpo nao re-suja mais na ingestao (a fabrica fechou o
         ciclo: filtro L353 + parser L356 + dedup L372)
HYPOTHESIS a taxa de duplicacao se mantem estavel ao longo do tempo
UNKNOWN volume de re-sujamento ja acumulado desde a L360 (se houver)
DECISION dedup preventivo ATIVO em producao. Revisao L367: 3 de 4 itens
         (breakdown, recall real, dedup); resta o guard ativo (decisao).
