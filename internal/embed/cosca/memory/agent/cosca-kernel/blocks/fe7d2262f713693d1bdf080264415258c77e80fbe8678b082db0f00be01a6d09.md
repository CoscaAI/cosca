PREV: 4a4e53f7cd3fd175ca00b484043b0d3fca840484c548e6b9d2e7ff736e09958d
ID: L50
TIME: 2026-08-12
LEVEL: 4
TAGS: #desktop #wails #react #errorboundary #nil-slice #resizer #drag #acumulação-quadrática #slashmenu #search #portas-linkaveis #docker #notificações #level-4
---
## L50 — 2026-08-12 — cosca-desktop: fix crash Debug (tela sumia), SlashMenu compacto, /search no sidebar, resizers 1:1, portas linkáveis, Docker por categoria + sino de notificações | Level 4

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don testou o desktop (F1-F5 do L202) e reportou: (1) clicar no ícone DEBUG "some tudo" (tela branca); (2) busca abre caixa de diálogo gigante; (3) busca de arquivo deve ser no sidebar não no prompt; (4) resizers "duros" — arraste rápido dispara, lento não pega; (5) portas devem abrir no navegador; (6) docker deve agrupar por categoria com saúde verde/vermelho; (7) sino de notificações no header. |
| **Technique** | Level 4 — **(1) Crash Debug**: `DebugGetState()` retornava slices nil (Go nil slice → JSON null) quando sem sessão — `DebugPanel` acessava `state.console_lines.length`/`.map` direto → React 19 SEM ErrorBoundary desmonta a árvore inteira (tela branca). Fix em 3 camadas: helper `emptyDebugState()` + `cloneSlice[T]()` no Go (nunca mais nil slices), null-safe locals (`?? []`) no front, e `ErrorBoundary` novo com fallback estilizado. **(2) SlashMenu gigante**: CSS `left:12px; right:12px` esticava pela largura toda → compacto `width:max-content; min-width:340px; max-width:min(480px, calc(100vw - 24px))`. **(3) /search**: comando novo em commands.ts + `case 'panel:search'` → `setActivity('search')` (a SearchView do sidebar já existia, faltava rota); UX melhorada (contagem, botão ×, Enter abre top result). **(4) Resizers**: hook `useDragResize` com BUG clássico de acumulação quadrática — `dx` era delta total desde o mousedown mas o estado fazia `w + dx` em cima de valor já incrementado → pane dispara além do cursor (crescimento quadrático). Fix: capturar `base` no mousedown (`getBase()`) e aplicar `base + dx` → tracking 1:1 suave. Também: `.content-split` de grid → flex com panes em px, clamps (sidebar 160-480, chat 240-900, tree 200-800). **(5) Portas linkáveis**: binding `OpenPortURL` (pkg/browser, cross-platform) com `validatePortURL()` pura — loopback-only (127.0.0.1/localhost/::1), http(s) only, porta 1-65535 obrigatória (9 testes de segurança: loopback OK, 8 rejeições incl. example.com, 169.254.169.254, file://, sem porta). **(6) Docker por categoria**: `containerCategory()` (parte antes do 1º/2º hífen) + `isUnhealthy()` (running + status contém "unhealthy") no Go; front agrupa por categoria com dropdown colapsável, dot verde/vermelho/dim por grupo, pesquisa filtra flat. **(7) Sino de notificações**: hook `useNotifications()` — assina `exec:event`, erro/falha vira notificação (bounded 100, newest first); bell no header com badge unread; popover lista com borda por severidade, timeAgo, dismiss; clique redireciona (source esteira → bottom OUTPUT, docker → activity docker). DockerPanel ganhou `onNotify` nos catch de start/stop/compose. |
| **Level** | 4 |
| **Outcome** | success — Desktop rebuildado (wails build -tags webkit2_41, ~35-40s) e rodando sem panics. Go: `go test -count=1 ./...` PASS (inclui TestContainerCategory, TestIsUnhealthy, TestValidatePortURLAllowsLoopback, TestValidatePortURLRejectsExternalHosts). Front: `npx tsc --noEmit` 0 erros, `npm run build` OK. Fixes validados pelo Don em uso real: "ficou otimo obrigado" (resizers 1:1). |
| **Confidence** | 0.95 (validado: testes Go novos, tsc/build limpos, app vivo, feedback do Don nos resizers; não validado headless: cor dos dots, popover visual) |
| **Tags** | #desktop #wails #react #errorboundary #nil-slice #resizer #drag #acumulação-quadrática #slashmenu #search #portas-linkaveis #docker #notificações #level-4 |
| **Related** | L202 (cosca-desktop fundação), L186 (chain git-anchored), L174 (auditoria enterprise) |
| **Learned** | **(1) React 19 SEM ErrorBoundary desmonta a árvore inteira em qualquer erro de render — um único `.length`/`.map` de null vira tela branca; ErrorBoundary é infraestrutura obrigatória em app desktop.** **(2) Go nil slice → JSON `null` é uma bomba silenciosa para frontend tipado: `append([]T(nil), ...)` com fonte vazia produz nil; helper `cloneSlice[T]()` que garante `[]T{}` é o padrão canônico (defesa também no front com `?? []`).** **(3) Bug clássico de drag: quando o handler recebe delta TOTAL desde o mousedown, NUNCA acumular no estado (`w + dx` sobre valor já incrementado = crescimento quadrático, pane dispara além do cursor); capturar a base no mousedown e aplicar `base + dx`.** **(4) Binding de "abrir URL" no desktop precisa de validação própria: loopback-only + esquema restrito + porta obrigatória — senão vira vetor de SSRF/clique malicioso; extrair `validate*()` pura para testes.** **(5) UX de busca: o usuário espera buscar arquivo NO SIDEBAR (padrão VS Code), não no prompt — o comando `/search` + rota explícita fez a descoberta.** **(6) Sinalização visual por agregação (docker): categoria + dot de saúde agregado permite glance sem abrir cada grupo — "só exibir verde/vermelho se tiver algo errado".** |
| **Next** | (1) Commit + push do cosca-desktop (21+ arquivos pendentes, master ahead 1). (2) Commit+push pendentes do cosca principal (L205-L206, 5 commits, chain 68 blocos). (3) Wire do mcp.json no runtime MCP. (4) Testar notificação real com run falhando (precisa de execução com erro de verdade). |
