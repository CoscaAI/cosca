PREV: 20790af04c858ae9a366776a751354747eef2454967abc448b48a2047b49160a
ID: L118
TIME: 2026-08-13
LEVEL: 2
TAGS: #desktop #wails #webkit #gui #install #cosca-desktop #level-2
---
## L118 — 2026-08-13 — cosca-desktop instalado: wails v2.14.0 + build webkit2_41 + ~/.cosca/bin | Level 2

| Field | Value |
|-------|-------|
| **Agent** | cosca-kernel |
| **Task** | Don's order: "install cosca desktop" — instalar o app desktop do cosca (Wails GUI) globalmente. |
| **Technique** | Level 2 — **(1)** Verificação de pré-requisitos: fonte em `~/Documents/projects/cosca-desktop` (COSCA_DESKTOP_ROOT) EXISTE com `go.mod` apontando `replace github.com/CoscaAI/cosca => /home/cosca/Documents/cosca` (workspace local) e `wails v2.14.0`; headers `webkit2gtk-4.1` + `gtk+-3.0` OK via pkg-config; **wails CLI ausente** → `go install github.com/wailsapp/wails/v2/cmd/wails@v2.14.0` (alinhado à versão do go.mod). **(2)** Build: `wails build -tags webkit2_41` na pasta da fonte (1m02s: bindings → npm install → compilar frontend → compilar app → package). **(3)** Instalação global com rename atômico (padrão Makefile install-desktop): `cp → ~/.cosca/bin/.cosca-desktop.new → chmod +x → mv -f` para `~/.cosca/bin/cosca-desktop` (94MB). **(4)** Smoke test: `--help` carregou user config (`~/.config/cosca/config.yaml`) + knowledge engine (`/home/cosca/Documents/cosca/.cosca/knowledge.db`) sem erro fatal. |
| **Level** | 2 |
| **Outcome** | success — `~/.cosca/bin/cosca-desktop` instalado e executável; engine de knowledge inicializa com o root do projeto; `~/.cosca/bin` restaurado (cosca 01:27, cosca-chat 01:27, cosca-desktop 01:30). |
| **Confidence** | 0.95 (validado: build wails limpo, binário 94MB executável, smoke test inicializa config+knowledge sem erro) |
| **Tags** | #desktop #wails #webkit #gui #install #cosca-desktop #level-2 |
| **Related** | L190 (binário global ausente), L187 (incidente — ~/.cosca destruído), L174 (make install) |
| **Learned** | **(1) O cosca-desktop é um app Wails v2 que aponta (via replace no go.mod) para o workspace `/home/cosca/Documents/cosca` — o binário desktop usa a MESMA knowledge.db do projeto.** **(2) Build Wails exige o CLI na versão do go.mod (v2.14.0) — `go install` do CLI é rápido; o build inteiro (npm + CGO) levou ~1min.** **(3) O comando "pare o binario local" do Don foi abortado no meio: `serve` caiu, `runtime` seguiu de pé — estado dos serviços deve ser re-verificado antes de qualquer uso.** |
| **Next** | (1) Re-verificar estado dos serviços (serve caiu no abort do "pare o binario local"). (2) Testar `cosca desktop` com display. (3) Decidir com o Don: binário global vs local, units systemd. |
