# L399 — TRANSFORMACAO E-007 INSTALADA no serve (aprovado) + guardiao pos

## status: DECISION (aprovado pelo Don: "aprovado" — a instalacao)

## O que foi feito
- binario rebuildado (commit 88e593c — FTS incremental) e instalado
  (130.994.920 bytes)
- serve reiniciado (graceful — kill -TERM, sem pkill): PID antigo 2507639
  morreu limpo; PID novo 2742336 UP — sem erros no boot

## GUARDIAO POS-INSTALACAO (CARRO_PROTOCOL)
- serve operacional (PID 2742336)
- doctor: 1 issue (o mismatch por design — INALTERADO vs PRE_STATE)
- verify: 1 issue (o mesmo mismatch por design — inalterado)
- busca real funcional (search "fts rebuild incremental" — resultados)
- o db de producao NAO foi tocado (o FTS continua o estado do rebuild do
  binario antigo — o incremental so age nas proximas indexacoes)

## O que muda em producao a partir de agora
- cada IndexDirectory/indexacao usa o incremental — o FTS atual sem rebuild
- o custo do rebuild (~700ms por IndexDirectory) eliminado — o ganho
  estimado ~69x-5769x por operacao (E-007)
- o comportamento de busca inalterado (mesmo indice — recall 200/200)

## Classificacao
MEASURED serve UP; doctor/verify inalterados (1 issue por design)
EVIDENCE a busca real funciona no binario novo
INFERRED o ganho em producao comeca a valer nas proximas indexacoes
UNKNOWN a primeira indexacao real em producao com o incremental (a
  observar — o watcher/CLI)
DECISION instalado; a campanha PERF-2026-001 com a transformacao VERIFIED
  no ambiente real; PARAR (nenhuma outra otimizacao sem nova autorizacao)
