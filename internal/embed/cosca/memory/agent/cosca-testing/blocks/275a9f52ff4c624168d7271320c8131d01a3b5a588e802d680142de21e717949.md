PREV: b6929875225744485af727fcff1f4e5074c1edd877a78540131a2eee0a6d33b1
ID: 2026-07-31
TIME: 2026-07-31
LEVEL:
TAGS: #testing #agents #markdown-parsing #embedded-assets #handler #audit #consolidation
---
### 2026-07-31 — Handler Fix + Agents Manager Coverage 11.8% → 96.3% (Level 3)

| Field | Value |
|-------|-------|
| **Agent** | cosca-testing |
| **Task** | Consolidate: fix non-compiling `knowledge_test.go` (undefined `auditFiltersFor`) + write tests for `internal/agents` (Manager, was 11.8%) to ≥70% |
| **Technique** | White-box same-package tests for unexported parsers, `t.TempDir()` SKILL.md fixtures, embedded-asset deterministic assertions, engine-behavior-driven test expectations |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #testing #agents #markdown-parsing #embedded-assets #handler #audit #consolidation |
| **Related** | `api/rest/handler/knowledge_test.go`, `internal/agents/agents_test.go`, `internal/agents/agents.go` |
| **Learned** | **Legacy test fix first, write new tests second.** When a predecessor leaves a non-compiling test file, the undefined helper is often a small, intentional addition (here `auditFiltersFor(action) audit.AuditFilters` — a 3-line helper + one import). Also verify test *expectations* against real engine behavior: `{}` search body returns 500 "query or filter required", not 200 — the test's assertion was the bug, not the handler. For `internal/agents`: `NewManager("")` deterministically loads 41 embedded agents (via `//go:embed`) — perfect for stable, env-independent assertions. Parser quirks to encode as spec: table HEADER rows are parsed as data rows (`extractTableRows` doesn't skip `| Header |`); `extractListItem` only handles single-digit items with no leading whitespace; `containsIgnoreCase` requires substr ALREADY lowercased (Search normalizes query but not stored fields — hyphen in stored text is never matched by a spaced query); version is first-wins while status is last-wins. |
| **Next** | Audit other parser-heavy packages (skills, workflows) for the same markdown-parsing coverage patterns; encode header-row-as-data behavior in docs or fix parser. |

---
