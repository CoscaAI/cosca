PREV: ef1fbb59ec29d507969ebd6a4e3aa7032e2264c0ce194596a118575d36027eed
ID: 2026-08-25
TIME: 2026-08-25
LEVEL: 3
TAGS: #design-system #cosca-desktop #enterprise #css-variables #tokens #accessibility #density #build-verified
---
### 2026-08-25 — COSCA Desktop · Elevação do Design System a padrão enterprise
| Field | Value |
|-------|-------|
| **Agent** | cosca-uiux |
| **Task** | Refinar design.system + visual do fluxo HOME → CREATE → WORKSPACE → AGENT (precise/technical/calm/dense/premium/fast) |
| **Technique** | Tokens derivados via `color-mix`, matriz completa de estados (hover/focus/active/disabled/loading), `:focus-visible` global, `prefers-reduced-motion`, scrollbars por tema, estados ativos "quietos" (VS Code-style inset bar) em vez de preencher tudo de azul |
| **Level** | 2→3 |
| **Outcome** | success |
| **Tags** | #design-system #cosca-desktop #enterprise #css-variables #tokens #accessibility #density #build-verified |
| **Related** | projects/cosca-desktop/frontend/src/design.css + style.css |
| **Files Modified** | 2 (design.css, style.css) — somente visual; App.tsx intocado |
| **Learned** | 1) `--accent-soft` deve ser derivado por `color-mix(accent 16%, transparent)` p/ funcionar nos 2 temas (nunca hex fixo `#1d3a6e`). 2) Estado ativo de item/nav em tool enterprise ≠ preenchimento azul sólido: `background: color-mix(accent 14%, surface-2)` + `box-shadow: inset 2px 0 0 accent` lê como "selecionado" sem gritar (VS Code, Anthropic). 3) Densidade premiada: gap `--sp-3` (12px) na grid de projetos, controles unificados em `--ctrl-h:32px`, topbar min-height 46px. 4) `.light` precisa completar tokens (surface-2/3, border, shadow, overlay, code-bg) — não só bg/text. 5) Editor de código mantém fundo escuro nos 2 temas via `--code-bg`. |
| **Palette** | Dark: bg `#070b14` → surface `#0e1522`/`#172131`/`#1d2a3f` → border `#22304a`/`#33455f`; text `#e6edf7`/`#98a6c3`/`#7b8aa7`. Accent `#2563eb`, strong `#1d4ed8` (hover). Tinta azulada em NENHUM cinza puro (anti A4). |
| **Accessibility** | Foco visível em todo interativo: `.btn/.iconbtn/.input/.rail-btn/.item/.proj-card/.code` via `:focus-visible`. `prefers-reduced-motion` desliga transições. `white-space:nowrap` em badges (anti reflow U11). Contraste AAA conferido (on-accent sobre accent ≈5:1; text-3 ≥4.5:1 sobre surface). |
| **Anti-patterns evitados** | Sem emoji como ícone (marca é glyph `◆`). Sem tile arredondado gigante (hero-mark reduzido p/ accent-soft quieto, anti A8). Sem janelas-95 (scrollbars custom por tema). Sem dashboard genérico (density real). |
| **Build** | ✓ `npm run build` (tsc + vite) passa — CSS 15.49 kB / gzip 3.58 kB. |
| **Next** | Fase 2: aplicar `:focus-visible`/estados a componentes não cobertos; command palette com `.kbd`/`.kbd-group` já prontos; revisar em 375/1024/1440. |
