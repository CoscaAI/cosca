PREV: 85a8809a8b6f0cf93788322eb1eb381f338dc82db0f12e552e8c9396b9142b12
ID: 2026-08-04
TIME: 2026-08-04
LEVEL:
TAGS: #runtime #fail-closed #memory-integrity #bootstrap #cli #governance
---
### 2026-08-04 — Gate de integridade antes do bootstrap
| Field | Value |
|-------|-------|
| **Agent** | cosca-runtime |
| **Task** | Bloquear novas sessões sem manifesto de integridade válido, preservando apenas comandos offline explícitos |
| **Technique** | Level 3 — enforcement no entrypoint antes de providers/runtime/memória, verificação reutilizando `internal/memoryintegrity`, detecção única de raiz e bypass por allowlist de caminho de comando |
| **Level** | 3 |
| **Outcome** | success — `gofmt`, `go test ./...` e `go vet ./...` passaram |
| **Tags** | #runtime #fail-closed #memory-integrity #bootstrap #cli #governance |
| **Related** | cmd/cosca/main.go, internal/memoryintegrity/root.go, internal/cli/memory_integrity.go |
| **Learned** | Gate de sessão deve rodar no entrypoint antes de inicializar providers, sem variável de ambiente de bypass. O mesmo resolvedor de raiz precisa ser usado pelo gate e pelos comandos `memory integrity`; manifests também devem rejeitar entradas inseguras/duplicadas, não apenas JSON inválido. |
| **Next** | Considerar expor o resolvedor de raiz em um pacote de filesystem compartilhado se outros entrypoints precisarem da mesma política. |

### L-AUDIT-2 | 2026-08-03 | Bug raiz: RootDir apontava para o PAI do workspace — DB de 20,7GB | Level 4
| Field | Value |
|-------|-------|
| **Agent** | cosca-runtime |
| **Task** | knowledge.db reached 20.7GB and a rebuild ran 71h. Root cause: adapters_knowledge_adapter.go used RootDir: filepath.Dir(a.dir) — indexing the PARENT directory, sweeping sibling projects (order-system, ai-sdk...). |
| **Technique** | Level 4 — Scope bug: corrected to RootDir: a.dir (workspace itself). The indexer skips hidden dirs (.cosca, .git) automatically, so rebuild now indexes only the workspace. |
| **Level** | 4 |
| **Outcome** | success — fix applied; old 20.7GB DB preserved as knowledge.db.legacy-20260803 (reversible backup); fresh rebuild running with correct scope. |
| **Tags** | #rootdir #escopo #indexer #20gb #rebuild #workspace #level-4 |
| **Related** | internal/cli/adapters_knowledge_adapter.go, internal/indexer/indexer.go |
| **Learned** | **(1) Um bug de escopo (1 linha) gerou 20,7GB de lixo indexando projetos vizinhos — sempre auditar o ROOT de qualquer varredura.** **(2) filepath.Dir(x) ≠ x: o workspace é o diretório que contém .cosca/, não o pai.** **(3) Operações destrutivas em banco: renomear para .legacy é backup reversível (mv, não rm).** |
| **Next** | Vacuum pós-rebuild para encolher o arquivo; remover .legacy só após validação + aprovação do Don. |

### L-AUDIT-3 | 2026-08-03 | AutoMigrate ausente quebrava rebuild em banco novo | Level 4
| Field | Value |
|-------|-------|
| **Agent** | cosca-runtime |
| **Task** | Fresh DB rebuild failed: "no such table: documents_fts". Cause: adapter created knowledge.Config{DBPath, RootDir} without AutoMigrate:true → sqlite.Open skipped migrations → FTS virtual tables never created. |
| **Technique** | Level 4 — Init-path bug: added AutoMigrate:true to the adapter's Config literal. sqlite.Open only runs migrations when cfg.AutoMigrate is set (db.go:123). |
| **Level** | 4 |
| **Outcome** | success — rebuild progressed past FTS stage. |
| **Tags** | #automigrate #fts #migration #banco-novo #rebuild #level-4 |
| **Related** | internal/cli/adapters_knowledge_adapter.go, internal/sqlite/db.go, internal/knowledge/knowledge.go |
| **Learned** | **(1) Config literals omitidos = zero-value = comportamento diferente do DefaultConfig — SEMPRE comparar com o default.** **(2) Banco novo + migrações não rodadas = tabelas faltando em runtime; o erro só aparece no primeiro rebuild.** |
| **Next** | Adicionar teste de rebuild em banco vazio. |
