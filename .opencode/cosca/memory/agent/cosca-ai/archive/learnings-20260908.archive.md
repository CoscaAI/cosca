# Agent learnings - ARCHIVE (historico; NAO ler integralmente)

> Movido de learnings.md em 20260908. Proveniencia preservada; leia por grep, nunca inteiro.

### 2026-08-31 — FASE A do Perception Bus IMPLEMENTADA (bus + handler + rotas + testes)
| Field | Value |
|-------|-------|
| **Agent** | cosca-ai |
| **Task** | Codificar a FASE A do Perception Bus (sincronização multimodal visão+áudio no runtime): package `internal/perception/bus`, config, wiring em serve.go, rotas `/v1/perception/bus` + handler, testes e verificação build/vet/test |
| **Technique** | Native-Go additive seam over existing Perception Loop: MonotonicClock (route `time.Since(epoch)`, NO `//go:linkname`), O(1) circular Ring (eviction por window+maxObs), temporal overlap matcher (`tol` default 300ms), Bus as pure consumer/adapter (never mutates perception.Service), lazy closure registration of routes (avoids nil-capture bug) |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #multimodal #perception-bus #fase-a #monotonic-clock #ring-buffer #overlap #sse #bus-handler #worldstate #aditive #opt-in #golang |
| **Related** | internal/perception/bus/{clock,types,ring,match,bus}.go, internal/perception/bus/bus_test.go, api/rest/handler/perception_bus.go(+_test.go), api/rest/server.go (SetBusService + rotas), internal/cli/serve.go (buildPerceptionBus), internal/config/{config,defaults}.go (AudioConfig) |
| **Learned** | (1) **Clock**: escolhi `time.Since(epoch)` (via `sync.Once` + `time.Now()` epoch) em vez de `//go:linkname runtime.nanotime` — linkname é unsafe/sem contrato e quebra entre GO versions & build modes; `time.Since` usa a leitura monotônica embutida no `time.Time`, então é monotônico, sem syscall/alloc. `MonotonicTime int64` ns + `Add/Sub/Since/Duration`; `ToMonotonic` converte `time.Duration`. Unity de clock: todo o bus referenceia um único processo. (2) **Ring**: buffer circular O(1) com eviction por window (baseada no `newest` timestamp, não wall-clock — determinístico e testável) e por capacidade `maxObs` (cap hard). `Snapshots` ordenado por timestamp, `Since`, `WindowAround` (o primitivo "o que estava vendo quando ouviu X"). (3) **Overlap**: `match` liga cada obs de áudio às obs de visão via intervalo `[Ts, Ts+Duration]` com `tol` (0→intersecção estrita). `overlap` normaliza pra `aStart<=bStart`, gap<=tol, `overlapEnd=min(aEnd,bEnd)`, `ov`. Confiança = fração do áudio coberta. `MultiRel` = audio_seg + text_hint + WindowRefs(sequence/timestamp/entities) + conf. (4) **Bus**: adapter puro sobre `perception.Service.Subscribe()` — reconstrói `vision.Observation` a partir de `perception.State` (o State NÃO carrega o Observation cru; usei Entities/Relations/Warnings/LatencyMS). `Duration` da visão = LatencyMS. Áudio via `AudioSource.Stream() <-chan AudioSample` (seam injetável; `NoopAudioSource` Fase A). Ingest stamps `Now()`; publish non-blocking. `copyWorldState` defensiva. (5) **Bug latente que NÃO toquei**: em `registerRoutes`, `NewPerceptionHandler(s.perceptionSvc)` captura `s.perceptionSvc` no momento de `New()` (quando é nil — `SetPerceptionService` roda depois). Para o bus usei CLOSURE lento (`func(w,r){ handler.NewBusHandler(s.busSvc).State(w,r) }`) que lê `s.busSvc` a cada request — robusto a ordenação. (6) **Config**: `AudioConfig` (Enabled/Window/Tolerance/ModelsDir/SampleRate/ChunkMS) sob `PerceptionConfig.Audio`; defaults 5s/300ms/16000/100; env `COSCA_PERCEPTION__AUDIO__*`; validação `window/tolerance/sample_rate/chunk_ms > 0`. (7) **Testes**: bus_test.go usa seams injetáveis SEM onnxruntime — `ingestVision`/`ingestAudio` diretos (package bus) para projeção determinística; e um teste de integração real (`perception.NewService` + `WithCaptor/WithVision/WithLogger` + `Interval` curto) provando que o bus assina o loop. Handler test usa `httptest.NewServer` + `http.NewRequestWithContext` p/ cancelar o SSE (senão a Stream loop prende o `Close()`). |


---

### 2026-08-31 — FASE D do Perception Bus IMPLEMENTADA (memória episódica multimodal)
| Field | Value |
|-------|-------|
| **Agent** | cosca-ai |
| **Task** | FECHAR a FASE D — memória episódica multimodal: o COSCa lembra o que viu/ouviu sincronizado entre sessões. Modelo EpisodicRecord + storage reusando `internal/memory` + MemorySink no bus + config opt-in + CLI `cosca memory episodic` + REST `/v1/memory/episodic` + testes + build/vet/test |
| **Technique** | Reuse the existing MemoryEngine as the persistence layer (FileStore + SQLite FTS) via a NEW opt-in layer registered ON-DEMAND (not in the default 6-layer LayerManager, avoiding breaking `layers_test.go` count asserts) + JSON-in-Content serialization + non-blocking async MemorySink (buffered channel + worker, dedup by Sequence/AudioSeg) + exact Go-side time filtering (avoid fragile RFC3339 string comparison in SQLite) |
| **Level** | 4 |
| **Outcome** | success |
| **Tags** | #episodic-memory #multimodal #fase-d #perception-bus #memory-engine #jsonl-content #on-demand-layer #non-blocking-sink #dedup #privacy-representation-only #opt-in #golang |
| **Related** | internal/memory/episodic.go(+_test.go), internal/memory/memory.go (episodicTTL/episodicMaxRecords + ConfigureEpisodicRetention), internal/perception/bus/memory_sink.go(+_test.go), internal/perception/bus/clock.go (MonoToWall), internal/config/{config,defaults}.go (PerceptionConfig.Episodic), internal/cli/{serve.go,memory_episodic.go,adapters_memory_adapter.go,memory.go}, api/rest/handler/memory.go (Episodic), api/rest/server.go (route) |
| **Learned** | (1) **Reuse over duplicate**: `internal/memory` ja tinha FileStore+SQLite FTS+TTL+prune+owner-scope; reutilizei gravando o `EpisodicRecord` como JSON no campo `Content` de um `MemoryRecord` da camada `LayerEpisodic`. Nao duplicar engine. (2) **Camada on-demand em vez de default**: adicionar `LayerEpisodic` ao `NewLayerManager` quebraria `layers_test.go` (assert `len!=6`) e `TestStatistics`; solucao: `EnsureEpisodicLayer()` registra a camada + cria o store sob demanda (idempotente), mantendo o contrato das 6 camadas intacto. (3) **Dedup de MultiRel foi o bug real**: `buildRecords` dedupa observacoes por `Sequence`, mas o registro MULTIMODAL vem do loop de `Relations` (nao de uma Sequence de observacao) — sem dedup, cada publicacao do bus re-gravava o mesmo sync. Fix: segundo set `seenRels` keyed por `AudioSeg` (namespace separado para nao colidir com Sequences). (4) **Teste assincrono sem flakiness**: em vez de depender de goroutines/timers para `buildRecords`, o teste chama o metodo direto e drena `s.ch` com timeout; para o worker usa poll por deadline com `mockEpisodicWriter` (mutex) — deterministico, sem `time.Sleep` fragil. (5) **Filtro temporal exact em Go, nao no SQLite**: os `created_at` estao como RFC3339 no `memory_meta`; comparar strings RFC3339 e fragil (offset/zone). `QueryEpisodic` filtra por `time.Time` em Go sobre a representacao persistida — robusto. (6) **Privacy**: persiste so representacao (texto/entidades/relacoes), nunca frame bruto — documentado no codigo (politica). (7) **Config/BuildTag limpo**: `PerceptionConfig.Episodic{Enabled,TTL,MaxRecords}` opt-in (`perception.episodic.enabled`), defaults 30d/5000; `buildEpisodicSink` chama `ConfigureEpisodicRetention` (campos `episodicTTL/episodicMaxRecords` no engine, defaults no NewEngine). (8) **Wiring serve**: `serveStartServers` ganhou `episodicSink *bus.MemorySink` (passado de `serveComposeEngines`); `Start` junto do `busSvc.Start`, `Stop` junto do `busSvc.Stop` no `serveEventLoop`. Nil-safe: sem engine/bus → sink nil, nao grava. |
| **Next** | (Pendencia honesta) (a) busca semantica/embeddings opcionais na QueryEpisodic (hoje e match AND por palavra sobre texto+labels; embedding so da observacao relevante, custo contido); (b) endpoint/CLI para DELETE/prune manual de episodicos via API publica (ja ha `PruneEpisodic`); (c) considerar gravar tambem Snapshots de episodicos (replay entre sessoes) e um comando `cosca memory episodic --replay` para reconstruir uma narrativa temporal; (d) validar build com tag `stt_sherpa`/`tts_sherpa` (nao testei com as tags cgo). |

---

### 2026-09-01 �?" HF optimum mining (onshape ONNX vision acceleration)
| Field | Value |
|-------|-------|
| **Agent** | cosca-ai |
| **Task** | Mine `huggingface/optimum` clone for ONNX vision acceleration gems (COSCa CLIP/GroundingDINO/Depth on CPU/AMD) |
| **Technique** | ONNX graph fusion patterns + int8 quantization theory + ONNX Runtime provider matrix |
| **Level** | 2 |
| **Outcome** | learnings |
| **Tags** | #onnx #onnxruntime #quantization #graph-optimization #vision #clip #groundingdino #amd #rocm #migraphx #cpuml |
| **Related** | optimum/fx/optimization/transformations.py, docs/source/concept_guides/quantization.mdx, optimum/utils/import_utils.py, optimum/exporters/base.py, tests/cli/test_cli.py |
| **Learned** | (1) **Repo post-split CRITICO**: este clone de `optimum` NAO contem `optimum/onnxruntime/` nem `optimum/exporters/onnx/` �?" o codigo real de ORTOptimizer/ORTQuantizer/OptimizationLevel moveu para `huggingface/optimum-onnx`. Para params exatos (optimization_level, op_types_to_quantize, per_channel, reduce_range, QuantizationMode) minerar optimum-onnx. (2) **AMD GPU gold**: `import_utils.py` enumera `onnxruntime-rocm` e `onnxruntime-migraphx` como packages de ONNX Runtime. Para RX 6700 XT (sem CUDA), DirectML falhou �?" tentar Execution Provider ROCm ou MIGraphX (alta chance de speedup vs CPU). Em Go: AddExecutionProvider(ROCm/MigraphX) no SessionOptions. (3) **Graph fusion recipe p/ ViT/CLIP** (transformations.py): Conv+BN->Conv, Linear+BN->Linear, bias fold no weight, e `ChangeTrueDivToMulByInverse` (attention scale x/sqrt(d) -> x*(1/sqrt(d)), node Div e dobrado no MatMul). Habilitar ORT_OPT_LEVEL EXTENDED/ALL para isso. (4) **fp16 LayerNorm NaN**: eps~1e-12 vs fp16 min ~6e-5 -> NaN. CLIP/ViT tem LayerNorm -> NAO converter o modelo todo pra fp16. Preferir int8 (QDQ) deixando Gemm/Linear em int8 e LayerNorm/Gelu em fp32. (5) **int8 recipe**: quantize os MatMul/Gemm (dominam compute: attention + FFN), dynamic first, static (calib ~200 samples) se precisar; symmetric+per-channel p/ weights; calib minmax(weights)/moving-avg(activ)/histogram+entropy/MSE/percentile. (6) **CLI confirma**: `optimum-cli onnxruntime optimize ... -O1` (ORT BASIC) e `quantize ... --avx2` (kernel AVX2/SIMD). Em Go mapear p/ SetGraphOptimizationLevel + AVX2 no CPU EP. (7) **Output names p/ Go**: `_TASK_TO_COMMON_OUTPUTS` em exporters/base.py: zero-shot-image-classification -> logits_per_image/logits_per_text/text_embeds/image_embeds (CLIP); zero-shot-object-detection -> logits/pred_boxes/text_embeds/image_embeds (GroundingDINO); depth-estimation -> predicted_depth. (8) **Shape ViT**: encoder_sequence_length=(image_size//patch_size)^2+1 (+1=CLS). CLIP ViT-B/32 @224 -> (224/32)^2+1=50 tokens. (9) Export aceita float_dtype fp32/fp16/bf16 e int_dtype int64/int32/int8, e seta use_cache=False em encoder-only. (10) Enums de config: CalibrationMethods{minmax,percentile,entropy}, QuantizationApproach{static,dynamic}, per_channel (utils/runs.py). |
| **Next** | Minerar `huggingface/optimum-onnx` (codigo real de otimizacao/quantizacao) para pegar params exatos de ORTOptimizer/ORTQuantizer; definir pipeline Go: off-line quantizar int8 (quantize_dynamic) -> carregar .onnx QDQ no onnxruntime-go; testar EP ROCm/MIGraphX na RX 6700 XT; validar que GraphOpt ALL nao quebra LayerNorm. |
