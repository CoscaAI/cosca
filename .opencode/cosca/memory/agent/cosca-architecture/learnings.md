# cosca-architecture — Semantic Learnings

> Auto-evolution memory. Search before acting. Record after learning.

## Seed Knowledge

### 2026-07-27 — Baseline
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Initial capability establishment |
| **Technique** | Standard architecture patterns — project conventions |
| **Level** | 1 |
| **Outcome** | success |
| **Tags** | #architecture #baseline #initialization |
| **Related** | See .opencode/cosca/memory/codebase/overview.md, .opencode/cosca/memory/pattern/ |
| **Learned** | Project established. Core architecture patterns documented. Ready for level 2 techniques. |
| **Next** | Level 2: Identify first advanced technique to master |

### 2026-07-28 — gRPC Server Planning
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture + cosca-backend |
| **Task** | Architectural planning for gRPC server (3 services, 12 RPCs) |
| **Technique** | Full-stack analysis: proto files → engine interfaces → handler patterns → startup lifecycle. DRY via engine-sharing. |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #grpc #architecture #planning #adr #protobuf #server |
| **Related** | `.opencode/cosca/memory/architecture/grpc-server-plan.md`, `api/rest/server.go`, `internal/runtime/runtime.go`, `internal/cli/serve.go` |
| **Learned** | 1) Proto files at `proto/aos/v1/` with go_package `api/grpc/pb` — mismatch requires move. 2) REST handlers call engines directly (no service layer) — gRPC follows same pattern. 3) `memory.MemoryEngine.Delete()` exists and is functional (not placeholder). 4) Start gRPC in `internal/cli/serve.go` alongside REST/metrics servers with shared engine instances. 5) 12 RPCs confirmed (4 Knowledge + 6 Memory + 2 Runtime), not 15 as initially stated. 6) gRPC deps already in go.mod (grpc v1.64.0, protobuf v1.33.0). 7) All RPCs are unary — no streaming needed. 8) Server reflection should be opt-in flag for production safety. 9) Graceful shutdown: gRPC first (GracefulStop), then REST, then engines Close. 10) Mapping layer (pb ↔ domain) in separate files keeps service implementations clean. |
| **Next** | Execute Phase 0: move .pb.go to api/grpc/pb/, verify compilation. Then Phase 1: RuntimeService as template. |

### 2026-07-28 — Streaming Architecture Planning
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture + cosca-backend |
| **Task** | Discovery & planning for WebSocket/SSE streaming in Cosca serve.go |
| **Technique** | Full-stack discovery: go.mod deps → grep for stream/websocket/sse → chat/types.go interface → handler/run.go SSE impl → provider ChatStream → knowledge Sync → workflow Run → frontend hooks. Decision matrix SSE vs WS. |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #streaming #sse #websocket #architecture #planning #adr |
| **Related** | `.opencode/cosca/memory/architecture/streaming-plan.md`, `api/rest/handler/run.go`, `internal/chat/types.go`, `api/middleware/security.go`, `internal/knowledge/knowledge.go`, `internal/workflows/workflows.go` |
| **Learned** | 1) SSE already mature for chat — `POST /v1/run/stream` uses `sendSSE()` with `text/event-stream` + `http.Flusher`. 2) Zero WebSocket code or deps — no gorilla, nhooyr, or gobwas. 3) All 10+ providers implement `ChatStream` via `chat.ChatStream` interface. 4) Knowledge `Sync()` and Workflow `Run()` are fully sync — no progress callbacks exist yet. 5) CSP already has `ws://` in connect-src (line 46 of security.go). 6) Frontend `use-provider-stream.ts` is a placeholder waiting for backend wiring. 7) `sendSSE()` is trapped in handler/run.go — needs extraction to shared package. 8) Auth middleware uses JWT via cookie/Bearer header — WebSocket needs query param pattern. 9) Recommended lib: `nhooyr.io/websocket` (context-aware, Go std interfaces, pure Go). 10) 4 SSE + 1 WS endpoints planned. Total effort 40-55h across 5 phases. |
| **Next** | Phase 0: Extract SSE utilities to `api/stream/sse.go`. Phase 1: Add progress callbacks to knowledge Sync. |

### 2026-08-23 — Mega Brain: deliberação evidência-gated + plan-only
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | ADR-011 — adotar padrões-ouro (A1-A7, D1-D6) do Mega Brain no Cosca, sem implementar |
| **Technique** | Mapa gap: o Cosca TEM RAG/critic/gates/DAG/Ciclo-de-Decisão, mas é 1-agente auto-avaliação (passo 8), confiança intuitiva, sem convergência calculada, sem plan-only nem gate 3-estados. Solução: `internal/deliberate` determinístico (Convergence/Confidence/Emit/VoteCross/ValidateSpec) para P0/P1 + Plan-only no `Engine` reusando `internal/gate` e `internal/workflow`. Régua P9: modelo propõe, sistema decide. |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #mega-brain #deliberation #plan-only #adr #convergence #confidence #cpu #over-engineering |
| **Related** | `docs/adr/ADR-011-mega-brain-deliberation-and-plan-only.md`, `.cosca/fallback/knowledge/patterns/mega-brain-patterns.md`, `internal/orchestration/orchestrator.go`, `internal/orchestration/router.go`, `internal/gate/gate.go`, `internal/workflow/workflow.go`, `internal/confidence/tracker.go`, `internal/embed/cosca/{CONSTITUTION,QUALITY_GATES,DECISION_PROTOCOL,CONFIDENCE_MODEL}.md` |
| **Learned** | 1) Não existe `internal/review` (cosca-review/qa são agentes-texto) — deliberação deve viver num pacote determinístico `internal/deliberate`. 2) `Engine.Execute` roda inline (sem plan-only); a guarda já está em `internal/gate.GateStore` (plan→approving→approved→executed) — estender p/ 3-estados, não recriar. 3) `internal/workflow` já é DAG typed-routing com JoinNode paralelo + ErrCycle — usar como substrato, só somar CPM/FMEA. 4) Reuso é a régua: NÃO tocar `internal/embed/cosca/*` (P8). 5) Over-engineering honesto: batalha adversarial completa (A5), arquétipos D1-D10 (A6), D7 token-fencing, e todo o bloco C (RAG) ficam de fora — RAG merece ADR próprio. 6) Fatia 1 = plan-only + gate 3-estados (bloco 2) e `internal/deliberate` só P0/P1 (bloco 1). |
| **Next** | Se aprovado: fatiar 1 dos 2 blocos; considerar ADR-012 para RAG "zero achismo" (C2/C4/C5/C6). |

### 2026-08-24 — ADR-012: Arquitetura de 2 Zonas (Cofre air-gap + Kernel internet)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | ADR-012 — formalizar o desenho de 2 zonas (Cofre=IA local+Oracle puro sem internet; Kernel=internet) que só existia fragmentado no cognitive-state.md |
| **Technique** | Mapa gap honesto: o Oráculo JÁ existe e está conectado (internal/oracle oracle.go/search.go + oracleGate no api/rest/handler/run.go, REJECT→HTTP400); o data-vault é AES-256-GCM (internal/secrets/vault.go); o jail é fail-closed (pkg/cosca/jail.go) mas Linux-only. Gap real = air-gap de rede, IA local compreendendo (nunca decidindo), e envelope íntegro. Solução: definir zonas + princípio de rede + fluxo Kernel→Oráculo→DECISÃO com a regra "o determinístico decide, a IA compreende" — a saída da IA local é INPUT, nunca VEREDITO (proibido REJECT→ACCEPT). |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #2-zonas #cofre #kernel #air-gap #oracle #adr #seguranca #wsl2 #bwrap #fail-closed |
| **Related** | `docs/adr/ADR-012-2-zone-cofre-kernel.md`, `.opencode/cosca/memory/context/cognitive-state.md`, `internal/embed/cosca/{ORACLE_PROTOCOL,MODEL_PROTOCOL,SECURITY_PROTOCOL}.md`, `internal/oracle/{oracle,search}.go`, `internal/secrets/vault.go`, `pkg/cosca/{jail.go,jail_windows.go}`, `deploy/README-WINDOWS.md` |
| **Learned** | 1) **Desambiguação crítica**: há DOIS "Cofre" — SECURITY_PROTOCOL (data-vault: knowledge.db/secrets.db/chain) vs a ZONA Cofre (runtime isolado que CONTAINS o data-vault + IA local + Oracle). O ADR deve explicitar isso ou o security chief confunde os dois. 2) O Oráculo de fronteira (internal/oracle) é CONCEITUALMENTE distinto do Oráculo de medição (internal/evals/ORACLE_SPEC.md — secret_verify caixa-preta) — não misturar. 3) O `oracleGate` no RunHandler JÁ é o ingress point da fronteira; o ADR só formaliza. 4) **Air-gap no Windows/WSL2 é best-effort**: WSL2 default NAT tem internet; sem derrubar rota default + resolv.conf, o "sem internet" é meia-verdade. Bwrap é Linux-only e não está instalado. 5) Regra de ouro do loop: determinístico decide; IA local só compreende (Comprehend devolve insights, nunca Decision) — senão vira "jaula de LLM". 6) Número do ADR: há DOIS ADR-011 (duplicata), próximo é 012. 7) Não existe INDEX de ADRs (só o INDEX.md da memória de contexto) — nada a atualizar. |
| **Next** | Quando WSL2-distro+bwrap estiverem de pé (ou AppContainer/Low-IL nativo validado), implementar a fatia 1: `internal/oracle/ai_adapter.go` (Comprehend sem Decision), envelope assinado Kernel→Oráculo, `cosca cofre netcheck --strict` (falha se houver egresso). |

### 2026-08-24 — ADR-013: Bancos de Dados Modulares (Core imutável como âncora + módulos coesos + índice agregador)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | ADR-013 — formalizar a visão do Don de bancos modulares: memória da família imutável como base de comparação, bancos por módulo idempotentes, índice agregador vetorial para busca semântica unificada |
| **Technique** | Mapa gap HONESTO + inspeção real do banco (somente leitura via Python sqlite3, sem tocar o arquivo) + tese do Don ("ler cada módulo = entender de uma vez"). Fatos medidos: `knowledge.db`=257,6MB, 38 tabelas, documents 2383/chunks 38742/entities 36539/relationships 32535/vectors 28888/users 1, `user_version=0` apesar de 6 migrações em `migration_history`. Solução: Core=NÃO é SQLite reescritível (chain+blocks+embed já são arquivos append-only assinados); projects/graph/vector/core split; `internal/modlink` (Ref por ID+hash, resolução app-layer); `internal/vectoragg` (read-model via ATTACH read-only). |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #bancos-modulares #modulos #imutavel #ancora #oracle #indice-agregador #adr #sqlite #busca-semantica #tese #apreensao-holistica |
| **Related** | `docs/adr/ADR-013-modular-knowledge-databases.md`, `docs/adr/ADR-012-2-zone-cofre-kernel.md`, `docs/adr/ADR-002-knowledge-engine.md`, `internal/sqlite/{schema,migrations}.go`, `internal/integrity/*` (chain), `internal/oracle/{oracle,search}.go`, `internal/memory/store.go`, `.cosca/knowledge.db` (estado atual) |
| **Learned** | 1) **Desambiguação do mapa**: o "banco único" é verdade SÓ para o conhecimento — o runtime JÁ é multi-banco (audit/auth_tokens/department/memory-index/secrets/trace são .db separados). O outlier é o `knowledge.db` (257MB). 2) **A memória imutável da família NÃO está em SQLite** — family_chain.dat + blocks/*.md + embed já são append-only/read-only assinados; forçá-los para um SQLite reescritível DESFAZERIA a imutabilidade. 3) **Honestidade técnica obrigatória**: SQLite NÃO tem trigger nem FK cross-database; os links são na camada de aplicação (Ref{Module,ID,Hash} + Resolve + detecção de Drift). `ATTACH DATABASE` read-only serve à LEITURA agregada (JOIN cross-db), mas NÃO é link semântico nem constraint. 4) **Agregador é read-model derivado** (reconstruível, nunca fonte primária), para centralizar o espaço vetorial comparável — sem ele, buscar em N índices vetoriais fragmenta e reintroduz ruído. 5) Migração de 257MB é a etapa mais perigosa: fazer offline com backup, split de leitura (espelho via ATTACH) antes de qualquer escrita, jamais com serve rodando. 6) Número do ADR: dado que há dois ADR-011, o próximo é 013 — confirmado que ADR-013 não existia. |
| **Next** | Se aprovado: fatia 1 = `internal/modlink` (Ref+Resolve+Drift) + `cosca index rebuild --verify` (agregador idempotente) + `core.db` query_only. Fatia 2+ = migração destrutiva dos 257MB com backup e validação, e NUNCA tocar a política de versionamento do git sem aprovação do Don (porque hoje é snapshot consistente versionado por ordem expressa). |

### 2026-08-27 — ADR-015: F1 — elevar as primitivas neutras de control plane do cosca-trader ao Root
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | F1 da elevação — copiar/adaptar `internal/{task,orchestrator,decision}` do cosca-trader para o Cosca Root como infra neutra (stdlib only), frente `Domain Adapter → Generic Control Plane`, sem quebrar o Root |
| **Technique** | Mapa de reuso honesto ANTES de criar: ler `internal/pipeline`, `internal/durable`, `internal/orchestration`, `internal/aitask`, `internal/audit`. Invariante verificada na prática com `go list -deps`. Reuso real DELEGADO à borda (adapter concreto), não à primitiva. |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #f1-elevacao #task-continuation-loop #primitivas-neutras #stdlib-only #adr-015 #reuso #boundary #adapter #go-list-deps |
| **Related** | `docs/adr/ADR-015-task-continuation-loop.md`, `internal/task/{task,events,repository}.go`, `internal/orchestrator/orchestrator.go`, `internal/decision/decision.go`, campo de prova `C:\Users\Henrique\Documents\projects\cosca-trader\internal\{task,orchestrator,decision}` |
| **Learned** | 1) **Não colidem**: `internal/task` (livre), `internal/orchestrator` (livre — `orchestration` é outro), `internal/decision` (livre — `audit.DecisionStore` é OUTRO conceito: explicabilidade SQLite D-xxxx vs memória event-sourced latest-winner). Criei pacotes novos, sem duplicar conceito. 2) **Não reusar durable/pipeline DENTRO da primitiva**: `durable` importa `internal/sqlite` → `modernc.org/sqlite` (dep externa) que derrubaria a invariante `go list -deps = stdlib only`; `pipeline` é neutro mas não puro (Plan/TaskNode/TaskResult). Reuso delegado à borda (TaskRepository/DecisionStore concretos, F2/F3). 3) **Trader x Root — o que quebrava a pureza**: `orchestrator` importava `github.com/google/uuid` (troquei por `crypto/rand`→hex + fallback timestamp+counter) e `internal/event` (não existe no Root). Movi o envelope mínimo `task.Event` + constantes `task.EventXxx`/`task.SeverityXxx` para DENTRO de `internal/task`; NÃO criei `internal/event` de propósito (o adapter mapeia p/ o transporte real). Renomeei env `COSCA_TRADER_TASK_MAX_CONTINUE`→`COSCA_TASK_MAX_CONTINUE`. 4) **Invariante confirmada**: `go list -deps` = só stdlib + `internal/task` (p/ orchestrator); 0 domínio, 0 externa. 5) **`go build ./...` verde; testes das 3 primitivas verdes** (adaptados do campo de prova: task, orchestrator, orchestrator_f2, decision). 6) **Nag**: `internal/integrity` FALHA por razão AMBIENTAL (Ed25519 machine-bound, "cannot load private key (only the kernel on this machine can sign)" → IDENTIDADE-BREACH). Provei que é pré-existente: movendo os 3 pacotes para fora da árvore, o `internal/integrity` segue falhando. NÃO é causada pela F1 (packages isolados, zero relação). |
| **Next** | F2: implementações concretas de `TaskRepository`/`DecisionStore` reusando `internal/durable`/`pipeline` (borda) + ligar `MaxContinue` ao Don-escalate real. F3: compor `TaskOrchestrator` no Kernel (workers/edições/provedores) + transporte real de eventos. F4: `WithObjectiveSatisfied` real + integração `internal/deliberate`/`internal/gate`. F5: observabilidade do loop + cross-trace com `internal/audit`. |

### 2026-08-28 — Mineração profunda: gods-eye-view (geospatial/OSINT num World Model)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Mineração de código (não só README) de `bilawalsidhu/gods-eye-view` (JS/Cesium) para extrair ideias de "construir o mundo"/Living World, via ADR-017 |
| **Technique** | Leitura de código-fonte (data/manager.js, flights.js, satellites.js, motionModel.js, geoid.js, worldOverlay.js, aisStreamAdapter.js) + mapa gap contra `internal/worldmodel/*` e `internal/world/*` + veredito I1–I8 |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #mineração #geospatial #osint #worldmodel #cesium #sgp4 #interpolacao #adr-017 #invariantes #temporal-model #provenance |
| **Related** | `.opencode/cosca/.../gods-eye-view` (clone), `docs/adr/ADR-017-capability-borrowing-protocol.md`, `internal/worldmodel/{types,providers}.go`, `internal/world/{world,spatial,renderer}.go`, `docs/roadmap/evo-plano-evolutivo.md §1` |
| **Learned** | 1) **A ideia-ouro = modelo temporal**: renderizar/avaliar **um tick atrás** do real + interpolar entre dois fixes conhecidos (bracketing) + dead-reckon constante-virada (arcOffsetEnu) + coast limitado (staleCoastLimit) + warm-up extrapolando para trás. Transforma feed externo ruidoso/infrequente em estado de mundo **determinístico e sem "snap-back"** — puro math, zero LLM (I1/I2), e explicitamente estamável (interpolado/extrapolado = INFERRED, I4). Fita I1–I8 como um ajuste, não um regresso. 2) **Cosca já tem epistemologia (I3/I4 provenance) q** falta **fix-history/interpolacao** e **per-entity source+freshness+feed-state** + **registry de atribuição por fonte** (`layerFeedState`, `dataCredits.js`). 3) **SGP4/TLE determinístico** + **shared-epoch-per-frame** (dot/câmera/readout num só sample por frame) — portável e I1-compatível; anel GMST é truque de render (não precisa no modelo). 4) **Datum geo**: `internal/world.GeoCoordinates` é equiretangular flat; para globo real falta WGS84→ECEF + EGM96 (h=H+N) — ADAPTAR condicional (só se Cosca for a globo/escala-mundo). 5) **OSINT**: ADOTAR a disciplina de proveniência/licença/carve-out e a ingestão via proxy com budget-governed; **REJEITAR** OSINT/feed-ao-vivo como **autoridade** (I8 — "incomplete, not evidence of absence" é a regra que o clone já respeita). 6) **Rejeitar o stack Cesium/WebGL/JS e o agente de voz OpenAI Realtime** como capacidade a adotar (I7 isolamento + I1/I2 gate ZERO-LLM); emprestar só os *padrões* (compositor screen-space, orientação world-stable, shared-epoch) como contratos de RendererAdapter. |
| **Next** | Levar o par "temporal consistency model" + "per-entity provenance stamp" como hipótese de ADAPTER em `internal/world` ou `internal/worldmodel`; medir antes/depois (benchmark/ADR). Se Cosca for a escala-globo, propor `internal/world/geo` (WGS84+EGM96). |

### 2026-08-28 — Mineração profunda: ruvnet/claude-flow→ruflo (meta-harness self-optimizing)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Mineração de código de `ruvnet/ruflo` (TypeScript, 69,6k⭐) — o "original agent meta-harness". Foco: orquestração de agentes/flows, runtime, grafo, state, harness; determinismo vs LLM vs gate I1; FIT Cosca |
| **Technique** | Leitura de código (não README): `v3/src/task-execution/application/WorkflowEngine.ts`, `v3/src/coordination/application/SwarmCoordinator.ts`, `agent-lifecycle/domain/Agent.ts`, `task-execution/domain/Task.ts`, `v3/@claude-flow/cli/src/services/{harness-loop,harness-flywheel,harness-verify,checkpoint-gate,fable-harness,distill-oracle,policy-runtime}.ts`. Gap: espelhei `internal/{orchestration/chain.go,workflow/workflow.go,gate/gate.go,deliberate/confidence.go,evolution/stage.go}` |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #mineração #ruflo #meta-harness #flywheel #self-optimization #checkpoint-gate #tiered-oracle #policy-runtime #adr-017 #invariantes #banco #I1 |
| **Related** | `v3/@claude-flow/cli/src/services/*` (clone em awslabs-mining/ruflo), `docs/adr/ADR-017-capability-borrowing-protocol.md`, `internal/evolution/stage.go`, `internal/deliberate/*`, `internal/orchestration/chain.go`, `internal/workflow/workflow.go` |
| **Learned** | 1) **O ruflo chegou (independente) à mesma régua I1 do Cosca**: `harness-loop` = OBSERVE→QUALIFY→BENCHMARK(held-out)→VERIFY(adversarial+drift)→CANARY→ACCEPT(conjunção). O LLM (darwin/evolve) só PROPÕE; o sistema decide via conjunção determinística. E é **fail-closed e $0**: sem verifier/redblue wired → SKIPPED → nada promove; canary obrigatória; "avaliação nunca muta política ativa; promoção é transação separada"; trust split local-unsig / cross-install-signed. 2) **FALTAM no Cosca, que a Evolution Engine atual NÃO cobre**: `stage.go` diz explicitamente "projeção/observabilidade, NUNCA control plane". O ruflo tem o LOOP FECHADO que falta: gera candidato paramétrico (neighbor hill-climb determinístico sobre TRAIN split), avalia em HELD-OUT, exige **bootstrap CI low > 0** (significância 95% unicaudal — SOTA noise guard), exige **sequential-evidence threshold** (tamanho informativo mínimo p/ promoção), grava **receipt imutável assinado** (I5 ledger) + ledger de melhoria + segurança envelope (authorization/network/spend expansion=false). 3) **checkpoint-gate** = checkpoint O(1) (162 bytes) + rollback O(edits-since-checkpoint), bracket guard num tick arriscado, degrade gracioso (optional dep), kill-switch. Relevante ao ADR-021 World Building (mundo editado por tick). 4) **distill-oracle** = oracle TIERED com proveniência honesta: mechanic test-exec (ground truth) > judge:fable (LLM proxy, NUNCA ground truth) > proxy:structural. Fable judge é **cost-disciplined**: `claude -p` de cwd TEMP vazia (não carrega CLAUDE.md → 5x custo), batch ~20 itens (~$0.02/item), hard budget USD, `--max-budget-usd` por call, opt-in off-by-default. 5) **policy-runtime** = AgenticPolicyEngine determinístico: trust-anchor HMAC por projeto (por caminho real), lock file, ledger de política, capability-envelope p/ worker isolado, `classifyMcpTool` classifica por NOME → actionType/rede/destrutivo/namespaceAccess (nunca por input). 6) **Consensus BFT** (byzantine/raft/gossip, monorepo `@claude-flow/swarm`) é ampliação que o Cosca NÃO precisa AGORA (voteCross no deliberate já basta). 7) **Rejeitar o monolito TS/Node gigante, MCP-heavy, o Svelte/huggingface fork e o "swarm de agentes" como capacidade** — emprestar só os PADRÕES (± falta no Cosca). |
| **Next** | Se aprovado: ADR para "Evolution Engine como loop fechado" — `internal/evolution` + um determinístico `internal/evoluate` (bootstrap CI, receipt assinado, promoção transacional) + reuso de `internal/deliberate`/`gate`; e um `CheckpointGate` estilo ruflo em `internal/world`/`internal/engine` p/ ticks que mutam mundo. |

### 2026-08-28 �?" RuView: RF/CSI sensing e a epistemologia "saber vs ver" (mina profunda)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Minera��ǜo de uvnet/RuView (clone em awslabs-mining/RuView): como WiFi/CSI vira rastreamento espacial, e o que emprestar (ADR-017) |
| **Technique** | Leitura de c��digo Rust (nao README): hardware/firmware EP32, sensing-server/field_localize, ruvsense (coherence_gate, evolution, rf_slam), ruvector/crv, mat/localization, core/pose_observation_v2, ruview-track, ruview-spatial-memory |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #ruview #rf-sensing #csi #epistemologia #saber-vs-ver #I4 #adr-017 #borrowing #spatialprovider #worldmodel #confidence |
| **Related** | 2/crates/wifi-densepose-{core,hardware,signal,ruvector,mat}/src, wifi-densepose-sensing-server/src/field_localize.rs, irmware/esp32-csi-node/README.md, docs/adr/ADR-021-*, internal/worldmodel*, internal/embed/cosca/* |
| **Learned** | 1) **Insight honesto**: RuView N�?O tem um algoritmo m��gico. Tier multistatic: (a) pressence/vitals via vari��ncia de subcarrier + motion-band power + Welford + bandpass BPM (o robusto/mesmo commodity); (b) "signal field" 20x20 -> pico de espalhamento -> posi��ǜo em coords do room, mas ield_localize.rs:16-27 DIZ: "representation, not calibrated multistatic triangulation... NOT a calibrated person fix... pose= None when no model (never fabricated)"; (c) localiza��ǜo M��TRICA s�� por trilatera��ǜo cl��ssica RSSI path-loss + TDoA via NeumannSolver (mat/localization/triangulation.rs) com **GDOP real** + LocationUncertainty + is_actionable() <=3m & conf>=0.8; (d) pose 17-keypoint = ML mas SEM pesos treinados (firmware README: "does NOT run a trained neural model", ADR-079 P7-P9 Pending). 2) **Epistemologia de ouro (== I4)**: core/pose_observation_v2.rs PoseObservationV2 �? o template: trust_state Known/Degraded/Unknown, uncertainty_calibrated, dimensionality Image2d(audit)/Metric3d, SpatialFrameRef(metric/right_handed/z_up), SourceProvenance(sensor_id/authenticated/replay_protected), calibration_id, ModelRef(id+artifact_hash), por-joint SymmetricCovariance3+Probability+JointVisibility, observer_confidence, canonical_hash blake3. E coherence_gate.rs GateDecision = Accept(full Kalman update)/PredictOnly(predict-only, noise x3)/Reject/Recalibrate �?" a FORMA OPERACIONAL de "saber vs ver": observa��ǜo n�?o-confi��vel -> usa MODELO/prior, nunca fabrica percep��ǜo. 3) **Mundo q aprende passivo + recalibragem**: f_slam.rs descobre reflectors persistentes (Wall/Furniture/Mobile por migra��ǜo m/dia) vira ObjectAnchor; evolution.rs TemporalVoxelMap occupancy Bayesiano com confidence saturando por evid��ǜncia (regra 5 frames) + VoxelGate(Full/Anonymous/Restricted) + EvolutionTracker change-point (>=3 links 2sigma). 4) **FIT Cosca**: Cosca TEM WorldEntity/SpatialObservation/SpatialRelation/worldframe. FALTA: (i) proveniǦncia+trust_state+uncertainty por observa��ǜo espacial (o campo de maior valor, ADOTAR); (ii) gate que demo te "saber vs ver" (PredictOnly/Reject) ao alimentar o worldmodel; (iii) RF/CSI como SpatialProvider externo (I8) -- Cosca n�?o tem sensor RF, embedding seria I7-violation. 5) **Rejeitar**: toda a implementa��ǜo CSI (firmware ESP32 promiscuous, mesh 3-6 nos sincronizado, multistatic, RuVector/CIR, DensePose NN, e especialmente o CRV "Coordinate Remote Viewing" pseudo-cient��fico com gestalt/AOL/emotional-impact). "Commodity WiFi" �? marketing: chip commodity mas firmware custom + promiscuous + calibra��ǜo por sala. 6) **Ativo real**: a HONESTIDADE epistemica no c��digo (field_localize "do not over-claim", "never asserted as certainty", firmware "What this firmware does NOT do") �? o que o Cosca deve emular �?" rotular inferǦncia como inferǦncia. |
| **Next** | Proposta ADR: stamp de confi/trust (Known/Degraded/Unknown + covariance + source.provenance + calibration_id) em toda SpatialObservation do Cosca (fonte do modelo -> I4 por constru��ǜo), e um gate "demo te para previs��ǜo" no feed do worldmodel. N�?o portar nada de RF/DSP. |

### 2026-08-28 — Mineração profunda: kubernetes/sample-controller (control loop canônico)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Mineração de `kubernetes/sample-controller` (Go, Apache-2.0): o canonical controller pattern (CRD+informer+workqueue+reconcile+status). Foco: loop completo, determinismo/idempotência (I1/I2), requeue/backoff, generation |
| **Technique** | Leitura de código (não README): `controller.go`, `main.go`, `controller_test.go`, `pkg/apis/.../types.go`, `docs/controller-client-go.md` + `go.mod`. Gap: espelhei `internal/pipeline/{workqueue,reconciler,recovery}.go`, `internal/durable/ledger.go`, `internal/bootstrap/bootstrap.go` |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #mineração #kubernetes #controller-pattern #workqueue #reconcile #observedGeneration #idempotencia #adr-017 #I1 #I2 #reuso |
| **Related** | clone `awslabs-mining/sample-controller`, `internal/pipeline/{workqueue,reconciler,recovery}.go`, `internal/durable/ledger.go`, `internal/bootstrap/bootstrap.go`, `docs/architecture/PIPELINE_TECNICO.md §5.2`, `docs/roadmap/evo-plano-evolutivo.md` |
| **Learned** | 1) **O Cosca JÁ PORTOU o core do k8s control-loop**: `internal/pipeline/workqueue.go` é cópia funcional de `client-go/util/workqueue` (contrato When/Forget/NumRequeues, `ItemExponentialFailureRateLimiter` base 5ms cap 1000s — MESMOS defaults do sample, `MaxOfRateLimiter`, Add/AddAfter/AddRateLimited/Get/Done, heap delaying queue). 2) **O sample NÃO implementa `generation/observedGeneration`** (honestidade): o idiom "não re-processar se já convergiu" só se manifesta de 2 formas parciais — (a) `UpdateFunc` fast-path por `ResourceVersion` igual (controller.go L145-149, skip de evento de resync); (b) dedup por key no workqueue (`Add` idempotente). Não há contador de generation no `Foo`. observedGeneration é idiom *recomendado* (k8s controllers.md), NÃO demonstrado neste sample. 3) **Idempotência real vem de**: leitura do objeto do Indexer (nunca da request), `Get`→`Create`/`Update` idempotentes + `FieldManager` (server-side apply com owner refs), `IsControlledBy` (owner ref) como guarda anti-clobber, e status via `UpdateStatus` subresource. 4) **`processNextWorkItem` (L200-236) é o contrato de erro**: sucesso→`Forget` (limpa backoff), falha→`AddRateLimited` (requeue com backoff). `syncHandler` (L241-312) compara desired vs actual e RETORNA erro p/ disparar requeue. 5) **owner-ref cascade** (handleObject L345-383): filho (Deployment) mudou→anda ownerReferences→enfileira o pai (Foo). O Reconciler do Cosca NÃO tem isso (é pull por plan/history; não re-enfileira o pai quando um dependente muda). 6) **FIT Cosca**: TEM workqueue+reconciler+recovery+durable(generation/fencing) como PEÇAS SEPARADAS, mas elas NÃO formam 1 loop contínuo (workqueue→runWorker→processNextWorkItem→syncHandler→AddRateLimited) — o Reconciler é one-shot (via REST handler/bootstrap) e não há observedGeneration p/ skip de convergência. Gap real = (a) loop contínuo ligando as 3 peças; (b) skip por generation/revision-hash; (c) dependente→pai. |
| **Next** | Se levar ao design: propor ADAPTER em `internal/pipeline` de um "ControlLoop" contínuo (workqueue→runWorker→reconcile→AddRateLimited) + `observedGeneration`/revision-hash no estado do plan/world p/ skip de convergência, SEM tocar k8s CRD/informer (camada infra é distinta — REJEITAR). |

### 2026-08-28 — Mineracao Google/agents-cli (ADK): harness = toolchain, nao runtime; diamante = eval-as-flywheel
| Field | Value |
|-------|-------|
| Agent | cosca-architecture |
| Task | Mineracao de codigo de google/agents-cli (Python, Apache-2.0): harness que transforma assistente em agente + skill format + gate I1; FIT Cosca |
| Technique | Codigo (nao README): main.py, _click.py (LazyGroup+experiment), _runner.py, _adk_client.py, run/cmd_run.py, run/_local_server.py, _tools.py, _skills_check.py, _trust.py, agents/adk/app/agent.py, setup/cmd_setup.py, eval/*, data/_recipe.py, skills/SKILL.md. Gap vs internal/{engine,agents,agentbridge,orchestration,skills,skilleval,evolution,gate,proposal,deliberate} |
| Level | 3 |
| Outcome | success |
| Tags | #mineracao #adk #agents-cli #toolchain #eval-flywheel #loss-clustering #clone-and-study #skill-version #adr-017 #invariantes #I1 #I7 |
| Related | clone E:\cosca-tmp\awslabs-mining\agents-cli, docs/adr/ADR-017-capability-borrowing-protocol.md, docs/roadmap/evo-plano-evolutivo.md (I1-I8) |
| Learned | 1) ACHADO HONESTO: o harness NAO esta neste repo. agents-cli e toolchain meta-CLI (shells para uv/npx skills/adk api_server/go/terraform/gcloud), cliente HTTP/SSE/A2A do servidor ADK (_adk_client.py, run/cmd_run.py). O loop tool-calling/contexto/sessao/subagentes/workflows vive na lib externa google-adk; o repo so DECLARA o agente declarativamente (Agent(name,model,instruction,tools=[fn]) em agent.py). "Transformar assistente em agente" = instalar skills (setup/cmd_setup.py -> npx skills) + scaffold (create). 2) Skill format = Agent Skills standard (SKILL.md + frontmatter name/description/metadata{author,license,version,requires{bins,install}}); _skills_check.py le metadata.version (L58-61) p/ drift e pina skills@1.5.9 (L28) — supply-chain I5-compativel. 3) DIAMANTE = eval-as-flywheel (maior lacuna do Cosca): eval generate (coleta deterministica de traces sobre dataset, concorrente, isolamento de falha por caso — run_case L217, _dispatch_cases L322-376), eval grade (metricas; LLM-as-judge response_quality.py temperature=0+schema JSON; metricas deterministicas agent_turn_count; CodeExecutionMetric em sandbox Vertex), eval compare (regressao), eval analyze (LOSS-CLUSTERING em taxonomia L1/L2 — generate_loss_clusters L144), eval optimize (auto-tune prompt — optimize_utils.py EvaluationDataset->ADK EvalSet train/val), eval dataset synthesize (MODEL-BASED USER SIMULATOR — cenarios multi-turn N+1, split_case_history prior_events seed). 4) Stance clone-and-study > construir capacidade no toolchain: RAG tombstoned (data/_recipe.py aponta p/ samples) + workflow skill Fase 1 "Study reference recipes" via samples.md topic index — casa com Borrow-capabilities-not-assumptions do ADR-017. 5) REJEITAR: stack Python/uv (I7 monolito, ADR-017 §7), harness google-adk (Cosca ja tem kernel melhor), modelo application-server GCP (FastAPI/Agent Runtime/Vertex/Cloud Run/GKE/Terraform — invertido ao Cosca local-first/2-zonas), agentplatform/Vertex como backend de grade, posture eval run exit-0-whatever-scores (sem gate de promocao — contraria I1). 6) NUANCE I2: fail-closed em erro de comando (ClickException, isolamento de falha) mas fail-open em decisao de politica — _trust.require_confirmation default (sem flags) auto-procede silencioso; eval run nao gatea. 7) I7 CONTRA: eval grade roda metricas custom IN-PROCESS com privilegios do CLI (cmd_grade.py avisa) e shella p/ subprocessos sem jaula — nao emprestar como esta. |
| Next | Se aprovado: ADR p/ "Agent Behavior Qualification" como loop fechado no Cosca — internal/evalgo deterministico (generate->grade->cluster->gate) reusando internal/{engine,agentbridge,skilleval,proposal,gate,evolution}, grade local-first (LLM local temperature=0) e GATE de promocao (evidencia, ADR-016/017); + Fase clone-and-study como disciplina de skill (adotar a postura, nao a capability). |

### 2026-08-29 — Mineracao profunda: qdrant/qdrant (vector / hybrid search engine, Rust)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Mineracao de codigo (nao README) de `qdrant/qdrant` (clone em Temp/opencode/qdrant, v1.19.0): componentes internos, modelo de dados, indexacao/busca, hybrid search, distribuicao, perf, deploy; FIT Cosca (Go single-binary + SQLite FTS5+vector) |
| **Technique** | Codigo (nao README): `lib/segment/src/index/{query_estimator,query_optimization/optimized_filter}.rs`, `lib/segment/src/common/{reciprocal_rank_fusion,score_fusion}.rs`, `lib/collection/src/operations/universal_query/collection_query.rs`, `lib/{sparse,bm25}/src/*`, `lib/segment/src/{index/hnsw_index/config,segment/mod,vector_storage/quantized/quantized_vectors}.rs`, `lib/collection/src/hash_ring.rs`, `lib/wal/src/lib.rs`, `lib/edge/src/lib.rs`, `config/config.yaml`. Gap: espelhei `internal/knowledge`, `internal/embed`, ADR-013 (agregador vetorial) |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #mineracao #qdrant #hybrid-search #rrf #dbsf #query-planner #cardinalidade #adr-017 #invariantes #I1 #indexacao #vector-db |
| **Related** | clone `C:\Users\Henrique\AppData\Local\Temp\opencode\qdrant`, docs/adr/ADR-017-capability-borrowing-protocol.md, `internal/knowledge`, ADR-013-modular-knowledge-databases, docs/roadmap/evo-plano-evolutivo.md (I1-I8) |
| **Learned** | 1) **Arquit contraria a do Cosca**: Qdrant = storage-first de VETORES (payload e metadado de filtro colado; document-first e o Cosca em SQLite). Hierarquia Collection→Shard→Segment; 17 crates em lib/. Segment com index auto por campo (Keyword/Integer/Float/Geo/Text/Bool/Datetime/Uuid, cada um 3 backends RAM/mmap). 2) **O 2 ativos reais p/ Cosca**: (a) **QUERY PLANNER POR CARDINALIDADE** (`query_estimator.rs`): estima quantos pontos um filtro seleciona (must=produto, should=prob-complementar `1-∏(1-pᵢ)`, min_should=combinações, min/max/exp) e decide **filter→vector vs vector→filter** por seletividade da condicao primaria (PrimaryCondition) — Cosca NAO tem isso; (b) **Fusao ajustavel**: `rrf_scoring` (`segment/src/common/reciprocal_rank_fusion.rs`) formula `1/(pos+1)/weight + k - 1`, DEFAULT_K=2, weights por fonte; `score_fusion` DBSF com `distr_norm` (Welford, 3σ) e `min_max_norm`, Aggregation::Sum. Deterministico, ~60 linhas, zero LLM (I1). 3) **Sparse=BM25 via inverted index**: `lib/bm25` (k1=1.2,b=0.75,avg=256) puro compute-only, token→u32 via **Murmur3 32-bit |i32|** (hash estavel). Query=sparse vector peso 1.0, doc=TF saturado; IDF guardado NO indice (nao calculado na query). 4) **Prefetch aninhavel** (`CollectionQueryRequest.prefetch`) + fusao no local shard (`fusion_rescore`), suporta weights e score_threshold; MAX_PREFETCH_DEPTH limita recursao. 5) **Distribuicao**: `hash_ring.rs` (HASH_RING_SHARD_SCALE=100, `HashRingRouter::Single|Resharding{old,new}`), resharding zero-downtime, shard transfer por stream_records/snapshot/**wal_delta**, Raft (`compact_wal_entries`). 6) **Perf**: RAM vs on-disk por `memory: cold|cached|pinned`; `low_memory_mode` (no_resident/no_populate) como recovery knob; io_uring (`async_scorer`, `io_uring: auto`) Linux-only; SIMD (`spaces/simple_{avx,sse,neon}.rs` + metric_f16), GPU WGPU p/ INDEXACAO (feature flag `gpu`). 7) **Edge** (`lib/edge/`): `EdgeShard`/`ReadOnlyEdgeShard`/`UpdateOnlyEdgeShard` + builders — EMbutido no processo (conceito igual ao Cosca, mas Rust/nao-Go). 8) **REJEITAR**: portar Rust→Go; HNSW fechado reimplementado; quantizacao scalar/PQ/binary + GPU indexing (over-engineering p/ 257MB); io_uring (Linux-only); consensus/shard-transfer (multi-no, fora do caso single-binary/2-zonas); Edge em Rust. Benchmarks publicados sao no site externo, NAO reproduziveis no repo (ceticismo). 9) **FIT Cosca**: Cosca JA faz hybrid FTS5+vetorial em SQLite (paridade funcional ok); gaps reais = query planner por cardinalidade + fusao RRF(k,weights)/DBSF(3σ) + score_threshold por fonte. Prioridade P1 fusao (baixo esforco) / P2 planner (medio). Nao substituir FTS5 por murmur3 hashing (FTS5 nativo ja faz BM25 correto). |
| **Next** | Se aprovado: ADR "hybrid-search deterministico no Cosca" — `internal/knowledge` ganha (a) fusao RRF com k+weights e DBSF por 3σ (reusando `internal/embed`, zero LLM, I1) e (b) estimador de cardinalidade + plano de ordem filter→vector vs vector→filter; + `score_threshold`/`offset` consistentes por fonte. Considerar shard-key para multitenancy so se roadmap pedir. Nao tocar HNSW/quantizacao/GPU/io_uring. |

### 2026-08-29 — ADR-027: NÃO adotar Qdrant; absorver payload index (colunas materializadas + B-tree) + facets SQL
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | ADR-027 — formalizar a decisao de NAO adotar o Qdrant como engine vetorial agora e absorver as boas praticas (payload index + facets via SQL + RRF/DBSF) como melhorias aditivas no `internal/knowledge` |
| **Technique** | Mapa gap honesto + leitura de codigo-fonte real (nao README): `internal/knowledge/knowledge.go` L915 (`json_extract(metadata_json,'$.epistemic')` SEM indice), `internal/vector/sqlite_vec.go` L407-420 (`filterClause` aplicado a CADA row candidata), `internal/vector/index.go` (indice in-mem quantizado SoA int8/int16 AVX2 recall@10 0.99). Referencias: ADR-002 (alvo <20ms, local-first), ADR-013 (modulos <100MB, single-binary, vector.db ~88% do teto), ADR-017. Reusei o formato/tonalidade dos ADRs recentes (024/026/020: PT, secoes numeradas, bloco `>` Status/Owner/Revisao/Referencia, tabelas, rodape autor) |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #adr-027 #qdrant #payload-index #facets #rrf #dbsf #vector-db #single-binary #zero-infra #adr-002 #adr-013 #document-first #joelho-de-performance |
| **Related** | `docs/adr/ADR-027-qdrant-decision-payload-index.md`, `docs/adr/ADR-002-knowledge-engine.md`, `docs/adr/ADR-013-modular-knowledge-databases.md`, `docs/adr/ADR-017-capability-borrowing-protocol.md`, `internal/knowledge/knowledge.go`, `internal/vector/{sqlite_vec,index}.go`, clone `Temp/opencode/qdrant` |
| **Learned** | 1) **A arq do Qdrant e OPOSTA a do Cosca** e isso e a resposta a duvida do Don: Qdrant = vetor-first (payload e tag de filtro COLADA ao vetor); Cosca = document-first (conteudo e fonte, vetor e indice derivado, payload e epistemologia real CKL/EvidenceClass/proveniencia). Trocar de modelo quebraria a LEITURA UNIFICADA (grafo+FTS5+epistemia+proveniencia na mesma query) — a vantagem central. 2) **Estamos a ~2,8% do joelho de perf** (28.888 vetores de ~1M); o joelho (~1M, slab ~768MB saindo do L3) e onde ANN/HNSW importa. 3) **O deficit real NAO e ANN** (que o indice quantizado in-mem ya resolve, recall@10 0.99) — e FILTRO POR PAYLOAD SEM INDICE: hoje `json_extract` em documents/vectors SEM indice → full-scan. O que o payload index do Qdrant resolve. 4) **Pecas a absorver (aditivo, zero infra)**: (a) colunas materializadas + B-tree p/ epistemic/tier/document_id/entity_type (backfill idempotente — Fase 4 ja faz epistemic); (b) estimador de cardinalidade + plano filter→vector vs vector→filter (replicar `query_estimator.rs`, ~60 linhas, I1); (c) facets via `GROUP BY`/`COUNT` (nao pos-corte sobre top-N); (d) fusao deterministica RRF (k=2, weights por fonte) + DBSF (distr_norm Welford 3σ / min_max, Aggregation::Sum) + score_threshold/offset; (e) rescore 2-estagios via seam `internal/embeddings` default off (I1). 5) **Rejeicoes**: (a) Qdrant standalone (servidor + Raft/sharding → quebra ADR-002 zero-infra + ADR-013 <100MB single-binary); (b) Qdrant Edge (so Rust/Python, SEM binding Go, beta → inviavel p/ Go sem CGO); (c) manter tudo igual (perde payload index/facets); (d) portar Rust→Go (over-engineering; o ativo real e adaptavel em ~60-200 linhas). 6) **Gates p/ reabrir** G1-G5: vetores >500k, latencia com filtro p95 >20ms, facets como requisito sobre corpus inteiro, multivetor/multitenancy fisica, ~1M vetores (joelho). 7) **Design aditivo**: NADA de servico/dep externa/processo; ADR e document-only, nao roda build/test. |
| **Next** | Se o Don aprovar, fatiar: backfill idempotente das colunas materializadas (epistemic/tier/document_id/entity_type) + `CREATE INDEX` B-tree; depois o estimador de cardinalidade + plano de ordem (o ativo mais tangivel — ele e o que da o ganho real de filtro); depois facets via GROUP BY e RRF/DBSF. Nao tocar HNSW/GPU/io_uring ate os gates G1-G5. |

### 2026-08-31 � ADR-033: Cognitive Shadow Mode (decora��o observacional da delibera��o ADR-032)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | ADR-033 � design do Shadow Mode: medir a delibera��o (ADR-032) SEM aplicar a autoridade; observar antes de ganhar autoridade |
| **Technique** | Mapa de reuso honesto: o ADR-032 J� EST� IMPLEMENTADO (deliberation.go, orchestrator.go:327-366, DeliberateConfig.Enabled fail-closed). O gap N�O � implementar delibera��o, � um MODO observacional que reusa Deliberator.Deliberate e descarta o efeito (WithLLMResponse/WithDeliberationHandled/WithAugmentedPrompt). |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #shadow-mode #deliberation #adr-032 #adr-033 #nao-interferencia #observability #feature-flag #jsonl-store #bridge-discipline |
| **Related** | `docs/adr/ADR-033-cognitive-shadow-mode.md`, `docs/adr/ADR-032-kernel-first-deliberation.md`, `internal/orchestration/{orchestrator,deliberation,executor,types}.go`, `internal/deliberate/*`, `internal/config/config.go:408,1129`, `internal/cost/cost.go` (padr�o store JSONL), `internal/brainweb/observatory.go` |
| **Learned** | 1) **A diferen�a cr�tica ativo vs Shadow**: o ativo APLICA o verdict (EmitOK?WithLLMResponse+WithDeliberationHandled=true ? executor pula LLM em executor.go:238; incerto?WithAugmentedPrompt contexto limpo); o Shadow APENAS registra (mesmo Deliberate(), mas NUNCA toca os 3 knobs de autoridade). O "decidir" � id�ntico; muda s� o "o que se faz com o trace". 2) **Nome de knob (verdade de c�digo)**: executor.go:238 (DeliberationHandled), orchestrator.go:352 (WithLLMResponse/WithDeliberationHandled/WithExecutorDeterministic), orchestrator.go:362 (WithAugmentedPrompt). 3) **Deliberator.Deliberate � PURO** (deliberation.go:209) � retorna trace, n�o muta pc (PipelineContext valor imut�vel) ? Shadow � naturalmente side-effect-free; s� falta N�O chamar os setters. 4) **Feature flag independente**: shadow_mode=true roda MESMO com enabled=false; valida��o de thresholds deve ser `Enabled || ShadowMode` porque o Shadow usa os mesmos thresholds. 5) **N�o-interfer�ncia do professor = goroutine destacada + context.WithTimeout + defer recover() + descarte silencioso (debug) ? lat�ncia/erro zero na resposta**. 6) **Persist�ncia**: N�O usar `pc.Data.DeliberationTrace`?Extra (vol�til, some no fim, n�o agreg�vel); usar store JSONL append-only `internal/shadow` (.cosca/shadow/records.jsonl) espelhando o padr�o do `internal/cost` (padr�o, n�o pacote � ADR-015). 7) **Taxonomia ShadowDecision**: RETRIEVAL_INSUFFICIENT (0 posi��es efetivas, WouldEscalate=true) / EMIT_OK (WouldEscalate=false) / EMIT_WITH_RESERVATIONS / ESCALATE. 8) **Teste-espinha = equival�ncia**: resposta byte-a-byte ID�NTICA Shadow ON vs OFF (mock ChatProvider que falha se chamado; called==true prova que Shadow n�o curto-circuita). 9) **Ressalva honesta**: dados contrafactuais (WouldRespond = o que teria dito, n�o o que foi dito) � indicador, n�o alvo. |
| **Next** | Se aprovado: Fase 0 (config ShadowMode + internal/shadow store + wiring) primeiro; Fase 1 (ramo Shadow no Engine.Execute + toShadowTrace); Fase 2 (cosca shadow summary/list/report + --json); Fase 4 (calibrar thresholds com dados reais ANTES de promover enabled=true � a ponte ganha autoridade depois de medida). |

### 2026-09-01 - Mineracao diffusers (pipeline composition / memory / config contracts)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Mineracao de codigo (nao README) de huggingface/diffusers: encontrar JOIAS transferiveis para o Cosca (Go, percepcao multimodel visao/audio/voz, RX 6700 XT sem CUDA) |
| **Technique** | Codigo (nao README): modular_pipelines/{modular_pipeline,modular_pipeline_utils,components_manager}.py, pipelines/pipeline_utils.py, configuration_utils.py, utils/{outputs,import_utils}.py, models/auto_model.py, hooks/group_offloading.py, image_processor.py. Gap: espelhei internal/perception (conceito, pipeline de visao 6-10s/frame CPU), internal/observe (Obs/State), ADR-012/013/017 |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #mineracao #diffusers #pipeline-composition #modular-pipeline #state-block #component-spec #offload #memory #configmixin #baseoutput #adr-017 #invariantes #observability #perception |
| **Related** | clone Temp/opencode/hfmining/diffusers, docs/adr/ADR-017-capability-borrowing-protocol.md, internal/perception, internal/observe, docs/roadmap/evo-plano-evolutivo.md (I1-I8) |
| **Learned** | 1) **MAIOR JOA = ModularPipeline + Blocks + PipelineState/BlockState**: pipeline decomposto em blocos declarativos (Sequential/Conditional/LoopSequential/Auto) que leem/escrevem um PipelineState compartilhado; cada bloco declara inputs/outputs/equired_inputs/expected_components/expected_configs; __call__(state, output=...) retorna ou o state inteiro ou um intermediate por nome. � EXATAMENTE o contrato de Obs/State do Cosca (percepcao = pipeline de blocos) - mapear PipelineState.values+kwargs_mapping = estado da percepcao, BlockState = estado local do bloco. 2) **ComponentSpec/ConfigSpec** = "carregar so o necessario": cada componente tem load_id (path|subfolder|variant|revision) e default_creation_method (from_pretrained vs from_config); componentes SEM peso (scheduler/guider) recriam de config, modelos com peso carregam on-demand. _is_single_file_path_or_url + AutoModel dispatch. 3) **pipeline_utils.py**: egister_modules(**kwargs) injeta componentes nomeados; components property retorna dict; .to(device) mapeia; model_cpu_offload_seq string de ordem; _exclude_from_cpu_offload lista. 4) **Offload em 3 niveis** (o ativo p/ memoria): enable_model_cpu_offload (um modelo inteiro por vez, baixa penalidade), enable_sequential_cpu_offload (leaf-level, max economia, mais lento), enable_group_offload (meio-termo: grupos de camadas internas + prefetch assincrono via streams) - mapeia para rodar visao lenta mantendo modelo na GPU e trocando visao/audio/voz com budget. 5) **ConfigMixin + @register_to_config** (decorator que introspeciona __init__, guarda kwargs nao-underscore, rastreia _use_default_values, ignora ignore_for_config, serializa JSON, escreve uto_map) + FrozenDict - padrao declarativo de config. 6) **BaseOutput** (dataclass OrderedDict): __post_init__ promove campos nao-None como items, 	o_tuple(), bloqueia del/pop/update (semi-imutavel), registra subclasses como pytree p/ DDP/static_graph. 7) **import_utils**: _LazyModule (defer de import via __getattr__) + is_x_available() + dummy_*_objects que raise OptionalDependencyNotAvailable - gate de backend opcional (mapeia p/ ONNX/directml/whisper detection). 8) **AutoModel.from_config** resolve classe via config.auto_map/_class_name + ALL_IMPORTABLE_CLASSES + dynamic module load - registry/descoberta por string (mapeia p/ provider/engine registry do Cosca). 9) **Ressalva honesta**: repo e Python/torch (I7 - NAO portar); emprestar so os PADROES. |
| **Next** | Se o Don aprovar (ADR-017): levar a "pipeline de percepcao por blocos + PipelineState" como hipotese de desenho em internal/perception (blocos Sequential/Conditional com declared inputs/outputs), e o "offload 3-niveis + budget" como padrao de memoria p/ troca de modelos visao/audio/voz na RX 6700 XT. Nao portar torch/diffusers. |

### 2026-09-01 — Mineracao zernio-dev (blueprint de plataforma de gestao de redes sociais via API)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Mineracao externa (pesquisa, so cosca.web) da org GitHub `zernio-dev` (27 repos) para extrair blueprint de arquitetura de plataforma social-media-API — aplicar na NOSSA plataforma |
| **Technique** | API GitHub publica (org/repos, git trees) + raw READMEs + codigo-fonte dos SDKs/node n8n + docs.zernio.com via `llms-full.txt` (Mintlify/Next SPA: paginas .mdx devolvem shell, so llms.txt/llms-full.txt renderizam markdown). Limite do cosca.web = ~8k chars/resposta: arquivos grandes (openapi.yaml 2,4MB, README 50KB) so lidos parcialmente; paginacao de repos via per_page pequeno. |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #mineracao #zernio #social-media-api #arquitetura #blueprint #webhooks #oauth #multi-tenancy #adr-017 #spec-first #retry |
| **Related** | `docs/reports/zernio-mineracao/arquitetura-zernio.md`, ADR-017 (capability borrowing), `internal/pipeline` (workqueue/retry), `internal/webhook` (a criar), `internal/gate` |
| **Learned** | 1) **Contrato-ouro**: 1 post → N targets com status POR PLATAFORMA + agregado `post.partial` + logs por tentativa + retry por target + unpublish por plataforma — modelar Post→PostTarget(status)→PublishAttempt(log) desde o dia 1. 2) **Webhooks**: event-id p/ dedup no consumidor + HMAC-SHA256 header + timeout 5s + retry c/ mesmo id + /test + delivery logs + redeliver manual (zernflow prova: ack-antes-processa-depois). 3) **Idempotencia 2 camadas**: Idempotency-Key do cliente + content-hash dedup 24h → 409+existingPostId. 4) **Spec-first**: openapi.yaml (2,4MB!) como fonte unica, SDKs gerados via pipeline (OpenAPI Generator 7.19.0); drift quando ha SDKs hand-written + gerados coexistindo — escolher 1 caminho. 5) **Multi-tenancy**: team→profile→account (accountId nos webhooks; perfil = pasta/tenant); OAuth broker centralizado `connect/{platform}?profileId=` + clone-connection p/ reusar OAuth entre profiles/paginas/orgs. 6) **API key com escopo por resource group** (disabledResourceGroups) — chaves minimas p/ multi-tenancy. 7) **Health check de conta** (token vs permissao vs link vivo p/ WhatsApp) + billing metered por account-day (Metronome+Stripe, tiers graduados) + rate limit escalando com nº contas. 8) **Erros padrao** `{code, error}` machine-readable + versionamento em path `/v1` + deprecation explicita. 9) Riscos a evitar: drift SDKs, spec sprawl (2,4MB), vazamento de `_id` Mongo no contrato, heranca de rebrand mal-saneada (x-late-*, late-sdk), dependencia de wrappers nao-oficiais (fork TikTok-Api), retry-storm com timeout 5s. 10) **Aprendizado de ferramenta**: cosca.web cap 8k → p/ ler arquivos grandes do GitHub use git trees p/ achar caminhos e leia arquivos pequenos/multiplos; p/ docs SPA Mintlify use `llms.txt`/`llms-full.txt`. |
| **Next** | Quando formos desenhar a plataforma: ADR p/ (a) modelo de publicacao fan-out com status por plataforma (reuso `internal/pipeline` workqueue/retry/backoff), (b) webhooks com event-id+HMAC+delivery logs, (c) spec-first OpenAPI como fonte unica de SDKs. Nao copiar codigo da Zernio — copiar os padroes de contrato. |

### 2026-09-05 � Re-mineracao ruvnet/ruflo (olhar de hoje: modulos, L1/L2/L3, LoRA, memoria semantica)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Re-mineracao de ruvnet/ruflo (clone em Temp/opencode/remina) ja minerado antes (35 padroes). Foco novo: memoria imutavel/supersession, soberania L1/L2/L3, meta-harness degradavel, swarm muitos agentes, padroes que so agora fazem sentido |
| **Technique** | Leitura de codigo/ADR (nao README). Gap vs COSCA evoluiu: ADR-013 modular, busca híbrida, memoria semantica, esteira LoRA, kernel L1/L2/L3 |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #remineracao #ruflo #bitemporal #supersession #capability-plane #soberania #cow-branch #trust-weighted #consolidacao #L1L2L3 #adr-017 #I1 |
| **Related** | clone Temp/opencode/remina/ruvnet_ruflo, docs/adr/ADR-017-capability-borrowing-protocol.md, ADR-013-modular-knowledge-databases, internal/{knowledge,memory,deliberate,gate}, kernel L1/L2/L3 |
| **Learned** | 1) **BITEMPORAL (temporal.ts)**: assertion_time (assertedAt/retractedAt) NE global de validity_time (validFrom/validUntil) — 2 relogios independentes. TemporalStatus 5 estados future/active/expired/superseded/retracted, COMPUTADO por consulta (computeStatus L872: retracted>superseded>expired>active>future), nunca setado. TemporalReasoner: whatWasTrue(T)/whatIsTrue/whatWillBeTrue(T), hasChanged(since), conflictsAt (reconcile), projectForward. COSCA tem so "invalida-nao-sobrescreve" — falta bitemporal + raciocinio temporal. 2) **WRITE-VERIFY (ADR-354)**: MemoryWriteVerifier coverage<0.8 retry / faithfulness<0.85 rollback / preservation<0.9 rollback, verdict commit|retry|rollback; EntityPredicateSupersessionLayer com entityPredicateKey deterministica substituindo similaridade (0.95-1.00 vs 0.47). Anti-hallucination gate na ESCRITA de memoria. 3) **CAPABILITY PLANE (ADR-325)**: soberania != escala 1-3, sao 4 responsabilidades ortogonais (work ownership/workload identity/authorization grants/policy enforcement) + invariante "nenhum termo substitui outro" (signed envelope != permissao; Agent Card != autoridade; work claim != acesso; grant != posse; consensus != policy local). Modos legacy|observe|enforce fail-closed + maximumDelegationDepth narrowing. WorkOwnershipRecord com epoch monotônico de fencing + CAS (L368-400). Isso e o modelo operacional dos niveis L1/L2/L3. 4) **SPEND-GATED DEGRADE (weight-eft.ts)**: degraded nao e bool, e maquina: dry-run(offline , sem host) -> preflight(read-only probes) -> refused(execute sem --yes) -> executed. "no implicit remote execution" (dry-run offline NAO roda probe). Esteira LoRA: transcripts -> SFT+DPO JSONL + guard report (contaminacao/reward-hack/long-context) + cost-Pareto delta + ruvllm microlora sft->dpo. 5) **COW BRANCH agenticow (swarm-memory-branches.ts)**: 162 bytes vs 3.3GB (regressao v3.14.4 git-worktree-per-agent). fork no spawn -> isolado -> sucesso=promote(merge edits+tombstones) / falha=discard. Idempotente + kill-switch env + {degraded:true} se lib opcional. Resolve dilema N agentes numa memoria sem clonar tudo. 6) **TRUST-WEIGHTED CONSENSUS (ADR-350)**: trust_i=0.9*trust_i+0.1*outcome_i, votacao ponderada, eviccao trust<0.2x3ciclos->degraded+fora do quorum. IMPORTANTE honestidade: grep confirma NAO implementado em swarm/consensus — so ADR Proposed. 7) **RECALL pipeline (smart-retrieval.ts)**: RRF + recency half-life boost + MMR(cosine com fallback token-Jaccard + isWellFormedEmbedding guard L347 anti-NaN) + sessionRoundRobin. Complementa a mineracao qdrant (que parou no RRF). 8) **CONSOLIDACAO (consolidator.ts)**: sweepExpired->dedup(sha256, keep-newest/oldest/merge-tags)->compactHnsw(rebuild+swap atomico). GC de memoria; agregador de ADR-013 e read-model reconstruivel -> compactHnsw e o rebuild idempotente. |
| **Next** | Se levar ao design: (a) bitemporal em internal/memory + whatWasTrue/conflictsAt (alimenta orbita da IA local); (b) MemoryWriteVerifier + entityPredicateKey no gate de escrita da memoria semantica (anti-sicofancia via provenance_type); (c) ADR para "capability plane" como modelo formal dos niveis L1/L2/L3 (4 eixos + inherit-por-grant-nao-por-naivel + fencing epoch); (d) spend-gated degrade ladder + cost-Pareto como gate da esteira LoRA; (e) COW branch para isolar N agentes num nucleo compartilhado; (f) consolidation loop no vector.db. NAO portar trust-weighted consensus como se fosse provado (e ADR). |

### 2026-09-05 - RE-Mineracao openai-agents-python (olhar v1.5): approval dur�vel + determinismo + as_tool tipado
| Field | Value |
|-------|-------|
| Agent | cosca-architecture |
| Task | RE-minerar openai/openai-agents-python (ja minerado antes, 7 padroes) com o olhar de HOJE: kernel Don->Kernel->CEO->CTO->Chiefs->Specialists, gate de aprovacao, esteira LoRA, L1/L2/L3 |
| Technique | Codigo (nao README): run_context.py, run_state.py, model_settings.py, agent.py, tool.py, memory/session.py, lifecycle.py, run_config.py, run_internal/guardrails.py + docs/{guardrails,handoffs,human_in_the_loop,tracing}.md. Gap vs .cosca/fallback/knowledge/patterns/openai-agents-sdk-patterns.md (7 padroes) |
| Level | 3 |
| Outcome | success |
| Tags | #mineracao #re-mineracao #openai-agents-sdk #hitl #durable-runstate #determinism #lora-esteira #as_tool #sessions #tool_use_behavior #adr-017 #invariantes #I1 |
| Related | .cosca/fallback/knowledge/patterns/openai-agents-sdk-patterns.md, docs/adr/ADR-017-capability-borrowing-protocol.md, internal/gate, internal/deliberate, internal/orchestration, internal/audit |
| Learned | 1) O SDK MADUROU muito desde a mineracao anterior. A maior lacuna do Cosca eh approval DURAVEL: approval = interrupt (nao falha) + RunState serializavel (to_json/to_state/add_input/get_interruptions), RECHEADO com CURRENT_SCHEMA_VERSION 1.17 + SCHEMA_VERSION_SUMMARIES 1.0-1.17 (forward-compat fail-fast), sticky(bool) vs per-call(list call_id) com approval_scope, nested as_tool approval que SOBE para o run EXTERNO, strict_context/context_serializer/context_override, e versionar agent-definitions/prompt junto do snapshot. 2) DETERMINISMO por camadas: ModelSettings.resolve(override) merge agente->RunConfig->run + temperature/top_p/tool_choice(auto|required|none|MCPToolChoice)/parallel_tool_calls/store/reasoning/verbosity/retry(runner-managed). Eh o mecanismo da esteira LoRA L1/L2/L3. 3) as_tool = contrato TIPADO (parameters/input_builder/include_input_schema) + custom_output_extractor + on_stream + session+max_turns proprios + needs_approval -> Chief invoca cosca-specialist-* como tool sem trocar o dono da conversa (contraste com handoff que troca). 4) Sessions separam memoria de conversa duraver (get_items/add_items/pop_item/clear_session + run_compaction) do RunContextWrapper.context (deps do app) -> e o limite entre .cosca/ e o state do run. 5) tool_use_behavior('run_llm_again'|'stop_on_first_tool'|StopAtTools|ToolsToFinalOutput) + reset_tool_choice = 'resultado de tool deterministica vira output final SEM LLM re-interpretar' -> traduz 'o deterministico decide, a IA compreende' p/ runtime (L1/L2 determinismo, Critic tripwire). 6) Guardrails refinados: input=1o agente / output=agente final; tool_guardrails por-INVOCACAO; pre_approval_tool_input_guardrails (checa args ANTES de pedir aprovacao); tripwire cancela task LLM em voo (should_cancel_parallel_model_task_on_input_guardrail_trip). 7) Tracing: group_id agrupa a jornada Don, trace_include_sensitive_data (2-zonas), include_task_and_turn_spans, flush_traces p/ workers, set/add_trace_processor plugavel. 8) 4 refinam (P1 handoff+input_type+is_enabled, P3 guardrails, P5 as_tool, P7 tracing), 3 NOVOS (P4 determinismo, P6 sessions, P8 tool->final), P2 refine profundo (approval dur�vel). |
| Next | Se aprovado (ADR-017): levar P2 (RunState versionado + approval sticky-escopado + serialize/resume p/ migration/release) como a TESE para o gate de aprovacao; e P4 (ModelSettings por nivel L1/L2/L3) + P8 (tool->final p/ veredito deterministico) como o par de determinismo da esteira LoRA. |

### 2026-09-05 - RE-Mineracao temporalio/sdk-go (olhar v1.5): versionamento de replay, coroutines, updates, sessions GPU
| Field | Value |
|-------|-------|
| Agent | cosca-architecture |
| Task | RE-minerar temporalio/sdk-go (clone shallow em Temp/opencode/remina) ja minerado antes (padrao Temporal -> internal/dflow). Foco SDK Go (mesma linguagem do COSCA), olhar de HOJE: corte modular, durable execution, gates, esteira LoRA, 61 agentes |
| Technique | Codigo Go (internal/): internal_event_handlers.go, internal_workflow.go, internal_task_handlers.go, internal_command_state_machine.go, workflow_deadlock.go, internal_update.go, workflow_random.go, internal/session.go. Gap vs internal/dflow/dflow.go atual |
| Level | 3 |
| Outcome | success |
| Tags | #remineracao #temporal-sdk #durable-execution #replay #getversion #coroutines #updates #sessions-gpu #continueasnew #mutable-side-effect #rng-streams #parent-close-policy #dflow #adr-023 |
| Related | internal/dflow/dflow.go, internal/gate, internal/orchestration, projetos/cosca-code (esteira LoRA, GPU ROCm RX 6700 XT) |
| Learned | 1) **GetVersion** (internal_event_handlers.go:958) = versionamento tolerante a replay: changeID + minSupported/maxSupported -> marker VERSION + UpsertSearchAttributes(TemporalChangeVersion); validateVersion (:945) fail-fast se a versao historica sai do range. dflow falha fechado (ErrReplayDiverged) em QUALQUER divergencia, mas run longo (esteira LoRA cujo codigo evolui por step) precisa de ramos compat, nao aborto. 2) **Coroutines deterministicas** (internal_workflow.go:1281 ExecuteUntilAllBlocked; dispatcherImpl :163; coroutineState :152): loop cooperativo 1-por-vez em ordem deterministica ate tudo bloquear (channel/selector/timer); deadlock detector (workflow_deadlock.go:29-146) falha rapido se tudo bloqueia sem desbloqueio. dflow e sequencial -> nao orquestra fan-out dos 61 agentes em paralelo deterministico. 3) **Update durable em workflow RUNNING** (internal_update.go:135 HandleMessage, :242 defaultUpdateHandler, :336 newUpdateHandler; workflow.go:655 SetUpdateHandler): protocolo validate->Accept->execute->Complete, validator roda SO em non-replay (:302), accept/complete via mensagens. Substitui ErrPending+ResolveEvent (re-run do zero) por canal vivo replay-safe p/ gate/HITL sem reiniciar historico. 4) **Sessions recurso-afinidade** (internal/session.go:185 CreateSession, :204 RecreateSession, :221 CompleteSession, :287 createSession, :552 token-bucket): pina activities NO MESMO host (GPU) + admission control por token bucket + RecreateSession token atravessa ContinueAsNew. dflow nao tem afinidade nem backpressure de recurso. 5) **ContinueAsNew** (internal/error.go:195, :640 NewContinueAsNewError; internal_task_handlers.go:1887; suggested :1335) = historico limitado p/ vidas longas; com RecreateSession mantem continuidade de recurso. 6) **MutableSideEffect/SideEffect** (internal_event_handlers.go:1054, :1155, :1231; workflow.go:2361) = escape hatch deterministico p/ config volátil/RNG injetado; MutableSideEffect so grava novo marker se o valor MUDOU (equals). 7) **Named RNG streams** (workflow_random.go:11 deriveSeed(runID,name), :25 reseedRandoms, :61 GetRandomStream) = ChaCha8 por NOME independente, reseeded no ContinueAsNew; dflow tem UM wc.Rand (seed fixa) -> nao da RNG reproduzivel por agente no fan-out. 8) **Child workflow + ParentClosePolicy** (internal_event_handlers.go:602, :565; internal_workflow.go:479; workflow/context.go:66 NewDisconnectedContext): ABANDON/TERMINATE/REQUEST_CANCEL decidem sub-flows quando o pai cancela + NewDisconnectedContext permite CLEANUP apos cancel (soltar GPU, abortar job). Saga/compensacao NAO esta no SDK: e COMPOSTA em cima (ContinueAsNew + child + signals). |
| Next | Se levar ao design (ADR p/ dflow v2): (a) GetVersion-style changeID+range no WorkflowCtx p/ replay tolerante a evolucao da esteira LoRA; (b) coroutine dispatcher + deadlock detector p/ fan-out deterministico dos 61 agentes; (c) Update (validate/accept/complete) como canal vivo dos gates no lugar de ErrPending+ResolveEvent; (d) Session/ResourceAffinity + token bucket p/ governar a GPU unica (RX 6700 XT) na esteira LoRA; (e) ContinueAsNew + RecreateSession p/ historico limitado + continuidade de recurso; (f) MutableSideEffect p/ ler config do run sem quebrar replay; (g) per-agent named RNG stream; (h) ParentClosePolicy + NewDisconnectedContext p/ rollback/cleanup multi-agente. Nao portar cegamente: comeca pelo que a esteira LoRA e os gates ganham imediatamente. |

### 2026-09-05 - RE-Mineracao kubernetes/kubernetes (olhar de HOJE: ADR-013 modular, gates, esteira LoRA, bug escrita qualifier-sem-ATTACH)
| Field | Value |
|-------|-------|
| Agent | cosca-architecture |
| Task | Re-mineracao de k8s/kubernetes (clone shallow em Temp/opencode/remina/kubernetes_kubernetes, commit b2ec8b6f), ja minerado antes (17 core + 27 org + sample-controller). Foco: reconcile/spec-status, controller/workqueue, admission chain/webhook, CRD conversion webhook, padroes NOVOS (leader election, deterministic scheduling) |
| Technique | Codigo (nao README): deployment_controller.go, sync.go, progress.go, controller_utils.go (ComputeHash), apiserver/pkg/admission/{chain,reinvocation}.go, admission/plugin/webhook/generic/webhook.go, apiextensions conversion/*webhook_converter.go + registry/customresource/strategy.go, client-go tools/leaderelection/leaderelection.go, util/workqueue/parallelizer.go. Gap vs COSCA atual: internal/pipeline/{control_loop,reconciler,workqueue}.go (loop base JA fechado - ADR-023 F1), internal/gate, internal/knowledge (migracao 257MB), esteira LoRA |
| Level | 3 |
| Outcome | success |
| Tags | #remineracao #kubernetes #generation-gate #spec-status-split #deterministic-identity #progress-deadline #ownerref-cascade #ratcheting #identity-anchored-conversion #leader-election #adr-013 #adr-017 #I1 #I2 #write-bug |
| Related | clone Temp/opencode/remina/kubernetes_kubernetes, docs/adr/ADR-013-modular-knowledge-databases, internal/pipeline/{control_loop,reconciler,workqueue}.go, internal/gate/gate.go, docs/adr/ADR-017-capability-borrowing-protocol.md |
| Learned | 1) O gap do loop base JA fechou: internal/pipeline/control_loop.go (ADR-023 F1) solda workqueue->reconcile->Done->AddRateLimited(falha)/Forget(sucesso) com drop apos maxRetries fail-closed; reconciler.go faz desired-vs-actual por drift de plano. O que resta sao REFINAMENTOS, nao furos brutos. 2) IDENTIDADE DETERMINISTICA (flag = bug escrita): k8s resolve "reconcilacao duplicitando objeto" por nome = hash-de-conteudo (ComputeHash fnv32a+DeepHashObject, controller_utils.go:1395-1407) + "nao duplica se deep-equal" + CollisionCount deterministico como escotilha anti-colisao (sync.go:198-268: AlreadyExists->deep-equal->se difere bump collisionCount e re-tenta; nome truncado sync.go:557-567). Traduz p/ "qualifier sem ATTACH": nome/ref do objeto = funcao pura do conteudo -> re-executar NUNCA duplica; colisao resolve por counter deterministico, nao sufixo aleatorio. 3) GENERATION + SPEC/STATUS SPLIT: deployment_controller.go:603-606 (Status.ObservedGeneration<Generation skip), sync.go:464 (nao apagar objeto ainda convergindo), strategy.go:145-187 (PrepareForCreate apaga status+seta generation=1; PrepareForUpdate preserva status e so incrementa generation em mudanca NAO-metadata via copyNonMetadata), strategy.go:131-142 (GetResetFields status=server-owned). A separacao spec/status e ENFORCEMENT do storage (status resetado/preservado pela camada), nao convencao. 4) PROGRESS-DEADLINE (time-based requeue): progress.go:160-199 computa proxima checagem de LastUpdateTime+deadline-now e AddAfter (nao polling fixo); 3 estados Complete/Progressing/TimedOut; preserva LastTransitionTime quando razao nao muda (nao maquia progresso). Perfeito p/ esteira LoRA (convergencia com deadline, nao re-poll infinito) e gates (proposta sem evidencia em N tempo expira). 5) ADMISSION MULTI-CAMADA + FAIL-CLOSED: ShouldCallHook (webhook.go:296-385) = namespaceSelector->objectSelector->rules->matchConditions CEL; erro de avaliacao da condicao -> Forbidden fail-closed (webhook.go:374-376); chain.go:47-60 valida short-circuit. Gate de aprovacao deveria decidir cedo se e do seu caso (barato-primeiro) e nunca fail-open. 6) GUARDA AUTO-REFERENCIAL (webhook.go:394-433): recursos virtuais auth/authz excluidos de todo hook (nao se trancar fora do proprio auth); config de admission excluida dos hooks via API p/ evitar dependencia circular MAS avaliada por hooks estaticos de disco (268-284 composite source + RunReloadLoop) que funcionam com control plane fora. 7) CONVERSAO IDENTITY-ANCHORED + RATCHETING (o mais valioso p/ ADR-013): webhook_converter.go:126-142 so converte o que NAO esta na versao-alvo; 236-388 batch de lista inteira em 1 chamada; 392-479 validateConvertedObject exige preservar kind/name/namespace/uid e restoreObjectMeta DESCARTAR metadata da resposta e restaurar do original (webhook so mantem labels/annotations, re-validadas) -> conversao evolve o payload, NUNCA a identidade/geracao; strategy.go:287-302 RATCHETING (so bloqueia se o update INTRODUZ nova violacao, nao por violacao pre-existente) + 78/221/309 CEL cost-budget. Isso executa a "ancora imutavel" do ADR-013 e impede a migracao de 257MB de colapsar em itens legados nao-conformes. 8) LEADER ELECTION LEASE (write-fence NOVO): leaderelection.go:211-312 acquire/renew; 364-439 end-of-term (PreferredHolder) = parar de renovar p/ handoff limpo; 454-467 fast-path otimista com fallback slow-path em conflito; 528-546 Check health adaptor (deadlock -> unhealthy); 77-101 LeaseDuration>RenewDeadline>RetryPeriod*1.2. Junto ao epoch/fencing do ruflo (WorkOwnershipRecord L1-M2-M3) fecha "um writer por modulo" - nunca dois agentes dedicando o mesmo qualifier. 9) ParallelizeUntil refinado (parallelizer.go:46-97): chunkSize p/ pieces>>workers + workers=chunks cap + cancelamento por ctx + HandleCrash por goroutine. 10) REJEITAR: informer/DeltaFIFO + lister/indexer (ja portados; infra distribuida, I7), scheduler framework (nao compensa; o que vale e a ordenacao deterministica proporcional sync.go:307-400). |
| Next | Se aprovado (ADR-017): (a) determinismo de escrita como hipotese de ADR - identidade deterministica + CollisionCount em internal/knowledge/memory p/ o bug qualifier-sem-ATTACH; (b) gate de convergencia por generation/observedGeneration no state do plano; (c) progress-deadline 3-estados no ControlLoop/esteira LoRA; (d) conversao identity-anchored + ratcheting como plano de migracao do KB-257MB; (e) write-fence por lease/epoch nos escritores de modulo. |

### 2026-09-05 � Re-mineracao backstage_backstage (olhar de HOJE: 61 agentes, 88 skills, gate, LoRA)
| Field | Value |
|-------|-------|
| **Agent** | cosca-architecture |
| **Task** | Re-minerar spotify/backstage (ja minerado: 15 padroes). Foco novo: AI Resource Model (skill/plugin/rule/marketplace), plugin como artefato runtime (dynamic feature), auth condicional 2-fases (policy to CONDITIONAL to apply|toQuery), scaffolder checkpoint (resumivel), package role taxonomy, orphan eviction, alpha/main API gating |
| **Technique** | Codigo (nao README): plugin-manager.ts, CommonJSModuleLoader.ts, permission/{router,types,createConditionTransformer,createConditionFactory}.ts, scaffolder-node/src/alpha/checkpoints/types.ts, catalog-model/src/schema/kinds/AiResource.*.json, catalog-backend/src/processing/evictEntitiesFromOrphanedProviders.ts, cli-node/src/roles/types.ts, catalog-node/src/extensions.ts |
| **Level** | 3 |
| **Outcome** | success |
| **Tags** | #remineracao #backstage #ai-resource-model #dynamic-plugin #resource-conditional-auth #checkpoint-resumavel #role-taxonomy #orphan-eviction #alpha-gating #adr-017 #I1 |
| **Related** | clone Temp/opencode/remina/backstage_backstage, docs/adr/ADR-017, internal/plugin (WASM), internal/agents, internal/skills, internal/gate, ADR-013, internal/durable |
| **Learned** | 1) **AI Resource Model (`AiResource.v1alpha1`)** e o achado-ouro p/ 61 agentes+88 skills+plugins+governanca: entidade de catalog com spec.type = skill|plugin|rule|marketplace. skill tem agents (alvo), dependsOn (relacao a outros AI resources), allowedTools (allowlist de tools pre-aprovadas - agentskills.io), license, compatibility, disciplines, categories; plugin tem skills[]+version; marketplace tem plugins[]+version (registry); rule tem category(security/architecture/style)+rationale+disciplines. Construido via catalogModelExtensionPoint.addModelSource(static). E EXATAMENTE o modelo de knowledge de agentes/skills/plugins que falta no COSCA. 2) **Plugin como artefato runtime** (backend-dynamic-feature-service): PluginScanner varre dir por role `*-plugin`/`*-plugin-module`/frontend-dynamic-container; CommonJSModuleLoader faz override de `_nodeModulePaths`/`_resolveFilename` p/ resolver deps do proprio node_modules do plugin; resolveInstallerFromModule aceita default export (BackendFeature|BackendFeatureFactory) OU campo `dynamicPluginInstaller`; deteccao por magic-tag `$$type==='@backstage/BackendFeature'` (estrutural, nao instanceof); entrada alpha vs main (preferAlpha). Plugavel no runtime sem rebuild. 3) **Auth condicional 2-fases**: policy.handle() retorna ALLOW|DENY|CONDITIONAL (router.ts L141-179); se CONDITIONAL -> applyConditions (in-memory) ou transformer recusivo mantendo allOf/anyOf/not (createConditionTransformer L31-53) que usa `rule.toQuery(params)` p/ pushdown no DB; PermissionRule tem CONTRATO DUPLO `apply(resource,params)` + `toQuery(params)` (types.ts L52-91) - resultado logico igual, senao divergencia; batch via DataLoader por plugin; guarda "resource permissions exigem resourceRef" (L243-250). 4) **Checkpoint resumivel** (scaffolder): `ctx.checkpoint({key, fn})` - wrap de step idempotente; retorna valor JSON-serializado salvo; CheckpointState = failed{reason}|success{value}; em recovery reaproveita sem re-exec fn. Igual primitiva de durable/continuation que COSCA quer na esteira LoRA. 5) **Package role taxonomy como contrato** (cli-node/src/roles): PackageRole tipado (backend-plugin, backend-plugin-module, frontend-dynamic-container...) -> platform+outputType; CLT e scanner descubrem por role. Organizacao de monorepo de plugins. 6) **Orphan eviction**: entidades de provider que sumiu -> evict, nao delete hard; reconcilia fonte-da-verdade vs cache (evictEntitiesFromOrphanedProviders L21-51). 7) **alpha vs main API gating**: package exporta index.ts (estavel) + alpha.ts (experimental); API Extractor gera report.api.md/report-alpha.api.md -> enforce diff da superfice publica em review. |
| **Next** | Se o Don aprovar: (a) ADR "Modelo de AI Resource no COSCA" - catalogo de agente/skill/plugin/regra como entidades com relacoes (agents/dependsOn/skills/plugins) + `allowedTools` como allowlist do gate de aprovacao; (b) usar role-tagged discovery + magic-tag `$$type` no loader WASM do internal/plugin; (c) PermissionRule como contrato duplo apply/toQuery p/ ao mesmo tempo checar em-memoria e empurrar filtro no DB; (d) checkpoint(key,fn) como step resumivel na esteira LoRA. NAO portar: Nunjucks/scaffolder TS (Cosca ja tem templates), permission policy em JS/express, module-federation Scalprum. |
